// Good_WordBoundaryNoise.ts — adversarial PASS fixture for
// check-herd-signals-language.mjs. Round 5: BANNED_TERM_RE used to match
// each alternative as a bare SUBSTRING with no boundary at all, so
// "interesting" and "treating" (and "arrested", "restore", "restart",
// "wrestling", once the posture-paraphrase "resting"/"rest" family was
// added) all matched "eating"/"resting"/"rest" as pure noise inside
// unrelated English words. Every line below must produce ZERO findings —
// none of them mention an animal or a claim at all. This file must never
// be wired into a real build target; it exists only for the guard's
// --self-test.

// with viewer count in any interesting way
// before treating any row here as current
// the process was arrested mid-cycle
// restore the previous connection state
// restart the sweeper after config reload
// the parser is wrestling with malformed input
export const note = true;
