// Good_GaitTermsTechnicalProse.ts — adversarial PASS fixture for
// check-herd-signals-language.mjs. Round 5, gait/posture subject fix: the
// gait family (run, walk, stand, lie, sit, eat, graze, rest, idle,
// sleeping, dozing) has ordinary, innocent TECHNICAL meanings outside
// animal behavior. None of the sentences below mention an animal/goat/
// herd/tag or use a detection verb (detects/confirms/shows), so none of
// them may be flagged. This file must never be wired into a real build
// target; it exists only for the guard's --self-test.

// filters and search run in the query
// the sweeper runs every 5 minutes
// walk the cursor forward
// the test suite is standing up a container
// the batch is eating memory
// The connection pool is idle between polls.
// the goroutine is sleeping between polls
// the worker briefly dozes before the next tick
export const note = true;
