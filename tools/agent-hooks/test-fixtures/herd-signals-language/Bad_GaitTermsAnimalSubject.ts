// Bad_GaitTermsAnimalSubject.ts — adversarial FAIL fixture for
// check-herd-signals-language.mjs. Round 5: the flip side of
// Good_GaitTermsTechnicalProse.ts — the SAME gait/posture terms, now with
// a genuine animal subject or explicit detection verb, must still be
// caught. Every line below carries either an animal-subject cue
// (animal/goat/tag) or an assertion verb (confirms/shows/detects) and must
// produce a finding. This file must never be wired into a real build
// target; it exists only for the guard's --self-test.

// the animal is running
// the gateway confirms the goat is walking
// tag A0003B shows the animal standing
// detects grazing
// the animal is resting for short periods
// the goat is sleeping in the shade
export const note = true;
