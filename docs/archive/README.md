# Goat OS Archive

Status: archived source material, not authoritative build context.

Files here are old planning notes kept for archaeology and audit trail only.
Agents should not read or grep this folder during normal Goat OS build work.
Use `<mesha-workspace>/context/README.md` and the active docs under
`<mesha-workspace>/context/` for current decisions.

If an archived idea needs to return, copy the relevant fact into an active
context document with a new review note instead of linking agents here.
