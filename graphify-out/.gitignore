# Generated graph artifacts are local-only and must never be committed
# (AGENTS.md: commit ONLY generation scripts/skills, never graph output).
# Whitelist style: ignore EVERYTHING in this directory by default except this
# .gitignore itself, so any current OR future graphify/CRG output file is
# ignored without having to enumerate filenames.
*
!.gitignore
