openapi: 3.1.0
info:
  title: Goat OS App API
  version: 0.1.0
  summary: Goat OS app and operating API for field tasks, goat lookup, protocol config, vaccination, and process integrity.
  description: REST/JSON API for operator/mobile and current admin-web operating surfaces. Reads and writes are RBAC
    scope-filtered before response DTOs are built.
servers:
  - url: https://api.goatos.example.invalid
    description: Synthetic contract server placeholder.
security:
  - bearerAuth: []
tags:
  - name: Operator App Bootstrap
  - name: Admin Web Bootstrap
  - name: Operator Tasks
  - name: SOP Admin
  - name: Goats
  - name: Identifiers
  - name: Protocol Config
  - name: Action Center
  - name: Control Tower
  - name: Protocol Adherence
  - name: Workflows
  - name: Proof Media
  - name: Vaccination
  - name: Feed Direction
  - name: Feed Config
  - name: Health Config
  - name: Calendar
  - name: Vaccination Passport
  - name: Herd Register
  - name: Verification
  - name: Counts
  - name: Weighing
  - name: Sales
  - name: Herd Signals
paths:
  /herd-signals/packets:
    post:
      tags:
        - Herd Signals
      operationId: ingestHerdSignalPackets
      summary: Ingest a batch of BLE ear-tag packets from one gateway.
      description: >-
        Gateway device write path (backend/internal/herdsignals). Never anonymous: requires the
        herd_signals.ingest permission. Packet insert, gateway last_seen_at upsert, activity-window
        rollup (60s/300s/3600s tiers), and tag_latest state recompute all happen in ONE
        transaction. Idempotent: a replayed packet (same tenant, tag, received_at, motion_count) is
        silently deduplicated and does not double-count. Out-of-order safe: the "latest" snapshot
        only advances when a packet's seen_at is strictly newer than the currently stored
        last_seen_at for that tag.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/HerdSignalsIngestRequest'
      responses:
        '200':
          description: Packets accepted.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HerdSignalsIngestResponse'
        '400':
          description: Invalid request body.
        '401':
          description: Authentication required.
  /herd-signals/live:
    get:
      tags:
        - Herd Signals
      operationId: listHerdSignalsLive
      summary: Live tag status, tag-first (an unmapped tag is the normal case, not a degraded one).
      description: >-
        Every packet-derived field (id, MAC, gateway, RSSI/signal, battery, temperature, motion
        count and deltas, movement_state, pattern_state, last_seen_at, sensor bits) renders for a
        tag with no animal mapped behind it. Only goat_id/display_id/park/shed/location are
        animal-derived and may be null. `summary` is a whole-filter server-side aggregate over the
        same tenant-scoped query as `items` -- never summed from the returned page.
      parameters:
        - name: park_id
          in: query
          schema: { type: string, format: uuid }
          description: Filters to animals resolved to this park. An unmapped tag has no park and never matches.
        - name: shed_id
          in: query
          schema: { type: string, format: uuid }
          description: Filters to animals resolved to this shed. An unmapped tag has no shed and never matches.
        - name: movement_state
          in: query
          schema: { $ref: '#/components/schemas/HerdSignalMovementState' }
        - name: mapping_state
          in: query
          schema: { $ref: '#/components/schemas/HerdSignalMappingState' }
        - name: pattern
          in: query
          schema: { $ref: '#/components/schemas/HerdSignalPatternState' }
        - name: q
          in: query
          schema: { type: string }
          description: Free-text search over display id, tag id, MAC, shed name, gateway id.
        - name: cursor
          in: query
          schema: { type: string }
        - name: limit
          in: query
          schema: { type: integer, default: 25, minimum: 1, maximum: 200 }
      responses:
        '200':
          description: Live tag list with a whole-filter summary.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HerdSignalsLiveResponse'
        '401':
          description: Authentication required.
  /herd-signals/tags/{tag_id}/timeline:
    get:
      tags:
        - Herd Signals
      operationId: getHerdSignalsTagTimeline
      summary: Bucketed motion history for one tag.
      description: >-
        Bucket tier auto-selects from the range (<=1h -> 60s, <=24h -> 300s, >24h -> 3600s) unless
        `bucket_seconds` is given, in which case it must be one of 60/300/3600 or the request is
        rejected. The response is DENSE: a window with no packets is an explicit gap
        (`is_gap: true`), which must never collapse with a window that received packets but had
        zero movement (`packet_count > 0`, `motion_delta: 0`) -- that distinction is the whole
        product requirement. Bounded to at most 2000 buckets per request.
      parameters:
        - name: tag_id
          in: path
          required: true
          schema: { type: string }
        - name: from
          in: query
          required: true
          schema: { type: string, format: date-time }
        - name: to
          in: query
          required: true
          schema: { type: string, format: date-time }
        - name: bucket_seconds
          in: query
          schema: { type: integer, enum: [60, 300, 3600] }
      responses:
        '200':
          description: Dense bucketed motion history.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HerdSignalTimelineResponse'
        '400':
          description: Missing from/to, to before from, unsupported bucket_seconds, or the range spans too many buckets at the requested resolution.
        '401':
          description: Authentication required.
  /herd-signals/gateways:
    get:
      tags:
        - Herd Signals
      operationId: listHerdSignalsGateways
      summary: Gateway registration, location, and coverage health.
      responses:
        '200':
          description: Gateway list.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HerdGatewaysResponse'
        '401':
          description: Authentication required.
  /herd-signals/insights:
    get:
      tags:
        - Herd Signals
      operationId: getHerdSignalsInsights
      summary: 12 backend-computed insight cards (direct/derived/correlated/inferred signal types).
      description: >-
        Must not error when zero tags are mapped (the STG day-one state): the direct/derived cards
        (tags_live_now, weak_signal_tags, battery_attention, missing_signal, unmapped_smart_tags,
        shed_signal_coverage) still compute from packet-derived data alone. The four correlated
        cards (post_vaccination_movement_watch, health_case_activity_trend, feed_activity,
        weight_activity) return an honest zero/empty result rather than an error when no tags are
        mapped. Label/unit/signal_type/formula/caveat text is backend-owned copy; render it
        verbatim.
      responses:
        '200':
          description: Insight cards.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HerdInsightsResponse'
        '401':
          description: Authentication required.
  /herd-signals/tag-mappings:
    post:
      tags:
        - Herd Signals
      operationId: bindHerdSignalTagMapping
      summary: Bind a BLE tag to an animal.
      description: >-
        Requires herd_signals.map -- NOT the read permission. Deciding which animal a tag belongs
        to is a different authority from reading the dashboard: it decides whose body every
        animal-attributed number downstream is about.

        Claims the tag's id (and its MAC, when the tag reports a distinct one) as active,
        smart-tag-capable goat_identifiers rows for the animal, in ONE transaction. Values are
        normalised with the identity module's canonical normalizer, so a lowercase device MAC
        matches an uppercase-stored identifier.

        ANIMAL MONITORING STARTS HERE. The mapping instant is stamped on the identifier and
        denormalised onto the tag's live row; everything the tag emitted earlier stays visible as
        device telemetry but can never enter this animal's baseline, pattern window, or
        correlations.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/HerdSignalsBindTagMappingRequest'
      responses:
        '200':
          description: The binding now in force.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HerdSignalsTagMappingResponse'
        '400':
          description: Missing or malformed goat_id/tag_id, or an identifier_type outside the allowed set.
        '401':
          description: Authentication required.
        '404':
          description: No such animal in this tenant.
        '409':
          description: >-
            The tag value is already claimed by another animal, the animal already carries a live
            smart tag (use the replace endpoint), or the value is held by a non-active identifier.
  /herd-signals/tag-mappings/replace:
    post:
      tags:
        - Herd Signals
      operationId: replaceHerdSignalTagMapping
      summary: Replace an animal's smart tag with a new one, atomically.
      description: >-
        Requires herd_signals.map. Re-tagging is the real-world case -- a tag falls off, a
        replacement goes on -- and both halves commit together or neither does, so the animal is
        never left carrying two live smart tags (ambiguous telemetry) or none (silently
        unmonitored).

        The new tag starts a NEW monitoring period and the old tag's period ends, so nothing the
        replacement emitted while it sat unused can be attributed to this animal.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/HerdSignalsReplaceTagMappingRequest'
      responses:
        '200':
          description: The new binding, and the identifiers whose binding this ended.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HerdSignalsTagMappingResponse'
        '400':
          description: Missing or malformed goat_id/new_tag_id, or an identifier_type outside the allowed set.
        '401':
          description: Authentication required.
        '404':
          description: No such animal in this tenant.
        '409':
          description: >-
            The animal has no live smart tag to replace, already carries exactly this tag, or the
            new tag value is claimed by another animal.
  /herd-signals/tag-mappings/unmap:
    post:
      tags:
        - Herd Signals
      operationId: unmapHerdSignalTagMapping
      summary: Release a tag's binding with no replacement.
      description: >-
        Requires herd_signals.map. For a tag that was lost, an animal that was sold, or a mapping
        made in error.

        The tag returns to unmapped and its packets keep flowing as device telemetry: nothing is
        deleted and its stored history stays intact, it simply stops being attributed to an
        animal. Its monitoring period ends, so no further animal-attributed value is produced for
        it -- not a zero, not a default.

        There is no "mark as smart tag" counterpart anywhere in this API. Every tag the gateway
        reports is already a smart tag; smart_tag_capable is an internal consequence of binding,
        never a user action.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/HerdSignalsUnmapTagMappingRequest'
      responses:
        '200':
          description: The binding that was released.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HerdSignalsTagMappingResponse'
        '400':
          description: Missing tag_id.
        '401':
          description: Authentication required.
        '409':
          description: That tag is not mapped to an animal.
  /herd-signals/heartbeats:
    post:
      tags:
        - Herd Signals
      operationId: recordHerdSignalGatewayHeartbeat
      summary: Record a gateway heartbeat (sta_gw_hb).
      description: >-
        Gateway DEVICE write path: requires herd_signals.ingest, the same credential class as
        packet ingest, never an operator permission.

        The gateway emits this state message roughly every five minutes carrying no device rows at
        all, and it was previously discarded. Without it "gateway up but hearing no tags" (a dead
        antenna, a misaimed unit, an empty shed) is indistinguishable from "gateway down" -- two
        situations that demand opposite responses. It advances last_heartbeat_at, which is
        deliberately separate from last_seen_at: either kind of message proves we heard something,
        only a heartbeat proves the gateway itself is alive.

        A ticks_cnt that goes BACKWARDS is a reboot, handled like a reset motion counter or a
        decreasing pkt_sn: re-anchor and count the reboot, never record a negative.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/HerdSignalsGatewayHeartbeatRequest'
      responses:
        '200':
          description: Heartbeat recorded.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HerdSignalsGatewayHeartbeatResponse'
        '400':
          description: Missing gateway_id, or a state that is not the gateway heartbeat state.
        '401':
          description: Authentication required.
  /herd-signals/export.csv:
    get:
      tags:
        - Herd Signals
      operationId: exportHerdSignalsCsv
      summary: The current live view as a CSV file, honouring every filter the list honours.
      description: >-
        Streams the SAME filtered, keyset-ordered result as GET /herd-signals/live, through the
        same filter builder and the same per-page enrichment, so what downloads is what the
        operator is looking at -- not a second query that agrees with the screen by coincidence.
        Chunked: 500 rows are read, written and released at a time, so peak memory is one page
        regardless of how many tags match. Capped at 100000 rows, and a capped file says so in
        its last line rather than reading as complete data. An absent reading is an EMPTY cell,
        never a zero. `Tag Temperature (C)` is the tag housing's own reading -- the tag has no
        animal-contact sensor and reports no other temperature.
      parameters:
        - name: park_id
          in: query
          schema: { type: string, format: uuid }
        - name: shed_id
          in: query
          schema: { type: string, format: uuid }
        - name: movement_state
          in: query
          schema: { $ref: '#/components/schemas/HerdSignalMovementState' }
        - name: mapping_state
          in: query
          schema: { $ref: '#/components/schemas/HerdSignalMappingState' }
        - name: pattern
          in: query
          schema: { $ref: '#/components/schemas/HerdSignalPatternState' }
        - name: q
          in: query
          schema: { type: string }
          description: Free-text search over display id, tag id, MAC, shed name, gateway id.
      responses:
        '200':
          description: CSV file of the filtered live view.
          content:
            text/csv:
              schema: { type: string }
        '401':
          description: Authentication required.
  /herd-signals/tags/{tag_id}/activity:
    get:
      tags:
        - Herd Signals
      operationId: getHerdSignalsTagActivity
      summary: Farm records for this tag's mapped animal, to overlay on its movement history.
      description: >-
        Returns the recorded farm activity for the animal this tag is mapped to (and for its
        shed, where the record is shed-grain), so the records and the movement history can be
        read SIDE BY SIDE.

        A marker is CORRELATION IN TIME AND NOTHING MORE. A feed marker means feed was directed
        to the shed this animal is in, never that this animal took any of it. A vaccination
        marker beside a movement change means the two fall in the same window, never that one
        caused the other and never that anything is wrong. A health-case or treatment marker is
        a record a person made, never a diagnosis and never something inferred from motion. Each
        event carries a `grain` saying which of these it is; a client must not present a
        shed-grain or scanned-identifier record as an observation of one animal.

        MONITORING BOUNDARY (migration 000196): an animal's history with a tag starts at the
        instant the tag was mapped to it; everything earlier is device telemetry. `from` is
        clamped UP to that instant before any record is read, and the returned `from` is the
        EFFECTIVE window so a client can see the clamp happened. A tag with no animal behind it
        has no farm activity at all -- an empty `events` with a `reason`, never an error.

        Bounded: at most 31 days per request and at most 500 events; `truncated` says when the
        window holds more than were returned.
      parameters:
        - name: tag_id
          in: path
          required: true
          schema: { type: string }
        - name: from
          in: query
          required: true
          schema: { type: string, format: date-time }
        - name: to
          in: query
          required: true
          schema: { type: string, format: date-time }
      responses:
        '200':
          description: Farm-activity markers for the effective window.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HerdSignalActivityResponse'
        '400':
          description: Missing from/to, to before from, or a range longer than 31 days.
        '401':
          description: Authentication required.
        '404':
          description: No such tag for this tenant.
  /admin/tasks/review-fanouts/retry:
    post:
      tags:
        - SOP Admin
      operationId: retrySOPReviewFanouts
      summary: Retry pending or failed task review fanout rows.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RetryReviewFanoutsRequest"
      responses:
        "200":
          description: Review fanouts retried.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RetryReviewFanoutsResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /app/me:
    get:
      tags:
        - Operator App Bootstrap
      operationId: appMe
      summary: Return the authenticated actor's operator profile and active grants.
      responses:
        "200":
          description: Actor/operator context.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppMeResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /app/bootstrap:
    get:
      tags:
        - Operator App Bootstrap
      operationId: appBootstrap
      summary: Return the Android bootstrap manifest for the authenticated operator.
      parameters:
        - $ref: "#/components/parameters/AcceptLanguage"
        - $ref: "#/components/parameters/GoatOSLocale"
        - name: device_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Bootstrap manifest.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BootstrapResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /admin-web/bootstrap:
    get:
      tags:
        - Admin Web Bootstrap
      operationId: adminWebBootstrap
      summary: Return the backend-owned admin-web UI contract.
      description: >-
        Read-only shell/page contract for admin-web IA, nav, route labels, top-bar controls,
        role lenses, page/table/drawer metadata, disabled reasons, and summary-vs-detail
        display rules. Frontend renders this contract and owns only layout/local UI state.
      responses:
        "200":
          description: Admin-web UI contract.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminWebBootstrapResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /weighing/campaigns:
    get:
      tags:
        - Weighing
      operationId: listWeighingCampaigns
      summary: List leadership-visible Weighing campaigns.
      parameters:
        - name: park_id
          in: query
          required: false
          description: >-
            Optional park filter. It narrows the returned ROWS only; `counts` stays a
            whole-scope aggregate so the Active/Completed tallies do not move when the
            park chip changes.
          schema:
            type: string
            format: uuid
        - name: cursor
          in: query
          required: false
          schema:
            type: string
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 5000
            default: 20
      responses:
        "200":
          description: Weighing campaigns.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WeighingCampaignListResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
        - Weighing
      operationId: createWeighingCampaign
      summary: Create a weekly kids Weighing campaign draft.
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateWeighingCampaignRequest"
      responses:
        "200":
          description: Weighing campaign created or idempotently replayed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WeighingCampaignResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "500":
          $ref: "#/components/responses/ServerError"
  /weighing/campaigns/{campaign_id}:
    put:
      tags:
        - Weighing
      operationId: updateWeighingCampaign
      summary: Edit an existing weekly kids Weighing campaign without creating a duplicate park/week task.
      parameters:
        - $ref: "#/components/parameters/WeighingCampaignId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateWeighingCampaignRequest"
      responses:
        "200":
          description: Weighing campaign updated or idempotently replayed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WeighingCampaignResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "500":
          $ref: "#/components/responses/ServerError"
  /weighing/campaigns/{campaign_id}/publish:
    post:
      tags:
        - Weighing
      operationId: publishWeighingCampaign
      summary: Publish a Weighing campaign to operator-visible work.
      parameters:
        - $ref: "#/components/parameters/WeighingCampaignId"
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          description: Weighing campaign published or idempotently replayed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WeighingCampaignResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "500":
          $ref: "#/components/responses/ServerError"
  /weighing/campaigns/{campaign_id}/export:
    get:
      tags:
        - Weighing
      operationId: exportWeighingCampaignCsv
      summary: Export one Weighing campaign's observations as CSV.
      description: >
        Streams a CSV attachment (one row per shed/observation) for the campaign, requiring
        WeighingMonitor and enforcing the same park scoping as every other leadership-tier
        weighing read. The response is NOT JSON: once the first row has been written the status
        is already 200 and a mid-stream failure truncates the body rather than appending an
        error envelope, so a client must treat a short/invalid CSV as a failed export.
      parameters:
        - $ref: "#/components/parameters/WeighingCampaignId"
      responses:
        "200":
          description: CSV export of the campaign's shed/observation rows.
          content:
            text/csv:
              schema:
                type: string
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "500":
          $ref: "#/components/responses/ServerError"
  /app/weighing/campaigns:
    get:
      tags:
        - Weighing
      operationId: appListWeighingCampaigns
      summary: List Weighing campaigns for one weighing surface.
      description: >
        The three weighing surfaces are separate destinations with separate authority, so the
        caller names the one it wants instead of the server inferring it from the actor's roles.
        `mine` is the caller's own assigned sheds and is the only executable list
        (`weighing.execute`); `all` is the planner's flat all-tasks list across parks
        (`weighing.plan` or `weighing.monitor`); `operators` is read-only oversight of other
        people's weighing work (`weighing.oversee_operators`) and carries no scan action.
        Omitting the parameter means `mine`. No scope widens the write: recording a weight still
        requires the caller to be the shed's assignee.
      parameters:
        - name: scope
          in: query
          required: false
          schema:
            type: string
            enum: [mine, all, operators]
            default: mine
        - name: park_id
          in: query
          required: false
          description: >-
            Optional park filter. It narrows the returned ROWS only; `counts` stays a
            whole-scope aggregate so the Active/Completed tallies do not move when the
            park chip changes.
          schema:
            type: string
            format: uuid
        - name: cursor
          in: query
          required: false
          schema:
            type: string
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 5000
            default: 20
      responses:
        "200":
          description: Operator Weighing campaigns.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WeighingCampaignListResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /app/weighing/campaigns/{campaign_id}:
    get:
      tags:
        - Weighing
      operationId: appGetWeighingCampaign
      summary: Resolve ONE Weighing task by id.
      description: >
        The single-task read behind a notification deep link. The task list is a keyset page
        with no id filter, so a cold tap on a task further down the keyset could not be
        resolved: the client walked a few pages and then reported "not found" for work that
        exists.


        Authority is the SAME split the task's bucket page applies, and the two branches are
        bounded differently on purpose. A planner or monitor (`weighing.plan` or
        `weighing.monitor`) resolves the task UNFILTERED, so the task's own park is resolved
        first and the caller must hold plan or monitor IN THAT PARK -- naming another park's task
        id returns 404, not that park's task. An assignee (`weighing.execute`) is instead bounded
        by their OWN assignment: the task must carry a live bucket assigned to them, and they see
        only their own buckets on it. That branch runs NO park check and needs none, because an
        assignment is already park-bound -- nobody is assigned work in a park they do not work in.

        Both misses are 404, so neither branch confirms that a task it refused exists.
      parameters:
        - $ref: "#/components/parameters/WeighingCampaignId"
      responses:
        "200":
          description: The task, with its buckets and progress.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WeighingCampaignResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "500":
          $ref: "#/components/responses/ServerError"
  /app/weighing/parks:
    get:
      tags:
        - Weighing
      operationId: appListWeighingParks
      summary: List the parks whose Weighing this caller may look at.
      description: >
        Park VOCABULARY for the oversight surfaces' filter chips: identity only, no weigh date
        and no counts. It exists because the only other park list is the planner catalog, which
        is gated on `weighing.plan` -- CEO-only -- so a Growth Director (`weighing.monitor` +
        `weighing.oversee_operators`, never `weighing.plan`) had no park list they could read
        and the client fell back to whichever parks appeared on the rows it happened to have
        loaded. A vocabulary derived from the filtered data loses a park as soon as that park's
        tasks page out. The list is the caller's CAPABILITY-SCOPED parks, never every park in
        the tenant. Unpaged: parks are a handful and a chip row that pages cannot offer the
        parks it has not reached.
      responses:
        "200":
          description: The caller's authorized weighing parks.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WeighingParkListResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /weighing/export.csv:
    get:
      tags:
        - Weighing
      operationId: exportWeighingCsv
      summary: Export recent leadership-visible Weighing rows as CSV.
      description: >
        Streams a CSV attachment across the caller's WeighingMonitor park scope, optionally
        narrowed to one park and selected shed locations. The default window is today plus the
        previous 35 business dates (36 inclusive dates); an explicit range up to 366 days is
        served. Columns follow the operations Weight-check sheet, minus its video-link column:
        date, rfid, rfid_2, old_id, old_id_suffix, breed, gender, shed, type, count, operator,
        approval, verified_weight_kg. Pending video verification is included and reported in
        `approval` (approved / rejected / pending); it is not filtered out.
      parameters:
        - in: query
          name: from
          schema:
            type: string
            format: date
          description: Inclusive Asia/Kolkata business date. Defaults to 35 days before `to`.
        - in: query
          name: to
          schema:
            type: string
            format: date
          description: Inclusive Asia/Kolkata business date. Defaults to today.
        - in: query
          name: park_id
          schema:
            type: string
            format: uuid
          description: Optional park narrowing inside the caller's authorized monitor scope.
        - in: query
          name: shed_id
          schema:
            type: array
            items:
              type: string
              format: uuid
          style: form
          explode: true
          description: >
            Optional repeated shed location ids (the Weights table's location grain). Absent
            means every shed in scope.
      responses:
        "200":
          description: CSV export of recent weighing rows.
          content:
            text/csv:
              schema:
                type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /app/weighing/planner/catalog:
    get:
      tags:
        - Weighing
      operationId: appWeighingPlannerCatalog
      summary: Get every Weighing planner park for a weigh date, with the operator picker.
      description: >
        PARK-GRAIN read. It returns EVERY park the planner may use on the requested date --
        each with a park-grain shed COUNT, a park-grain kid count, and the park's existing task
        on that date -- plus the operator picker the wizard holds while it pages buckets.
        It carries NO shed rows and has no cursor: a park picker that paged could not offer the
        parks it had not reached yet. Read the chosen park's sheds, with their date-scoped
        availability, from /app/weighing/planner/parks/{park_id}/buckets.
      parameters:
        - name: period_start_date
          in: query
          required: true
          description: The Asia/Kolkata business DATE the existing-task decoration is read for.
          schema:
            type: string
            format: date
      responses:
        "200":
          description: Every planner park for the date, plus the operator picker.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WeighingPlannerCatalogResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /app/weighing/planner/parks/{park_id}/buckets:
    get:
      tags:
        - weighing
      operationId: appWeighingPlannerParkBuckets
      summary: Page one Weighing planner park's kid sheds with their availability on a weigh date.
      description: >
        Availability is scoped to ONE weigh date: each shed reports whether an open weighing
        task already claims it on that date, and by whom. A shed claimed on another date is
        not claimed here. A real park holds 76+ sheds, so this is a keyset page; the park list
        it drills from is a separate unpaged read.
      parameters:
        - name: park_id
          in: path
          required: true
          description: The park whose sheds are paged.
          schema:
            type: string
            format: uuid
        - name: period_start_date
          in: query
          required: true
          description: The Asia/Kolkata business DATE the availability is reported for.
          schema:
            type: string
            format: date
        - name: exclude_campaign_id
          in: query
          required: false
          description: >-
            The task currently being edited. Its own buckets are not reported as taken, so an
            edit can re-save the sheds it already owns.
          schema:
            type: string
            format: uuid
        - name: cursor
          in: query
          required: false
          description: >-
            Opaque cursor returned as next_cursor by the previous page of THIS park.
          schema:
            type: string
        - name: limit
          in: query
          required: false
          description: Shed rows per page.
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
      responses:
        "200":
          description: One keyset page of the park's buckets.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WeighingPlannerParkBucketsResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /app/weighing/campaigns/{campaign_id}/sheds:
    get:
      tags:
        - Weighing
      operationId: appListWeighingCampaignSheds
      summary: Page one Weighing task's shed buckets.
      description: >
        The task-detail bucket list. It exists so the detail screen stops rendering the bucket
        set that the task LIST embeds in every campaign row: a park holds 76+ sheds, so a
        20-task list page carried well over a thousand bucket rows. Keyset-paged on
        (display_name, campaign_shed_id). An assignee sees only their own buckets on the task;
        a planner or monitor sees all of them — the same split the task list already applies.
      parameters:
        - $ref: "#/components/parameters/WeighingCampaignId"
        - name: cursor
          in: query
          required: false
          description: Opaque cursor returned as next_cursor by the previous page.
          schema:
            type: string
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
      responses:
        "200":
          description: One page of the task's shed buckets.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WeighingCampaignShedPageResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /app/weighing/campaigns/{campaign_id}/sheds/{campaign_shed_id}/roster:
    get:
      tags:
        - Weighing
      operationId: appGetWeighingScopeRoster
      summary: Get a bounded operator-visible RFID roster for one Weighing scope.
      parameters:
        - $ref: "#/components/parameters/WeighingCampaignId"
        - name: campaign_shed_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
        - name: cursor
          in: query
          required: false
          schema:
            type: string
          description: Opaque roster cursor returned as next_cursor by the previous page.
        - name: observations_cursor
          in: query
          required: false
          schema:
            type: string
          description: >-
            Opaque observations cursor returned as next_observations_cursor by
            the previous page. Paginates the observations array independently
            of items, on a keyset of (accepted_at, observation_id) scoped to
            this campaign_shed_id; it does not advance in lockstep with the
            roster cursor.
        - name: include_roster
          in: query
          required: false
          schema:
            type: boolean
            default: true
          description: Set false for observation-only continuation pages after the roster cursor is exhausted.
      responses:
        "200":
          description: Weighing scope roster rows.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WeighingRosterResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "500":
          $ref: "#/components/responses/ServerError"
  /app/weighing/campaigns/{campaign_id}/animal-observations:
    post:
      tags:
        - Weighing
      operationId: recordWeighingAnimalObservation
      summary: Record one proof-backed individual animal Weighing observation.
      parameters:
        - $ref: "#/components/parameters/WeighingCampaignId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RecordWeighingAnimalObservationRequest"
      responses:
        "200":
          description: Animal observation accepted or idempotently replayed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WeighingObservationResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "500":
          $ref: "#/components/responses/ServerError"
  /app/weighing/campaigns/{campaign_id}/shed-observations:
    post:
      tags:
        - Weighing
      operationId: recordWeighingShedObservation
      summary: Record one proof-backed shed/partition Weighing observation.
      parameters:
        - $ref: "#/components/parameters/WeighingCampaignId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RecordWeighingShedObservationRequest"
      responses:
        "200":
          description: Shed/partition observation accepted or idempotently replayed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WeighingObservationResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "500":
          $ref: "#/components/responses/ServerError"
  /app/weighing/alerts:
    get:
      tags:
        - Weighing
      operationId: appListWeighingAlerts
      summary: List the weighing lifecycle alerts routed to the caller.
      description: >
        The weighing module's OWN alerts feed: the work-state transitions of weighing --
        work assigned, a shed submitted for verification, a proof sent back for rework, a
        shed reopened, work closed -- each routed to whoever owns the next action.


        This is NOT the vaccination process-integrity feed at /alerts, and it is gated on
        weighing capabilities only, never ObligationRead/VaccinationRead.


        Rows are the durable notifications that were ALREADY routed to this caller when the
        transition happened, so the feed needs no second audience model: an operator reads
        only their own buckets because they were never a recipient of anyone else's.
        Weighing is free-flow and fully herd-isolated, so no alert names a goat, resolves a
        herd identity, or reports a share of an expected roster.


        title and empty_message are BACKEND-OWNED copy. Clients render them and hardcode no
        weighing strings of their own.
      parameters:
        - name: cursor
          in: query
          required: false
          description: Opaque cursor returned as next_cursor by the previous page.
          schema:
            type: string
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 50
            default: 20
      responses:
        "200":
          description: One page of the caller's weighing alerts, newest first.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WeighingAlertPageResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /app/weighing/weight-history:
    get:
      tags:
        - Weighing
      operationId: appGetWeighingWeightHistory
      summary: Get CEO-tier per-tag/per-shed weight history across weigh days.
      description: >
        Requires WeighingMonitor. Park-scoped exactly like the leadership growth read: a
        park-scoped monitor may only request a park inside their own grant (404 otherwise), and a
        tenant-wide monitor may request any park in the tenant. Both query parameters are
        optional filters, not a required pair. The response also returns the parks and sheds that
        are actually represented in the result, so a client can render filter chips with
        backend-owned vocabulary instead of a separately-fetched list.
      parameters:
        - name: park_id
          in: query
          required: false
          description: Restrict to one park. Must be a park the caller is authorized to monitor.
          schema:
            type: string
            format: uuid
        - name: campaign_shed_id
          in: query
          required: false
          description: Restrict to one shed.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Weight history series for the requested scope.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WeighingWeightHistoryResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "500":
          $ref: "#/components/responses/ServerError"
  /app/weighing/leadership/growth:
    get:
      tags:
        - Weighing
      operationId: appGetWeighingLeadershipGrowth
      summary: Get CEO-tier ADG (Average Daily Gain) / growth aggregate for a park or the herd.
      description: >
        Requires WeighingMonitor, park-scoped exactly like weight-history. `park_id` is optional;
        when omitted, the response aggregates across every park the caller is authorized to
        monitor (never widened) -- see `park_ids` in the response. `from`/`to` are INCLUSIVE
        Asia/Kolkata business dates (YYYY-MM-DD) and default to the last 90 days ending today when
        omitted. Weighing is free-flow: there is no weighing cadence rule, so no field here reports
        an "overdue" or "missed" weigh.
      parameters:
        - name: park_id
          in: query
          required: false
          description: >-
            The park to report on. When omitted, aggregates across the caller's own
            authorized-park scope.
          schema:
            type: string
            format: uuid
        - name: from
          in: query
          required: false
          schema:
            type: string
            format: date
        - name: to
          in: query
          required: false
          schema:
            type: string
            format: date
      responses:
        "200":
          description: Growth/ADG read model for the requested park or herd-wide scope.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WeighingGrowthADGResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "500":
          $ref: "#/components/responses/ServerError"
  /weighing/leadership/growth:
    get:
      tags:
        - Weighing
      operationId: adminGetWeighingLeadershipGrowth
      summary: Get CEO-tier ADG (Average Daily Gain) / growth aggregate for a park or the herd.
      description: >
        Requires WeighingMonitor, park-scoped exactly like weight-history. `park_id` is optional;
        when omitted, the response aggregates across every park the caller is authorized to
        monitor (never widened) -- see `park_ids` in the response. `from`/`to` are INCLUSIVE
        Asia/Kolkata business dates (YYYY-MM-DD) and default to the last 90 days ending today when
        omitted. Weighing is free-flow: there is no weighing cadence rule, so no field here reports
        an "overdue" or "missed" weigh.
      parameters:
        - name: park_id
          in: query
          required: false
          description: >-
            The park to report on. When omitted, aggregates across the caller's own
            authorized-park scope.
          schema:
            type: string
            format: uuid
        - name: from
          in: query
          required: false
          schema:
            type: string
            format: date
        - name: to
          in: query
          required: false
          schema:
            type: string
            format: date
      responses:
        "200":
          description: Growth/ADG read model for the requested park or herd-wide scope.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WeighingGrowthADGResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "500":
          $ref: "#/components/responses/ServerError"
  /app/weighing/shed-weights:
    get:
      tags:
        - Weighing
      operationId: appGetWeighingShedWeights
      summary: Latest weight per shed, across both ways of weighing, plus the whole-filter rollup.
      description: >
        Requires WeighingMonitor, park-scoped exactly like leadership/growth. `park_id` is
        optional; when omitted the response covers every park the caller is authorized to
        monitor, never wider. `from`/`to` are INCLUSIVE Asia/Kolkata business dates
        (YYYY-MM-DD) and default to the last 28 days ending today.


        GRAIN: one row per SHED, not per campaign bucket -- a shed weighed in consecutive
        campaigns reports its most recent weigh only. `summary` is a WHOLE-FILTER aggregate
        and never changes with paging.


        Weighing is free-flow and isolated from the herd, so no field here carries breed,
        sex, age or management stage, and no shed is ever reported "overdue": there is no
        weighing cadence.
      parameters:
        - name: park_id
          in: query
          required: false
          description: >-
            The park to report on. When omitted, covers the caller's own authorized-park scope.
          schema:
            type: string
            format: uuid
        - name: from
          in: query
          required: false
          schema:
            type: string
            format: date
        - name: to
          in: query
          required: false
          schema:
            type: string
            format: date
      responses:
        "200":
          description: Per-shed weights and the whole-filter summary.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WeighingShedWeightsResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "500":
          $ref: "#/components/responses/ServerError"
  /weighing/shed-weights:
    get:
      tags:
        - Weighing
      operationId: adminGetWeighingShedWeights
      summary: Latest weight per shed, across both ways of weighing, plus the whole-filter rollup.
      description: >
        Requires WeighingMonitor, park-scoped exactly like leadership/growth. `park_id` is
        optional; when omitted the response covers every park the caller is authorized to
        monitor, never wider. `from`/`to` are INCLUSIVE Asia/Kolkata business dates
        (YYYY-MM-DD) and default to the last 28 days ending today.


        GRAIN: one row per SHED, not per campaign bucket -- a shed weighed in consecutive
        campaigns reports its most recent weigh only. `summary` is a WHOLE-FILTER aggregate
        and never changes with paging.


        Weighing is free-flow and isolated from the herd, so no field here carries breed,
        sex, age or management stage, and no shed is ever reported "overdue": there is no
        weighing cadence.
      parameters:
        - name: park_id
          in: query
          required: false
          description: >-
            The park to report on. When omitted, covers the caller's own authorized-park scope.
          schema:
            type: string
            format: uuid
        - name: from
          in: query
          required: false
          schema:
            type: string
            format: date
        - name: to
          in: query
          required: false
          schema:
            type: string
            format: date
      responses:
        "200":
          description: Per-shed weights and the whole-filter summary.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WeighingShedWeightsResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "500":
          $ref: "#/components/responses/ServerError"
  /weighing/weight-demographics:
    get:
      tags: [Weighing]
      operationId: adminGetWeighingWeightDemographics
      summary: Average weight by breed, sex and management stage.
      description: >
        Requires WeighingMonitor, park-scoped like the other leadership reads.


        This is the ONE weighing read that resolves a scanned tag to its animal
        (maintainer decision 2026-08-07), because breed, sex and stage exist only on
        the animal. Read-only, this reporting path only, never a gate on capture: a
        tag that resolves to nothing is counted in `unresolved_animals`, not rejected.


        `by_breed` and `by_sex` cover per-animal weighs only. `by_stage` also covers
        whole-shed weighs, attributed by the shed's own cohort, which is why its total
        exceeds the other two -- see `lump_sum_animals`. A shed whose residents do not
        share one stage is attributed nowhere and appears in
        `lump_sum_unattributed_animals`.
      parameters:
        - {name: park_id, in: query, required: false, schema: {type: string, format: uuid}}
        - {name: from, in: query, required: false, schema: {type: string, format: date}}
        - {name: to, in: query, required: false, schema: {type: string, format: date}}
      responses:
        "200":
          description: Weight broken down by breed, sex and stage.
          content:
            application/json:
              schema: {$ref: "#/components/schemas/WeighingWeightDemographicsResponse"}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFoundOrNotAllowed"}
        "500": {$ref: "#/components/responses/ServerError"}
  /growth-director/weights:
    get:
      tags: [Weighing]
      operationId: adminGetGrowthDirectorWeights
      summary: Growth Director widgets for the admin-web Weights screen.
      description: >
        Requires WeighingMonitor, park-scoped exactly like the other Weights-screen
        reads. `park_id` is optional; when omitted the response covers every park the
        caller is authorized to monitor, never wider. `from`/`to` are INCLUSIVE
        Asia/Kolkata business dates (YYYY-MM-DD) and default to the last 28 days
        ending today.


        Served by its OWN read-only reporting module, not by weighing: weighing is
        isolated from the herd, while these widgets resolve scanned tags to breed
        and sex through the herd register and read the feed-direction sheet. This
        read gates nothing — a tag that resolves to no animal is counted and
        reported, never rejected.


        There is NO expected-animal roster; every denominator here is an
        actual-scan/identity count. `period.resolution` discloses that weighing
        data snaps to overlapping campaign weeks while feed uses the exact day
        range.
      parameters:
        - name: park_id
          in: query
          required: false
          description: >-
            The park to report on. When omitted, covers the caller's own authorized-park scope.
          schema:
            type: string
            format: uuid
        - {name: from, in: query, required: false, schema: {type: string, format: date}}
        - {name: to, in: query, required: false, schema: {type: string, format: date}}
      responses:
        "200":
          description: The six Growth Director widgets plus the period/park envelope.
          content:
            application/json:
              schema: {$ref: "#/components/schemas/GrowthDirectorWeightsResponse"}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFoundOrNotAllowed"}
        "500": {$ref: "#/components/responses/ServerError"}
  /app/weighing/leadership/sheds:
    get:
      tags:
        - Weighing
      operationId: appListWeighingLeadershipSheds
      summary: List leadership-visible Weighing shed buckets with their captured evidence.
      description: >
        ONE keyset page of shed buckets across tasks, each with its own context and its first page
        of captured evidence. The leadership gallery's own read: building this page client-side
        meant one request per bucket (about 1,500 on a 76-shed park) on every screen open.
      parameters:
        - name: cursor
          in: query
          required: false
          description: Opaque cursor returned as next_cursor by the previous page.
          schema:
            type: string
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
      responses:
        "200":
          description: One page of shed buckets with their evidence.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WeighingLeadershipShedPageResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /app/weighing/campaigns/{campaign_id}/sheds/{campaign_shed_id}/videos:
    get:
      tags:
        - Weighing
      operationId: appGetWeighingShedVideos
      summary: Get leadership-visible proof videos for one Weighing scope.
      description: >
        The shed's captured evidence, plus the shed's own context (park name, weigh business
        date, assignee display name) so a cold deep link to this surface renders without the
        parent list having to hand those over. `individual` is a keyset page on
        (accepted_at, observation_id); `lump_sum` is a single latest row and is not paged.
      parameters:
        - $ref: "#/components/parameters/WeighingCampaignId"
        - name: campaign_shed_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: cursor
          in: query
          required: false
          description: Opaque cursor returned as next_individual_cursor by the previous page.
          schema:
            type: string
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
      responses:
        "200":
          description: Shed proof media and observations.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WeighingShedVideosResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "500":
          $ref: "#/components/responses/ServerError"
  /app/weighing/campaigns/{campaign_id}/sheds/{campaign_shed_id}/submit:
    post:
      tags:
        - Weighing
      operationId: submitWeighingIndividualScope
      summary: Idempotently close one individual-animal Weighing scope after proof-backed capture.
      parameters:
        - $ref: "#/components/parameters/WeighingCampaignId"
        - name: campaign_shed_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SubmitWeighingIndividualScopeRequest"
      responses:
        "200":
          description: Scope completed or idempotently replayed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WeighingSubmitResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "500":
          $ref: "#/components/responses/ServerError"
  /app/weighing/campaigns/{campaign_id}/sheds/{campaign_shed_id}/close:
    post:
      tags:
        - Weighing
      operationId: closeWeighingScope
      summary: Close one Weighing scope after verification-approved evidence.
      parameters:
        - $ref: "#/components/parameters/WeighingCampaignId"
        - name: campaign_shed_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WeighingCloseRequest"
      responses:
        "200":
          description: Scope closed or idempotently replayed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WeighingCloseResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "500":
          $ref: "#/components/responses/ServerError"
  /app/weighing/campaigns/{campaign_id}/close:
    post:
      tags:
        - Weighing
      operationId: closeWeighingCampaign
      summary: Close every verified open scope in one Weighing campaign.
      parameters:
        - $ref: "#/components/parameters/WeighingCampaignId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WeighingCloseRequest"
      responses:
        "200":
          description: Campaign closed or idempotently replayed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WeighingCloseResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "500":
          $ref: "#/components/responses/ServerError"
  /app/weighing/observations/{observation_id}/weight-correction:
    post:
      tags:
        - Weighing
      operationId: correctWeighingObservationWeight
      summary: Correct the recorded weight on one weighing observation.
      description: >-
        The VERIFIER's correction of the number an operator typed, made while she watches
        the proof video (maintainer decision 2026-08-17). The corrected value REPLACES the
        recorded one: on an individual capture it replaces that one animal's weight, and on
        a lump-sum capture it replaces the shed total, optionally with a corrected head
        count, and the stored average is recomputed from both.


        Authorization is `verification.verdict` -- the verifier-exclusive capability that
        owns approve/reject -- so the person who judges the evidence is the person who may
        fix what it shows. It is deliberately NOT tied to her verdict: she may correct
        before deciding or after, including on an item she already approved, until the
        bucket is closed.


        The observation id and ref type are the values the verification item already carries
        in `source.ref_id` and `source.ref_type`; clients echo them rather than composing an
        address of their own.
      parameters:
        - name: observation_id
          in: path
          required: true
          description: The observation the verification item points at (`source.ref_id`).
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WeighingWeightCorrectionRequest"
      responses:
        "200":
          description: Weight corrected, or the same correction idempotently replayed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WeighingWeightCorrectionResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "500":
          $ref: "#/components/responses/ServerError"
  /weighing/process-state:
    get:
      tags:
        - Weighing
      operationId: getWeighingProcessState
      summary: Get Calendar and Control Tower process state for Weighing.
      parameters:
        - name: campaign_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: from
          in: query
          required: false
          schema:
            type: string
            format: date
        - name: to
          in: query
          required: false
          schema:
            type: string
            format: date
      responses:
        "200":
          description: Weighing process state.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WeighingProcessState"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /app/devices/register:
    post:
      tags:
        - Operator App Bootstrap
      operationId: registerAppDevice
      summary: Register or refresh an Android app install for the authenticated operator.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegisterDeviceRequest"
      responses:
        "200":
          description: Registered device.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeviceResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /app/devices/{device_id}/heartbeat:
    post:
      tags:
        - Operator App Bootstrap
      operationId: heartbeatAppDevice
      summary: Update Android device heartbeat and app version state.
      parameters:
        - $ref: "#/components/parameters/DeviceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HeartbeatDeviceRequest"
      responses:
        "200":
          description: Device heartbeat accepted.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeviceResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /app/devices/{device_id}/deregister:
    post:
      tags:
        - Operator App Bootstrap
      operationId: deregisterAppDevice
      summary: Deregister the authenticated operator's own device on logout (decouples the FCM push binding).
      description: >
        Self-service logout decouple. Clears the stored FCM push token binding and revokes the
        caller's OWN device (scoped by the device's registered_by = actor), so a signed-out user
        stops receiving pushes on that device. Idempotent for an already-revoked device the actor
        owns; an unknown device, or a device registered by another operator, returns 404.
      parameters:
        - $ref: "#/components/parameters/DeviceId"
      responses:
        "200":
          description: Device deregistered (push binding cleared, device revoked).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeviceResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: No such device registered by this operator.
  /app/tasks:
    get:
      tags:
        - Operator Tasks
      operationId: listAppTasks
      summary: List assigned operator tasks.
      parameters:
        - $ref: "#/components/parameters/AcceptLanguage"
        - $ref: "#/components/parameters/GoatOSLocale"
        - name: state
          in: query
          required: false
          schema:
            type: string
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 20
            default: 20
        - name: cursor
          in: query
          required: false
          description: Opaque keyset cursor returned as next_cursor by the previous task page.
          schema:
            type: string
            minLength: 1
            maxLength: 512
      responses:
        "200":
          description: Assigned tasks.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TaskListResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /app/tasks/{task_id}:
    get:
      tags:
        - Operator Tasks
      operationId: getAppTask
      summary: Get task detail and pinned SOP version.
      parameters:
        - $ref: "#/components/parameters/TaskId"
        - $ref: "#/components/parameters/AcceptLanguage"
        - $ref: "#/components/parameters/GoatOSLocale"
      responses:
        "200":
          description: Task detail.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TaskResponse"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
  /app/tasks/{task_id}/shed-completion-summary:
    get:
      tags:
        - Operator Tasks
      operationId: getShedCompletionSummary
      summary: >-
        Read-only vaccination shed-completion / submit acknowledgement summary. Shed completion
        is NOT a manual form: the operator already scanned every animal and attached camera proof
        at the animal level; this summary reports whether the shed is ready to acknowledge.
      parameters:
        - $ref: "#/components/parameters/TaskId"
        - name: shed_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
          description: Optional operational shed id for partition-scoped shed completion summaries.
        - name: partition_label
          in: query
          required: false
          schema:
            type: string
          description: Optional partition label for partitioned sheds; blank/omitted means whole shed.
      responses:
        "200":
          description: Shed completion summary.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ShedCompletionSummary"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
  /app/sop-versions/{sop_version_id}:
    get:
      tags:
        - Operator Tasks
      operationId: getAppSOPVersion
      summary: Download a pinned SOP version.
      parameters:
        - $ref: "#/components/parameters/SOPVersionId"
      responses:
        "200":
          description: SOP version.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SOPVersionResponse"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
  /app/proofs/uploads:
    post:
      tags:
        - Proof Media
      operationId: createProofUpload
      summary: Create a server-owned proof record and signed upload target.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateProofUploadRequest"
      responses:
        "201":
          description: Proof upload target.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateProofUploadResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /app/proofs/{proof_id}/upload:
    put:
      tags:
        - Proof Media
      operationId: uploadProofLocal
      summary: Local-dev signed proof upload target.
      parameters:
        - $ref: "#/components/parameters/ProofId"
        - name: expires
          in: query
          required: true
          schema:
            type: string
        - name: tenant_id
          in: query
          required: true
          schema:
            type: string
            format: uuid
        - name: sig
          in: query
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/octet-stream:
            schema:
              type: string
              format: binary
      responses:
        "200":
          description: Completed local proof upload.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProofResponse"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "409":
          $ref: "#/components/responses/WriteConflict"
  /app/proofs/{proof_id}/complete:
    post:
      tags:
        - Proof Media
      operationId: completeProofUpload
      summary: Finalize a proof upload with captured metadata.
      parameters:
        - $ref: "#/components/parameters/ProofId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CompleteProofUploadRequest"
      responses:
        "200":
          description: Completed proof record.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProofResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "500":
          $ref: "#/components/responses/ServerError"
  /app/proofs/{proof_id}/download:
    get:
      tags:
        - Proof Media
      operationId: downloadProof
      summary: Get or follow a signed proof download URL.
      parameters:
        - $ref: "#/components/parameters/ProofId"
        - name: expires
          in: query
          required: false
          schema:
            type: string
        - name: sig
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: Signed download URL for non-local clients.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DownloadProofResponse"
        "307":
          description: Redirect to signed storage URL.
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
  /app/tasks/{task_id}/submissions:
    post:
      tags:
        - Operator Tasks
      operationId: submitAppTask
      summary: Submit an idempotent SOP task response.
      parameters:
        - $ref: "#/components/parameters/TaskId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SubmitTaskRequest"
      responses:
        "200":
          description: Submission result.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SubmissionResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "409":
          $ref: "#/components/responses/WriteConflict"
  /goats/search:
    get:
      tags:
        - Goats
      operationId: searchGoats
      summary: Search visible goat passports by identifier, display ID, location, status, breed, or sex.
      parameters:
        - $ref: "#/components/parameters/Limit"
        - $ref: "#/components/parameters/Cursor"
        - name: q
          in: query
          required: false
          schema:
            type: string
            minLength: 1
            maxLength: 200
        - name: goat_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: identifier_type
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/IdentifierType"
        - name: scope_key
          in: query
          required: false
          schema:
            type: string
            minLength: 1
            maxLength: 120
        - name: breed
          in: query
          required: false
          schema:
            type: string
            minLength: 1
            maxLength: 120
        - name: sex
          in: query
          required: false
          schema:
            type: string
            minLength: 1
            maxLength: 40
        - name: farm_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: park_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: location_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: status
          in: query
          required: false
          schema:
            type: string
            minLength: 1
            maxLength: 80
      responses:
        "200":
          description: Scope-filtered goat search results.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoatSearchResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /goats/{goat_id}:
    get:
      tags:
        - Goats
      operationId: getGoatPassport
      summary: Get a scope-filtered goat passport.
      parameters:
        - $ref: "#/components/parameters/GoatId"
      responses:
        "200":
          description: Goat passport detail.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoatPassportResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
  /goats/{goat_id}/timeline:
    get:
      tags:
        - Goats
      operationId: getGoatTimeline
      summary: Get a scope-filtered goat identity timeline.
      parameters:
        - $ref: "#/components/parameters/GoatId"
        - $ref: "#/components/parameters/Limit"
        - $ref: "#/components/parameters/Cursor"
      responses:
        "200":
          description: Goat identity timeline.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GoatTimelineResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
  /identifiers/{type}/{value}/resolve:
    get:
      tags:
        - Identifiers
      operationId: resolveIdentifier
      summary: Resolve an identifier under explicit scope context when needed.
      parameters:
        - name: type
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/IdentifierType"
        - name: value
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            maxLength: 200
        - name: scope_key
          in: query
          required: false
          schema:
            type: string
            minLength: 1
            maxLength: 120
        - name: farm_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: park_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: location_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Identifier resolution result.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ResolveIdentifierResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /protocols:
    get:
      tags:
        - Protocol Config
      operationId: listProtocolConfigs
      summary: List protocol config versions for a category (Config authority screen).
      description: >-
        Tenant- and category-scoped list of every protocol version (draft, published, retired) with
        rule-row count, linked SOP, effective window, scope, and publisher metadata.
        Read-only; executable readiness is enforced on publish.
      parameters:
        - name: category
          in: query
          required: false
          schema:
            type: string
          description: Protocol category to list (defaults to vaccination — today's visible slice).
      responses:
        "200":
          description: Protocol config versions.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProtocolConfigListResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
        - Protocol Config
      operationId: createProtocolDefinition
      summary: Create a protocol definition.
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateProtocolDefinitionRequest"
      responses:
        "201":
          description: Protocol definition created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateProtocolDefinitionResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "500":
          $ref: "#/components/responses/ServerError"
  /protocols/animal-stages:
    get:
      tags:
        - Protocol Config
      operationId: listAnimalStages
      summary: List active animal-stage reference data (Config authoring stage picker).
      description: >-
        Tenant-scoped active rows of animal_stage_lookup (e.g. K1 ≈ milk training, K2 ≈ milk
        drinking), in display order. Drives the Config authoring stage picker so stage bands are
        backend-driven and never hardcoded frontend literals (Preventive Care (PC) vaccination TRD). Read-only; an
        empty list is honest (no stages seeded) — the UI shows a seed-stages state, not fallback codes.
      responses:
        "200":
          description: Active animal stages.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AnimalStageListResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /protocols/{protocol_id}/versions:
    post:
      tags:
        - Protocol Config
      operationId: createProtocolVersion
      summary: Create a draft protocol version.
      parameters:
        - $ref: "#/components/parameters/ProtocolId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateProtocolVersionRequest"
      responses:
        "201":
          description: Protocol version created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateProtocolVersionResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "500":
          $ref: "#/components/responses/ServerError"
  /protocols/versions/{version_id}/rules:
    post:
      tags:
        - Protocol Config
      operationId: addProtocolRule
      summary: Add a dose/phase rule to a draft protocol version.
      parameters:
        - $ref: "#/components/parameters/ProtocolVersionId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddProtocolRuleRequest"
      responses:
        "201":
          description: Protocol rule created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AddProtocolRuleResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "500":
          $ref: "#/components/responses/ServerError"
  /protocols/versions/{version_id}:
    get:
      tags:
        - Protocol Config
      operationId: getProtocolVersion
      summary: Get a protocol version.
      parameters:
        - $ref: "#/components/parameters/ProtocolVersionId"
      responses:
        "200":
          description: Protocol version.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProtocolVersionResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "500":
          $ref: "#/components/responses/ServerError"
  /protocols/versions/{version_id}/publish:
    post:
      tags:
        - Protocol Config
      operationId: publishProtocolVersion
      summary: Publish a protocol version.
      parameters:
        - $ref: "#/components/parameters/ProtocolVersionId"
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "204":
          description: Protocol version published.
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "500":
          $ref: "#/components/responses/ServerError"
  /protocols/vaccination/impact-preview:
    post:
      tags:
        - Vaccination
      operationId: vaccinationImpactPreview
      summary: Aggregate config impact preview (eligible animals, vaccination cells, affected sheds, estimated days) from the eligibility rollup read model.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ImpactPreviewInput"
      responses:
        "200":
          description: Vaccination impact preview.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ImpactPreviewResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /action-center/obligations:
    get:
      tags:
        - Action Center
      operationId: listActionCenterObligations
      summary: Top-level Action Center process-integrity rows across supported categories.
      parameters:
        - name: category
          in: query
          required: false
          description: Optional top-level command-lens category filter. Omit to include every supported category.
          schema:
            type: string
            enum:
              - vaccination
              - feed_direction
        - name: park_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: as_of
          in: query
          required: false
          description: Reconstruct process state as of this instant (top-bar date scope). Defaults to now.
          schema:
            type: string
            format: date-time
        - name: shed_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: status
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/WorkState"
          description: Current compatibility alias for work_state.
        - name: work_state
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/WorkState"
        - name: severity
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/ProcessIntegritySeverity"
        - name: owner_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: protocol_version_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: due_after
          in: query
          required: false
          schema:
            type: string
            format: date-time
        - name: due_before
          in: query
          required: false
          schema:
            type: string
            format: date-time
        - name: cursor
          in: query
          required: false
          schema:
            type: string
            minLength: 1
            maxLength: 512
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 500
        - name: cursor
          in: query
          required: false
          description: Opaque pagination cursor returned as next_cursor by the previous response.
          schema:
            type: string
      responses:
        "200":
          description: Action Center rows.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ActionCenterResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /vaccination/action-center:
    get:
      tags:
        - Action Center
      operationId: listVaccinationActionCenter
      summary: List actionable vaccination process-integrity rows with server-side filters and keyset pagination.
      parameters:
        - name: park_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: as_of
          in: query
          required: false
          description: Reconstruct process state as of this instant (top-bar date scope). Defaults to now.
          schema:
            type: string
            format: date-time
        - name: shed_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: work_state
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/WorkState"
        - name: severity
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/ProcessIntegritySeverity"
        - name: owner_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: protocol_version_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: due_after
          in: query
          required: false
          schema:
            type: string
            format: date-time
        - name: due_before
          in: query
          required: false
          schema:
            type: string
            format: date-time
        - name: cursor
          in: query
          required: false
          schema:
            type: string
            minLength: 1
            maxLength: 512
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 500
      responses:
        "200":
          description: Vaccination Action Center rows.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ActionCenterResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /vaccination/action-center/counts:
    get:
      tags:
        - Action Center
      operationId: countVaccinationActionCenter
      summary: Count vaccination process-integrity rows by work state without returning row payloads.
      parameters:
        - name: park_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: as_of
          in: query
          required: false
          description: Reconstruct process state as of this instant (top-bar date scope). Defaults to now.
          schema:
            type: string
            format: date-time
        - name: shed_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: work_state
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/WorkState"
        - name: severity
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/ProcessIntegritySeverity"
        - name: owner_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: protocol_version_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: due_after
          in: query
          required: false
          schema:
            type: string
            format: date-time
        - name: due_before
          in: query
          required: false
          schema:
            type: string
            format: date-time
      responses:
        "200":
          description: Vaccination Action Center work-state counts.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ActionCenterCountsResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /vaccination/adherence:
    get:
      tags:
        - Protocol Adherence
      operationId: getVaccinationProtocolAdherence
      summary: Read expected-vs-actual vaccination protocol adherence rows from process integrity.
      parameters:
        - name: park_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: as_of
          in: query
          required: false
          description: Reconstruct process state as of this instant (top-bar date scope). Defaults to now.
          schema:
            type: string
            format: date-time
        - name: shed_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: work_state
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/WorkState"
        - name: severity
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/ProcessIntegritySeverity"
        - name: owner_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: protocol_version_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: due_after
          in: query
          required: false
          schema:
            type: string
            format: date-time
        - name: due_before
          in: query
          required: false
          schema:
            type: string
            format: date-time
        - name: cursor
          in: query
          required: false
          schema:
            type: string
            minLength: 1
            maxLength: 512
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 500
      responses:
        "200":
          description: Vaccination Protocol Adherence ledger.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProtocolAdherenceResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /control-tower/vaccination:
    get:
      tags:
        - Control Tower
      operationId: getVaccinationControlTower
      summary: Summarize broken or at-risk vaccination process integrity for Control Tower.
      parameters:
        - name: park_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: as_of
          in: query
          required: false
          description: Reconstruct process state as of this instant (top-bar date scope). Defaults to now.
          schema:
            type: string
            format: date-time
        - name: shed_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: due_before
          in: query
          required: false
          schema:
            type: string
            format: date-time
        - name: work_state
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/WorkState"
        - name: severity
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/ProcessIntegritySeverity"
        - name: cursor
          in: query
          required: false
          schema:
            type: string
            minLength: 1
            maxLength: 512
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 500
      responses:
        "200":
          description: Vaccination Control Tower summary and alerts.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ControlTowerResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /workflows/{row_id}:
    get:
      tags:
        - Workflows
      operationId: getWorkflowDrilldown
      summary: Explain one top-level process chain or exception workflow.
      parameters:
        - name: row_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            maxLength: 256
        - name: category
          in: query
          required: false
          schema:
            type: string
            enum:
              - vaccination
              - feed_direction
        - name: due_before
          in: query
          required: false
          schema:
            type: string
            format: date-time
      responses:
        "200":
          description: Workflow drilldown.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkflowDrilldownResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "500":
          $ref: "#/components/responses/ServerError"
  /vaccination/workflows/{row_id}:
    get:
      tags:
        - Workflows
      operationId: getVaccinationWorkflowDrilldown
      summary: Explain one vaccination process chain from config through completion.
      parameters:
        - name: row_id
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            maxLength: 256
        - name: due_before
          in: query
          required: false
          schema:
            type: string
            format: date-time
      responses:
        "200":
          description: Vaccination workflow drilldown.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkflowDrilldownResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "500":
          $ref: "#/components/responses/ServerError"
  /vaccination/command:
    get:
      tags:
        - Vaccination
      operationId: getVaccinationCommandBoard
      summary: CEO closure view — KPIs, cohort matrix, shed dose matrix, weekly given, verification queue.
      parameters:
        - name: drive_batch_id
          in: query
          required: false
          description: >-
            Optional: narrow KPIs to this drive batch. When absent, KPIs aggregate all-history.
          schema:
            type: string
            format: uuid
        - name: park_id
          in: query
          required: false
          description: >-
            Optional: narrow every board section to sheds under this park. When absent, all parks in scope.
          schema:
            type: string
            format: uuid
        - name: as_of
          in: query
          required: false
          description: >-
            Scope instant (defaults to now in business timezone). Past values are supported for historical
            closure review; future values clamp to now.
          schema:
            type: string
            format: date-time
        - name: drive_park_id
          in: query
          required: false
          description: >-
            Park of the drive named by `drive_batch_id`. Narrows the board SECTIONS to that park's
            share of the drive — a batch can span parks, so a selected drive is one park's operator
            day — while `driveOptions` stays at `park_id`'s scope so the other parks' drives remain
            selectable. Without it a caller wanting both had to send two requests, one purely to
            keep its picker. Ignored unless `drive_batch_id` is present; authorized exactly like
            `park_id`.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: CEO vaccination command board — KPIs, matrices, charts, and queue.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VaccinationCommandBoardResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /vaccination/live-tracker:
    get:
      tags:
        - Vaccination
      operationId: getVaccinationLiveTracker
      summary: Live drive-day tracker — KPIs, operator board, shed proof progress, combo doses, activity feed, attention, verification.
      description: >-
        One read backing the whole live drive tracker. Every section derives from a single membership
        set (the day's protocol-category 'vaccination' obligations at ADMINISTRATION grain), so the
        KPI tiles reconcile with the tables beneath them.

        Filter scope differs by filter kind and this is deliberate. park_id, shed_id,
        partition_label, operator_id and vaccine_code are MEMBERSHIP predicates pushed into that one
        CTE, so they narrow every section including the combo card and the activity feed. `status`
        is a DERIVED row state (computed from counts plus an elapsed clock), so it narrows only what
        is folded from those rows: the KPI tiles, the operator board, the shed board and the
        attention list. The combo card and the activity feed always describe the whole drive day.
      parameters:
        - name: business_date
          in: query
          required: false
          description: >-
            Drive day in the business timezone (defaults to today). This is a BUSINESS DATE, not a
            projection as_of: the response is always reconstructed from canonical rows, so a past
            drive day is a supported read. Bounded to the last seven days.
          schema:
            type: string
            format: date
        - name: park_id
          in: query
          required: false
          description: Optional park narrowing. Clamped server-side to the caller's vaccination park grants.
          schema:
            type: string
            format: uuid
        - name: shed_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: partition_label
          in: query
          required: false
          description: >-
            Partition within the shed. Normalised server-side ("Part 3" and "3" are the same
            partition) because the goat-side and assignment-side tables spell them differently.
          schema:
            type: string
            maxLength: 64
        - name: operator_id
          in: query
          required: false
          description: workforce_members.workforce_member_id of the drive operator.
          schema:
            type: string
            format: uuid
        - name: vaccine_code
          in: query
          required: false
          description: Antigen family code from filter_options.vaccines (for example goat_pox).
          schema:
            type: string
            maxLength: 64
        - name: status
          in: query
          required: false
          description: >-
            Derived row state. Narrows the KPI tiles, the operator board, the shed board and the
            attention list. It does NOT narrow the combo card or the activity feed, which always
            describe the whole drive day.
          schema:
            type: string
            enum: [active, done, pending, review]
        - name: activity_limit
          in: query
          required: false
          description: >-
            Both bounds are enforced with 400 invalid_activity_limit. A value above the maximum is
            REFUSED, not silently clamped: answering a clamped page with HTTP 200 tells a paging
            client the limit it asked for was honoured.
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 40
        - name: activity_before
          in: query
          required: false
          description: >-
            Timestamp half of the activity feed's keyset cursor. Send it together with
            activity_before_id — occurred_at alone is NOT a key, because burst-written scan captures
            and scan attempts routinely share a timestamp and a strict comparison skips every event
            tied with the previous page's last row.
          schema:
            type: string
            format: date-time
        - name: activity_before_id
          in: query
          required: false
          description: >-
            Tiebreaker half of the activity feed's keyset cursor — the next_cursor_event_id returned
            with the previous page. Omitting it drops events sharing the page boundary's timestamp.
          schema:
            type: string
            maxLength: 128
      responses:
        "200":
          description: The live drive tracker for one business date.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VaccinationLiveTrackerResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /vaccination/operations:
    get:
      tags:
        - Vaccination
      operationId: getVaccinationOperations
      summary: Configured vaccination operations — cohort × protocol matrix + per-cohort detail.
      parameters:
        - name: park_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: as_of
          in: query
          required: false
          description: >-
            Current-view scope only (top-bar date). Defaults to now; future values clamp to now. A past
            instant is rejected with 400 historical_as_of_unsupported — this read keeps only the current
            serving projection, so historical point-in-time reconstruction is not supported.
          schema:
            type: string
            format: date-time
        - name: due_before
          in: query
          required: false
          schema:
            type: string
            format: date-time
        - name: cursor
          in: query
          required: false
          description: Opaque pagination cursor returned as next_cursor by the previous response.
          schema:
            type: string
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 500
      responses:
        "200":
          description: Vaccination operations matrix + per-cohort detail.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VaccinationOperationsResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /vaccination/schedule:
    get:
      tags:
        - Vaccination
      operationId: getVaccinationSchedule
      summary: Canonical vaccination Full Schedule for one business-calendar month.
      parameters:
        - name: park_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: year
          in: query
          required: false
          description: Calendar year in the business timezone. Defaults to the current year.
          schema:
            type: integer
            minimum: 2000
            maximum: 2100
        - name: month
          in: query
          required: false
          description: Calendar month in the business timezone. Defaults to the current month.
          schema:
            type: integer
            minimum: 1
            maximum: 12
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 500
        - name: cursor
          in: query
          required: false
          description: Opaque pagination cursor returned as next_cursor by the previous schedule response.
          schema:
            type: string
      responses:
        "200":
          description: Vaccination schedule matrix + per-cohort detail for the materialized month.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VaccinationOperationsResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "503":
          $ref: "#/components/responses/ServerError"
  /vaccination/schedule/drive-date-overrides:
    post:
      tags:
        - Vaccination
      operationId: upsertVaccinationDriveDateOverride
      summary: Postpone one vaccine within a scheduled vaccination drive.
      description: >
        CEO/CXO vaccination campaign command that records a bounded sidecar override for one
        vaccine code in one park drive date. The sweeper treats the override as a proposed
        planned date only; max two vaccines per animal session, live/killed spacing, adult booster
        rules, the +1 week medical window, operator cap, and shed/partition assignment constraints
        are still enforced by the backend planner.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [park_id, vaccine_code, original_drive_date, override_date, reason]
              properties:
                park_id:
                  type: string
                  format: uuid
                vaccine_code:
                  type: string
                  minLength: 1
                original_drive_date:
                  type: string
                  format: date
                override_date:
                  type: string
                  format: date
                  description: Must be after original_drive_date.
                reason:
                  type: string
                  minLength: 1
      responses:
        "200":
          description: Active drive date override.
          content:
            application/json:
              schema:
                type: object
                required: [park_id, vaccine_code, original_drive_date, override_date, reason, created_by, created_at]
                properties:
                  park_id:
                    type: string
                    format: uuid
                  vaccine_code:
                    type: string
                  original_drive_date:
                    type: string
                    format: date
                  override_date:
                    type: string
                    format: date
                  reason:
                    type: string
                  created_by:
                    type: string
                    format: uuid
                  created_at:
                    type: string
                    format: date-time
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /vaccination/drive-assignments:
    get:
      tags:
        - Vaccination
      operationId: listVaccinationDriveAssignments
      summary: Persisted operator-cap vaccination drive assignments for one business-calendar month.
      parameters:
        - name: park_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: year
          in: query
          required: false
          description: Calendar year in the business timezone. Defaults to the current year.
          schema:
            type: integer
            minimum: 2000
            maximum: 2100
        - name: month
          in: query
          required: false
          description: Calendar month in the business timezone. Defaults to the current month.
          schema:
            type: integer
            minimum: 1
            maximum: 12
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 500
      responses:
        "200":
          description: Operator/date/shed/partition assignment rows.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VaccinationDriveAssignmentResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "503":
          $ref: "#/components/responses/ServerError"
  /vaccination/execution:
    get:
      tags:
        - Vaccination
      operationId: listVaccinationExecution
      summary: List vaccination execution rows grouped by park and shed context.
      parameters:
        - name: park_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: as_of
          in: query
          required: false
          description: >-
            Current-view scope only (top-bar date). Defaults to now; future values clamp to now.
            A past instant is rejected with 400 historical_as_of_unsupported — these reads keep only
            the current serving projection, so historical point-in-time reconstruction is not supported.
          schema:
            type: string
            format: date-time
        - name: work_state
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/VaccinationExecutionWorkState"
        - name: due_before
          in: query
          required: false
          schema:
            type: string
            format: date-time
        - name: cursor
          in: query
          required: false
          description: Opaque pagination cursor returned as nextCursor by the previous response.
          schema:
            type: string
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 500
      responses:
        "200":
          description: Vaccination execution rows.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VaccinationExecutionResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /vaccination/execution/sheds/{shed_id}:
    get:
      tags:
        - Vaccination
      operationId: getVaccinationExecutionShedDrilldown
      summary: Get vaccination execution context for one shed.
      parameters:
        - $ref: "#/components/parameters/ShedId"
        - name: partition_label
          in: query
          required: false
          description: Raw operational partition label within the physical shed. Omit only for an undivided shed or an intentional all-partitions read.
          schema:
            type: string
        - name: as_of
          in: query
          required: false
          description: >-
            Current-view scope only (top-bar date). Defaults to now; future values clamp to now.
            A past instant is rejected with 400 historical_as_of_unsupported — these reads keep only
            the current serving projection, so historical point-in-time reconstruction is not supported.
          schema:
            type: string
            format: date-time
        - name: due_before
          in: query
          required: false
          schema:
            type: string
            format: date-time
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 500
      responses:
        "200":
          description: Vaccination execution shed drilldown.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VaccinationExecutionShedDrilldown"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "500":
          $ref: "#/components/responses/ServerError"
  /vaccination/sheds:
    get:
      tags:
        - Vaccination
      operationId: listVaccinationShedSummary
      summary: List shed-wise vaccination summaries with optional filtering and pagination.
      parameters:
        - name: park_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: shed_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: status
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/VaccinationShedStatus"
        - name: capacity
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/VaccinationCapacityStatus"
        - name: q
          in: query
          required: false
          description: Search query string (park or shed name).
          schema:
            type: string
        - name: sort
          in: query
          required: false
          description: Sort key.
          schema:
            $ref: "#/components/schemas/VaccinationShedSortKey"
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 200
        - name: offset
          in: query
          required: false
          schema:
            type: integer
            minimum: 0
        - name: page
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
        - name: as_of
          in: query
          required: false
          description: >-
            Current-view scope only. Defaults to now; future values clamp to now. A past instant is
            rejected with 400 historical_as_of_unsupported — these reads keep only the current serving
            projection, so historical point-in-time reconstruction is not supported.
          schema:
            type: string
            format: date-time
      responses:
        "200":
          description: Shed-wise vaccination summaries.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VaccinationShedSummaryResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /vaccination/sheds/{shed_id}:
    get:
      tags:
        - Vaccination
      operationId: getVaccinationShedDetail
      summary: Get vaccination detail for one shed with per-vaccine obligation counts.
      parameters:
        - $ref: "#/components/parameters/ShedId"
        - name: as_of
          in: query
          required: false
          description: >-
            Current-view scope only. Defaults to now; future values clamp to now. A past instant is
            rejected with 400 historical_as_of_unsupported — these reads keep only the current serving
            projection, so historical point-in-time reconstruction is not supported.
          schema:
            type: string
            format: date-time
      responses:
        "200":
          description: Vaccination shed detail with vaccine breakdown.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VaccinationShedDetail"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "500":
          $ref: "#/components/responses/ServerError"
  /vaccination/sheds/{shed_id}/animals:
    get:
      tags:
        - Vaccination
      operationId: getVaccinationShedAnimals
      summary: Get keyset-paginated roster of animals in a shed for vaccination detail.
      parameters:
        - $ref: "#/components/parameters/ShedId"
        - name: cursor
          in: query
          required: false
          description: Keyset cursor (last goat_id seen, exclusive). Omit to start from beginning.
          schema:
            type: string
            format: uuid
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 200
      responses:
        "200":
          description: Keyset-paginated shed animal roster.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VaccinationShedAnimalPage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "500":
          $ref: "#/components/responses/ServerError"
  /vaccination/capacity-config:
    get:
      tags:
        - Vaccination
      operationId: getVaccinationCapacityConfig
      summary: Read the tenant's daily operator animal capacity config for the admin Config screen.
      responses:
        "200":
          description: The tenant's daily operator animal capacity config.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VaccinationCapacityConfig"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
    put:
      tags:
        - Vaccination
      operationId: putVaccinationCapacityConfig
      summary: >-
        Write the tenant's daily operator animal cap + per-animal shot-cap override
        (validate-or-reject; optimistic concurrency via rowVersion). A successful write cascades
        vaccination.capacity.changed (one per active park) which re-plans future vaccination drives.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateVaccinationCapacityConfigRequest"
      responses:
        "200":
          description: The updated capacity config.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VaccinationCapacityConfig"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: The supplied rowVersion no longer matches the stored config (concurrent edit).
        "500":
          $ref: "#/components/responses/ServerError"
  /vaccination/operator-assignment/config:
    get:
      tags:
        - Vaccination
      operationId: getVaccinationOperatorAssignmentConfig
      summary: >-
        Phase 1 CONFIG-ONLY: read a park's N-active-operators-per-day + default-operator config plus
        every operator's authored shift, for the admin Config screen. Not yet consumed by the drive
        scheduler (Phase 5).
      description: >-
        park_id is OPTIONAL: omit it and the backend resolves the caller's single authorized park
        scope and echoes it back as parkId, which is the park the client must scope its roster reads
        to. An actor whose grants do not resolve to exactly one park (for example a tenant-wide CEO
        grant in a multi-park tenant) receives 409 park_scope_ambiguous and must pass park_id
        explicitly -- the client must never blend several parks' rosters into one view.
      parameters:
        - name: park_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: The park's operator assignment config + shifts.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VaccinationOperatorAssignmentConfig"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "409":
          description: >-
            The caller's scope does not resolve to exactly one park. The body carries the parks the
            caller may choose from so the client renders a backend-owned selector and re-requests with
            park_id; it must never assemble or label a park list of its own.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VaccinationParkScopeAmbiguous"
        "500":
          $ref: "#/components/responses/ServerError"
    put:
      tags:
        - Vaccination
      operationId: putVaccinationOperatorAssignmentConfig
      summary: >-
        Phase 1 CONFIG-ONLY: write a park's active-operators-per-day + default-operator config
        (validate-or-reject; optimistic concurrency via rowVersion). Not yet consumed by the drive
        scheduler (Phase 5).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateVaccinationOperatorAssignmentConfigRequest"
      responses:
        "200":
          description: The updated operator assignment config.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VaccinationOperatorAssignmentConfig"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: The supplied rowVersion no longer matches the stored config (concurrent edit).
        "500":
          $ref: "#/components/responses/ServerError"
  /feed-direction/generation-preview:
    get:
      tags:
        - Feed Direction
      operationId: getFeedDirectionGenerationPreview
      summary: Preview Feed Direction generation inputs from the bounded Counts/Shifting projection.
      description: >-
        Returns Feed Direction's read-only view of Counts/Shifting projected_count_for rows and
        aggregate shed+breed totals. The preview surfaces pregnant/lactating/warm-up shifted-cohort
        ration blockers, destination shortages, unsafe surplus, and unresolved ration context before
        generation. Generation is allowed only when the returned page carries no blockers.
      parameters:
        - name: park_id
          in: query
          required: true
          schema:
            type: string
            format: uuid
        - name: target_date
          in: query
          required: true
          description: Feed projection date in YYYY-MM-DD form.
          schema:
            type: string
            format: date
        - name: shed_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: breed_key
          in: query
          required: false
          schema:
            type: string
        - name: cursor
          in: query
          required: false
          schema:
            type: string
          description: Opaque keyset cursor returned by the previous page.
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 200
          description: Defaults to 50.
      responses:
        "200":
          description: Fail-closed Feed Direction generation preview.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FeedDirectionGenerationPreviewResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /feed-direction/counts-projection/exceptions:
    get:
      tags:
        - Feed Direction
      operationId: listFeedDirectionCountsProjectionExceptions
      summary: List bounded Counts/Shifting projection exceptions for Feed Direction review.
      description: >-
        Returns owner-visible Counts/Shifting exception work for Feed Direction before any resolve
        or dismiss action. This queue is for reviewed closure of blockers such as pregnant shifted
        cohorts with unresolved destination ration context, count mismatches, alias conflicts,
        unsafe surplus, or query-plan proof gaps. It does not allow generation by itself.
      parameters:
        - name: status
          in: query
          schema:
            type: string
            enum:
              - open
              - resolved
              - dismissed
          description: Defaults to open.
        - name: park_id
          in: query
          schema:
            type: string
            format: uuid
        - name: shed_id
          in: query
          schema:
            type: string
            format: uuid
        - name: exception_type
          in: query
          schema:
            type: string
            enum:
              - missing_base_count
              - missing_structured_impact
              - unreported_shifting
              - count_mismatch
              - alias_conflict
              - ration_context_unresolved
              - destination_shortage
              - unsafe_surplus
              - query_plan_unproven
        - name: severity
          in: query
          schema:
            type: string
            enum:
              - warning
              - blocking
              - critical
        - name: owner_ref
          in: query
          schema:
            type: string
        - name: work_state
          in: query
          schema:
            type: string
            enum:
              - blocked
              - resolved
              - dismissed
        - name: cursor
          in: query
          schema:
            type: string
          description: Opaque keyset cursor returned by the previous page.
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 200
          description: Defaults to 50.
      responses:
        "200":
          description: Bounded Counts/Shifting projection exception queue.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FeedDirectionCountsProjectionExceptionListResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /feed-direction/counts-projection/exceptions/{exception_id}/resolve:
    post:
      tags:
        - Feed Direction
      operationId: resolveFeedDirectionCountsProjectionException
      summary: Resolve a Counts/Shifting projection exception after reviewed feed-safety closure.
      description: >-
        Closes an open Counts/Shifting projection exception through the Feed Direction surface with
        actor, reason, optional source reference, and idempotent replay protection. This does not
        make Feed generation ready by itself; the next projection recompute must still prove the
        underlying shed/cohort ration context.
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
        - name: exception_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FeedDirectionCountsProjectionExceptionActionRequest"
      responses:
        "200":
          description: Counts/Shifting projection exception resolution.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FeedDirectionCountsProjectionExceptionActionResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "500":
          $ref: "#/components/responses/ServerError"
  /feed-direction/counts-projection/exceptions/{exception_id}/dismiss:
    post:
      tags:
        - Feed Direction
      operationId: dismissFeedDirectionCountsProjectionException
      summary: Dismiss a duplicate or intentionally non-blocking Counts/Shifting projection exception.
      description: >-
        Dismisses an open Counts/Shifting projection exception with actor, reason, optional source
        reference, and idempotent replay protection. This path is for reviewed duplicates or
        explicitly accepted non-blocking findings; it must not be used as a hidden generation bypass.
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
        - name: exception_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FeedDirectionCountsProjectionExceptionActionRequest"
      responses:
        "200":
          description: Counts/Shifting projection exception dismissal.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FeedDirectionCountsProjectionExceptionActionResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "500":
          $ref: "#/components/responses/ServerError"
  /feed-analytics/directed:
    get:
      tags:
        - Feed Direction
      operationId: getFeedAnalyticsDirected
      summary: Windowed rollup of directed feed for the Feed Analytics page.
      description: >-
        Aggregates the FROZEN feed sheet (`feed_direction_issue_rows`) over a business-date window:
        per-day totals and per-(day, feed item) series of DIRECTED kg, head-days, and grams per
        head per day. Normal workflow only -- experiment sheets author absolute kg with
        informational head counts and are served by their own read.


        DIRECTED, NOT CONSUMED. Every figure is what the sheet instructed, never a measured
        weight; completions carry proofs, not kg. Clients render the word "directed".


        Head-days count each pen-grain (shed, partition, shed tag, breed) ONCE per feed day --
        never once per session or per feed-item cell. A per-item figure divides by the heads whose
        sheet carried that item; the day figure divides by the day's distinct heads.


        Blocked cells (`quantity_kg` null on the sheet) contribute nothing and are not counted
        here; config gaps surface on the Feed Direction screen instead. A day with no issued sheet
        is ABSENT from `days`, never fabricated as zero.
      parameters:
        - name: park_id
          in: query
          required: false
          description: >-
            Narrow to one park. Absent means every park the caller is authorized for -- a
            park-scoped principal can never widen past their grant.
          schema:
            type: string
            format: uuid
        - name: date_from
          in: query
          required: false
          description: >-
            Inclusive window start (Asia/Kolkata business date). Defaults to 29 days before
            `date_to`. The window is capped at 92 days, keeping the most recent days.
          schema:
            type: string
            format: date
        - name: date_to
          in: query
          required: false
          description: >-
            Inclusive window end. Defaults to YESTERDAY: today's sheet is still being executed, so
            the backend owns the exclude-today rule rather than each client subtracting a day.
          schema:
            type: string
            format: date
      responses:
        "200":
          description: Day totals and per-item series, ordered by feed day ascending.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FeedAnalyticsDirectedResponse"
        "400":
          description: Malformed date or park id.
        "403":
          description: Caller lacks feed direction read for the requested scope.
  /feed-analytics/execution:
    get:
      tags:
        - Feed Direction
      operationId: getFeedAnalyticsExecution
      summary: Per-day execution adherence for the Feed Analytics page.
      description: >-
        Status counts per business date across the three proof-gated feed stages -- packing and
        distribution pen-session completions (verified / awaiting verdict / rework) and transport
        shed tasks (completed / open / awaiting verdict / rework) -- plus the daily median
        submit-to-verdict latency of packing and distribution verdicts, bucketed by the
        Asia/Kolkata date the verdict landed. STATUS COUNTS ONLY: completions carry proofs, never
        kg, so execution is judged on whether work was proved and verified, not on quantity.
      parameters:
        - name: park_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: date_from
          in: query
          required: false
          schema:
            type: string
            format: date
        - name: date_to
          in: query
          required: false
          description: Inclusive window end, defaulting to yesterday; window capped at 92 days.
          schema:
            type: string
            format: date
      responses:
        "200":
          description: Per-day status counts, ordered by date ascending.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FeedAnalyticsExecutionResponse"
        "400":
          description: Malformed date or park id.
        "403":
          description: Caller lacks feed direction read for the requested scope.
  /feed-analytics/experiment:
    get:
      tags:
        - Feed Direction
      operationId: getFeedAnalyticsExperiment
      summary: Experiment feed-item kg series plus one day's per-pen wastage.
      description: >-
        The experiment workflow's authored kg per (feed day, feed item) — the farm reads this
        screen in feed items (masoor, bhusa), never trial-arm labels — plus ONE selected day's
        per-pen leftover-feed (wastage) table. Experiment rations are ABSOLUTE shed/pen totals --
        head counts on those sheet rows are informational, so no per-head figure exists here and
        none may be derived by a client.
      parameters:
        - name: park_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: date_from
          in: query
          required: false
          schema:
            type: string
            format: date
        - name: date_to
          in: query
          required: false
          description: Inclusive window end, defaulting to yesterday; window capped at 92 days.
          schema:
            type: string
            format: date
        - name: wastage_day
          in: query
          required: false
          description: >-
            The single business day the per-pen wastage table describes, defaulting to TODAY
            (Asia/Kolkata) — wastage is collected live during the feed day, unlike the kg window
            which ends yesterday.
          schema:
            type: string
            format: date
      responses:
        "200":
          description: Per-(day, feed item) authored kg plus the selected day's per-pen wastage.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FeedAnalyticsExperimentResponse"
        "400":
          description: Malformed date or park id.
        "403":
          description: Caller lacks feed direction read for the requested scope.
  /feed-analytics/stock:
    get:
      tags:
        - Feed Direction
      operationId: getFeedAnalyticsStock
      summary: Feed stock positions and daily expenditure for the Feed Analytics page.
      description: >-
        Per-farm, per-feed-item stock cards from the bootstrapped purchase ledger (feed_purchases, one-time
        sheet import; entry screens arrive with the Procurement vertical) plus the window's daily
        expenditure series.


        STOCK DEPLETES AT SHEET LOCK: balance = (purchased - consumed-at-import snapshot) -
        directed kg of LOCKED sheets from the bootstrap cutoff onward, both workflows. Days left
        divides the balance by the item's average directed kg over its 3 most recent locked feed
        days (a short window so a ration-regime change moves days-left immediately); a negative balance is served as-is, saying the ledger is missing a load.
        Expenditure prices each (day, item)'s directed kg at the item's most recent load rate on
        or before that day. Empty arrays mean the ledger is not bootstrapped for this tenant.
      parameters:
        - name: park_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: date_from
          in: query
          required: false
          schema:
            type: string
            format: date
        - name: date_to
          in: query
          required: false
          description: Inclusive expenditure-window end, defaulting to yesterday; capped at 92 days.
          schema:
            type: string
            format: date
      responses:
        "200":
          description: Stock cards (all items) and the expenditure series for the window.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FeedAnalyticsStockResponse"
        "400":
          description: Malformed date or park id.
        "403":
          description: Caller lacks feed direction read for the requested scope.
  /feed-direction/preview:
    get:
      tags:
        - Feed Direction
      operationId: getFeedDirectionPreview
      summary: Generated feed direction for one park and one feed day.
      description: >-
        Turns the projected shed counts for `target_date` (live herd plus the movements that are
        approved but not yet executed) and the authored ration grid into per-session feed
        quantities.


        Resolution rule: the ration group is `Kid` when the animals' management stage maps onto a
        shed tag whose `applies_to` is `kid`, otherwise the breed's authored ration group; the grid
        key is (park, ration_group, shed_tag, feed_item); the shed quantity is
        `projected_head_count x grams_per_head x shed_factor`, split across the park's sessions.
        The kid/adult branch comes from the SHED TAG, never from the animal's stored age band --
        the two disagree on live data and the tag vocabulary is what the grid is indexed by.


        `items` is a page of SHEDS (a shed's grains never straddle a page boundary). `summary`
        covers the WHOLE filtered result set and is invariant to `limit`/`offset` -- see
        `FeedDirectionPreviewSummary`.


        CONFIGURED ZERO IS NOT MISSING CONFIGURATION. An authored rate of 0 is a real feeding
        instruction (milk-fed kids) and appears as `status: resolved` with `quantity_kg: "0.000"`.
        A cell with NO authored rate appears as `status: blocked` with `quantity_kg: null` and a
        reason. The two are never collapsed: a silently zeroed cell is a shed that goes unfed while
        the sheet looks complete. Clients MUST NOT treat a blocked quantity as zero, and
        `session_total_kg` deliberately sums resolved items only -- `blocked: true` on the row is
        what says the total is partial.


        Paging is by SHED, so a shed's rows never straddle a page boundary and every
        `session_total_kg` is complete. `summary` is PAGE-SCOPED and declares so in `summary.scope`;
        it is not a park total.
      parameters:
        - name: park_id
          in: query
          required: true
          description: >-
            The park to generate for. REQUIRED: the ration grid, the session split and the dispatch
            clock are all park-scoped, so a tenant-wide generation would mix two parks' rations.
          schema:
            type: string
            format: uuid
        - name: target_date
          in: query
          required: true
          description: >-
            The feed day, as an India business-calendar date (Asia/Kolkata). A date, never an
            instant: an instant would reintroduce the UTC-vs-IST day-boundary bug and generate the
            sheet for the wrong day.
          schema:
            type: string
            format: date
        - name: shed_id
          in: query
          required: false
          description: Narrow to a single shed.
          schema:
            type: string
            format: uuid
        - name: session
          in: query
          required: false
          description: >-
            Narrow to one feeding session number. Absent means every session. Narrowing does NOT
            rescale: the morning batch stays the morning batch.
          schema:
            type: integer
            minimum: 1
            maximum: 99
        - name: workflow
          in: query
          required: false
          description: >-
            Narrow the served issue to one dispatch workflow. Absent unions both (normal packing +
            hand-entered experiment sheds), which is the whole park-day's sheet. An unrecognised
            value is rejected rather than silently widened.
          schema:
            type: string
            enum: [normal, experiment]
        - name: draft
          in: query
          required: false
          description: >-
            The ONLY live-compute path. `draft=true` returns a live what-if sheet computed from the
            CURRENT herd + config WITHOUT reading or writing any issue, for tuning ration rates
            before the sheet is issued. The response carries `draft: true` and `lifecycle.state:
            draft` so a what-if can never be mistaken for a frozen issued sheet. Absent/false serves
            the frozen issued sheet (or its pending/never-issued state).
          schema:
            type: boolean
        - $ref: "#/components/parameters/FeedDirectionLimit"
        - $ref: "#/components/parameters/FeedDirectionOffset"
      responses:
        "200":
          description: >-
            One page of the FROZEN issued sheet plus its lifecycle, OR (when nothing is issued yet)
            an explicit pending/never-issued state, OR (draft=true) a live what-if.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FeedDirectionPreviewPage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "500":
          $ref: "#/components/responses/ServerError"
  /feed-direction/distribution/captures:
    get:
      tags:
        - Feed Direction
      operationId: getFeedDistributionCaptures
      summary: Proof slots already recorded for one pen-session, by any operator.
      description: >-
        Which of a pen-session's three proof slots (feed weight photo, feed-distribution video,
        water-distribution video) have ALREADY been recorded, and the server `proof_ref` of each.


        A pen-session's three proofs may be shot by THREE DIFFERENT operators on three phones
        (maintainer decision 2026-08-14). Before this read a proof was discoverable only on the
        device that shot it, so the others could not tell a slot was done, and no single phone held
        all three references -- the pen could not be submitted at all. Clients render "already
        recorded" from this and send the returned `proof_ref` for slots they did not shoot.


        Read-only: it changes no completion state and gates nothing, and a client that ignores it
        behaves exactly as before. It deliberately returns NO media url and NO uploader name -- the
        footage stays a verifier surface, so this adds no way to view another operator's media.


        `partition_label` is part of the IDENTITY, not decoration: omitting it on a partitioned shed
        answers for the shed as a whole and would tell an operator standing in one pen that another
        pen's work is theirs.
      parameters:
        - name: park_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
          description: Optional; the server resolves the tenant's default park when omitted.
        - name: shed_id
          in: query
          required: true
          schema:
            type: string
            format: uuid
        - name: partition_label
          in: query
          required: false
          schema:
            type: string
          description: >-
            The PEN inside the shed ("2", "Part 3"). Omit or send "" for an undivided shed.
        - name: session_no
          in: query
          required: true
          schema:
            type: integer
            format: int32
            minimum: 1
          description: A pen's morning and evening are separate bags; 0 matches no worklist line.
        - name: target_date
          in: query
          required: true
          schema:
            type: string
            format: date
        - name: workflow
          in: query
          required: true
          schema:
            type: string
            enum: [normal, experiment]
      responses:
        "200":
          description: The pen-session's already-recorded proof slots (at most one row per slot).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FeedDistributionCapturesResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /feed-packing/worklist:
    get:
      tags:
        - Feed Direction
      operationId: getFeedPackingWorklist
      summary: Per-pen, per-session packing worklist for one park and one feed day.
      description: >-
        The same generated quantities as `/feed-direction/preview`, collapsed to the line a packer
        works from: ONE ROW PER OPERATIONAL LOCATION PER FEEDING SESSION, with the pen's ration
        grains already summed, because a packer fills one bag per feed item per pen per session
        rather than one per grain. The totals are the ALREADY-ROUNDED session quantities, so the
        worklist matches the direction sheet exactly rather than differing by a rounding step.


        The SESSION is part of the grain (maintainer decision 2026-08-11, REVERTING the 2026-08-10
        pen-day row). A pen's morning and evening shares are two separate bags: each is packed on its
        own, filmed on its own, and verified on its own. One video cannot prove two bags.


        The PARTITION is part of the grain for a separate reason: Castro 1 and Castro 2 are
        physically different pens holding different animals on different rations, and are separate
        rows.


        `status` is DERIVED from the generation result -- `ready` when every item resolved,
        `blocked` when any item has no authored ration (the line must not be packed from the
        resolved remainder, which would send the pen short), and `empty` when the pen holds no
        projected animals, which is not a configuration gap. `lifecycle_status` and `completed`
        report that shed-session's verification state, recorded through
        `POST /feed-direction/packing/complete`.


        `items` is a page of SHEDS; `summary` is the store draw for the WHOLE filtered worklist and
        is invariant to `limit`/`offset` -- see `FeedPackingWorklistSummary`.
      parameters:
        - name: park_id
          in: query
          required: true
          schema:
            type: string
            format: uuid
        - name: target_date
          in: query
          required: true
          description: The feed day, as an India business-calendar date (Asia/Kolkata).
          schema:
            type: string
            format: date
        - name: workflow
          in: query
          required: false
          description: >-
            Narrow the served issue to one dispatch workflow. Absent unions both. See the preview.
          schema:
            type: string
            enum: [normal, experiment]
        - name: session
          in: query
          required: false
          description: >-
            Narrow the worklist to one feeding session. Absent or `0` returns every session, the
            same contract as the preview. A present-but-out-of-range value is rejected, never
            widened.
          schema:
            type: integer
            minimum: 0
            maximum: 99
        - name: draft
          in: query
          required: false
          description: >-
            The only live-compute path; see the preview's `draft`. Absent/false serves the frozen
            issued worklist (or its pending/never-issued state).
          schema:
            type: boolean
        - $ref: "#/components/parameters/FeedDirectionLimit"
        - $ref: "#/components/parameters/FeedDirectionOffset"
      responses:
        "200":
          description: >-
            One page of the FROZEN issued worklist plus its lifecycle, its pending/never-issued
            state, or (draft=true) a live what-if.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FeedPackingWorklistPage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "500":
          $ref: "#/components/responses/ServerError"
  /feed-direction/complete:
    post:
      tags:
        - Feed Direction
      operationId: completeFeedDirectionSession
      summary: Record that one shed-session's feed direction was carried out.
      description: >-
        Marks ONE shed's ONE feeding session on ONE feed day as fed ("feed direction completed"),
        with OPTIONAL video proof. The completion grain is the operator's unit of work --
        (park, shed, session_no, target_date, workflow) -- not the ration grain: one shed's session
        is done or it is not, regardless of how many ration rows it printed. After a completion the
        `/feed-direction/preview` and `/feed-packing/worklist` rows for that shed-session report
        `completed: true`.


        Idempotent on two axes: the `Idempotency-Key` header (an exact replay returns the original
        result and runs no side effects; the same key with a different payload is `409`), and the
        shed-session natural key (a second completion of the same shed-session, under any key, is a
        no-op that returns `applied: false`).


        Video is OPTIONAL: `proof_refs` may be empty. When present, each reference must resolve to a
        real, completed upload minted through `/app/proofs/*`; the bytes live in GCS and only the
        reference is recorded here.
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FeedDirectionCompleteRequest"
      responses:
        "200":
          description: The shed-session is recorded completed (or was already completed).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FeedDirectionCompleteResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "500":
          $ref: "#/components/responses/ServerError"
  /feed-direction/distribution/complete:
    post:
      tags:
        - Feed Direction
      operationId: completeFeedDistribution
      summary: Submit one shed-session's feed distribution for verifier approval.
      description: >-
        The verifier-GATED feed DISTRIBUTION completion (maintainer decision, 2026-07-26), entirely
        separate from `POST /feed-direction/complete` (feed PACKING, which is unchanged: instant,
        optional-video, no verifier). The operator submits THREE mandatory proofs -- a feed-weight
        PHOTO (`feed_weight_proof_ref`, which must come from the live in-app camera), a
        feed-distribution VIDEO (`distribution_proof_ref`) and a water-distribution VIDEO
        (`water_proof_ref`) -- which writes a `pending_verification` row and enqueues ONE
        verification item carrying all three proofs. NOTHING is completed here.


        The capture KIND is part of the contract, not a client preference: a still frame where a clip
        was promised leaves the verifier something they cannot judge. The weight photo additionally
        demands a live capture, because a gallery pick is a photo of a scale from some other day and
        only a live one ties the reading to this pen's feed.


        The three proofs may be captured by THREE DIFFERENT operators on three different phones
        (maintainer decision, 2026-08-14). Read `GET /feed-direction/distribution/captures` to learn
        which slots a pen-session already has and each one's server proof id, then send those ids
        here -- a phone that shot none of them can still submit.


        The session is `completed` only when a verifier APPROVES the item; a rejection bounces it to
        `rework` for a re-shoot, and re-submitting returns it to `pending_verification`. After
        verifier approval the `/feed-direction/preview` rows for that shed-session report
        `completed: true`.


        All three proofs are MANDATORY: a request missing any of `feed_weight_proof_ref`,
        `distribution_proof_ref` or `water_proof_ref` -- or carrying one of the wrong capture kind --
        is rejected `422 proof_required` before any state changes, because there is nothing for a
        verifier to approve. Idempotent on the `Idempotency-Key` header (an exact replay returns the
        original result and runs no side effects; the same key with a different payload is `409`) and
        on the shed-session natural key.
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FeedDistributionCompleteRequest"
      responses:
        "200":
          description: >-
            The shed-session is recorded pending_verification (or was already completed). The session
            is not done until a verifier approves.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FeedDistributionCompleteResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "422":
          description: >-
            A mandatory proof is missing or is the wrong capture kind (`code: proof_required`): the
            feed-weight photo, the feed-distribution video, or the water-distribution video. The
            message names which one; the code is the same for all three.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "500":
          $ref: "#/components/responses/ServerError"
  /feed-direction/packing/complete:
    post:
      tags:
        - Feed Direction
      operationId: completeFeedPacking
      summary: Submit one pen's packing for ONE feeding session for verifier approval.
      description: >-
        The verifier-GATED feed PACKING completion (maintainer decision, 2026-07-26, SUPERSEDING the
        earlier "packing stays instant, no verifier" rule). The operator submits ONE mandatory packing
        VIDEO (`packing_proof_ref`), which writes a `pending_verification` row and enqueues ONE
        verification item carrying the video. NOTHING is completed here.


        ONE VIDEO PROVES ONE SESSION'S BAG (maintainer decision 2026-08-11, REVERTING the 2026-08-10
        pen-day completion). A pen's morning and evening shares are packed and filmed separately, so
        `session_no` is REQUIRED here and is part of the completion's natural key -- (tenant, park,
        shed, partition, session_no, target_date, workflow). A request without it, or with `0`, is
        rejected: a completion that does not say which bag it proves cannot be recorded against the
        right line. The verification item's expected-ration context names THAT SESSION's quantities,
        because that is what the clip should show.


        The PEN is part of the identity for a separate reason. A partitioned shed has one completion
        PER PEN PER SESSION, and one pen's video must never close out its neighbours -- see
        `partition_label` and migration 000137.


        The shed-session is `completed` only when a verifier APPROVES the item; a rejection bounces it
        to `rework` for a re-shoot, and re-submitting returns it to `pending_verification`. After
        verifier approval the `/feed-packing/worklist` row for that shed-session reports
        `completed: true`; the pen's other session is unaffected and still owes its own video.


        This is a SEPARATE record from the old instant `POST /feed-direction/complete` path (now inert)
        and from the distribution gate. The packing video is MANDATORY: a request missing
        `packing_proof_ref` is rejected `422 proof_required` before any state changes -- there is
        nothing for a verifier to approve. Idempotent on the `Idempotency-Key` header (an exact replay
        returns the original result and runs no side effects; the same key with a different payload is
        `409`) and on the shed-session natural key.
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FeedPackingCompleteRequest"
      responses:
        "200":
          description: >-
            The shed-session is recorded pending_verification (or already held THIS SAME video). The
            packing is not done until a verifier approves.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FeedPackingCompleteResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "409":
          description: >-
            Either the `Idempotency-Key` was reused with a different payload, or this shed-session
            already holds a DIFFERENT packing video (`code: packing_already_recorded`).


            A packing line accepts exactly ONE video, so a second, different one is a CONFLICT and not
            a replay -- it cannot be stored, and answering success would tell the operator their
            recording was accepted while nothing recorded it and no verifier ever saw it. A genuine
            re-send is unaffected: an identical request replays on its idempotency key, and re-sending
            the SAME `packing_proof_ref` under a new key still matches the stored proof and returns
            `200`.


            The client must treat this as TERMINAL and surface it, never retry it.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "422":
          description: >-
            The mandatory packing video is missing (`code: proof_required`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "500":
          $ref: "#/components/responses/ServerError"
  /feed-wastage/worklist:
    get:
      tags:
        - Feed Direction
      operationId: getFeedWastageWorklist
      summary: Per-pen Feed Wastage worklist for one park and one feed day.
      description: >-
        Feed Wastage (maintainer decision 2026-08-18) is a daily task on EXPERIMENT pens only: each
        pen on a hand-authored feed experiment owes ONE leftover-feed video per feed day. The rows
        are DERIVED from the day's frozen EXPERIMENT sheet — the same rows the packing worklist
        reads — so a pen appears here if and only if the experiment sheet covers it, and there is no
        second planner to drift from.


        The grain is the PEN-DAY. There is deliberately NO session: packing and distribution are
        per-bag work (morning and evening are two bags, two videos), but wastage is what is LEFT
        OVER after the day's feeding, measured once. One pen, one feed day, one video, one recorded
        value.


        `lifecycle_status` and `completed` report the pen-day's verification state, recorded through
        `POST /feed-direction/wastage/complete`. `wastage_kg` is the VERIFIER'S measured leftover
        weight, present only once she has recorded one ("0" is a real measurement — an empty
        trough).


        `items` is a page of SHEDS; `summary` covers the WHOLE filtered worklist and is invariant to
        `limit`/`offset`.
      parameters:
        - name: park_id
          in: query
          required: true
          schema:
            type: string
            format: uuid
        - name: target_date
          in: query
          required: true
          description: The feed day, as an India business-calendar date (Asia/Kolkata).
          schema:
            type: string
            format: date
        - name: shed_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: partition_label
          in: query
          required: false
          description: Narrow to one pen; only meaningful with shed_id.
          schema:
            type: string
        - name: status
          in: query
          required: false
          description: >-
            Narrow to one verification-lifecycle bucket. Absent returns every status. Applied over
            the whole scope before paging, so a filtered page and its summary agree.
          schema:
            type: string
            enum: [pending, pending_verification, completed]
        - $ref: "#/components/parameters/FeedDirectionLimit"
        - $ref: "#/components/parameters/FeedDirectionOffset"
      responses:
        "200":
          description: >-
            One page of the wastage worklist plus its lifecycle, or the sheet's pending/never-issued
            state with zero rows.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FeedWastageWorklistPage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "500":
          $ref: "#/components/responses/ServerError"
  /feed-direction/wastage/complete:
    post:
      tags:
        - Feed Direction
      operationId: completeFeedWastage
      summary: Submit one experiment pen's daily wastage video for verifier review.
      description: >-
        The verifier-GATED feed WASTAGE completion (maintainer decision 2026-08-18). The operator
        submits ONE mandatory wastage VIDEO (`wastage_proof_ref`) — the leftover feed in the pen —
        which writes a `pending_verification` row and enqueues ONE verification item carrying the
        video. NOTHING is completed here.


        The grain is the PEN-DAY: no `session_no` (wastage is measured once per day) and no
        `workflow` (the server stamps `experiment` — wastage exists only for experiment pens). A
        completion naming a pen the day's experiment sheet does not cover is rejected
        `422 not_experiment_pen`: it would be work no worklist line ever matches.


        The verifier watches the clip. When she can read the leftover weight in it she RECORDS that
        weight (her own route, declared to clients by the item's `measurement_correction` block) and
        APPROVES; when she cannot, she REJECTS and this pen-day bounces to `rework` for a re-shoot.
        Re-submitting returns it to `pending_verification`.


        The wastage video is MANDATORY: a request missing `wastage_proof_ref` is rejected
        `422 proof_required` before any state changes. Idempotent on the `Idempotency-Key` header
        (an exact replay returns the original result and runs no side effects; the same key with a
        different payload is `409`) and on the pen-day natural key.
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FeedWastageCompleteRequest"
      responses:
        "200":
          description: >-
            The pen-day is recorded pending_verification (or already held THIS SAME video). The
            wastage task is not done until a verifier approves.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FeedWastageCompleteResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "409":
          description: >-
            Either the `Idempotency-Key` was reused with a different payload, or this pen-day
            already holds a DIFFERENT wastage video. A pen-day accepts exactly ONE video, so a
            second, different one is a CONFLICT and not a replay — the client must treat this as
            TERMINAL and surface it, never retry it. A genuine re-send of the SAME
            `wastage_proof_ref` under a new key still returns `200`.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "422":
          description: >-
            The mandatory wastage video is missing (`code: proof_required`), or the pen is not on
            that day's experiment sheet (`code: not_experiment_pen`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "500":
          $ref: "#/components/responses/ServerError"
  /feed-direction/wastage/{completion_id}/measurement:
    post:
      tags:
        - Feed Direction
      operationId: recordFeedWastageMeasurement
      summary: Record the leftover feed weight the VERIFIER reads off a wastage video.
      description: >-
        The VERIFIER'S measurement (maintainer decision 2026-08-18), the second producer-owned
        measurement route after the weighing weight correction. She watches the pen's wastage video
        and records the leftover weight she can see, in kg; a later entry REPLACES the value. ZERO
        IS VALID — an empty trough is a real, good measurement — so clients must keep a blank field
        distinct from an explicit 0 and never coerce one into the other.


        It is deliberately SEPARATE from her verdict: she records the value and then approves the
        video on the ordinary verdict route; when she cannot read a value at all, she rejects for a
        re-shoot and records nothing. Recording never changes the completion's status.


        ONE route serves her phone and her admin-web drawer. Authorization is the verifier-exclusive
        `verification.verdict` capability, so the person who judges the evidence is the person who
        records what it shows. The `completion_id` comes from the verification item's own
        `measurement_correction.observation_id` (which echoes `source.ref_id`); clients never
        compose that address themselves.
      parameters:
        - name: completion_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FeedWastageMeasurementRequest"
      responses:
        "200":
          description: The measurement is recorded and the queue item's label restated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FeedWastageMeasurementResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "422":
          description: >-
            The value is missing (`code: missing_wastage`) or out of range
            (`code: wastage_out_of_range` — 0 to 10000 kg). The message is the sentence to render.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "500":
          $ref: "#/components/responses/ServerError"
  /feed-transport/tasks:
    get:
      tags: [Feed Direction]
      operationId: listFeedTransportTasks
      summary: List today's daily shed-grain Feed Transport tasks.
      parameters:
        - in: query
          name: business_date
          required: true
          schema: { type: string, format: date }
        - in: query
          name: park_id
          schema: { type: string, format: uuid }
          description: Optional Farm filter. The response still returns the complete Farm vocabulary for the selected date.
        - in: query
          name: shed_id
          schema: { type: string, format: uuid }
          description: Optional physical-shed filter; must remain shed-grain and never imply a feed session.
        - in: query
          name: status
          schema: { type: string, enum: [due, verification_due, rework, completed] }
          description: Optional verification-lifecycle filter.
        - in: query
          name: cursor
          schema: { type: string }
        - in: query
          name: limit
          schema: { type: integer, minimum: 1, maximum: 100, default: 20 }
      responses:
        "200":
          description: One bounded page of daily tasks; grain is tenant + business_date + active shed, never session.
          content: { application/json: { schema: { $ref: "#/components/schemas/FeedTransportTaskPage" } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /feed-transport/tasks/{task_id}/submit:
    post:
      tags: [Feed Direction]
      operationId: submitFeedTransportTask
      summary: Submit a fresh live-camera Feed Transport video for verification.
      parameters:
        - in: path
          name: task_id
          required: true
          schema: { type: string, format: uuid }
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/FeedTransportSubmitRequest" } } }
      responses:
        "200":
          description: A new immutable attempt was submitted and the task is verification_due.
          content: { application/json: { schema: { $ref: "#/components/schemas/FeedTransportSubmitResponse" } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/WriteConflict" }
        "422": { description: Camera video proof is missing, content: { application/json: { schema: { $ref: "#/components/schemas/ErrorEnvelope" } } } }
  /app/pc-care/planner/catalog:
    get:
      tags: [PC Care]
      operationId: appPCCarePlannerCatalog
      summary: PC Care planner vocabulary (parks, assignable operators, categories).
      description: >-
        PC Care (maintainer decision 2026-08-21) is the planner-assigned deworming / ticks removal /
        hoof trimming / hair trimming module. The catalog is the park-grain create-wizard
        vocabulary: every park the planner may pick, the assignable operator roster, and the
        module's backend-owned category vocabulary. Planning is CEO-only (pc_care.plan, the
        weighing.plan precedent).
      responses:
        "200":
          description: The planner vocabulary, filtered to the caller's authorized parks.
          content: { application/json: { schema: { $ref: "#/components/schemas/PCCarePlannerCatalog" } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "500": { $ref: "#/components/responses/ServerError" }
  /app/pc-care/planner/parks/{park_id}/sheds:
    get:
      tags: [PC Care]
      operationId: appPCCarePlannerParkSheds
      summary: One keyset page of a park's pens for the PC Care create wizard.
      description: >-
        Pens come from the shed_partitions CATALOG (an undivided shed is one whole-shed row), each
        decorated with any existing live task for the chosen category+date so the wizard greys a
        taken pen instead of letting the create collide.
      parameters:
        - in: path
          name: park_id
          required: true
          schema: { type: string, format: uuid }
        - name: category
          in: query
          required: true
          schema: { $ref: "#/components/schemas/PCCareCategory" }
        - name: date
          in: query
          required: true
          description: The planned business date (Asia/Kolkata).
          schema: { type: string, format: date }
        - name: cursor
          in: query
          required: false
          schema: { type: string }
        - name: limit
          in: query
          required: false
          schema: { type: integer, minimum: 1, maximum: 100, default: 25 }
      responses:
        "200":
          description: One page of pens.
          content: { application/json: { schema: { $ref: "#/components/schemas/PCCarePlannerSheds" } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFoundOrNotAllowed" }
        "500": { $ref: "#/components/responses/ServerError" }
  /app/pc-care/tasks:
    get:
      tags: [PC Care]
      operationId: appListPCCareTasks
      summary: Flat PC Care task list for plan/monitor/oversee holders.
      parameters:
        - name: date
          in: query
          required: true
          description: The due business date (Asia/Kolkata).
          schema: { type: string, format: date }
        - name: park_id
          in: query
          required: false
          schema: { type: string, format: uuid }
        - name: category
          in: query
          required: false
          schema: { $ref: "#/components/schemas/PCCareCategory" }
        - name: limit
          in: query
          required: false
          schema: { type: integer, minimum: 1, maximum: 100, default: 25 }
        - name: cursor
          in: query
          required: false
          description: Opaque keyset cursor returned as next_cursor by the previous page.
          schema: { type: string }
      responses:
        "200":
          description: One bounded page of tasks.
          content: { application/json: { schema: { $ref: "#/components/schemas/PCCareTaskPage" } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "500": { $ref: "#/components/responses/ServerError" }
    post:
      tags: [PC Care]
      operationId: appCreatePCCareTask
      summary: Plan one PC Care task (CEO-only).
      description: >-
        Creates ONE task per (category, pen, planned business date) with one or MORE assigned
        operators — multi-operator by design, deliberately unlike weighing's
        one-operator-per-bucket. A live task already covering that pen-day answers 409
        task_already_planned.
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/PCCareCreateTaskRequest" } } }
      responses:
        "200":
          description: The planned task.
          content: { application/json: { schema: { $ref: "#/components/schemas/PCCareTask" } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409":
          description: >-
            A live task already covers this pen, category and date (task_already_planned) or the
            idempotency key was reused with a different request.
          content: { application/json: { schema: { $ref: "#/components/schemas/ErrorEnvelope" } } }
        "422":
          description: Unknown category, no assignees, or a pen outside the shed's partition catalog.
          content: { application/json: { schema: { $ref: "#/components/schemas/ErrorEnvelope" } } }
        "500": { $ref: "#/components/responses/ServerError" }
  /app/pc-care/tasks/{task_id}/cancel:
    post:
      tags: [PC Care]
      operationId: appCancelPCCareTask
      summary: Cancel an unsubmitted PC Care task (CEO-only).
      parameters:
        - in: path
          name: task_id
          required: true
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: The task is canceled (idempotent — an already-terminal task is a no-op).
          content:
            application/json:
              schema:
                type: object
                properties:
                  status: { type: string }
                required: [status]
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFoundOrNotAllowed" }
        "500": { $ref: "#/components/responses/ServerError" }
  /app/pc-care/worklist:
    get:
      tags: [PC Care]
      operationId: appPCCareWorklist
      summary: The operator's assigned PC Care tasks for one category tab and one day.
      parameters:
        - name: category
          in: query
          required: true
          schema: { $ref: "#/components/schemas/PCCareCategory" }
        - name: date
          in: query
          required: true
          schema: { type: string, format: date }
        - name: limit
          in: query
          required: false
          schema: { type: integer, minimum: 1, maximum: 100, default: 25 }
        - name: cursor
          in: query
          required: false
          description: Opaque keyset cursor returned as next_cursor by the previous page.
          schema: { type: string }
      responses:
        "200":
          description: One bounded page of the caller's assigned tasks.
          content: { application/json: { schema: { $ref: "#/components/schemas/PCCareTaskPage" } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "500": { $ref: "#/components/responses/ServerError" }
  /app/pc-care/tasks/{task_id}:
    get:
      tags: [PC Care]
      operationId: appGetPCCareTask
      summary: One PC Care task with its backend-owned expected slot contract.
      description: >-
        expected_slots is the BACKEND-OWNED proof contract for the task's category (one video for
        deworming/ticks removal; before/during/after for the trimming categories). capture_mode is
        the BACKEND-OWNED capture flow: scan_record (deworming/ticks removal — scan a tag and the
        recorder opens immediately) or roster_pick (the trimming categories — tap an RFID off the
        pen roster and the screen walks the animal's slots). Clients branch on both verbatim and
        never hardcode a category-to-slot or category-to-mode map.
      parameters:
        - in: path
          name: task_id
          required: true
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: The task.
          content: { application/json: { schema: { $ref: "#/components/schemas/PCCareTask" } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFoundOrNotAllowed" }
        "500": { $ref: "#/components/responses/ServerError" }
  /app/pc-care/tasks/{task_id}/roster:
    get:
      tags: [PC Care]
      operationId: appPCCareTaskRoster
      summary: The roster-pick tap list — RFIDs of animals currently in the task's pen.
      description: >-
        Read-only, for capture_mode roster_pick (the trimming categories). Lists the active RFIDs
        of alive animals resident in the task's shed, narrowed to the task's pen when it has a
        partition. Tapping one records a normal free-flow scan; this list never gates what a scan
        may store. Keyset-paged on identifier value.
      parameters:
        - in: path
          name: task_id
          required: true
          schema: { type: string, format: uuid }
        - name: cursor
          in: query
          required: false
          schema: { type: string }
        - name: limit
          in: query
          required: false
          schema: { type: integer, minimum: 1, maximum: 100, default: 50 }
      responses:
        "200":
          description: One page of the pen's resident RFIDs.
          content: { application/json: { schema: { $ref: "#/components/schemas/PCCareTaskRoster" } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFoundOrNotAllowed" }
        "500": { $ref: "#/components/responses/ServerError" }
  /app/pc-care/tasks/{task_id}/captures:
    get:
      tags: [PC Care]
      operationId: appPCCareTaskCaptures
      summary: The peer-visibility poll — scanned animals and their slot states, by any assignee.
      description: >-
        Read-only. It is what lets several assigned phones split one task's videos: each slot
        carries who captured it ("Captured by X") so peers see each other's work. Keyset-paged
        over one task's animal rows.
      parameters:
        - in: path
          name: task_id
          required: true
          schema: { type: string, format: uuid }
        - name: cursor
          in: query
          required: false
          schema: { type: string }
        - name: limit
          in: query
          required: false
          schema: { type: integer, minimum: 1, maximum: 100, default: 50 }
      responses:
        "200":
          description: One page of scanned animals with slot attribution.
          content: { application/json: { schema: { $ref: "#/components/schemas/PCCareCapturesResponse" } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFoundOrNotAllowed" }
        "500": { $ref: "#/components/responses/ServerError" }
  /app/pc-care/tasks/{task_id}/animals:
    post:
      tags: [PC Care]
      operationId: appScanPCCareAnimal
      summary: Scan one RFID into the task (verbatim, free-flow).
      description: >-
        The tag is stored VERBATIM — no herd lookup. The ONE business rule is that a tag cannot be
        scanned twice into the same task (409 duplicate_scan). Only an ASSIGNEE of the task may
        scan (403 task_not_assigned); a locked task answers 409 task_locked.
      parameters:
        - in: path
          name: task_id
          required: true
          schema: { type: string, format: uuid }
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/PCCareScanRequest" } } }
      responses:
        "200":
          description: The durable scan row.
          content: { application/json: { schema: { $ref: "#/components/schemas/PCCareScanResponse" } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFoundOrNotAllowed" }
        "409":
          description: Already scanned in this task (duplicate_scan) or the task is locked (task_locked).
          content: { application/json: { schema: { $ref: "#/components/schemas/ErrorEnvelope" } } }
        "500": { $ref: "#/components/responses/ServerError" }
  /app/pc-care/tasks/{task_id}/animals/{animal_row_id}/proofs/{slot}:
    put:
      tags: [PC Care]
      operationId: appRegisterPCCareSlotProof
      summary: Attach one slot's live-camera video to one scanned animal.
      description: >-
        slot is one of the task category's expected_slots field keys. Any ASSIGNEE may fill any
        slot on any scanned animal; pre-submit a re-record REPLACES the slot's clip (the phone
        retires the old clip only after the new one is uploaded). The proof must be a completed,
        tenant-owned, in-app-camera VIDEO.
      parameters:
        - in: path
          name: task_id
          required: true
          schema: { type: string, format: uuid }
        - in: path
          name: animal_row_id
          required: true
          schema: { type: string, format: uuid }
        - in: path
          name: slot
          required: true
          schema: { type: string, enum: [video, before_video, during_video, after_video] }
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/PCCareSlotProofRequest" } } }
      responses:
        "200":
          description: The slot's video reference is recorded.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status: { type: string }
                required: [status]
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFoundOrNotAllowed" }
        "409":
          description: The task is locked (task_locked).
          content: { application/json: { schema: { $ref: "#/components/schemas/ErrorEnvelope" } } }
        "422":
          description: >-
            The slot does not belong to this category (invalid_slot) or the proof could not be
            verified (invalid_proof / proof_required).
          content: { application/json: { schema: { $ref: "#/components/schemas/ErrorEnvelope" } } }
        "500": { $ref: "#/components/responses/ServerError" }
  /app/pc-care/tasks/{task_id}/submit:
    post:
      tags: [PC Care]
      operationId: appSubmitPCCareTask
      summary: Submit the whole task for verifier review (any assignee).
      description: >-
        Refused until every scanned animal carries its full slot set (422 proof_incomplete) and
        while no animal is scanned (422 no_animals). On success the task flips to
        pending_verification, locks for every assignee, and ONE verification item carries every
        animal's clips. Verifier approve completes the task; reject returns it for rework.
      parameters:
        - in: path
          name: task_id
          required: true
          schema: { type: string, format: uuid }
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          description: The submit outcome (idempotent — a resend echoes the current state).
          content: { application/json: { schema: { $ref: "#/components/schemas/PCCareSubmitResponse" } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFoundOrNotAllowed" }
        "422":
          description: >-
            Some animals still miss required videos (proof_incomplete) or none are scanned
            (no_animals).
          content: { application/json: { schema: { $ref: "#/components/schemas/ErrorEnvelope" } } }
        "500": { $ref: "#/components/responses/ServerError" }
  /feed-config/ration-rates:
    get:
      tags:
        - Feed Config
      operationId: listFeedConfigRationRates
      summary: List the currently-in-force ration grid for a park.
      description: >-
        Returns one bounded page of the authored ration grid: (ration_group, shed_tag, feed_item) ->
        grams per head per day, for the park's rows that are currently in force (valid_to is null).
        Rates are PARK-SCOPED because parks genuinely differ, so park_id is required rather than
        optional -- a tenant-wide listing would interleave two parks' rows under identical labels.
        grams_per_head is returned as an exact decimal STRING, never a JSON number, so an authored
        rate cannot be altered by float round-tripping. A rate of 0 is a real authored value
        ("milk-fed kids get 0 g of solids"); the ABSENCE of a row means "not configured" and is a
        blocking gap the feed path must surface rather than read as 0.
      parameters:
        - name: park_id
          in: query
          required: true
          schema:
            type: string
            format: uuid
        - name: ration_group
          in: query
          required: false
          schema:
            type: string
        - name: breed
          in: query
          required: false
          description: >-
            Narrow by BREED, which is not the same filter as ration_group even though both land on
            the same column. feed_ration_groups maps breed -> ration group MANY-TO-ONE (Beetal and
            Sirohi both resolve to "Beetal/Sirohi"), so this resolves the breed to its group first
            and then filters. A breed that maps to no group returns NO rows -- an unknown breed
            narrows to nothing rather than widening to everything. No breed maps to the "Kid" group
            by design: kids resolve to one group by age band and their breed is deliberately
            ignored, so a breed filter correctly excludes kid rates.
          schema:
            type: string
        - name: shed_tag
          in: query
          required: false
          schema:
            type: string
        - name: feed_item
          in: query
          required: false
          description: >-
            Repeatable. Each occurrence adds an item to the match set
            (?feed_item=Hybrid&feed_item=COFS returns rows for either), and omitting it entirely
            means no feed-item filter. Values are matched on the normalized feed-item key, so
            casing and separator differences resolve the same way the stored key does.
          schema:
            type: array
            items:
              type: string
          style: form
          explode: true
        - name: grams_op
          in: query
          required: false
          description: >-
            Comparison applied to grams_per_head, paired with grams_value. Both are supplied
            together or neither is; sending one alone is rejected 400 rather than defaulted,
            because every possible default silently answers a different question than the one
            asked. The common use is grams_op=gt&grams_value=0 -- "show me only combinations that
            actually get some of this item" -- and its mirror grams_op=eq&grams_value=0, which
            isolates the authored zeros (correct and deliberate for milk-fed K0/K1 kids).
          schema:
            type: string
            enum: [gt, gte, eq, lte, lt, neq]
        - name: grams_value
          in: query
          required: false
          description: >-
            The value grams_op compares against, as an exact decimal STRING with at most three
            decimal places -- the same representation grams_per_head is returned in, and for the
            same reason: numeric(12,3) is exact and a float round-trip is not. A value that is not
            an exact decimal is rejected 400.
          schema:
            type: string
            pattern: '^-?\d{1,9}(\.\d{1,3})?$'
        - $ref: "#/components/parameters/FeedConfigLimit"
        - $ref: "#/components/parameters/FeedConfigOffset"
      responses:
        "200":
          description: One bounded page of currently-in-force ration rates.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FeedConfigRationRatePage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
        - Feed Config
      operationId: upsertFeedConfigRationRate
      summary: Author one cell of the ration grid, effective-dated and non-destructive.
      description: >-
        Authors grams_per_head for one (park, ration_group, shed_tag, feed_item) key. The write is
        NEVER destructive: if a row is already in force from an earlier business day it is CLOSED
        (valid_to set) and a new row is opened, so the previous rate survives as history. A re-edit
        on the SAME business day corrects the open row in place, because a window closed on the day
        it opened cannot satisfy valid_to > valid_from. Re-authoring the identical value writes
        nothing and reports outcome "unchanged". The response names which of the four happened.
        grams_per_head is REQUIRED and is validated, not defaulted: absent fails the request (absence
        of a rate means "not configured", a blocking state -- it must never be filled in as 0), an
        explicit 0 is accepted as a real authored value, and a negative or over-precise value is
        rejected with a field error rather than clamped or rounded. Requires an Idempotency-Key: an
        exact replay returns the original result with idempotent_replay=true and re-runs no side
        effect, and reusing the key with a different payload is a 409.
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpsertFeedConfigRationRateRequest"
      responses:
        "200":
          description: The authored edit's outcome.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FeedConfigWriteResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "500":
          $ref: "#/components/responses/ServerError"
  /procurement/source-entry/loads:
    get:
      tags:
        - Procurement
      operationId: listProcurementSourceEntryLoads
      summary: List procurement source-entry loads.
      description: >-
        One bounded page of source-entry loads for leadership and operator follow-up.
        This is the read side for supplier warmup, source health, pre-dispatch,
        in-transit, arrival review, accepted intake, rejected, deferred, and blocked
        load states. `expected_count` is the planned animals on each load; do not
        treat a single page as a company total when `next_cursor` is present.
      parameters:
        - name: status
          in: query
          required: false
          schema:
            type: string
            maxLength: 80
          description: Optional load status filter.
        - name: cursor
          in: query
          required: false
          schema:
            type: string
          description: Opaque keyset cursor from a previous response.
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 100
      responses:
        "200":
          description: One page of source-entry loads.
          content:
            application/json:
              schema:
                type: object
                required: [items, trace_id]
                properties:
                  items:
                    type: array
                    items:
                      type: object
                      required: [load_id, tenant_id, source_party_id, expected_count, status, created_at, updated_at, row_version]
                      properties:
                        load_id: { type: string }
                        tenant_id: { type: string }
                        source_party_id: { type: string }
                        source_party_name: { type: string }
                        source_location_id: { type: string, nullable: true }
                        source_location_code: { type: string, nullable: true }
                        source_location_name: { type: string, nullable: true }
                        expected_count: { type: integer }
                        purchase_date: { type: string, format: date-time, nullable: true }
                        planned_dispatch_at: { type: string, format: date-time, nullable: true }
                        status: { type: string }
                        notes: { type: string }
                        context: { type: object, additionalProperties: true }
                        created_at: { type: string, format: date-time }
                        updated_at: { type: string, format: date-time }
                        row_version: { type: integer }
                  next_cursor:
                    type: string
                    nullable: true
                  trace_id:
                    type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /procurement/vendors:
    get:
      tags:
        - Procurement
      operationId: listProcurementVendors
      summary: List the procurement vendor register.
      description: >-
        One keyset page of the vendor register -- the farm's counterparty contact book, spanning
        livestock agents and stockists, transport, feed, manure, pellet factories, labour,
        insurance, test labs and site trades.


        `total` is the WHOLE-FILTER count, not the page length: the screen renders it as
        "306 vendors" while showing 25 of them. Paging is by opaque `cursor`, never offset.


        Payment instruments (bank_name, account_no, ifsc_code, upi_id, pan_number) are returned only
        to a caller holding `procurement.vendor.finance.read`. Otherwise they come back null with
        `finance_redacted: true`, which lets the client say "hidden" rather than render a blank that
        reads as "no bank details on file". The caller cannot request them via a parameter.
      parameters:
        - name: search
          in: query
          required: false
          schema: { type: string, maxLength: 120 }
          description: >-
            Matches a fragment anywhere in business name, contact, phone, city or record type.
            LIKE metacharacters are treated as literal text.
        - name: record_type
          in: query
          required: false
          schema: { type: string }
        - name: status
          in: query
          required: false
          schema: { type: string, enum: [active, inactive, negotiating, banned] }
        - name: state
          in: query
          required: false
          schema: { type: string }
        - name: city
          in: query
          required: false
          schema: { type: string }
        - name: breed
          in: query
          required: false
          schema: { type: string }
        - name: offset
          in: query
          required: false
          schema: { type: integer, minimum: 0, maximum: 10000, default: 0 }
          description: >-
            Rows to skip. Bounded on purpose -- the register is an authored contact book that grows
            with supplier count, never with herd size, so a capped offset stays cheap while giving
            the operator a Back control and a page number, which a forward-only cursor cannot.
            A request past the cap is REJECTED rather than clamped, so a page number never shows
            the wrong rows.
        - name: limit
          in: query
          required: false
          schema: { type: integer, minimum: 1, maximum: 100, default: 25 }
      responses:
        "200":
          description: One page of the register plus the whole-filter total.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProcurementVendorPage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
        - Procurement
      operationId: createProcurementVendor
      summary: Add a vendor to the register.
      description: >-
        Rejects a duplicate with 409 `vendor_duplicate`. A duplicate is the same business name,
        record type, state AND phone number (compared as digits only, so spacing cannot defeat it) --
        deliberately narrow, because one business legitimately has several contacts on different
        numbers and each is its own row.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProcurementVendorWrite"
      responses:
        "201":
          description: The stored vendor.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProcurementVendor"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "500":
          $ref: "#/components/responses/ServerError"
  /procurement/vendors/{vendor_id}:
    get:
      tags:
        - Procurement
      operationId: getProcurementVendor
      summary: Read one vendor.
      parameters:
        - name: vendor_id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: The vendor.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProcurementVendor"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "500":
          $ref: "#/components/responses/ServerError"
    put:
      tags:
        - Procurement
      operationId: updateProcurementVendor
      summary: Replace a vendor's fields.
      description: >-
        A REPLACE, not a patch: an omitted optional field and a cleared one both store NULL.
        Fenced on `row_version` -- a stale value is rejected with 409 `vendor_stale_write` rather
        than silently overwriting another editor's save.
      parameters:
        - name: vendor_id
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProcurementVendorWrite"
      responses:
        "200":
          description: The updated vendor.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProcurementVendor"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "500":
          $ref: "#/components/responses/ServerError"
  /procurement/vendors/{vendor_id}/status:
    post:
      tags:
        - Procurement
      operationId: updateProcurementVendorStatus
      summary: Change only a vendor's trading status.
      description: >-
        A narrow, intention-revealing write for "stop buying from this vendor" / "start again".


        It exists separately from PUT /procurement/vendors/{vendor_id} because that endpoint is a
        REPLACE: driving a status flip through it would require the caller to resend every other
        field, and any field their screen did not render would be cleared as a side effect.


        Fenced on row_version like every other vendor write.
      parameters:
        - name: vendor_id
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [status, row_version]
              properties:
                status: { type: string, enum: [active, inactive, negotiating, banned] }
                row_version: { type: integer, format: int64 }
      responses:
        "200":
          description: The updated vendor.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProcurementVendor"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "500":
          $ref: "#/components/responses/ServerError"
  /procurement/vendor-catalog:
    get:
      tags:
        - Procurement
      operationId: listProcurementVendorCatalog
      summary: The business-managed dropdown vocabularies behind the register.
      description: >-
        Record types, breeds, states, cities, statuses and feed kinds. Entries with
        `is_active: false` are retired: still rendered on a vendor that carries one, never offered
        for a new row.
      responses:
        "200":
          description: The vocabularies, grouped by kind.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProcurementVendorCatalog"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /sales/overview:
    get:
      tags:
        - Sales
      operationId: listSalesOverview
      summary: The whole sales page in one read.
      description: >-
        Whole-filter aggregates for the sales board: headline summary, monthly series, price bands,
        buyer board, demand pipelines, sale evidence and market benchmarks. Only deals with status
        `Deal Closed` count toward the summary, monthly, price-band and buyer blocks; the pipelines
        and evidence panels summarise their own tables.


        The farm filter applies to the blocks whose source carries a farm (deals, buyer leads,
        sold tags); the FPO pipeline, weight audit and market benchmarks are company-wide because
        their source records no farm.
      parameters:
        - name: farm
          in: query
          required: false
          schema: { type: string, enum: [all, CBE, CPT], default: all }
          description: Farm scope. Absent or `all` means the whole company; anything else is rejected.
      responses:
        "200":
          description: The whole page contract.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SalesOverview"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /sales/deals:
    get:
      tags:
        - Sales
      operationId: listSalesDeals
      summary: One page of the sales ledger.
      description: >-
        Every recorded deal in every status, newest sale first. `total` is the WHOLE-FILTER count,
        not the page length; the client derives the page count from it.
      parameters:
        - name: farm
          in: query
          required: false
          schema: { type: string, enum: [all, CBE, CPT], default: all }
        - name: limit
          in: query
          required: false
          schema: { type: integer, minimum: 1, maximum: 100, default: 25 }
        - name: offset
          in: query
          required: false
          schema: { type: integer, minimum: 0, maximum: 10000, default: 0 }
          description: >-
            Rows to skip. Bounded on purpose -- the ledger is an authored commercial record that
            grows with deals closed, never with herd size. A request past the cap is REJECTED
            rather than clamped, so a page number never shows the wrong rows.
      responses:
        "200":
          description: One page of the ledger plus the whole-filter total.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SalesDealPage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
        - Sales
      operationId: createSalesDeal
      summary: Record a sale.
      description: >-
        Records one deal into the ledger with status `Deal Closed`. The `Idempotency-Key` header is
        REQUIRED: an exact replay returns the originally recorded deal with no new side effects,
        and the same key replayed with different fields is rejected with 409
        `idempotency_conflict`, so a retried submit can never record a sale twice. Farm and product
        type are validated against their closed vocabularies and rejected -- never silently
        rewritten -- when unrecognised.
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SalesDealWrite"
      responses:
        "201":
          description: The recorded deal.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SalesDeal"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "500":
          $ref: "#/components/responses/ServerError"
  /sales/buyer-leads:
    get:
      tags:
        - Sales
      operationId: listSalesBuyerLeads
      summary: One page of the buyer demand pipeline.
      description: >-
        Buyer leads newest first, plus the whole-filter total and the tenant's existing call-status
        vocabulary (`status_options`) for the status picker. Statuses are free text bucketed by
        exact string on the overview, so the picker offers the stored spellings rather than
        inventing a new vocabulary that would fragment the pipeline chart.
      parameters:
        - name: limit
          in: query
          required: false
          schema: { type: integer, minimum: 1, maximum: 100, default: 20 }
        - name: offset
          in: query
          required: false
          schema: { type: integer, minimum: 0, maximum: 10000, default: 0 }
      responses:
        "200":
          description: One page of buyer leads.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SalesBuyerLeadPage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
        - Sales
      operationId: createSalesBuyerLead
      summary: Record a buyer lead.
      description: >-
        Records one buyer lead into the demand pipeline. Same idempotency contract as recording a
        sale: the `Idempotency-Key` header is required, an exact replay returns the original lead,
        and a same-key/different-payload replay is rejected 409.
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SalesBuyerLeadWrite"
      responses:
        "201":
          description: The recorded lead.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SalesBuyerLead"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "500":
          $ref: "#/components/responses/ServerError"
  /sales/buyer-leads/{lead_id}/status:
    post:
      tags:
        - Sales
      operationId: setSalesBuyerLeadStatus
      summary: Update a buyer lead's call status.
      description: >-
        Sets one lead's call status. An empty `call_status` clears it back to "not yet called".
        Same idempotency contract as the other sales writes.
      parameters:
        - name: lead_id
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: Idempotency-Key
          in: header
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SalesLeadStatusWrite"
      responses:
        "200":
          description: The updated lead.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SalesBuyerLead"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "500":
          $ref: "#/components/responses/ServerError"
  /sales/fpo-leads:
    get:
      tags:
        - Sales
      operationId: listSalesFpoLeads
      summary: One page of the farmer-group pipeline.
      description: >-
        Farmer-group (FPO) leads newest first, plus the whole-filter total and the stored
        call-status vocabulary. Company-wide -- the source carries no farm.
      parameters:
        - name: limit
          in: query
          required: false
          schema: { type: integer, minimum: 1, maximum: 100, default: 20 }
        - name: offset
          in: query
          required: false
          schema: { type: integer, minimum: 0, maximum: 10000, default: 0 }
      responses:
        "200":
          description: One page of farmer-group leads.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SalesFpoLeadPage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
        - Sales
      operationId: createSalesFpoLead
      summary: Record a farmer-group lead.
      description: Records one farmer-group lead. Same idempotency contract as the other sales writes.
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SalesFpoLeadWrite"
      responses:
        "201":
          description: The recorded lead.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SalesFpoLead"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "500":
          $ref: "#/components/responses/ServerError"
  /sales/fpo-leads/{lead_id}/status:
    post:
      tags:
        - Sales
      operationId: setSalesFpoLeadStatus
      summary: Update a farmer-group lead's call status.
      description: >-
        Sets one farmer group's call status. An empty `call_status` clears it back to
        "not yet called". Same idempotency contract as the other sales writes.
      parameters:
        - name: lead_id
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: Idempotency-Key
          in: header
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SalesLeadStatusWrite"
      responses:
        "200":
          description: The updated lead.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SalesFpoLead"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "500":
          $ref: "#/components/responses/ServerError"
  /sales/market-benchmarks:
    post:
      tags:
        - Sales
      operationId: createSalesMarketBenchmark
      summary: Record a market quote.
      description: >-
        Records one comparable market quote for the Market check panel. Same idempotency contract
        as the other sales writes.
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SalesBenchmarkWrite"
      responses:
        "201":
          description: Recorded.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SalesRecorded"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "500":
          $ref: "#/components/responses/ServerError"
  /sales/sold-tags:
    post:
      tags:
        - Sales
      operationId: createSalesSoldTags
      summary: Record a sold-animal tag list.
      description: >-
        Records the tag list handed over at one sale -- up to 200 animals in ONE atomic batch: the
        whole list commits or none of it does. Same idempotency contract as the other sales writes;
        an exact replay reports the original batch size without inserting anything.
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SalesSoldTagsWrite"
      responses:
        "201":
          description: How many animals were recorded.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SalesSoldTagsResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "500":
          $ref: "#/components/responses/ServerError"
  /sales/weight-checks:
    post:
      tags:
        - Sales
      operationId: createSalesWeightCheck
      summary: Record a weight check.
      description: >-
        Records one video-vs-book weight audit row for the Weight check panel. Same idempotency
        contract as the other sales writes.
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SalesWeightCheckWrite"
      responses:
        "201":
          description: Recorded.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SalesRecorded"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "500":
          $ref: "#/components/responses/ServerError"
  /feed-config/ration-groups:
    get:
      tags:
        - Feed Config
      operationId: listFeedConfigRationGroups
      summary: List the breed to ration-group mapping.
      description: >-
        Returns the authored map from a live breed label to its ration group. ADULT breeds only:
        kids resolve to the fixed "Kid" group by age band and never consult this map. The mapping is
        data rather than an identity function because it carries a real merge -- two live breeds can
        share one ration group.
      parameters:
        - $ref: "#/components/parameters/FeedConfigLimit"
        - $ref: "#/components/parameters/FeedConfigOffset"
      responses:
        "200":
          description: One bounded page of breed to ration-group mappings.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FeedConfigRationGroupPage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /feed-config/shed-tags:
    get:
      tags:
        - Feed Config
      operationId: listFeedConfigShedTags
      summary: List the authored shed-tag vocabulary the ration grid is indexed by.
      description: >-
        Returns the tag vocabulary matched against a live animal's management stage. applies_to
        splits the kid course from the adult course; the two sets are disjoint, which is why the
        field is single-valued. A present but unrecognised applies_to filter is REJECTED rather than
        ignored -- silently dropping it would return the full vocabulary to a caller who asked for
        one course.
      parameters:
        - name: applies_to
          in: query
          required: false
          schema:
            type: string
            enum: [adult, kid]
        - $ref: "#/components/parameters/FeedConfigLimit"
        - $ref: "#/components/parameters/FeedConfigOffset"
      responses:
        "200":
          description: One bounded page of shed tags.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FeedConfigShedTagPage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /feed-config/feed-items:
    get:
      tags:
        - Feed Config
      operationId: listFeedConfigFeedItems
      summary: List the feed-item catalog.
      description: >-
        Returns the feed items and their nutritional/handling attributes. Those attributes are
        NULLABLE on purpose and are omitted when unset: unlike a ration rate, a missing energy value
        blocks only a rollup, never a feeding decision, so an honest gap is preferable to an invented
        number.
      parameters:
        - $ref: "#/components/parameters/FeedConfigLimit"
        - $ref: "#/components/parameters/FeedConfigOffset"
      responses:
        "200":
          description: One bounded page of feed items.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FeedConfigFeedItemPage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
        - Feed Config
      operationId: createFeedConfigFeedItem
      summary: Add one entry to the tenant's feed-item catalog.
      description: >-
        Adds a feed item -- a name the ration grid, the shed factors and the experiment sheds can
        then be indexed by. It is TENANT-scoped, not park-scoped: both parks author quantities
        against the same vocabulary, so there is no park_id.
        ADDING AN ITEM AUTHORS NO QUANTITY. The new label becomes selectable immediately, and every
        combination using it stays UNCONFIGURED -- and therefore blocking -- until someone authors a
        rate for it. This endpoint never creates a rate to go with the item, not even 0, because an
        implicit 0 would read as "feed none of it" for every group and tag in the tenant.
        The three nutritional attributes are OPTIONAL and are stored as NULL when omitted, which is
        the honest "not measured" state: unlike a ration rate, a missing energy value blocks only a
        rollup, never a feeding decision. An explicit 0 stays a measured zero, and a present but
        out-of-range value is rejected with a field error rather than clamped into the column's
        range. display_order is optional and defaults to the END of the catalog -- it is a
        presentation position no feeding decision reads.
        A name the catalog already holds (compared on the same normalization the storage key uses,
        so trailing whitespace and case do not create a second entry) is a 409 rather than an
        in-place update: this is an Add, and silently rewriting an existing item's attributes would
        change data the author never opened. Requires an Idempotency-Key: an exact replay returns
        the original result with idempotent_replay=true and re-runs no side effect, and reusing the
        key with a different payload is a 409.
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateFeedConfigFeedItemRequest"
      responses:
        "200":
          description: The add's outcome. Always "inserted" on success -- there is no update branch.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FeedConfigWriteResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "500":
          $ref: "#/components/responses/ServerError"
  /feed-config/feed-items/status:
    post:
      tags:
        - Feed Config
      operationId: setFeedConfigFeedItemStatus
      summary: Retire one feed item, or restore a retired one.
      description: >-
        RETIRE, NEVER DELETE. This is how a feed item is removed, and it is a status flip rather than
        a deletion on purpose: the item's authored ration rates, shed factors and experiment cells
        are left exactly as they are, so every past feed sheet stays explainable and a later restore
        returns the item fully configured.
        Deleting would take the rates with it, and a restore would then hand back an item whose every
        combination is UNCONFIGURED -- which on this screen does not mean "no quantity", it means
        BLOCKED, and a blocked shed is not fed.
        THIS CHANGES WHAT ANIMALS ARE FED; it is not a display filter. Feed generation loads the
        catalog with status = 'active', so a retired item leaves every sheet issued from that point
        onward, and its authored rates stop being listed on the ration grid to match. Restoring puts
        both back.
        The status is a required closed enum with no default -- one value keeps the item in every
        sheet and the other removes it from all of them, so there is no safe value to fall back to.
        An item already in the requested status returns outcome "unchanged" rather than an error: the
        caller asked for a state and that state holds. Requires an Idempotency-Key: an exact replay
        returns the original result with idempotent_replay=true, and reusing the key with a different
        payload is a 409.
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SetFeedConfigFeedItemStatusRequest"
      responses:
        "200":
          description: >-
            The flip's outcome -- "corrected" when the status changed, "unchanged" when it already
            held.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FeedConfigWriteResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "500":
          $ref: "#/components/responses/ServerError"
  /feed-config/session-templates:
    get:
      tags:
        - Feed Config
      operationId: listFeedConfigSessionTemplates
      summary: List a park's feeding-session split.
      description: >-
        Returns the park's feeding sessions and the fraction of the day's computed quantity each one
        carries. The fractions across a park's active sessions are expected to sum to 1.0 -- a
        cross-row invariant no column constraint can express, so it is enforced by whoever writes
        them.
      parameters:
        - name: park_id
          in: query
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/FeedConfigLimit"
        - $ref: "#/components/parameters/FeedConfigOffset"
      responses:
        "200":
          description: One bounded page of session templates.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FeedConfigSessionTemplatePage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /feed-config/schedule:
    get:
      tags:
        - Feed Config
      operationId: listFeedConfigSchedule
      summary: Read a park's currently-in-force feed dispatch clock.
      description: >-
        Returns the park's in-force dispatch clock per workflow. The times are LOCAL India business
        calendar (Asia/Kolkata) wall-clock values and carry NO UTC offset: they are recurring
        business-calendar rules, not instants, and a consumer combines them with a business date in
        Asia/Kolkata to schedule. transport_time is omitted when the park has not declared a cutoff;
        that absence means UNKNOWN and must never be read as "no deadline".
      parameters:
        - name: park_id
          in: query
          required: true
          schema:
            type: string
            format: uuid
        - name: workflow
          in: query
          required: false
          schema:
            type: string
            enum: [normal, experiment]
        - $ref: "#/components/parameters/FeedConfigLimit"
        - $ref: "#/components/parameters/FeedConfigOffset"
      responses:
        "200":
          description: One bounded page of in-force dispatch clocks.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FeedConfigSchedulePage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
        - Feed Config
      operationId: upsertFeedConfigSchedule
      summary: Author a park's feed dispatch clock for one workflow, effective-dated.
      description: >-
        Authors the direction, correction, and transport times for one (park, workflow). The clock is
        per-workflow because the normal and experiment workflows genuinely run on different cutoffs;
        a single global time would issue one of them at the wrong hour every day. correction_time is
        when approved emergency-shifting corrections are BATCHED and the amended direction is
        reissued -- a fixed time rather than fire-on-approval, so a shed receives at most one amended
        direction per day instead of several racing ones. Times are LOCAL Asia/Kolkata wall-clock
        values ("07:00" or "07:00:00") with no offset; a value carrying an offset is rejected.
        Ordering is enforced (correction may equal but not precede direction; transport may not
        precede correction). transport_time may be sent as null to record "no declared cutoff", but a
        present-but-malformed value is REJECTED rather than dropped to null. Effective-dated and
        idempotent on the same terms as the ration-rate write.
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpsertFeedConfigScheduleRequest"
      responses:
        "200":
          description: The authored edit's outcome.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FeedConfigWriteResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "500":
          $ref: "#/components/responses/ServerError"
  /feed-config/session-template-items:
    post:
      tags:
        - Feed Config
      operationId: setFeedConfigSessionTemplateItem
      summary: Declare a feed on one feeding session's recipe, or withdraw it.
      description: >-
        THIS IS THE WRITE THAT DECIDES WHETHER A FEED REACHES AN ANIMAL. Generation walks a session's
        declared slots and looks each one up in the ration grid, so a feed with a grid quantity but
        no slot is never looked up: it is absent from the row, the summary, the totals and the
        packing worklist, and no gap is raised. Authoring grams for an undeclared feed therefore
        looks entirely correct on screen and feeds nobody.

        Declaring REFUSES with 409 `slot_rates_incomplete` when the feed has no ration rate in every
        cell of the park. A declared slot is priced for EVERY shed, and a missing rate is BLOCKED
        rather than zero, so serving it would take those sheds' whole sheets down at the next issue.
        The write fails closed instead of silently authoring the missing quantities, because
        declaring a feed and setting its quantities are different decisions.

        Withdrawing CLOSES the row rather than deleting it, so sheets already issued from it stay
        explainable. A slot declared and withdrawn on the same business day is retired in place,
        since the schema rules out a same-day window and no sheet was issued from it.

        A declare that is already in force returns `unchanged`; a withdrawal of a feed the session
        does not serve is 404 `slot_not_declared` rather than a silent success, because the author
        may be looking at the wrong session. Idempotent on the same terms as every other feed-config
        write.
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SetFeedConfigSessionTemplateItemRequest"
      responses:
        "200":
          description: The authored edit's outcome.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FeedConfigWriteResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "500":
          $ref: "#/components/responses/ServerError"
  /feed-config/shed-factors:
    get:
      tags:
        - Feed Config
      operationId: listFeedConfigShedFactors
      summary: List a park's authored per-shed feed multipliers.
      description: >-
        Returns the currently-in-force per-shed, per-feed-item multipliers -- the shed_factor term of
        head_count x grams_per_head x shed_factor. Only AUTHORED factors are returned: a shed with no
        row is treated as 1.0 by the read path, and materializing that default here would make an
        unauthored shed indistinguishable from one someone deliberately set to 1.0.
      parameters:
        - name: park_id
          in: query
          required: true
          schema:
            type: string
            format: uuid
        - name: shed_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: feed_item
          in: query
          required: false
          schema:
            type: string
        - $ref: "#/components/parameters/FeedConfigLimit"
        - $ref: "#/components/parameters/FeedConfigOffset"
      responses:
        "200":
          description: One bounded page of authored shed factors.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FeedConfigShedFactorPage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
        - Feed Config
      operationId: upsertFeedConfigShedFactor
      summary: Author one per-shed feed multiplier, effective-dated.
      description: >-
        Authors the multiplier for one (park, shed, feed_item). multiplier is REQUIRED and validated
        rather than defaulted: absent fails the request, an explicit 0 is accepted as a deliberate
        authored value, and a negative value is rejected with a field error. Effective-dated and
        idempotent on the same terms as the ration-rate write.
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpsertFeedConfigShedFactorRequest"
      responses:
        "200":
          description: The authored edit's outcome.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FeedConfigWriteResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "500":
          $ref: "#/components/responses/ServerError"
  /feed-config/experiment:
    get:
      tags:
        - Feed Config
      operationId: listFeedConfigExperiment
      summary: List hand-authored experiment pens, for one park or the whole tenant.
      description: >-
        Returns the authored ABSOLUTE kg per feed item for each experiment pen (an undivided shed is
        its single whole-shed pen). absolute_kg is a PEN TOTAL, never a per-head rate, and head_count
        travels as informational context only -- multiplying the two would overfeed the pen by its
        entire population. Active membership puts that pen on the experiment workflow; a pen with no
        active row is fed from the per-head ration grid instead. Both statuses are returned by
        default so a withdrawn pen's authored quantities stay visible
        and can be restored without re-keying them. Pagination counts complete operational pens,
        not individual cells: every feed-item row for a selected pen is returned on the same page,
        and limit/offset therefore refer to pens.
      parameters:
        - name: park_id
          in: query
          required: false
          description: >-
            Narrow to one park. ABSENT returns every authored experiment cell in the tenant, across
            both parks -- this read is deliberately the one exception to the park-scoped rule on this
            screen, because an experiment cell carries its own park while a ration rate, a session
            split and a dispatch clock are park-OWNED and have no cross-park meaning. A company-wide
            caller must be able to see all authored experiments rather than one park's silently.
          schema:
            type: string
            format: uuid
        - name: shed_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: partition_label
          in: query
          required: false
          description: >-
            Narrow to ONE PEN of the selected shed, by its human partition label ("2", "Part 3").
            Absent returns every pen of that shed. This read is pen-grained -- one shed holds many
            pens, each with its own arm, head count and authored quantities -- so a shed-only filter
            answers a coarser question than the rows it returns.
            Matched on the same normalization the stored partition key uses, so case and spacing do
            not matter. An undivided shed needs no value here: it has exactly one pen, which shed_id
            alone already selects.
          schema:
            type: string
        - name: status
          in: query
          required: false
          description: Narrow to one status. Absent returns BOTH active and retired rows.
          schema:
            type: string
            enum: [active, retired]
        - name: feed_item
          in: query
          required: false
          description: >-
            Repeatable, matching the ration grid's parameter of the same name. Each occurrence adds
            an item to the match set; omitting it means no feed-item filter. Because pagination
            counts PENS, a pen survives the filter when at least one of its cells matches, and only
            its matching cells are returned.
          schema:
            type: array
            items:
              type: string
          style: form
          explode: true
        - name: experiment_category
          in: query
          required: false
          description: >-
            Narrow to one experiment ARM ("Sheep M NEW"). Matched on the normalized label key, so
            casing and separator differences resolve the same way every other feed-config label
            does.
          schema:
            type: string
        - name: kg_op
          in: query
          required: false
          description: >-
            Comparison applied to absolute_kg, paired with kg_value; both are supplied together or
            neither is, and sending one alone is rejected 400 rather than defaulted. Named kg_ rather
            than grams_ on purpose: the ration grid compares a PER-HEAD RATE in grams and this
            compares an ABSOLUTE PEN TOTAL in kg. They are not the same quantity and must not read
            as one parameter shared between two screens.
          schema:
            type: string
            enum: [gt, gte, eq, lte, lt, neq]
        - name: kg_value
          in: query
          required: false
          description: >-
            The value kg_op compares against, as an exact decimal STRING with at most three decimal
            places -- the representation absolute_kg is returned in. A value that is not an exact
            decimal is rejected 400.
          schema:
            type: string
            pattern: '^-?\d{1,9}(\.\d{1,3})?$'
        - $ref: "#/components/parameters/FeedConfigLimit"
        - $ref: "#/components/parameters/FeedConfigOffset"
      responses:
        "200":
          description: One bounded page of complete experiment pens and all of their authored cells.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FeedConfigExperimentPage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
        - Feed Config
      operationId: upsertFeedConfigExperiment
      summary: Edit one feed-item cell of an already-enrolled experiment pen.
      description: >-
        Authors the absolute kg for one (park, shed, partition, feed_item). The pen must already have
        experiment configuration; first enrollment is accepted only by the atomic batch endpoint,
        so this route cannot leave a new pen with a partial feed set. absolute_kg is REQUIRED and
        validated rather than defaulted: absent fails the request, an explicit 0 is accepted (an arm
        that deliberately gets none of an item), and a negative or over-precise value is rejected
        with a field error. Any edit forces the pen's rows back to status='active' -- a quantity stored on a
        retired row is a number nothing reads. Unlike the ration-rate write this is NOT
        effective-dated: an existing row is corrected in place, so the outcome is never "superseded".
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpsertFeedConfigExperimentRequest"
      responses:
        "200":
          description: The authored edit's outcome.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FeedConfigWriteResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "500":
          $ref: "#/components/responses/ServerError"
  /feed-config/pens:
    get:
      tags:
        - Feed Config
      operationId: listFeedConfigPens
      summary: List operational locations (sheds and their pens).
      description: >-
        Returns every active shed in scope, and every active pen of a subdivided shed, each flagged with
        whether it already carries experiment configuration. This is the experiment enroller's
        candidate source. It cannot be derived from the experiment list (that only knows locations
        already enrolled) nor from the shed list (one enrolled pen makes the whole building look
        enrolled, which is how a partly-experimental shed's remaining pens became unreachable). Reads
        the location/partition catalog and NO per-animal table, so a pen holding zero animals is
        still listed -- usually the pen about to be filled and configured first.
      parameters:
        - name: park_id
          in: query
          required: false
          description: >-
            Omitted means every park in the tenant, matching listFeedConfigExperiment. The enroller
            needs that when the top bar reads company-wide: the experiment table spans both parks in
            that mode, so a park-locked candidate list would offer nothing for the other park's rows.
            Every returned pen carries its own park_id.
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/FeedConfigLimit"
        - $ref: "#/components/parameters/FeedConfigOffset"
      responses:
        "200":
          description: One bounded page of the park's operational locations.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FeedConfigPenPage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /feed-config/experiment/batch:
    post:
      tags:
        - Feed Config
      operationId: upsertFeedConfigExperimentBatch
      summary: Enrol one previously-unconfigured pen atomically.
      description: >-
        Authors the complete set of absolute-kg quantities for ONE pen in a single transaction. The
        whole set commits or none of it does, and that is a safety property rather than a
        convenience: a pen's authored cells are the COMPLETE list of what it is fed -- the planner
        does not fall back to the ration grid for a missing item -- so a partly-applied enrolment
        leaves the pen ON the experiment workflow fed only the items that committed, on a sheet that
        looks complete. The experiment arm and head count are carried once for the pen, never per
        item. A feed item named twice is rejected rather than de-duplicated, because the two cells
        collapse onto one row and the survivor would be arbitrary. As with the single-cell write, an
        item the author left blank must be OMITTED from items entirely -- a null absolute_kg is a
        rejected request, never an instruction to feed nothing. If the pen already has any authored
        cell, the request returns 409 rather than treating a stale or concurrent enrollment form as
        a partial update; existing pens are changed through the explicit cell editor.
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpsertFeedConfigExperimentBatchRequest"
      responses:
        "200":
          description: The authored enrolment's outcome.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FeedConfigWriteResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "500":
          $ref: "#/components/responses/ServerError"
  /feed-config/experiment/shed-status:
    post:
      tags:
        - Feed Config
      operationId: setFeedConfigExperimentShedStatus
      summary: Switch one pen between the experiment workflow and the normal ration grid.
      description: >-
        Flips the status of EVERY authored experiment row of the addressed pen in one statement. This
        changes WHAT THE ANIMALS ARE FED, not merely what is displayed: status='active' feeds the
        pen the absolute kg authored here, and status='retired' returns it to the normal per-head
        grid (projected head count x grams per head x shed factor). All cells of that pen flip
        atomically because a half-enrolled pen has no representable feed; sibling pens in the same
        shed are untouched. partition_label is required for a subdivided shed and blank for an
        undivided shed. The legacy path name is retained for compatibility.
        Withdrawal is a status flip rather than a delete, so the authored quantities survive and a
        pen can be restored without re-keying them. A pen with no authored rows at all is 404: there
        is no experiment configuration to switch, and a caller wanting to enrol one submits its
        complete item set through POST /feed-config/experiment/batch instead.
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SetFeedConfigExperimentShedStatusRequest"
      responses:
        "200":
          description: The switch's outcome.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FeedConfigWriteResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "500":
          $ref: "#/components/responses/ServerError"
  /calendar/vaccination/events:
    get:
      tags:
        - Calendar
      operationId: listCalendarVaccinationEvents
      summary: List bounded Calendar events for the Preventive Care (PC) vaccination slice.
      parameters:
        - name: park_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: shed_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: owner_key
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/CalendarOwnerFilter"
        - name: status
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/CalendarStatus"
        - name: date_from
          in: query
          required: false
          schema:
            type: string
            format: date
        - name: date_to
          in: query
          required: false
          description: Inclusive date; API rejects ranges greater than 45 days.
          schema:
            type: string
            format: date
        - name: include_date_markers
          in: query
          required: false
          description: Include bounded per-day counts for complete month-grid rendering.
          schema:
            type: boolean
            default: false
        - name: markers_only
          in: query
          required: false
          description: Return date_markers without running the paged event-list query. Intended for the month-picker marker request.
          schema:
            type: boolean
            default: false
        - name: include_reminder_rail
          in: query
          required: false
          description: Include the whole-filtered-week reminder/escalation rail summary (week view). Separate from include_date_markers so the week list gets the rail without paying for month date-markers.
          schema:
            type: boolean
            default: false
        - name: vaccine
          in: query
          required: false
          description: Exact backend-provided vaccine label from filter_options.
          schema:
            type: string
            minLength: 1
            maxLength: 120
        - name: include_filter_options
          in: query
          required: false
          description: Include caller-scoped park, shed, vaccine, status, month, and year choices on the first page.
          schema:
            type: boolean
            default: false
        - name: include_drive_summary
          in: query
          required: false
          description: Include rich park-day vaccination drive summary JSON. Omit for fast mobile/month list cards.
          schema:
            type: boolean
            default: false
        - name: cursor
          in: query
          required: false
          schema:
            type: string
            minLength: 1
            maxLength: 512
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 200
      responses:
        "200":
          description: Calendar events.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CalendarEventListResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /calendar/vaccination/events/{event_id}:
    get:
      tags:
        - Calendar
      operationId: getCalendarVaccinationEvent
      summary: Get one Calendar event with vaccination-specific sidebar detail.
      parameters:
        - $ref: "#/components/parameters/CalendarEventId"
      responses:
        "200":
          description: Calendar event detail.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CalendarEventDetail"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "500":
          $ref: "#/components/responses/ServerError"
  /calendar/vaccination/events/{event_id}/targets:
    get:
      tags:
        - Calendar
      operationId: listCalendarVaccinationDriveTargets
      summary: List herd animals eligible for a vaccination drive calendar event with cursor pagination.
      parameters:
        - $ref: "#/components/parameters/CalendarEventId"
        - name: cursor
          in: query
          required: false
          schema:
            type: string
            minLength: 1
            maxLength: 512
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 50
            default: 10
        - name: q
          in: query
          required: false
          description: Search roster by display id, tag, shed, stage, lifecycle, health, reason, or status.
          schema:
            type: string
            maxLength: 128
      responses:
        "200":
          description: Paginated herd animals for the selected vaccination drive.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CalendarDriveTargetListResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "500":
          $ref: "#/components/responses/ServerError"
  /calendar/vaccination/events/{event_id}/history:
    get:
      tags:
        - Calendar
      operationId: getCalendarVaccinationEventHistory
      summary: Get audit, reminder, nudge, snooze, proof, and verification history for one Calendar event.
      parameters:
        - $ref: "#/components/parameters/CalendarEventId"
        - name: cursor
          in: query
          required: false
          schema:
            type: string
            minLength: 1
            maxLength: 512
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 100
      responses:
        "200":
          description: Calendar event history.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CalendarHistoryResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "500":
          $ref: "#/components/responses/ServerError"
  /calendar/vaccination/events/{event_id}/nudge:
    post:
      tags:
        - Calendar
      operationId: sendCalendarVaccinationEventNudge
      summary: Idempotently queue a nudge notification for a Calendar event.
      parameters:
        - $ref: "#/components/parameters/CalendarEventId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CalendarNudgeRequest"
      responses:
        "200":
          description: Nudge queued or replayed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CalendarActionResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "500":
          $ref: "#/components/responses/ServerError"
  /calendar/vaccination/events/{event_id}/snooze:
    post:
      tags:
        - Calendar
      operationId: snoozeCalendarVaccinationEvent
      summary: Idempotently record a durable snooze for a Calendar event.
      parameters:
        - $ref: "#/components/parameters/CalendarEventId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CalendarSnoozeRequest"
      responses:
        "200":
          description: Snooze recorded or replayed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CalendarActionResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "500":
          $ref: "#/components/responses/ServerError"
  /calendar/vaccination/events/{event_id}/escalation/acknowledge:
    post:
      tags:
        - Calendar
      operationId: acknowledgeCalendarVaccinationEscalation
      summary: Idempotently acknowledge the active escalation for a vaccination Calendar obligation.
      parameters:
        - $ref: "#/components/parameters/CalendarEventId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CalendarEscalationAcknowledgeRequest"
      responses:
        "200":
          description: Escalation acknowledged or replayed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CalendarActionResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "500":
          $ref: "#/components/responses/ServerError"
  /calendar/vaccination/events/{event_id}/escalation/resolve:
    post:
      tags:
        - Calendar
      operationId: resolveCalendarVaccinationEscalation
      summary: Idempotently resolve the active escalation for a vaccination Calendar obligation.
      parameters:
        - $ref: "#/components/parameters/CalendarEventId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CalendarEscalationResolveRequest"
      responses:
        "200":
          description: Escalation resolved or replayed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CalendarActionResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "500":
          $ref: "#/components/responses/ServerError"
  /vaccination/verification-queue:
    get:
      tags:
        - Vaccination
      operationId: vaccinationVerificationQueue
      summary: List recorded vaccination completions awaiting proof verification.
      parameters:
        - name: park_id
          in: query
          required: false
          description: Optional park scope (top-bar park); filters the queue to that park's completions.
          schema:
            type: string
            format: uuid
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 500
        - name: cursor
          in: query
          required: false
          description: Opaque keyset cursor returned by the previous verification-queue page.
          schema:
            type: string
      responses:
        "200":
          description: Vaccination verification queue.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VaccinationQueueResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /goats/{goat_id}/passport:
    get:
      tags:
        - Vaccination Passport
      operationId: getGoatVaccinationPassport
      summary: "Get a goat vaccination passport: open obligations, next due, last accepted dose, and history."
      parameters:
        - $ref: "#/components/parameters/GoatId"
      responses:
        "200":
          description: Vaccination passport.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VaccinationPassport"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /app/roster/timetable:
    get:
      tags:
        - Operator Roster
      operationId: getOperatorTimetable
      summary: Get the operator's center timetable with enriched position and coverage data.
      parameters:
        - name: center_id
          in: query
          required: true
          schema:
            type: string
            format: uuid
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 50
      responses:
        "200":
          description: Operator timetable.
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: "#/components/schemas/EnrichedPosition"
                  trace_id:
                    type: string
                    format: uuid
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /app/roster/my-coverage:
    get:
      tags:
        - Operator Roster
      operationId: getMyCoverage
      summary: Get the authenticated operator's current coverage status.
      responses:
        "200":
          description: Operator's current coverage.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MyCoverageResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /admin/roster/coverage:
    get:
      tags:
        - Operator Roster
      operationId: adminRosterCoverage
      summary: List workforce coverage rows for leadership roster coverage questions.
      parameters:
        - name: scope_type
          in: query
          required: false
          schema:
            type: string
            maxLength: 80
        - name: scope_id
          in: query
          required: false
          schema:
            type: string
            maxLength: 120
        - name: active
          in: query
          required: false
          schema:
            type: boolean
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 500
      responses:
        "200":
          description: Workforce coverage rows.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CoverageListResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /app/vaccination/obligations/{obligation_id}/reschedule:
    post:
      tags:
        - Vaccination
      operationId: appRescheduleObligation
      summary: Reschedule an open vaccination obligation to a new due date.
      description: >-
        Reschedules an open (scheduled/due) vaccination obligation to a new due date,
        targeted by obligation_id. Idempotent via the Idempotency-Key header: an exact
        replay returns the original result without re-running the write; a same-key/different-payload
        replay is rejected with 409. A health-held ('deferred') obligation cannot be reached
        through this endpoint. A 'missed' target is immutable closed history — this endpoint
        creates a brand-new obligation for the new due date and returns the new obligation_id
        rather than mutating the missed row.
      parameters:
        - $ref: "#/components/parameters/ObligationId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RescheduleObligationRequest"
      responses:
        "200":
          description: Obligation rescheduled successfully.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RescheduleObligationResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "500":
          $ref: "#/components/responses/ServerError"
  /app/vaccination/gaps:
    get:
      tags:
        - Vaccination
      operationId: appVaccinationGaps
      summary: List animals excluded from the vaccination coverage denominator, with the reason.
      description: >-
        Bounded, cursor-paginated animals whose identity data is incomplete (no date of birth, no
        breed on record) and are therefore excluded from the coverage %. Scoped by tenant + optional
        park_id, mirroring the same park-scope mechanism /vaccination/execution and
        /vaccination/operations already use. Backs the mobile "Data gaps" overlay.
      parameters:
        - name: park_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 500
        - $ref: "#/components/parameters/Cursor"
      responses:
        "200":
          description: Bounded gap rows + scoped reason-count summary.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VaccinationGapsResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /app/vaccination/coverage:
    get:
      tags:
        - Vaccination
      operationId: appVaccinationCoverage
      summary: Per-vaccine given-dose count + coverage % rollup for a scope.
      description: >-
        Reuses the same indexed cohort×protocol rollup /vaccination/operations already reads,
        re-aggregated by protocol only (no new hot-table query). Backs the mobile "Doses given"
        overlay.
      parameters:
        - name: park_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: as_of
          in: query
          required: false
          description: >-
            Current-view scope only (top-bar date). Defaults to now; future values clamp to now. A past
            instant is rejected with 400 historical_as_of_unsupported — this read keeps only the current
            serving projection, so historical point-in-time reconstruction is not supported.
          schema:
            type: string
            format: date-time
        - name: due_before
          in: query
          required: false
          schema:
            type: string
            format: date-time
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 500
      responses:
        "200":
          description: Per-vaccine given/coverage rollup.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VaccinationCoverageResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /app/vaccination/execution/sheds/{shed_id}/roster:
    get:
      tags:
        - Vaccination
      operationId: appScanRoster
      summary: Per-animal vaccination scan roster for a shed (keyset-paginated).
      description: >-
        Bounded per-animal obligations for the operator scan screen (RFID tags + vaccine label +
        status). task_id is OPTIONAL: given -> roster pinned to that task's batch; omitted ->
        shed-wide roster. Park-scoped actors only see sheds in their authorized parks.
      parameters:
        - $ref: "#/components/parameters/ShedId"
        - name: partition_label
          in: query
          required: false
          description: Raw operational partition label within the physical shed. Operators send the label from the selected execution card.
          schema:
            type: string
        - name: task_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: cursor
          in: query
          required: false
          description: Opaque keyset cursor for the next page. Omit to start from the beginning.
          schema:
            type: string
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 5000
      responses:
        "200":
          description: Keyset page of per-animal scan obligations.
          content:
            application/json:
              schema:
                type: object
                properties:
                  source:
                    type: string
                  rows:
                    type: array
                    items:
                      type: object
                      properties:
                        goatId: { type: string }
                        primaryTag: { type: string }
                        secondaryTag: { type: string, nullable: true }
                        vaccineLabel: { type: string }
                        status: { type: string }
                        scannedAt:
                          type: string
                          format: date-time
                          nullable: true
                          description: Exact RFID scan timestamp persisted by the backend for this task row.
                        obligationId: { type: string }
                        obligationRowVersion:
                          type: integer
                          description: obligation_instances.row_version for this row's obligation — bumps on every transition, including a verifier rejection reopening it for re-capture. The scan-capture idempotency key discriminator; a mobile client must fold this into the key so a genuinely-new scan after a reopen is not deduped away as a replay of the prior cycle's capture.
                        taskId: { type: string }
                        batchId: { type: string }
                        sopVersionId: { type: string }
                        taskRowVersion: { type: integer }
                  taskId: { type: string }
                  batchId: { type: string }
                  sopVersionId: { type: string }
                  taskRowVersion: { type: integer }
                  next_cursor:
                    type: string
                    nullable: true
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: Task not found or outside your scope.
        "500":
          $ref: "#/components/responses/ServerError"
  /app/vaccination/tasks/{task_id}/option-values:
    get:
      tags:
        - Vaccination
      operationId: appTaskOptionValues
      summary: Pinned capture-form option sources (vaccine lots FEFO, route/site) for a SOP task.
      parameters:
        - $ref: "#/components/parameters/TaskId"
        - $ref: "#/components/parameters/AcceptLanguage"
        - $ref: "#/components/parameters/GoatOSLocale"
      responses:
        "200":
          description: Option sources for the task's capture form.
          content:
            application/json:
              schema:
                type: object
                properties:
                  task_id: { type: string }
                  batch_id: { type: string }
                  sop_version_id: { type: string }
                  task_row_version: { type: integer }
                  sources:
                    type: array
                    items:
                      type: object
                      properties:
                        source: { type: string }
                        disabled_reason: { type: string, nullable: true }
                        options:
                          type: array
                          items:
                            type: object
                            properties:
                              value: { type: string }
                              label: { type: string }
                              disabled: { type: boolean }
                              disabled_reason: { type: string, nullable: true }
                              available_quantity: { type: string, nullable: true }
                              quantity_unit: { type: string, nullable: true }
                              expiry_date: { type: string, format: date-time, nullable: true }
                              fefo_rank: { type: integer, nullable: true }
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: Task not found or outside your scope.
        "500":
          $ref: "#/components/responses/ServerError"
  /app/config:
    get:
      tags:
        - Operator App Bootstrap
      operationId: appConfig
      summary: Get the mobile live-config bundle (nav/labels/flags/tunables/kill-switch).
      description: >-
        docs/mobile/backend-driven-config.md primary config poll. Returns presentationConfig
        (feature flags, department-sourced owned-module registry) + clientRuntimeConfig (bounded
        operational knobs) stamped with a monotonic revision + ETag, plus a read-only policy_revision
        traceability echo. Supports conditional GET: a matching If-None-Match returns 304 with no
        body. Side-effect-free; no FCM push is wired by this endpoint (poll/ETag only).
      parameters:
        - $ref: "#/components/parameters/AcceptLanguage"
        - $ref: "#/components/parameters/GoatOSLocale"
        - name: If-None-Match
          in: header
          required: false
          schema:
            type: string
      responses:
        "200":
          description: Full config bundle.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppConfigResponse"
        "304":
          description: Config unchanged since the given ETag.
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /herd-register/summary:
    get:
      tags:
        - Herd Register
      operationId: getHerdRegisterSummary
      summary: Get exact scoped herd counts from canonical goats.
      parameters:
        - name: lifecycle_status
          in: query
          required: false
          schema:
            type: string
            description: Filter by lifecycle status
        - name: park_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
            description: Filter by park ID
        - name: breed
          in: query
          required: false
          schema:
            type: string
            description: Filter by breed
        - name: sex
          in: query
          required: false
          schema:
            type: string
            description: Filter by sex
      responses:
        "200":
          description: Exact scoped counts from canonical goats.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HerdRegisterSummaryResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /counts/breakdown:
    get:
      tags:
        - Counts
      operationId: getCountsBreakdown
      summary: Census head counts grouped by farm, stage, breed, gender and shed.
      description: >-
        Pre-aggregated animal counts over canonical live goats, grouped by
        farm x management_stage x breed x sex x shed. `total_count` and every
        `charts` series are rolled up over the FULL filtered result set and are
        therefore independent of `limit`/`offset` — only `items` is a page.
        `management_stage` is raw source text with no controlled vocabulary, so
        near-duplicate labels can appear as distinct rows; `facets.stages`
        reports the values actually present so a filter can never offer an
        option that matches nothing.
      parameters:
        - name: park_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
            description: Filter by park location ID (top-bar scope)
        - name: shed_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
            description: Filter by shed location ID
        - name: partition_label
          in: query
          required: false
          schema:
            type: string
            nullable: true
            description: Filter by shed partition label (null/omitted means all partitions of shed)
        - name: management_stage
          in: query
          required: false
          schema:
            type: string
            description: Filter by raw management stage
        - name: breed
          in: query
          required: false
          schema:
            type: string
            description: Filter by breed
        - name: sex
          in: query
          required: false
          schema:
            type: string
            description: Filter by sex
        - name: lifecycle_status
          in: query
          required: false
          schema:
            type: string
            default: alive
            description: Filter by lifecycle status; defaults to alive
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 10
            description: Page size for items
        - name: offset
          in: query
          required: false
          schema:
            type: integer
            minimum: 0
            maximum: 5000
            default: 0
            description: Page offset over the aggregated grain set
      responses:
        "200":
          description: Aggregated census breakdown with whole-result totals and chart series.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CountsBreakdownResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /counts/herd-analytics:
    get:
      tags:
        - Counts
      operationId: getHerdAnalytics
      summary: Herd composition now, beside month-by-month births, exits and pen movements.
      description: >-
        The Counts leadership read. Composition series are the CURRENT live herd
        (canonical goats, merged identities excluded) — the same population
        `/counts/breakdown` reports, so the two screens cannot disagree about the
        denominator. Flow figures are counted off the canonical row that recorded
        each event: an animal's own origin columns for a birth, its own exit
        columns for a death/sale/other exit, and an APPLIED shifting event for a
        movement, dated the day the operator completed it. Buckets are
        `Asia/Kolkata` calendar months, never UTC and never a rolling day window,
        and a month with no activity is returned as an explicit zero rather than
        omitted. `totals` are whole-window rollups over exactly the requested days
        and must be read from the response, never re-derived from `months`. The
        `months` SERIES is bucketed by business month, so a window whose edge falls
        mid-month produces a partial first or last bucket.
      parameters:
        - name: park_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
            description: Filter by park location ID (top-bar scope). A movement matches on either end.
        - name: from
          in: query
          required: false
          schema:
            type: string
            format: date
            description: >-
              Inclusive first IST business day of the flow window. Must be given together
              with `to`.
        - name: to
          in: query
          required: false
          schema:
            type: string
            format: date
            description: >-
              Inclusive last IST business day. Omitting BOTH bounds selects the default
              window — the first of the month eleven back through today, which opens on a
              month boundary so the first chart column is a whole month. Anything else
              invalid is rejected with 400, never rewritten: only one bound, a malformed
              date, a reversed range, or a span wider than 1150 days.
      responses:
        "200":
          description: Live composition series plus the month-by-month flow series.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HerdAnalyticsResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /counts/milk-preparation:
    get:
      tags:
        - Counts
      operationId: getMilkPreparation
      summary: Get today's per-shed milk preparation direction from the canonical live herd.
      description: >-
        Returns one bounded page at physical park x shed x milk cohort grain for K1, K2 and K3,
        plus a whole-scope summary invariant to limit/offset. Quantities are exact integer
        millilitres. This is a current live-herd direction only: it accepts no historical date and
        therefore never presents today's animal locations as a past plan. K0 colostrum and ICU or
        other clinical feeding are excluded until an approved quantity rule exists; they are not
        represented as zero.
      parameters:
        - name: park_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
          description: Optional park scope from the admin top bar.
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 50
            default: 10
        - name: offset
          in: query
          required: false
          schema:
            type: integer
            minimum: 0
            maximum: 5000
            default: 0
          description: Offset over the bounded physical shed x milk cohort grain set.
      responses:
        "200":
          description: Current milk preparation direction and whole-scope totals.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MilkPreparationPage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /app/counts/milk-preparation:
    get:
      tags:
        - Counts
      operationId: getAppCountsMilkPreparation
      summary: Get today's operator Milk Preparation farm worklist and direction.
      description: >-
        Operator-authorized form of the current-day Milk Preparation read. farm_tasks is the
        bounded actionable farm_day worklist; items remains the paged internal milk-cohort
        direction. Summary and farm_tasks cover the whole selected scope and never depend on the
        requested item page.
      parameters:
        - name: park_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 20
            default: 20
        - name: offset
          in: query
          required: false
          schema:
            type: integer
            minimum: 0
            maximum: 5000
            default: 0
      responses:
        "200":
          description: Current operator shed-day worklist, direction page, and whole-scope totals.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MilkPreparationPage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /app/counts/milk-preparation/submit:
    post:
      tags:
        - Counts
      operationId: submitAppCountsMilkPreparation
      summary: Submit every applicable milk-preparation step video for one verifier verdict.
      description: >-
        Creates one pending_verification preparation attempt at shed x preparation_date grain.
        When goat_milk_used is true, five distinct completed in-app-camera videos are required:
        goat-milk quantity, boiling temperature, cooled temperature, UHT-milk quantity, and
        citric-acid mixing. When false, the last two videos are required. Each proof is bound to
        its exact step and shed; one proof cannot satisfy two steps. All applicable videos travel
        on one generic Verification item. This command never completes preparation: one verifier
        approval completes the whole attempt and rejection returns it to rework with immutable
        prior attempts preserved.
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MilkPreparationSubmissionRequest"
      responses:
        "202":
          description: The full step-video package is pending verifier review.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MilkPreparationSubmissionResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "500":
          $ref: "#/components/responses/ServerError"
  /app/counts/milk-feeding/tasks:
    get:
      tags: [Counts]
      operationId: listAppMilkFeedingTasks
      summary: List the four daily Milk Feeding actions at farm-session grain.
      description: Returns backend-owned farm x feeding_date x session tasks. Session times are 08:00, 12:00, 16:00, and 21:00 IST. Milk Feeding is a separate Action and never appears inside Milk Preparation.
      parameters:
        - name: feeding_date
          in: query
          required: false
          schema: { type: string, format: date }
        - name: park_id
          in: query
          required: false
          schema: { type: string, format: uuid }
        - name: session_no
          in: query
          required: false
          schema: { type: integer, minimum: 1, maximum: 4 }
        - name: limit
          in: query
          required: false
          schema: { type: integer, minimum: 1, maximum: 20, default: 20 }
        - name: offset
          in: query
          required: false
          schema: { type: integer, minimum: 0, maximum: 5000, default: 0 }
      responses:
        "200":
          description: Bounded Milk Feeding task page.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MilkFeedingPage" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "500": { $ref: "#/components/responses/ServerError" }
  /app/counts/milk-feeding/tasks/{task_id}/submit:
    post:
      tags: [Counts]
      operationId: submitAppMilkFeedingTask
      summary: Submit one Milk Feeding session's questions and two videos for verification.
      description: Stores an immutable attempt and moves the farm-session task to pending_verification. Approval completes it; rejection returns it to rework and requires fresh videos.
      parameters:
        - name: task_id
          in: path
          required: true
          schema: { type: string, format: uuid }
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/MilkFeedingSubmissionRequest" }
      responses:
        "202":
          description: Questions and both proofs are pending verifier review.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MilkFeedingSubmissionResponse" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/WriteConflict" }
        "422": { $ref: "#/components/responses/UnprocessableEntity" }
        "500": { $ref: "#/components/responses/ServerError" }
  /verification/queue:
    get:
      tags:
        - Verification
      operationId: listVerificationQueue
      summary: List the Verifier's keyset-paginated media queue (generic Verification vertical).
      description: >-
        The standalone Verifier section's queue: media items awaiting or already given a verdict,
        category/vertical/module filtered, oldest-captured-first. Gated on verification.review --
        the VISIBILITY permission, held by the Verifier and by CEO/CxO leadership so the same
        evidence and verdicts can be seen by whoever must act on them. Recording the verdict is a
        separate, narrower gate (verification.verdict; see the verdict endpoint). Media is returned
        as streamed signed download URLs resolved via the existing proof storage port -- never
        inline bytes.
      parameters:
        - name: category
          in: query
          required: false
          description: >-
            Verification type-registry category (e.g. vaccination_proof). When omitted, a verifier sees all evidence across their assigned categories (the "All evidence" landing view). Leadership (CEO/CxO) sees all categories when omitted. When specified, filters to a single category and requires authorization for that category.
          schema:
            type: string
            maxLength: 120
        - name: vertical
          in: query
          required: false
          schema:
            type: string
            maxLength: 120
        - name: module
          in: query
          required: false
          schema:
            type: string
            maxLength: 120
        - name: nav_module
          in: query
          required: false
          description: >-
            Verifier-drawer module key from filter_options.modules (e.g. feed_direction, counts).
            Filters to EVERY category registered under that module — Feed spans feed_distribution,
            feed_packing and feed_transport — so it is a wider selection than `category` and a
            narrower one than no filter at all. It never widens an authorized category set: a
            verifier asking for a module she holds no duty for answers 403 module_scope_forbidden,
            an unregistered key answers 400 invalid_module, and combining it with a `category` from
            a different module answers 400 module_category_conflict.
          schema:
            type: string
            maxLength: 120
        - name: status
          in: query
          required: false
          description: >-
            Defaults to pending when omitted. `all` applies no status filter and returns due,
            approved and rejected items together — it must be sent explicitly, because an absent
            parameter means the pending landing tab.
          schema:
            type: string
            enum: [all, pending, approved, rejected]
        - name: business_date
          in: query
          required: false
          description: Asia/Kolkata capture date. Defaults to today's business date for the verifier queue.
          schema:
            type: string
            format: date
        - name: business_date_from
          in: query
          required: false
          description: >-
            Inclusive start of an Asia/Kolkata capture-date range. Must be sent together with
            business_date_to (400 invalid_business_date_range otherwise), and cannot be combined
            with business_date or missed (400 invalid_date_scope).
          schema:
            type: string
            format: date
        - name: business_date_to
          in: query
          required: false
          description: >-
            Inclusive end of an Asia/Kolkata capture-date range. Cannot be earlier than
            business_date_from (400 invalid_business_date_range) or in the future
            (400 future_business_date).
          schema:
            type: string
            format: date
        - name: missed
          in: query
          required: false
          description: When true, returns pending items captured before today's Asia/Kolkata business day. Cannot be combined with business_date or a non-pending status.
          schema:
            type: boolean
            default: false
        - name: park_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: shed_id
          in: query
          required: false
          description: >-
            Opaque shed filter key returned by filter_options.sheds. Partitioned locations use
            `<shed UUID>#<normalized partition>`; a bare shed UUID remains supported and selects
            every partition of that physical shed.
          schema:
            type: string
        - name: cursor
          in: query
          required: false
          schema:
            type: string
            minLength: 1
            maxLength: 512
        - name: limit
          in: query
          required: false
          description: Defaults to 20, capped at 100.
          schema:
            type: integer
            minimum: 1
            maximum: 100
      responses:
        "200":
          description: One keyset page of the verification queue.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VerificationQueueResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /verification/items/{item_id}/verdict:
    post:
      tags:
        - Verification
      operationId: recordVerificationVerdict
      summary: Record the Verifier's approve/reject decision on one verification item.
      description: >-
        Reject REQUIRES a non-empty reason (422 otherwise -- a syntactically valid request that fails
        the business rule). Optimistic concurrency via row_version. Approving does NOT complete or act
        on the underlying producer record (e.g. a vaccination obligation) -- the verifier's verdict is
        advisory input; an authorized Park Head/Director/CEO/CxO closes the complete drive
        submission only after every goat proof in it has been approved.


        Gated on verification.verdict, held by the Verifier role ALONE (maintainer decision
        2026-08-03). CEO/CxO reads the same queue and still closes the work, but cannot record the
        verdict: the independent second check must not be signable by the people whose work it
        checks.

        APPROVE additionally verifies that every proof object still EXISTS in storage, not merely
        that a signed link can be issued for it (a link resolves from the DB row alone). A missing
        object answers 422 evidence_missing (terminal, retryable=false); a failed availability
        check answers 422 evidence_check_failed (retryable=true). This costs a stat per proof for
        ONE item at decision time and is deliberately NOT done on the queue read, where it would be
        an N+1. REJECT is never gated on evidence: when the proof is gone, sending the work back
        for rework is the only correct action left, so it must always remain available.
      parameters:
        - name: item_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VerificationVerdictRequest"
      responses:
        "200":
          description: Verdict recorded.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VerificationVerdictResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "500":
          $ref: "#/components/responses/ServerError"
  /verification/action-queue:
    get:
      tags:
        - Verification
      operationId: listVerificationActionQueue
      summary: List verifier-approved items awaiting scoped leadership closure.
      parameters:
        - name: category
          in: query
          required: false
          schema:
            type: string
            maxLength: 120
        - name: park_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: shed_id
          in: query
          required: false
          description: >-
            Opaque shed filter key returned by filter_options.sheds. Partitioned locations use
            `<shed UUID>#<normalized partition>`; a bare shed UUID selects every partition.
          schema:
            type: string
        - name: cursor
          in: query
          required: false
          schema:
            type: string
            maxLength: 512
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 100
      responses:
        "200":
          description: Scoped leadership action queue.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VerificationQueueResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /verification/items/{item_id}/close:
    post:
      tags:
        - Verification
      operationId: closeVerificationItem
      summary: Operationally close an independently approved item.
      parameters:
        - name: item_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VerificationCloseRequest"
      responses:
        "200":
          description: Item closed; the producer applies its business transition asynchronously.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VerificationVerdictResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "500":
          $ref: "#/components/responses/ServerError"
  /verification/submissions/{submission_id}/close:
    post:
      tags:
        - Verification
      operationId: closeVerificationSubmission
      summary: Operationally close one fully verifier-approved drive submission.
      description: >-
        Atomically closes every goat proof item in the operator submission. The request fails
        closed unless every item has an approved verifier verdict. Replays after a successful
        close return the already-closed item set.
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
        - name: submission_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Drive submission closed; producer transitions are emitted per goat.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VerificationCloseSubmissionResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "500":
          $ref: "#/components/responses/ServerError"
  /verification/review-events:
    post:
      tags:
        - Verification
      operationId: recordVerificationReviewEvents
      summary: Ingest one batch of verifier video-review telemetry events.
      description: >-
        CEO integrity signal: proves (or disproves) that a verifier actually WATCHED a proof video
        rather than rubber-stamping the verdict. The browser flushes a small batch periodically and
        Gated on verification.verdict, NOT verification.review. Ingest carries verifier-only
        authority: this stream measures whether the person who signs the second check actually
        watched the evidence, so a read-only leadership principal (CEO/CxO hold verification.review
        for visibility but never verification.verdict) must not be able to write rows into it under
        their own actor id. Reading the derived facts stays on verification.review -- leadership
        must be able to SEE the signal it cannot write.


        Idempotent per event: client_event_id is a client-minted UUID and is the idempotency key
        for that one event -- a replayed batch (retry after a network blip) inserts nothing new and
        `inserted` reports 0 on an exact replay.


        Every event's item_id is authorized against the caller's authorized categories (the SAME
        rule GET /verification/queue applies): an item outside those categories answers 403.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VerificationReviewEventBatchRequest"
      responses:
        "200":
          description: Batch accepted (idempotent count of newly-inserted rows).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VerificationReviewEventBatchResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "500":
          $ref: "#/components/responses/ServerError"
  /verification/items/{item_id}/review-facts:
    get:
      tags:
        - Verification
      operationId: getVerificationItemReviewFacts
      summary: Derived per-actor watch/timing integrity facts for one verification item.
      description: >-
        Computed read-time from the raw verification_review_events stream for this one item
        (bounded by that item's own event count, not a whole-table scan): total distinct-covered
        watch time, watch fraction against the proof's duration, play/pause/seek counts, time from
        item_opened to verdict_recorded, and whether the watch fraction cleared the configured
        watched_full threshold.
      parameters:
        - name: item_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Derived review facts, one entry per actor who reviewed this item.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VerificationItemReviewFactsResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "500":
          $ref: "#/components/responses/ServerError"
  /verification/oversight-analytics:
    get:
      tags:
        - Verification
      operationId: getVerificationOversightAnalytics
      summary: CEO/PC-Director-only aggregate analytics for the Verify oversight view.
      description: >-
        Server-computed aggregates rendered ABOVE the /verify queue table when the caller's page
        contract carries the oversight_analytics control: a KPI strip (videos waiting, oldest
        pending age, review speed, estimated days to clear the backlog, per-module median review
        latency, reject rate), pending backlog by module, and per-verifier last-14-day activity
        plus a watch-integrity aggregate. Gated on permissions.VerificationOversee -- the SAME
        capability as the oversight_analytics/oversight_filters page-contract controls, never a
        role string. A verifier who holds verification.review/verdict but not
        verification.oversee receives 403 here even though she can read the plain queue. All
        numbers are bounded, tenant-scoped aggregate reads, never a client-side mega-fetch or
        per-verifier fan-out.
      responses:
        "200":
          description: The tenant's oversight analytics aggregate.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VerificationOversightAnalyticsResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /verification/video-log:
    get:
      tags:
        - Verification
      operationId: getVerificationVideoLog
      summary: Per-shed video arrival log for one business day.
      description: >-
        For ONE Asia/Kolkata business day, which sheds had proof arrive and at what time each proof
        was uploaded -- feed distribution's three captures, feed packing's one, feed transport's
        one, and the vaccination, weighing, birth, death and shifting proofs beside them.
        Maintainer decision 2026-08-14.

        Gated on permissions.verification.evidence_timeline, which is a DIFFERENT capability from
        verification.oversee: the VERIFIER holds this one and not that one, so she sees the video
        log while the module chips, the capture-date range picker and the oversight analytics stay
        leadership-only. The log is cross-module for every caller who holds the capability, because
        the question is "what arrived from this shed today" and a shed's day spans modules. It
        grants no verdict authority and reshapes no queue. A caller whose grant is park-scoped is
        clamped to their parks, exactly as on the queue read.

        TWO LEVELS, both bounded. Without shed_id the response carries the day's per-shed summary
        (counts, first and last arrival, which modules contributed) and an empty rows array. With
        shed_id it additionally carries that one location's work in full. A flat list of every proof
        for a day is deliberately not offered: a vaccination drive raises one item per animal, so a
        park-day can hold several hundred items before any feed work is counted.

        Times are when the SERVER accepted the upload (proof_artifacts.uploaded_at), not a device
        capture time -- no per-proof capture timestamp exists in the schema, and inventing one from
        the registration time would present a guess as a fact. registered_at is exposed alongside so
        a reader can see real upload lag.
      parameters:
        - in: query
          name: business_date
          required: false
          schema:
            type: string
            format: date
          description: >-
            Asia/Kolkata calendar day, YYYY-MM-DD. Defaults to today. A future date is rejected
            (422 future_business_date) rather than returning an empty log that reads as "nothing was
            filmed".
        - in: query
          name: park_id
          required: false
          schema:
            type: string
            format: uuid
          description: Optional park filter, applied ON TOP of the caller's authorized park scope.
        - in: query
          name: shed_id
          required: false
          schema:
            type: string
          description: >-
            Selects one operational location for the detail level. Carries the composite
            "<shed_uuid>#<normalized partition>" form the queue's shed filter uses -- a bare shed
            uuid cannot tell Castro 1 from Castro 2, and is accepted as "any partition of this
            shed". Use the shed_key returned on each summary row.
        - in: query
          name: all_sheds
          required: false
          schema:
            type: boolean
          description: >-
            Whole-day EXPORT: returns rows for every shed in scope, each carrying its own location,
            instead of only the selected shed. Ignores shed_id. This is the one caller allowed to
            read the day at row grain and is intended for a CSV download, never for rendering a
            screen -- the panel stays two-level because a park-day can carry several hundred items.
            Bounded at 20000 rows, with rows_truncated set if the day exceeds that.
        - in: query
          name: limit
          required: false
          schema:
            type: integer
            minimum: 1
          description: >-
            Bounds the row read (default 200, maximum 500; for all_sheds the default and maximum are
            both 20000). When more work exists than the limit allows, rows_truncated is true so a
            partial day is never presented as a whole one.
      responses:
        "200":
          description: The day's per-shed arrival summary, plus one shed's detail when requested.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VerificationVideoLogResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "500":
          $ref: "#/components/responses/ServerError"
  /app/counts/shifting/destinations:
    get:
      tags:
        - Counts
      operationId: listAppCountsShiftingDestinations
      summary: List the park -> shed destinations an operator may shift animals into.
      description: >-
        Returns the destination options for the shifting form as a CASCADE: every active park, each
        carrying its own active sheds. It is the backend-owned option vocabulary for the two
        destination dropdowns - the client renders what it is given and must not hold a hardcoded
        shed list, re-sort the options, or construct a destination from a typed name.
        The cascade is required for CORRECTNESS, not presentation: shed names REPEAT across parks
        (for example a 'Castro 1' exists under more than one park), so a flat shed list shows the
        operator two identical-looking options. Nesting each shed under its park disambiguates the
        choice for a human, and every option carries its own shed_id so the subsequent write is
        addressed by id and is never ambiguous to the server. What the UI labels a 'farm' IS the
        park: the location hierarchy has exactly two live levels and no farm tier.
        Ordering is settled server-side (park name, then shed name, with an id tiebreak) so the
        dropdown is stable between calls. A park with no active sheds is still returned, with an
        empty sheds array, so a newly-created park is not invisible.
        This is a BOUNDED CONFIGURATION CATALOG - on the order of a few parks and ~150 sheds, sized
        by how many sheds the business physically builds rather than by herd size - so it is
        returned whole, is meant to be fetched once and cached on-device, and is deliberately NOT
        paginated. It is gated on the same counts.write permission as the shifting submit route, not
        on the admin-tier locations.read: the operator who must choose a destination is exactly the
        operator who may record the movement.
      responses:
        "200":
          description: The active park -> shed destination cascade for the caller's tenant.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ShiftingDestinationsResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /app/counts/shifting-events:
    post:
      tags:
        - Counts
      operationId: recordAppCountsShiftingEvent
      summary: Record an operator-reported shifting (movement) event for approval.
      description: >-
        Records a movement of a cohort into a destination shed, as reported by a field operator on
        the phone, and raises the approval request a park_head decides. The event is recorded for
        review: authorization_state is 'pending', verification_state is 'unverified' and
        event_status is 'pending' - an operator REPORTS a movement, they do not self-authorize it.
        Park Head approval and operator completion are independent; the transaction receiving the
        second fact relocates the animals named in goat_ids. goat_ids is REQUIRED and
        must name at least one animal: a submit that names nobody is rejected here, at submit time,
        with 400 missing_goat_ids, so the reporting operator learns immediately rather than the
        approver discovering it later. Every named animal must be a current, non-merged herd member;
        dead, sold, transferred, or otherwise exited animals are rejected with 422
        goat_not_shiftable before any event or approval request is written. A destination must stay
        inside the animal's current park. Idempotent via the
        Idempotency-Key header: an exact replay returns the original shifting_event_id with
        idempotent_replay=true and records no second movement; a same-key/different-payload replay
        is rejected with 409.
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RecordShiftingEventRequest"
      responses:
        "200":
          description: Shifting event recorded (or replayed).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecordShiftingEventResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "500":
          $ref: "#/components/responses/ServerError"
  /app/counts/birth-events:
    post:
      tags:
        - Counts
      operationId: recordAppCountsBirthEvent
      summary: Record a birth and create every child workflow immediately.
      description: >-
        Atomically creates one canonical child per litter member and one PENDING, litter-grain count
        approval. Every child gets a deterministic CBE-/CPT- provisional identifier and emits
        goat.created immediately so its Birth workflow opens without waiting for the web queue.
        Children remain excluded from herd counts until a ceo_internal approves the accompanying
        request. The payload is fully validated against the identity kernel at submit time. Exact
        Idempotency-Key replay returns the same approval and children; a same-key/different-payload
        replay is rejected with 409.
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RecordBirthEventRequest"
      responses:
        "202":
          description: >-
            Birth recorded (or replayed): canonical children exist and their count approval is pending.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CountsApprovalSubmitResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "500":
          $ref: "#/components/responses/ServerError"
  /app/counts/death-events:
    post:
      tags:
        - Counts
      operationId: recordAppCountsDeathEvent
      summary: Record a death from the operator app for approval, through the critical-death guardrail.
      description: >-
        Records the death of an animal AS A PENDING REQUEST. The animal is NOT exited: no goats row
        changes and no goat.exited event is emitted, so its open obligations stay open until a
        ceo_internal approves the request via POST /app/counts/approvals/{request_id}/approve.
        GUARDRAIL (unchanged, and enforced HERE at submit as well as at approval): lifecycle_status
        must be 'dead' AND exit_reason must be 'died'. Any other pairing is rejected with 400, so a
        payload the guarded path would refuse can never be parked in an approver's queue. Approving
        the request replays this exact payload through the SAME dedicated critical-death exit the
        admin POST /admin/goats/{goat_id}/critical-death-exit route uses - the transport and the
        allowed role widen for field operators, the medical semantics do not. The goat.exited event
        emitted on approval auto-cancels the animal's open obligations. Idempotent via the
        Idempotency-Key header: an exact replay returns the original approval_request_id with
        idempotent_replay=true; a same-key/different-payload replay is rejected with 409.
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RecordDeathEventRequest"
      responses:
        "202":
          description: >-
            Death request raised (or replayed). Accepted, not applied - the animal is still alive
            and its obligations are still open.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CountsApprovalSubmitResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "500":
          $ref: "#/components/responses/ServerError"
  /app/counts/goats/{goat_id}/promote-identifier:
    post:
      tags:
        - Counts
      operationId: promoteAppCountsIdentifier
      summary: Assign a permanent RFID to a temporary-tagged goat (temp -> permanent).
      description: >-
        Promotes a goat that currently carries a temporary tag to a permanent RFID. In ONE
        transaction the server retires the goat's active temporary_tag and attaches the supplied
        permanent_identifier as its primary animal_identifier_1, emitting both goat.identifier.retired
        and goat.identifier.added. Applied directly (NOT an approval): this is the operator's retag,
        gated on CountsWrite. The temp tag to retire is found server-side; the caller sends only the
        permanent RFID and the goat's row_version. Idempotent via the Idempotency-Key header: an exact
        replay returns idempotent_replay=true and moves nothing; a same-key/different-payload replay is
        409. A goat with no active temporary tag is rejected with 409 no_temporary_identifier.
      parameters:
        - $ref: "#/components/parameters/GoatId"
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PromoteIdentifierRequest"
      responses:
        "200":
          description: The permanent RFID was assigned (or the call was an idempotent replay).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PromoteIdentifierResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "500":
          $ref: "#/components/responses/ServerError"
  /app/counts/goats/temporary-tagged:
    get:
      tags:
        - Counts
      operationId: listAppCountsTemporaryTaggedGoats
      summary: List goats awaiting a permanent RFID (still carrying an active temporary tag).
      description: >-
        The operator "Awaiting RFID" work list: goats that were tagged at birth with a provisional
        temporary tag and have not yet been promoted to a permanent RFID. Each row carries what the
        promote form needs -- the goat, its display id, the temporary tag being replaced, the
        animal's location, and its current row_version (echoed back verbatim to POST
        /app/counts/goats/{goat_id}/promote-identifier so a stale in-hand row is rejected).
        Keyset-paginated with a maximum page size of 20: this is read from a phone, so a client
        asking for more receives one screen of work, not the whole backlog. Gated on CountsWrite --
        the operator who may promote is exactly the operator who sees the list. Tenant-scoped.
      parameters:
        - name: page_size
          in: query
          required: false
          description: Server-capped at 20.
          schema:
            type: integer
            minimum: 1
            maximum: 20
        - name: cursor
          in: query
          required: false
          description: Keyset cursor from a previous page's next_cursor (the last row's display_id).
          schema:
            type: string
            maxLength: 512
        - name: park_id
          in: query
          required: false
          description: >-
            Optional location filter: narrow the list to goats currently placed in this park. A
            malformed id is rejected with 400. Chosen from the shifting-destinations catalog on the
            client (park -> shed cascade), never free text.
          schema:
            type: string
            format: uuid
        - name: shed_id
          in: query
          required: false
          description: >-
            Optional location filter: narrow the list to goats currently placed in this shed. A shed
            belongs to exactly one park, so shed_id alone is sufficient. A malformed id is rejected
            with 400.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: One page of goats awaiting a permanent RFID.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TemporaryTaggedGoatsResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /app/workflows:
    get:
      tags:
        - Workflows
      operationId: listAppWorkflows
      summary: List birth/death/colostrum workflow cards for one module and business date.
      description: >-
        The operator's per-goat SOP work list (docs/decisions/birth-death-workflows.md). A card is
        one workflow instance opened when a birth applies or a death report is staged
        (goat.created with origin_type=birth opens the kid track plus the shared mother track;
        counts.death.reported opens the death evidence trail while the goat remains alive). The
        card fields (actions_done,
        actions_total, next_action, awaiting_verification) are write-maintained on every action
        write, so this list reads workflow_instances alone. Scoped to ONE module (birth or death)
        and ONE Asia/Kolkata business date (default today IST). Keyset-paginated over
        (next_due_at ASC NULLS LAST, workflow_id ASC) with a server cap of 20 cards; chips carry
        the requested day's bucket counts over the same key set the page reads, so page size never
        changes the chips. overdue_dates contains at most the five most recent PREVIOUS business
        dates with an open card whose next action is already overdue; it powers the bounded mobile
        attention popup without fetching historical card pages. Gated on CountsWrite - the operator who records the birth/death runs the
        follow-up work.


        module=colostrum is a LENS over the same cards, not a third workflow
        (docs/decisions/colostrum-milk-module.md). It serves the Colostrum page in the Milk module:
        the kids with a colostrum feed due on the requested date - the immediate 1st Colostrum plus
        the birth-time-derived 07:00/11:00/15:00/18:30/22:00 series. Two differences from
        birth/death, both deliberate. Cards are selected by the date each FEED is due rather than by
        workflow_instances.event_date, because a kid born yesterday has feeds today. And
        actions_done/actions_total count only THAT DATE's feeds rather than every operator action,
        so a kid appears on each of its two dates with independent counters (maintainer decision
        2026-08-06 - tomorrow's progress is seen tomorrow). state describes the DAY, and
        awaiting_verification is always false because verification is enqueued once per whole kid
        workflow; awaiting_video is therefore rejected as a filter for this module. Completing a
        feed uses the SAME action write as Birth against the same row - there is no colostrum table
        and no second copy of the state.
      parameters:
        - name: module
          in: query
          required: true
          schema:
            type: string
            enum:
              - birth
              - death
              - colostrum
        - name: date
          in: query
          required: false
          description: Business date (Asia/Kolkata) to list; defaults to today IST.
          schema:
            type: string
            format: date
        - name: filter
          in: query
          required: false
          description: Card bucket filter; defaults to all (excludes canceled).
          schema:
            type: string
            enum:
              - all
              - overdue
              - due
              - completed
              - awaiting_video
        - name: page_size
          in: query
          required: false
          description: Server-capped at 20.
          schema:
            type: integer
            minimum: 1
            maximum: 20
        - name: cursor
          in: query
          required: false
          description: Keyset cursor from a previous page's next_cursor.
          schema:
            type: string
            maxLength: 512
      responses:
        "200":
          description: One page of workflow cards plus the day's chip counts.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkflowListResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /app/workflows/{workflow_id}:
    get:
      tags:
        - Workflows
      operationId: getAppWorkflow
      summary: Get one workflow's card header, context facts, and operator action list.
      description: >-
        The per-goat detail behind a card: the card header, backend-owned context facts (event
        moment, park/shed, mother link), and every operator action row (at most 18 - the birth kid
        track's 8 main steps plus up to 10 birth-time-derived colostrum sessions). Every visible
        operator row, including scheduled colostrum, counts toward actions_total; internal
        approval/verification rows are omitted. Only
        the first incomplete action in each section is enabled; later siblings read blocked=true.
        Tenant-scoped; gated on CountsWrite.


        lens=colostrum narrows the ROWS to the colostrum feeds due on the given date and re-counts
        the card header at that day's grain, so the detail cannot disagree with the Colostrum list
        card the operator tapped (docs/decisions/colostrum-milk-module.md). Blocked state is still
        computed against the kid's COMPLETE action set: 1st Colostrum sits behind four earlier
        main-section steps, so a feed can legitimately return blocked=true with
        blocked_reason=previous_action even though those prerequisites are not themselves rendered
        here. Clients must show that reason rather than letting the operator tap into a 409.
      parameters:
        - name: workflow_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: lens
          in: query
          required: false
          description: >-
            Narrows the action list to one feature's rows. Only colostrum is defined; omit for the
            full operator action list.
          schema:
            type: string
            enum:
              - colostrum
        - name: date
          in: query
          required: false
          description: >-
            Business date (Asia/Kolkata) the lens is scoped to; defaults to today IST. Ignored when
            lens is absent.
          schema:
            type: string
            format: date
      responses:
        "200":
          description: The workflow detail.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkflowDetailResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "500":
          $ref: "#/components/responses/ServerError"
  /app/workflows/{workflow_id}/actions/{action_id}/answer:
    post:
      tags:
        - Workflows
      operationId: answerAppWorkflowAction
      summary: Answer a workflow question, including numeric kilograms or a configured selection.
      description: >-
        Records the operator's answer to a question ("Is the kid clean?") or question_select ("Take
        Weight of Kid" band) action and completes it. A question_select answer must be one of the
        action's declared options. A requires_video question must carry one server-minted proof_ref
        from /app/proofs/*; a proofless answer is rejected 422 proof_required. Idempotent via the
        Idempotency-Key header: first call applies;
        an exact replay returns the original result with no side effects; a same-key/
        different-payload replay is rejected with 409; a NEW key against an already-completed
        action is rejected with 409 action_already_completed. A later sibling submitted before its
        predecessor completes is rejected with 409 action_out_of_sequence. The card counters are
        maintained in the same transaction. Gated on CountsWrite.
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
        - name: workflow_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: action_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AnswerWorkflowActionRequest"
      responses:
        "200":
          description: Answer recorded (or replayed).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkflowActionWriteResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "500":
          $ref: "#/components/responses/ServerError"
  /app/workflows/{workflow_id}/actions/{action_id}/complete:
    post:
      tags:
        - Workflows
      operationId: completeAppWorkflowAction
      summary: Complete an action-type workflow step (optionally with a video proof).
      description: >-
        Marks an action-type step done ("Iodine dipping of umbilical cord", a colostrum session, a
        death evidence video). A requires_video action without proof_ref is rejected 422
        proof_required - the proof is a server-minted proof id from /app/proofs/*, never bytes.
        Initial death videos remain staged until the existing admin-web approval accepts the death;
        that approval applies the lifecycle/count change and releases ONE death_evidence item
        carrying BOTH videos. The hidden internal review completes only on the verifier's approve
        verdict; a rework verdict resets both operator videos for sequential re-shoot, and the
        completed re-shoot pair returns directly to Verify without a second admin approval.
        Internal approval actions are never operator-completable. A later sibling submitted before
        its predecessor completes is rejected with 409 action_out_of_sequence. Idempotent via the
        Idempotency-Key header exactly like answer. Gated on CountsWrite.
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
        - name: workflow_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: action_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CompleteWorkflowActionRequest"
      responses:
        "200":
          description: Completion recorded (or replayed).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkflowActionWriteResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "500":
          $ref: "#/components/responses/ServerError"
  /app/counts/shifting-events/pending-execution:
    get:
      tags:
        - Counts
      operationId: listAppCountsShiftingPendingExecution
      summary: List shifting Actions from raise through execution and evidence rework.
      description: >-
        The operator's Shifting Actions history. APPROVE FIRST: a newly raised movement is NOT in the
        work list and is not actionable until a Park Head approves it — it is reachable only through
        status=pending, which is read-only (`primary_action_key=none`, "Awaiting Park Head
        approval"). status=all therefore EXCLUDES unapproved movements. Authorized and
        evidence-rework movements are actionable; completed movements remain visible.
        LEAD TIME: an approved movement awaiting operator work appears when it is DUE. High priority
        is due the second it is approved. Low priority is planned work: raised before 13:30 IST it is
        due the next day, raised at or after 13:30 IST the day after that. It stays filed under its
        RAISED business date either way, so a held movement is simply absent until its due day. An
        already-applied movement (completed, or in evidence rework) is history and is never held.
        Each row carries where the animals are/were,
        where they are going, approval fields when present, plus the full animal_count and a preview of at
        most 5 animals (display id and ear tag). A movement may name up to 500 animals, so the full
        roster is deliberately NOT embedded in a list row; animals_truncated reports when the
        preview is partial.
        Keyset-paginated with a maximum page size of 20: this queue is read from a phone standing in
        a park, so a client asking for more receives one screen of work, not the whole backlog.
        Date is the movement's raised business day in Asia/Kolkata. Status buckets are disjoint and
        backend-owned; completed rows are read-only (`primary_action_key=none`).
      parameters:
        - name: date
          in: query
          required: false
          description: Optional Asia/Kolkata business date. Omit for all dates.
          schema:
            type: string
            format: date
        - name: status
          in: query
          required: false
          description: Disjoint workflow bucket. Defaults to all.
          schema:
            type: string
            enum: [all, pending, authorized, rework, completed]
        - name: page_size
          in: query
          required: false
          description: Server-capped at 20.
          schema:
            type: integer
            minimum: 1
            maximum: 20
        - name: cursor
          in: query
          required: false
          description: Opaque keyset cursor from a previous page's next_cursor.
          schema:
            type: string
            maxLength: 512
      responses:
        "200":
          description: One page of date/status-filtered Shifting Actions history.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CountsShiftingPendingExecutionResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /app/counts/shifting-events/{shifting_event_id}/complete:
    post:
      tags:
        - Counts
      operationId: completeAppCountsShiftingEvent
      summary: Confirm a shifting happened and submit its mandatory live-camera evidence.
      description: >-
        Records that an operator physically moved the animals AND uploaded the mandatory video proof.
        APPROVE FIRST (maintainer decision 2026-08-09, superseding the 2026-07-28 order-free gates):
        Park Head approval must already exist. This request atomically relocates the animals and
        moves the count. Verification reviews the video afterward. Approval marks evidence verified;
        rejection creates evidence rework and never rolls back goat location or census truth.
        Low priority requires proof_ref only. High priority embeds feed packing and feeding inside
        Shifting and requires proof_ref, feed_packing_proof_ref, and feed_given_proof_ref. All three
        are reviewed together in one Shifting verification item. The packing proof does not complete
        the separate Feed Packing workflow. High priority must also echo the feed_config_fingerprint
        returned by the pending-execution read; missing config blocks with 422 and changed config
        blocks with 409 so the server never guesses feed.
        Valid from event_status='authorized', and for evidence rework on an already-approved
        movement. An UNAPPROVED (event_status='pending'), rejected, or canceled movement is refused
        with 400 shifting_not_authorized and nothing is written.
        ANY operator holding counts.write may complete a movement, not only the operator who raised
        it: the person who witnesses the animals move is not reliably the person who typed the
        request.
        Requires the Idempotency-Key header. Re-submitting an already-submitted movement returns the
        ORIGINAL result with idempotent_replay=true and queues nothing new; a same-key request with a
        different video/tag is a 409 idempotency_conflict.
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
        - name: shifting_event_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - proof_ref
              properties:
                proof_ref:
                  type: string
                  minLength: 1
                  description: >-
                    MANDATORY. The proof_artifact id of the video the operator recorded to prove the
                    animals physically moved. Verification reviews it independently of movement apply.
                feed_packing_proof_ref:
                  type: string
                  minLength: 1
                  description: MANDATORY for high priority. Live-camera proof of Shifting's embedded feed-packing step.
                feed_given_proof_ref:
                  type: string
                  minLength: 1
                  description: MANDATORY for high priority. Live-camera proof of configured feed being given to the moved animals.
                feed_config_fingerprint:
                  type: string
                  minLength: 1
                  description: MANDATORY for high priority. Echo of the ready feed requirement fingerprint.
                destination_tag:
                  type: string
                  description: >-
                    OPTIONAL destination management_stage the moved animals adopt. Needed only when the
                    destination shed is empty; for an occupied shed the server derives it and a supplied
                    value must agree with the shed's configured profile.
      responses:
        "200":
          description: >-
            Operator completion recorded. Status remains pending while Park Head approval is
            absent, applied when both gates exist, or the original result replayed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CountsShiftingExecutionResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "500":
          $ref: "#/components/responses/ServerError"
  /app/counts/shifting-events/{shifting_event_id}/cancel:
    post:
      tags:
        - Counts
      operationId: cancelAppCountsShiftingEvent
      summary: Retire an authorized shifting that will never be executed. NOTHING moves.
      description: >-
        Cancels an approved movement nobody is going to walk, so abandoned shiftings do not sit on
        the execution queue forever. NO animal moves and no location changes.
        A reason is REQUIRED - an abandoned movement that vanishes with no explanation is
        indistinguishable from one that was executed - and is enforced by the database as well as
        the API.
        Valid ONLY from event_status='authorized'. A movement still pending approval is NOT
        cancellable here: it is retired by its approver REJECTING the approval request instead,
        which keeps the decision with the person who holds the authority to make it. Any other state
        is refused with 400 shifting_not_authorized.
        ANY operator holding counts.write may cancel, on the same reasoning as complete. Requires
        the Idempotency-Key header; cancelling an already-cancelled movement returns the original
        result with idempotent_replay=true.
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
        - name: shifting_event_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CountsShiftingCancelRequest"
      responses:
        "200":
          description: Movement cancelled (or replayed). Nothing moved.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CountsShiftingExecutionResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "500":
          $ref: "#/components/responses/ServerError"
  /app/health/cases:
    post:
      tags: [Health]
      operationId: openHealthCase
      summary: Diagnose a goat and open the configured disease treatment course.
      description: >-
        Snapshots the currently published disease/age protocol and creates its bounded daily
        treatment sessions. duration_days is disease-configured; missing source duration is
        normalized to three days at protocol publication. Critical cull/isolation/movement text is
        materialized only as a guarded handoff and never directly mutates goat lifecycle/location.
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OpenHealthCaseRequest"
      responses:
        "201":
          description: Treatment course opened.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OpenHealthCaseResponse"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/WriteConflict" }
        "422": { $ref: "#/components/responses/UnprocessableEntity" }
        "500": { $ref: "#/components/responses/ServerError" }
  /app/health/work-items:
    get:
      tags: [Health]
      operationId: listHealthWorkItems
      summary: List one day's Adult or Kids Health treatment sessions.
      description: >-
        Returns a keyset page capped at 20 treatment sessions. Summary buckets are whole-filter
        aggregates and do not change with page size. Date markers and filter options are
        backend-owned. Approved-death sessions are canceled and omitted from future actionable
        days; death reports hold sessions until the review resolves.
      parameters:
        - name: age_band
          in: query
          required: true
          schema: { type: string, enum: [adult, kid] }
        - name: date
          in: query
          required: false
          schema: { type: string, format: date }
        - name: status
          in: query
          schema: { type: string, enum: [scheduled, due, in_progress, completed, rework, held, canceled_death] }
        - name: disease_key
          in: query
          schema: { type: string }
        - name: park_id
          in: query
          schema: { type: string, format: uuid }
        - name: shed_id
          in: query
          schema: { type: string, format: uuid }
        - name: session
          in: query
          schema: { type: string, enum: [morning, afternoon, evening, unscheduled] }
        - name: cursor
          in: query
          schema: { type: string, maxLength: 512 }
        - name: limit
          in: query
          schema: { type: integer, minimum: 1, maximum: 20 }
      responses:
        "200":
          description: One Room-friendly page and whole-filter metadata.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthWorkItemPage"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "500": { $ref: "#/components/responses/ServerError" }
  /app/health/work-items/{health_session_id}:
    get:
      tags: [Health]
      operationId: getHealthWorkItem
      summary: Get one treatment session and its snapshotted action rows.
      parameters:
        - name: health_session_id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: Treatment session detail.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthWorkItemDetail"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFoundOrNotAllowed" }
        "500": { $ref: "#/components/responses/ServerError" }
  /app/health/work-items/{health_session_id}/complete:
    post:
      tags: [Health]
      operationId: completeHealthWorkItem
      summary: Complete one Health treatment session and record its medicines.
      description: >-
        Atomically completes pending medication/action rows and creates one immutable medicine
        administration per configured medication step. Guarded critical rows are not executed by
        this command. Idempotency-Key makes offline retries safe.
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
        - name: health_session_id
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CompleteHealthWorkItemRequest"
      responses:
        "200":
          description: Session completed or exact replay returned.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CompleteHealthWorkItemResponse"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFoundOrNotAllowed" }
        "409": { $ref: "#/components/responses/WriteConflict" }
        "500": { $ref: "#/components/responses/ServerError" }
  /health-config/protocols:
    get:
      tags: [Health Config]
      operationId: listHealthConfigProtocols
      summary: List the authored treatment protocols, one row per disease and age band.
      description: >-
        One bounded keyset page of the treatment rulebook behind the Health Config screen. Each row
        carries the LIVE published version and the OPEN DRAFT side by side, because an author needs
        to see that the live protocol still says 5 ml while their unpublished draft says 3 ml --
        collapsing them into one "current" object would hide exactly the state that most needs
        review. There is no total: counting the filtered catalog on every request is
        compute-on-read, and a page subtotal shown as a catalog total is a false statement.
      parameters:
        - name: age_band
          in: query
          required: false
          schema:
            type: string
            enum: [adult, kid]
        - name: search
          in: query
          required: false
          description: Case-insensitive substring match on the disease name.
          schema:
            type: string
        - name: draft_only
          in: query
          required: false
          description: When true, returns only protocols with an unpublished draft open.
          schema:
            type: boolean
        - name: cursor
          in: query
          required: false
          schema:
            type: string
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 50
      responses:
        "200":
          description: One page of authored protocols.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthConfigProtocolPage"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "500": { $ref: "#/components/responses/ServerError" }
  /health-config/protocols/{protocol_version_id}:
    get:
      tags: [Health Config]
      operationId: getHealthConfigProtocol
      summary: Read one protocol version with its ordered steps and version history.
      description: >-
        Returns the whole authored document -- name, duration, and the ordered day/session steps --
        plus the disease's version history and the number of open cases still being treated under
        THIS version. That case count is shown next to the publish control so an author can see
        that retiring a version does not stop those treatments: a case pins the version it was
        diagnosed under and finishes on it.
      parameters:
        - name: protocol_version_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: The protocol version.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthConfigProtocolDetail"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFoundOrNotAllowed" }
        "500": { $ref: "#/components/responses/ServerError" }
  /health-config/diseases:
    post:
      tags: [Health Config]
      operationId: createHealthConfigDisease
      summary: Add a disease, opening an empty draft for both age bands.
      description: >-
        Creates drafts for BOTH the adult and the kid band, because the phone picks the protocol
        from the GOAT's age band -- a disease authored for adults only fails at diagnosis for a kid
        with "protocol not published", which reads as a bug rather than as a deliberate gap. The two
        drafts start identical and are edited apart afterwards. disease_key is derived from the name
        when omitted. Nothing is live until each draft is published.
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateHealthConfigDiseaseRequest"
      responses:
        "201":
          description: The disease was created and both drafts are open.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthConfigWriteResult"
        "200":
          description: Idempotent replay of an earlier create.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthConfigWriteResult"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/WriteConflict" }
        "422":
          description: The submitted content was rejected. Every offending field is named.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthConfigValidationError"
        "500": { $ref: "#/components/responses/ServerError" }
  /health-config/drafts:
    post:
      tags: [Health Config]
      operationId: openHealthConfigDraft
      summary: Open the draft for a protocol, copying the published version if none is open.
      description: >-
        Copy-on-edit is the safety property: the live protocol keeps serving diagnoses untouched
        while an author works, and nothing typed here reaches a treating operator until publish.
        This is a POST because it may create a draft row, and it carries no Idempotency-Key because
        at most one draft can exist per protocol -- the uniqueness constraint IS the idempotency, so
        a repeated call returns the same draft rather than creating a second one.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OpenHealthConfigDraftRequest"
      responses:
        "200":
          description: The open draft.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthConfigProtocolDetail"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFoundOrNotAllowed" }
        "409": { $ref: "#/components/responses/WriteConflict" }
        "422":
          description: The request was rejected. Every offending field is named.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthConfigValidationError"
        "500": { $ref: "#/components/responses/ServerError" }
  /health-config/drafts/save:
    post:
      tags: [Health Config]
      operationId: saveHealthConfigDraft
      summary: Replace a draft's whole content.
      description: >-
        Saves are WHOLE-DOCUMENT, never per-field: a protocol is read by an operator as one
        sequence, and a partial save that left a step pointing at a day the duration no longer
        covers would be a protocol nobody can execute. Steps carry no seq -- order is positional and
        the server assigns seq 1..N, which is what makes a reorder a single atomic replacement.
        Submitting content identical to what is stored returns outcome "unchanged" and writes
        nothing. A draft may be saved incomplete; the strict rules apply at publish.
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SaveHealthConfigDraftRequest"
      responses:
        "200":
          description: The draft was saved, or already matched.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthConfigWriteResult"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFoundOrNotAllowed" }
        "409": { $ref: "#/components/responses/WriteConflict" }
        "422":
          description: The submitted content was rejected. Every offending field is named.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthConfigValidationError"
        "500": { $ref: "#/components/responses/ServerError" }
  /health-config/protocols/{protocol_version_id}/publish:
    post:
      tags: [Health Config]
      operationId: publishHealthConfigDraft
      summary: Publish a draft, retiring the version it replaces.
      description: >-
        Promotes the draft to the live protocol and retires the previously published version, in one
        transaction. Goats currently being treated are UNAFFECTED: each case pins the version it was
        diagnosed under and finishes on those dosages, so publishing changes what the NEXT diagnosis
        loads, never what an animal mid-course receives. Publish applies the strict rulebook -- at
        least one step, no step beyond the duration, no day without a step, and every medicine
        carrying a route and a complete dosage -- against what is actually stored.
      parameters:
        - name: protocol_version_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          description: The draft is now the live protocol.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthConfigWriteResult"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFoundOrNotAllowed" }
        "409": { $ref: "#/components/responses/WriteConflict" }
        "422":
          description: The stored draft is not publishable. Every offending field is named.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthConfigValidationError"
        "500": { $ref: "#/components/responses/ServerError" }
  /health-config/protocols/{protocol_version_id}/discard:
    post:
      tags: [Health Config]
      operationId: discardHealthConfigDraft
      summary: Discard a draft without publishing it.
      description: >-
        Deletes the draft and its steps. Only a draft can be discarded -- a published version is
        immutable because goats are being treated from it, and a retired one is history.
      parameters:
        - name: protocol_version_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          description: The draft was discarded.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthConfigWriteResult"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFoundOrNotAllowed" }
        "409": { $ref: "#/components/responses/WriteConflict" }
        "500": { $ref: "#/components/responses/ServerError" }
  /app/counts/approvals:
    get:
      tags:
        - Counts
      operationId: listAppCountsApprovals
      summary: List lifecycle approval requests the caller may decide.
      description: >-
        The approver's queue. Returns ONLY the request types the caller is authorised to decide -
        a park_head sees shifting requests, a ceo_internal sees birth and death requests - because
        the filter is derived from the caller's permissions, not from the query string. A caller
        with neither approval permission receives an empty list. Keyset-paginated with a maximum
        page size of 20: this queue is read from a phone, so a client asking for more receives one
        screen of work, not the whole backlog.
      parameters:
        - name: status
          in: query
          required: false
          description: Defaults to 'pending'.
          schema:
            type: string
            enum: [pending, approved, rejected]
        - name: page_size
          in: query
          required: false
          description: Server-capped at 20.
          schema:
            type: integer
            minimum: 1
            maximum: 20
        - name: cursor
          in: query
          required: false
          description: Opaque keyset cursor from a previous page's next_cursor.
          schema:
            type: string
            maxLength: 512
      responses:
        "200":
          description: One page of approval requests.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CountsApprovalListResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /app/counts/approvals/{request_id}/approve:
    post:
      tags:
        - Counts
      operationId: approveAppCountsApproval
      summary: Approve a pending lifecycle request.
      description: >-
        Applies the request. For a BIRTH this changes every already-created child in the litter from
        count-pending to count-approved; it never creates a goat. For a DEATH it exits the animal
        through the guarded critical-death path and cancels its open obligations.
        A SHIFTING IS DIFFERENT (maintainer decision, 2026-07-19). Approving a shifting AUTHORIZES
        it and MOVES NOTHING - the animals stay in the source shed and the herd register keeps
        reading it, because approval is a manager's permission slip, not evidence that anybody
        walked the animals anywhere. The relocation happens later, when an operator confirms the
        movement physically happened via POST
        /app/counts/shifting-events/{shifting_event_id}/complete.
        ATOMIC: the effect and the status flip commit in ONE transaction, so a request can never be
        readable as 'approved' while its effect failed to save, and never applied while the request
        still reads 'pending'.
        AUTHORITY IS PER TYPE, checked against the stored request: a park_head may approve a
        shifting but NOT a birth or a death, and receives 403 if they address one. Requires the
        Idempotency-Key header. A second approve of an already-approved request applies nothing and
        returns the original decision with idempotent_replay=true; approving a request that was
        already rejected is a 409.
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
        - name: request_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CountsApprovalDecisionRequest"
      responses:
        "200":
          description: Request approved and its effect applied (or replayed).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CountsApprovalDecisionResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "500":
          $ref: "#/components/responses/ServerError"
  /app/counts/approvals/{request_id}/reject:
    post:
      tags:
        - Counts
      operationId: rejectAppCountsApproval
      summary: Reject a pending lifecycle request, applying nothing.
      description: >-
        Rejects the request. NO effect is applied: no animal is created, exited, or moved, and no
        shifting event is authorized. A reason is REQUIRED - a rejection without one is not
        actionable by the operator who raised it - and is enforced by the database as well as the
        API. Authority is per type, checked against the stored request, exactly as for approve.
        Requires the Idempotency-Key header. A second reject of an already-rejected request returns
        the original decision with idempotent_replay=true; rejecting a request that was already
        approved is a 409.
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
        - name: request_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CountsApprovalDecisionRequest"
      responses:
        "200":
          description: Request rejected (or replayed). Nothing was applied.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CountsApprovalDecisionResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "409":
          $ref: "#/components/responses/WriteConflict"
        "500":
          $ref: "#/components/responses/ServerError"
  /ceo-ai/ask:
    post:
      operationId: askCeoAssistant
      summary: Ask the read-only leadership operations assistant
      description: >
        CEO/CxO leadership assistant. Read-only over business data. Tenant and
        role scope come ONLY from the authenticated session, never from the
        request body. The response carries ONLY the user-facing envelope
        (answer, source, mode, request_id, conversation_id, citations) — step
        traces / chain-of-thought are internal and never returned.
      tags:
        - ceo-ai
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CeoAssistantAskRequest"
      responses:
        "200":
          description: Assistant answer
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CeoAssistantAnswer"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  # --- ceo-ai conversation persistence (owned by ceoai/persistence) ---
  # Additive block. Tenant + actor scope come ONLY from the authenticated session,
  # never from the request body or path. All reads/writes are actor-scoped: one
  # leadership user can never see or mutate another's threads.
  /ceo-ai/starters:
    get:
      operationId: listCeoStarters
      summary: Leadership capability probe + starter questions
      description: >
        Returns the backend-owned, tenant/role-aware starter questions the
        assistant can truthfully answer. Doubles as the leadership capability
        probe the admin-web client uses to decide whether to render the
        assistant launcher: 200 means the session is authorized leadership
        (ceo_internal); 403 means it is not, so the launcher stays hidden.
      tags:
        - ceo-ai
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Backend-owned starter questions for this leadership session
          content:
            application/json:
              schema:
                type: object
                required:
                  - starters
                properties:
                  starters:
                    type: array
                    items:
                      type: string
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /ceo-ai/conversations:
    post:
      operationId: createCeoConversation
      summary: Open a leadership assistant conversation thread
      description: >
        Creates a durable thread. Idempotent when an Idempotency-Key header is
        supplied: a replay with the same key returns the original thread (200)
        instead of forking a new one (201).
      tags:
        - ceo-ai
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/CeoIdempotencyKey"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CeoConversationCreateRequest"
      responses:
        "201":
          description: Thread created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CeoConversation"
        "200":
          description: Idempotent replay — existing thread returned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CeoConversation"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
    get:
      operationId: listCeoConversations
      summary: List the actor's conversation threads (keyset paginated)
      tags:
        - ceo-ai
      security:
        - bearerAuth: []
      parameters:
        - name: page_size
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 20
            default: 20
        - name: cursor
          in: query
          required: false
          description: Opaque keyset cursor from a prior page's next_cursor.
          schema:
            type: string
        - name: include_archived
          in: query
          required: false
          schema:
            type: boolean
            default: false
      responses:
        "200":
          description: One keyset page of threads, newest activity first
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CeoConversationList"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /ceo-ai/conversations/{conversation_id}:
    parameters:
      - $ref: "#/components/parameters/CeoConversationId"
    get:
      operationId: getCeoConversation
      summary: Resume one thread
      tags:
        - ceo-ai
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Thread
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CeoConversation"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "500":
          $ref: "#/components/responses/ServerError"
    patch:
      operationId: updateCeoConversation
      summary: Rename or archive/unarchive a thread
      tags:
        - ceo-ai
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CeoConversationPatchRequest"
      responses:
        "200":
          description: Updated thread
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CeoConversation"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "500":
          $ref: "#/components/responses/ServerError"
    delete:
      operationId: deleteCeoConversation
      summary: Soft-delete a thread (hidden immediately, purged per retention policy)
      tags:
        - ceo-ai
      security:
        - bearerAuth: []
      responses:
        "204":
          description: Thread soft-deleted
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "500":
          $ref: "#/components/responses/ServerError"
  /ceo-ai/conversations/{conversation_id}/messages:
    parameters:
      - $ref: "#/components/parameters/CeoConversationId"
    get:
      operationId: listCeoConversationMessages
      summary: Read a thread's message history (keyset paginated, oldest-first)
      tags:
        - ceo-ai
      security:
        - bearerAuth: []
      parameters:
        - name: page_size
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 20
            default: 20
        - name: cursor
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: One keyset page of message history
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CeoMessageList"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFoundOrNotAllowed"
        "500":
          $ref: "#/components/responses/ServerError"
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  parameters:
    CeoConversationId:
      name: conversation_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
    CeoMessageId:
      name: message_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
    CeoIdempotencyKey:
      name: Idempotency-Key
      in: header
      required: false
      description: Replay-safety key; a repeat create with the same key returns the original thread.
      schema:
        type: string
        maxLength: 200
    GoatId:
      name: goat_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
    DeviceId:
      name: device_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
    TaskId:
      name: task_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
    SOPVersionId:
      name: sop_version_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
    Limit:
      name: limit
      in: query
      required: true
      schema:
        type: integer
        minimum: 1
        maximum: 100
    Cursor:
      name: cursor
      in: query
      required: false
      schema:
        type: string
        minLength: 1
        maxLength: 500
    FeedConfigLimit:
      name: limit
      in: query
      required: false
      description: >-
        Page size. Absent uses the server default (50); a PRESENT but out-of-range value is a 400,
        never silently clamped -- a caller that asked for 5000 rows and got 50 without being told has
        a truncated grid it believes is complete.
      schema:
        type: integer
        minimum: 1
        maximum: 200
    FeedConfigOffset:
      name: offset
      in: query
      required: false
      description: >-
        Row offset. Bounded rather than growable: these are authored config tables whose whole
        contents are small and stable, and an offset past the maximum is rejected outright.
      schema:
        type: integer
        minimum: 0
        maximum: 5000
    FeedDirectionLimit:
      name: limit
      in: query
      required: false
      description: >-
        Number of SHEDS per page (not rows). Absent uses the server default (25); a PRESENT but
        out-of-range value is a 400, never silently clamped.
      schema:
        type: integer
        minimum: 1
        maximum: 100
    FeedDirectionOffset:
      name: offset
      in: query
      required: false
      description: >-
        Shed offset. Bounded rather than growable: the paged set is the park's shed catalog, a small
        stable configuration list, so the offset cannot grow with herd size and a value past the
        maximum is rejected outright.
      schema:
        type: integer
        minimum: 0
        maximum: 5000
    IdempotencyKey:
      name: Idempotency-Key
      in: header
      required: true
      schema:
        type: string
        minLength: 8
        maxLength: 200
    AcceptLanguage:
      name: Accept-Language
      in: header
      required: false
      description: Standard language preference used to localize backend-owned labels/copy. Supported app languages are en, hi, kn, and te; unsupported values fall back to en.
      schema:
        type: string
        examples: ["hi-IN, en;q=0.8"]
    GoatOSLocale:
      name: X-GoatOS-Locale
      in: header
      required: false
      description: Exact Goat OS app language selected by the user. Takes precedence over Accept-Language for backend-owned labels/copy.
      schema:
        type: string
        enum: [en, hi, kn, te]
    ProtocolId:
      name: protocol_id
      in: path
      required: true
      schema:
        type: string
        minLength: 1
    ProtocolVersionId:
      name: version_id
      in: path
      required: true
      schema:
        type: string
        minLength: 1
    ShedId:
      name: shed_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
    CalendarEventId:
      name: event_id
      in: path
      required: true
      schema:
        type: string
        minLength: 1
        maxLength: 256
    ProofId:
      name: proof_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
    ObligationId:
      name: obligation_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
    WeighingCampaignId:
      name: campaign_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
  responses:
    BadRequest:
      description: Validation error.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
    Unauthorized:
      description: Missing or invalid authentication.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
    Forbidden:
      description: Permission denied.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
    NotFoundOrNotAllowed:
      description: Resource is missing or outside the caller's visibility scope.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
    WriteConflict:
      description: Optimistic concurrency, stale SOP version, or idempotency conflict.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
    UnprocessableEntity:
      description: Request is syntactically valid but cannot pass the current executable-contract gate.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
    ServerError:
      description: Unexpected server error.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
    ServiceUnavailable:
      description: Dependency or feature wiring is unavailable.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
  schemas:
    HerdSignalMovementState:
      type: string
      enum: [moving, low, quiet, not_moving, stale]
    HerdSignalMappingState:
      type: string
      enum: [mapped, unmapped, conflict]
    HerdSignalPatternState:
      type: string
      enum: [no_movement, quiet_watch, inactive, missing, spike, recovered, normal]
    HerdSignalTone:
      type: string
      enum: [strong, ok, weak]
    HerdSignalBatteryState:
      type: string
      description: >-
        Composed from an absolute voltage band plus the tag's own voltage trend (maintainer
        decision, replacing a removed remaining-life estimate -- the tag reports voltage only).
        healthy >= 3.00V; watch is 2.80-2.99V OR voltage falling against the tag's own history;
        low < 2.80V; critical < 2.60V, or the tag went quiet (missing signal) after voltage was
        falling (critical-on-silence -- INFERRED, never a claim the tag is dead). All thresholds
        PROVISIONAL pending vendor confirmation.
      enum: [healthy, watch, low, critical]
    HerdSignalBatteryTrendDirection:
      type: string
      enum: [stable, falling]
    HerdSignalBatteryTrend:
      type: object
      description: >-
        Compact voltage trend: a direction plus the two endpoint readings that justify it.
        Absent/null when there is not enough history to say anything -- coin-cell voltage is
        noisy and temperature-sensitive, so a direction is never invented from two adjacent
        packets. NEVER a remaining-life estimate in any unit (days/weeks/months/years) -- the
        only acceptable durability statement is a vendor claim, quoted as a vendor claim, and it
        belongs in docs, not here.
      properties:
        direction: { $ref: '#/components/schemas/HerdSignalBatteryTrendDirection' }
        window_days: { type: integer, description: The trend window in days (PROVISIONAL default 30). }
        first_mv: { type: integer, description: First battery_mv reading in the window. }
        first_at: { type: string, format: date-time }
        last_mv: { type: integer, description: Last (most recent) battery_mv reading in the window. }
        last_at: { type: string, format: date-time }
      required: [direction, window_days, first_mv, first_at, last_mv, last_at]
    HerdSignalSensorState:
      type: string
      description: Computed "ok"/"abnormal" summary of temperature_sensor_ok/accelerometer_sensor_ok. Never the raw device sensor_state bitfield.
      enum: [ok, abnormal]
    HerdSignalsSummary:
      type: object
      description: Whole-filter server-side aggregate; never derived from the returned page.
      properties:
        tags_seen: { type: integer }
        mapped_animals: { type: integer }
        unmapped_tags: { type: integer }
        moving: { type: integer }
        quiet: { type: integer }
        not_moving: { type: integer }
        stale: { type: integer }
        weak_signal: { type: integer }
        low_battery: { type: integer }
        sensor_abnormal: { type: integer }
      required: [tags_seen, mapped_animals, unmapped_tags, moving, quiet, not_moving, stale, weak_signal, low_battery, sensor_abnormal]
    HerdSignalItem:
      type: object
      properties:
        tag_id: { type: string }
        tag_mac: { type: string }
        goat_id: { type: string, format: uuid, nullable: true }
        display_id: { type: string, nullable: true }
        park_id: { type: string, format: uuid, nullable: true }
        park_name: { type: string, nullable: true }
        shed_id: { type: string, format: uuid, nullable: true }
        shed_name: { type: string, nullable: true }
        partition_label: { type: string, nullable: true }
        operational_location_display: { type: string, nullable: true }
        gateway_id: { type: string, nullable: true }
        last_seen_at: { type: string, format: date-time, nullable: true }
        rssi_dbm: { type: integer, nullable: true }
        signal_state: { allOf: [{ $ref: '#/components/schemas/HerdSignalTone' }], nullable: true }
        battery_mv: { type: integer, nullable: true, description: Direct reading (voltage in mV). Must stay first among the battery fields. }
        battery_state: { allOf: [{ $ref: '#/components/schemas/HerdSignalBatteryState' }], nullable: true }
        battery_trend:
          allOf: [{ $ref: '#/components/schemas/HerdSignalBatteryTrend' }]
          nullable: true
        tag_temperature_c:
          type: number
          nullable: true
          description: Measured at the tag's own sensor housing, never the animal's body temperature.
        motion_count: { type: integer, format: int64, nullable: true }
        motion_delta: { type: integer, format: int64, nullable: true, description: 15-minute window delta. }
        motion_delta_1h: { type: integer, format: int64, nullable: true, description: Real 1-hour (3600s-tier) delta -- distinct from motion_delta. }
        motion_window_seconds: { type: integer, nullable: true }
        movement_state: { allOf: [{ $ref: '#/components/schemas/HerdSignalMovementState' }], nullable: true }
        pattern_state: { allOf: [{ $ref: '#/components/schemas/HerdSignalPatternState' }], nullable: true }
        baseline_delta: { type: integer, format: int64, nullable: true, description: Per-animal p75 24h baseline delta (300s tier). }
        sensor_state: { allOf: [{ $ref: '#/components/schemas/HerdSignalSensorState' }], nullable: true }
        temperature_sensor_ok: { type: boolean, nullable: true }
        accelerometer_sensor_ok: { type: boolean, nullable: true }
        mapping_state: { $ref: '#/components/schemas/HerdSignalMappingState' }
        gap_delta:
          type: boolean
          description: >-
            True when motion_delta is a reconnect TOTAL across a reception gap (maintainer
            decision on offline behaviour: the gateway does not buffer through a WAN outage, so a
            gap this long means nothing was received, and the delta on reconnect is a total with
            unknown time distribution), not this window's own movement. Render distinctly, never
            as an ordinary delta.
      required: [tag_id, tag_mac, goat_id, display_id, park_id, park_name, shed_id, shed_name, partition_label, operational_location_display, gateway_id, last_seen_at, rssi_dbm, signal_state, battery_mv, battery_state, battery_trend, tag_temperature_c, motion_count, motion_delta, motion_delta_1h, motion_window_seconds, movement_state, pattern_state, baseline_delta, sensor_state, temperature_sensor_ok, accelerometer_sensor_ok, mapping_state, gap_delta]
    HerdSignalsLiveResponse:
      type: object
      properties:
        summary: { $ref: '#/components/schemas/HerdSignalsSummary' }
        items:
          type: array
          items: { $ref: '#/components/schemas/HerdSignalItem' }
        next_cursor: { type: string, nullable: true }
      required: [summary, items, next_cursor]
    HerdSignalTimelineBucket:
      type: object
      properties:
        bucket_start: { type: string, format: date-time }
        bucket_seconds: { type: integer }
        first_motion_count: { type: integer, format: int64, nullable: true }
        last_motion_count: { type: integer, format: int64, nullable: true }
        motion_delta: { type: integer, format: int64, nullable: true }
        packet_count: { type: integer }
        avg_rssi_dbm: { type: number, nullable: true }
        min_rssi_dbm: { type: integer, nullable: true }
        max_rssi_dbm: { type: integer, nullable: true }
        is_gap:
          type: boolean
          description: True when the window has NO packets. Never collapsed with packet_count>0/motion_delta=0.
        gap_delta:
          type: boolean
          description: >-
            True only on a RECONNECT bucket: packets WERE received, and motion_delta is a TOTAL
            across a prior reception gap with unknown time distribution. Distinct from BOTH is_gap
            (no packets) and an ordinary zero delta (packets received, no movement) -- a client
            must be able to render all three as different facts.
      required: [bucket_start, bucket_seconds, first_motion_count, last_motion_count, motion_delta, packet_count, avg_rssi_dbm, min_rssi_dbm, max_rssi_dbm, is_gap, gap_delta]
    HerdSignalTimelineResponse:
      type: object
      properties:
        buckets:
          type: array
          items: { $ref: '#/components/schemas/HerdSignalTimelineBucket' }
      required: [buckets]
    HerdGatewayStatus:
      type: string
      description: Computed "online"/"offline" from last_seen_at freshness, not the raw stored gateway status text.
      enum: [online, offline]
    HerdGatewayNetworkMode:
      type: string
      enum: [wifi, ble, wifi_ble]
    HerdGateway:
      type: object
      properties:
        gateway_id: { type: string }
        label: { type: string, nullable: true }
        park_name: { type: string, nullable: true }
        shed_name: { type: string, nullable: true }
        network_mode: { allOf: [{ $ref: '#/components/schemas/HerdGatewayNetworkMode' }], nullable: true }
        wifi_mac: { type: string, nullable: true }
        ble_mac: { type: string, nullable: true }
        status: { $ref: '#/components/schemas/HerdGatewayStatus' }
        last_seen_at: { type: string, format: date-time, nullable: true }
        tags_seen_recently: { type: integer }
        weak_tags: { type: integer }
        unmapped_tags: { type: integer }
      required: [gateway_id, label, park_name, shed_name, network_mode, wifi_mac, ble_mac, status, last_seen_at, tags_seen_recently, weak_tags, unmapped_tags]
    HerdGatewaysResponse:
      type: object
      properties:
        gateways:
          type: array
          items: { $ref: '#/components/schemas/HerdGateway' }
      required: [gateways]
    HerdSignalType:
      type: string
      enum: [direct, derived, correlated, inferred]
    HerdInsightCard:
      type: object
      description: Label/formula/caveat are backend-owned copy; render verbatim (AGENTS.md backend-owns-labels rule).
      properties:
        key: { type: string }
        label: { type: string }
        value:
          nullable: true
          oneOf:
            - type: string
            - type: number
        unit: { type: string, nullable: true }
        signal_type: { $ref: '#/components/schemas/HerdSignalType' }
        formula: { type: string }
        caveat: { type: string, nullable: true }
      required: [key, label, value, unit, signal_type, formula, caveat]
    HerdInsightsResponse:
      type: object
      properties:
        cards:
          type: array
          items: { $ref: '#/components/schemas/HerdInsightCard' }
      required: [cards]
    HerdSignalActivityEventKind:
      type: string
      enum: [vaccination, feed_given, weighing, treatment, hoof_trimming, shed_move]
    HerdSignalActivityGrain:
      type: string
      description: >-
        What the underlying record is actually about. `animal`: the source row names this animal.
        `shed`: the source row names the SHED this animal is in -- every animal there shares the
        record and it says nothing about this one individually. `scanned_identifier`: the source
        row names a raw scanned string matching this tag's id or MAC, with no animal resolution
        anywhere in the chain (weighing and PC-care both record this way and this read never
        resolves a scan to an animal in either direction).
      enum: [animal, shed, scanned_identifier]
    HerdSignalActivityEvent:
      type: object
      properties:
        kind: { $ref: '#/components/schemas/HerdSignalActivityEventKind' }
        at: { type: string, format: date-time }
        label: { type: string, description: Backend-owned short label; render verbatim. }
        grain: { $ref: '#/components/schemas/HerdSignalActivityGrain' }
      required: [kind, at, label, grain]
    HerdSignalActivityUnavailableKind:
      type: object
      properties:
        kind: { $ref: '#/components/schemas/HerdSignalActivityEventKind' }
        reason: { type: string }
      required: [kind, reason]
    HerdSignalActivityReason:
      type: string
      description: >-
        Why `events` is empty for a STRUCTURAL reason -- never set merely because the window held
        no records. `tag_not_mapped_to_animal`: no animal is behind this tag, so there is no farm
        activity to show (not zero events for an animal -- no animal).
        `monitoring_boundary_unknown`: the tag resolves to an animal but carries no mapping
        instant, so the read fails closed rather than attributing a whole record history to a
        boundary it cannot state. `window_entirely_before_monitoring_start`: the requested window
        ends before the tag was mapped to the animal.
      enum:
        - tag_not_mapped_to_animal
        - monitoring_boundary_unknown
        - window_entirely_before_monitoring_start
    HerdSignalActivityResponse:
      type: object
      properties:
        tag_id: { type: string }
        from:
          type: string
          format: date-time
          description: The EFFECTIVE window start actually read, after clamping to monitoring_since.
        to: { type: string, format: date-time }
        monitoring_since:
          type: string
          format: date-time
          nullable: true
          description: When this tag became this animal's tag. Null for an unmapped tag.
        events:
          type: array
          description: Ordered by `at` ascending. Never null.
          items: { $ref: '#/components/schemas/HerdSignalActivityEvent' }
        unavailable_kinds:
          type: array
          description: >-
            Kinds with no source in this deployment, so a client hides that chip instead of
            showing one that can never light up. Empty today -- all six kinds have a real source.
          items: { $ref: '#/components/schemas/HerdSignalActivityUnavailableKind' }
        reason:
          allOf: [{ $ref: '#/components/schemas/HerdSignalActivityReason' }]
          nullable: true
        truncated:
          type: boolean
          description: True when the window holds more events than the 500-event cap returned.
        correlation_note:
          type: string
          description: >-
            Backend-owned copy stating what a marker does and does not mean. Render verbatim
            beside the overlay; it is the reason the claim boundary travels with the data.
      required: [tag_id, from, to, monitoring_since, events, unavailable_kinds, reason, truncated, correlation_note]
    HerdSignalsIngestPacket:
      type: object
      additionalProperties: false
      properties:
        tag_id: { type: string }
        tag_mac: { type: string }
        rssi_dbm: { type: integer, nullable: true }
        battery_mv: { type: integer, nullable: true }
        tag_temperature_c: { type: number, nullable: true }
        motion_count: { type: integer, format: int64, nullable: true, description: Cumulative on the tag. }
        sensor_state: { type: integer, nullable: true }
        temperature_sensor_ok: { type: boolean, nullable: true }
        accelerometer_sensor_ok: { type: boolean, nullable: true }
        raw_adv: { type: string, nullable: true }
        pkt_sn:
          type: integer
          format: int64
          nullable: true
          description: >-
            The GATEWAY scan report's sequence number, carried through per packet. The only
            packet-loss instrument this protocol offers: a forward jump means reports that were
            never delivered, and a DECREASE means the gateway rebooted (re-anchor, never a
            negative loss). Optional -- older firmware and replay paths omit it, and a packet
            without one is still real sensor data that simply cannot contribute to loss counts.
        seen_at: { type: string, format: date-time }
        gateway_seen_at:
          type: string
          format: date-time
          nullable: true
          description: >-
            This PACKET's own gateway-clock timestamp, uncorrected (a real gateway has been
            observed running a constant +02:30:00 ahead of IST -- a timezone misconfiguration).
            Optional: falls back to the envelope's gateway_seen_at (relay time) when absent.
            Diagnostic only -- never used for ordering or gap detection, which use seen_at
            (server-relevant capture time) exclusively.
      required: [tag_id, tag_mac, seen_at]
    HerdSignalsIngestRequest:
      type: object
      additionalProperties: false
      properties:
        gateway_id: { type: string }
        gateway_seen_at: { type: string, format: date-time }
        packets:
          type: array
          items: { $ref: '#/components/schemas/HerdSignalsIngestPacket' }
      required: [gateway_id, gateway_seen_at, packets]
    HerdSignalsIngestResponse:
      type: object
      properties:
        accepted: { type: integer }
        stored: { type: integer }
        latest_updated: { type: integer }
        trace_id: { type: string }
      required: [accepted, stored, latest_updated, trace_id]
    HerdSignalsBindTagMappingRequest:
      type: object
      additionalProperties: false
      properties:
        goat_id: { type: string, format: uuid }
        tag_id:
          type: string
          description: The tag's printed/reported id. Normalised (trimmed, uppercased) before it is claimed.
        tag_mac:
          type: string
          description: >-
            Optional. When the tag reports a MAC distinct from its id, that value is claimed as
            its own identifier row too -- the read path matches a packet by tag id OR MAC while a
            single identifier row carries one value, so claiming only one would leave half the
            tag's packets resolving to no animal.
        identifier_type:
          type: string
          enum: [animal_identifier_1, animal_identifier_2, temporary_tag]
          description: Slot the tag is bound in. Defaults to animal_identifier_2 -- a smart tag is normally the second thing an animal carries.
      required: [goat_id, tag_id]
    HerdSignalsReplaceTagMappingRequest:
      type: object
      additionalProperties: false
      properties:
        goat_id: { type: string, format: uuid }
        new_tag_id: { type: string }
        new_tag_mac: { type: string }
        identifier_type:
          type: string
          enum: [animal_identifier_1, animal_identifier_2, temporary_tag]
      required: [goat_id, new_tag_id]
    HerdSignalsUnmapTagMappingRequest:
      type: object
      additionalProperties: false
      properties:
        tag_id: { type: string }
        tag_mac:
          type: string
          description: Optional. Released alongside the id when the tag claimed both values.
      required: [tag_id]
    HerdSignalsTagMappingResponse:
      type: object
      properties:
        goat_id: { type: string, format: uuid }
        tag_id: { type: string, description: The normalised tag id now bound. }
        tag_mac: { type: string, nullable: true }
        identifier_ids:
          type: array
          description: The identifier rows now carrying this binding. Never null.
          items: { type: string, format: uuid }
        mapping_state:
          type: string
          enum: [mapped, unmapped]
        monitoring_since:
          type: string
          format: date-time
          nullable: true
          description: >-
            The instant animal monitoring starts for this tag. Null means unmapped, in which case
            NO animal-attributed value may be produced for the tag -- not a zero, not a default.
            Baselines, pattern windows and correlations never look back past this instant.
        unbound_identifier_ids:
          type: array
          description: >-
            Identifiers whose smart-tag binding this write ENDED -- the replaced tag on a replace,
            the unmarked identifier on an unmark. Never null.
          items: { type: string, format: uuid }
      required: [goat_id, tag_id, tag_mac, identifier_ids, mapping_state, monitoring_since, unbound_identifier_ids]
    HerdSignalsGatewayHeartbeatRequest:
      type: object
      additionalProperties: false
      properties:
        gateway_id: { type: string }
        state:
          type: string
          description: >-
            The gateway's own state name. Optional, but when present it must be the heartbeat
            state (sta_gw_hb): an unrecognised future state must not be allowed to assert proof of
            life by accident.
        ticks_cnt:
          type: integer
          format: int64
          nullable: true
          description: Gateway uptime tick counter. A value that goes backwards is a reboot, never a negative.
      required: [gateway_id]
    HerdSignalsGatewayHeartbeatResponse:
      type: object
      properties:
        gateway_id: { type: string }
        last_heartbeat_at: { type: string, format: date-time }
        reboot_detected:
          type: boolean
          description: True when ticks_cnt went backwards against the stored value.
        trace_id: { type: string }
      required: [gateway_id, last_heartbeat_at, reboot_detected, trace_id]
    CeoConversationCreateRequest:
      type: object
      additionalProperties: false
      properties:
        title:
          description: Optional initial title; when omitted the backend derives one.
          type: string
          maxLength: 200
      # tenant/actor are session-derived and MUST NOT appear in the body.
    CeoConversationPatchRequest:
      type: object
      additionalProperties: false
      description: >
        Exactly one intent per request. `title` renames; `archived=true` archives;
        `archived=false` unarchives.
      minProperties: 1
      properties:
        title:
          type: string
          maxLength: 200
        archived:
          type: boolean
    CeoConversation:
      type: object
      additionalProperties: false
      required:
        - id
        - title
        - created_at
        - updated_at
        - archived
      properties:
        id:
          type: string
          format: uuid
        title:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        archived:
          type: boolean
        archived_at:
          type: string
          format: date-time
          nullable: true
    CeoConversationList:
      type: object
      additionalProperties: false
      required:
        - items
        - has_more
      properties:
        items:
          type: array
          maxItems: 20
          items:
            $ref: "#/components/schemas/CeoConversation"
        has_more:
          type: boolean
        next_cursor:
          type: string
          nullable: true
    CeoMessage:
      type: object
      additionalProperties: false
      required:
        - id
        - conversation_id
        - role
        - content
        - created_at
      properties:
        id:
          type: string
          format: uuid
        conversation_id:
          type: string
          format: uuid
        role:
          type: string
          enum: [user, assistant, system]
        content:
          type: string
        source:
          description: Assistant turns only — resolved read surface / routing tier.
          type: string
        mode:
          description: Assistant turns only — governed | operational | exploratory.
          type: string
        request_id:
          type: string
        citations:
          description: User-visible provenance chips (structured JSON array).
          type: array
          items:
            type: object
            additionalProperties: true
        created_at:
          type: string
          format: date-time
      # tool_calls are INTERNAL routing detail and are intentionally NOT exposed here.
    CeoMessageList:
      type: object
      additionalProperties: false
      required:
        - items
        - has_more
      properties:
        items:
          type: array
          maxItems: 20
          items:
            $ref: "#/components/schemas/CeoMessage"
        has_more:
          type: boolean
        next_cursor:
          type: string
          nullable: true
    CeoAssistantAskRequest:
      type: object
      additionalProperties: false
      required:
        - question
      properties:
        question:
          type: string
          minLength: 1
          maxLength: 1200
          description: Natural-language leadership question. Treated as data, never as instructions.
        conversation_id:
          type: string
          description: Optional existing thread id to continue.
    CeoAssistantCitation:
      type: object
      additionalProperties: false
      required:
        - surface
        - route
        - as_of
        - planned_by_model
      properties:
        surface:
          type: string
          description: Human source label, e.g. "Cube · vaccination_overdue" or "Mesha read API".
        route:
          type: string
          enum: [cube, api, toolbox, sql, none]
        as_of:
          type: string
          format: date-time
          description: Freshness of the underlying read model (IST business day).
        metric_status:
          type: string
          enum: [approved, draft]
          description: Cube governance status; present only for governed metrics.
        planned_by_model:
          type: boolean
    CeoAssistantAnswer:
      type: object
      additionalProperties: false
      required:
        - answer
        - source
        - mode
        - request_id
      properties:
        answer:
          type: string
        source:
          type: string
          description: Route-attributed source label(s).
        mode:
          type: string
          enum: [planned, fallback, refused, partial]
        request_id:
          type: string
        conversation_id:
          type: string
        citations:
          type: array
          items:
            $ref: "#/components/schemas/CeoAssistantCitation"
    FeedConfigRationRate:
      type: object
      additionalProperties: false
      required:
        - ration_rate_id
        - park_id
        - ration_group
        - shed_tag
        - feed_item
        - grams_per_head
        - valid_from
        - source_system
      properties:
        ration_rate_id:
          type: string
          format: uuid
        park_id:
          type: string
          format: uuid
        ration_group:
          type: string
        shed_tag:
          type: string
        feed_item:
          type: string
        grams_per_head:
          type: string
          description: >-
            Authored grams per head per day, as an EXACT DECIMAL STRING rather than a JSON number, so
            an authored rate cannot drift through float round-tripping. "0.000" is a real authored
            value meaning "feed nothing"; the absence of a row entirely is the different, blocking
            state "not configured".
        valid_from:
          type: string
          format: date
        valid_to:
          type: string
          format: date
          nullable: true
          description: Absent while this row is the one in force.
        source_system:
          type: string
    FeedConfigRationRatePage:
      type: object
      additionalProperties: false
      required:
        - items
        - limit
        - offset
        - has_more
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/FeedConfigRationRate"
        limit:
          type: integer
        offset:
          type: integer
        has_more:
          type: boolean
          description: >-
            Whether another page exists. Deliberately NOT a total count: counting the filtered set on
            every page is compute-on-read, and the grid needs "is there more", not a total.
    HealthConfigStep:
      type: object
      additionalProperties: false
      required:
        - day_no
        - session
        - seq
        - record_type
      properties:
        step_id:
          type: string
          description: Absent on steps a client is submitting; present on stored steps.
        day_no:
          type: integer
          minimum: 1
          maximum: 90
        session:
          type: string
          enum: [morning, afternoon, evening, unscheduled]
          description: >-
            'unscheduled' is a real bucket, not a placeholder for "not decided": it is what the
            imported sheet uses for a step with no fixed time, such as a once-daily injection given
            whenever the operator reaches the animal.
        seq:
          type: integer
          minimum: 1
          description: >-
            Server-assigned position, 1..N in working order. Clients do NOT send it on save --
            order is positional there -- because health_protocol_steps is UNIQUE on
            (version, seq) and a client-driven renumber collides with itself mid-reorder.
        record_type:
          type: string
          enum: [action, medication, critical_action]
        medicine_name:
          type: string
          nullable: true
        dosage_text:
          type: string
          nullable: true
        dosage_denominator:
          type: string
          nullable: true
          enum: [ml, kg, none, null]
          description: >-
            What the dosage number is counted in. 'none' is a real authored value (a whole-unit dose
            such as one bolus) and is deliberately distinct from an ABSENT denominator, which means
            the author has not said.
        medicine_route:
          type: string
          nullable: true
          enum: [IM, SQ, IV, Oral, Topical, Intra Mammary, null]
          description: >-
            Closed vocabulary rather than free text because the route is a CLINICAL instruction: the
            difference between IM and IV is not a labelling preference, and a stored typo renders on
            the operator's phone as an instruction nobody can follow.
        instruction:
          type: string
          nullable: true
        critical_action_type:
          type: string
          nullable: true
          enum: [quarantine_or_movement, lifecycle_exit, null]
          description: >-
            Present only on a critical_action step. Both values hand the animal to a policy-pack
            transition Health does not itself perform, so an author may name one but the module
            never executes it.
        status:
          type: string
          nullable: true
    HealthConfigProtocolRow:
      type: object
      additionalProperties: false
      required:
        - disease_key
        - display_name
        - age_band
        - published_version_id
        - published_version
        - duration_days
        - step_count
        - medication_count
        - critical_action_count
        - source_ref
        - has_draft
        - draft_version_id
        - draft_version
        - draft_duration_days
        - draft_step_count
        - draft_medication_count
      properties:
        disease_key:
          type: string
        display_name:
          type: string
        age_band:
          type: string
          enum: [adult, kid]
        published_version_id:
          type: string
          description: Empty when this disease/band has no live protocol (drafted but never published).
        published_version:
          type: integer
        duration_days:
          type: integer
        step_count:
          type: integer
        medication_count:
          type: integer
        critical_action_count:
          type: integer
        published_at:
          type: string
          format: date-time
          nullable: true
        source_ref:
          type: string
          description: >-
            Where this version came from. 'health-config:app' means it was authored in the app; a
            'google-sheet:...' value means it is still the imported bootstrap.
        has_draft:
          type: boolean
        draft_version_id:
          type: string
        draft_version:
          type: integer
        draft_duration_days:
          type: integer
        draft_step_count:
          type: integer
        draft_medication_count:
          type: integer
        draft_updated_at:
          type: string
          format: date-time
          nullable: true
    HealthConfigProtocolPage:
      type: object
      additionalProperties: false
      required:
        - items
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/HealthConfigProtocolRow"
        next_cursor:
          type: string
          nullable: true
          description: >-
            Keyset position of the next page, absent on the last. Deliberately not a total count:
            counting the filtered catalog on every request is compute-on-read.
    HealthConfigVersionSummary:
      type: object
      additionalProperties: false
      required:
        - protocol_version_id
        - version
        - status
        - duration_days
        - step_count
        - source_ref
        - created_at
      properties:
        protocol_version_id:
          type: string
          format: uuid
        version:
          type: integer
        status:
          type: string
          enum: [draft, published, retired]
        duration_days:
          type: integer
        step_count:
          type: integer
        source_ref:
          type: string
        published_at:
          type: string
          format: date-time
          nullable: true
        created_at:
          type: string
          format: date-time
    HealthConfigProtocolDetail:
      type: object
      additionalProperties: false
      required:
        - protocol_version_id
        - disease_key
        - display_name
        - age_band
        - version
        - status
        - duration_days
        - source_ref
        - content_hash
        - created_at
        - updated_at
        - steps
        - open_case_count
      properties:
        protocol_version_id:
          type: string
          format: uuid
        disease_key:
          type: string
        display_name:
          type: string
        age_band:
          type: string
          enum: [adult, kid]
        version:
          type: integer
        status:
          type: string
          enum: [draft, published, retired]
        duration_days:
          type: integer
        source_ref:
          type: string
        content_hash:
          type: string
          description: >-
            Hash of THIS protocol's content for an app-authored version, which is what lets an
            identical re-save be recognised as "unchanged". Rows written by the sheet importer
            instead share one hash across the whole import snapshot.
        published_at:
          type: string
          format: date-time
          nullable: true
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        steps:
          type: array
          items:
            $ref: "#/components/schemas/HealthConfigStep"
        history:
          type: array
          nullable: true
          items:
            $ref: "#/components/schemas/HealthConfigVersionSummary"
        open_case_count:
          type: integer
          description: >-
            Goats currently being treated under THIS version. Shown next to the publish control so
            an author can see that retiring the version does not stop those treatments.
    CreateHealthConfigDiseaseRequest:
      type: object
      additionalProperties: false
      required:
        - display_name
      properties:
        display_name:
          type: string
          minLength: 1
          maxLength: 80
        disease_key:
          type: string
          description: >-
            Optional. Derived from the display name when omitted, which is what the UI does -- the
            key is an internal join identity an author has no reason to think about. Accepted so a
            seed command can reproduce the imported keys exactly.
        duration_days:
          type: integer
          minimum: 1
          maximum: 90
          nullable: true
          description: >-
            Absent takes the declared default of 3. A PRESENT out-of-range value is REJECTED, never
            rewritten to the default -- a business number the author did not type must never become
            authored truth.
    OpenHealthConfigDraftRequest:
      type: object
      additionalProperties: false
      required:
        - disease_key
        - age_band
      properties:
        disease_key:
          type: string
        age_band:
          type: string
          enum: [adult, kid]
    SaveHealthConfigDraftStep:
      type: object
      additionalProperties: false
      required:
        - day_no
        - session
        - record_type
      properties:
        day_no:
          type: integer
          minimum: 1
          maximum: 90
        session:
          type: string
          enum: [morning, afternoon, evening, unscheduled]
        record_type:
          type: string
          enum: [action, medication, critical_action]
        medicine_name:
          type: string
        dosage_text:
          type: string
        dosage_denominator:
          type: string
        medicine_route:
          type: string
        instruction:
          type: string
        critical_action_type:
          type: string
    SaveHealthConfigDraftRequest:
      type: object
      additionalProperties: false
      required:
        - disease_key
        - age_band
        - display_name
        - steps
      properties:
        disease_key:
          type: string
        age_band:
          type: string
          enum: [adult, kid]
        display_name:
          type: string
          description: >-
            A rename applies to the DISEASE, not to one band: the two bands are the same illness, and
            letting them drift would show an operator one name on an adult and another on a kid.
        duration_days:
          type: integer
          minimum: 1
          maximum: 90
          nullable: true
        steps:
          type: array
          description: >-
            The whole ordered step list. Order is positional; the server assigns seq 1..N. An empty
            array is a legitimate draft -- the strict rules apply at publish.
          items:
            $ref: "#/components/schemas/SaveHealthConfigDraftStep"
    HealthConfigWriteResult:
      type: object
      additionalProperties: false
      required:
        - outcome
        - disease_key
        - idempotent_replay
      properties:
        outcome:
          type: string
          enum: [created, saved, unchanged, published, discarded]
          description: >-
            The same vocabulary the audit ledger uses, so the API, the trail and the UI all say the
            same word for the same act.
        disease_key:
          type: string
        age_band:
          type: string
          nullable: true
        protocol_version_id:
          type: string
          nullable: true
        version:
          type: integer
          nullable: true
        retired_version_id:
          type: string
          nullable: true
          description: The version this publish retired. Absent on a disease's first publish.
        draft_version_ids:
          type: object
          nullable: true
          additionalProperties:
            type: string
          description: Age band -> draft id, returned by a disease create which opens two drafts.
        idempotent_replay:
          type: boolean
          description: True when the response was read back from the ledger rather than re-run.
    HealthConfigFieldError:
      type: object
      additionalProperties: false
      required:
        - field
        - message
      properties:
        field:
          type: string
          description: >-
            The exact rejected field, e.g. "steps[4].medicine_route", so the editor can mark that
            row rather than showing one message for the whole form.
        message:
          type: string
    HealthConfigValidationError:
      type: object
      additionalProperties: false
      required:
        - code
        - message
        - trace_id
        - retryable
        - errors
      properties:
        code:
          type: string
        message:
          type: string
        trace_id:
          type: string
        retryable:
          type: boolean
        errors:
          type: array
          description: >-
            EVERY field error from one validation pass, together. A 28-step protocol rejected one
            field per round trip is not authorable.
          items:
            $ref: "#/components/schemas/HealthConfigFieldError"
    FeedConfigRationGroup:
      type: object
      additionalProperties: false
      required:
        - ration_group_id
        - breed
        - ration_group
      properties:
        ration_group_id:
          type: string
          format: uuid
        breed:
          type: string
        ration_group:
          type: string
    ProcurementVendor:
      type: object
      description: >-
        One row of the procurement vendor register. Payment instruments are present only for a
        caller holding `procurement.vendor.finance.read`; see finance_redacted.
      required:
        - vendor_id
        - record_type
        - business_name
        - display_name
        - status
        - status_label
        - state
        - location_display
        - finance_redacted
        - created_at
        - updated_at
        - row_version
      properties:
        vendor_id: { type: string, format: uuid }
        record_type:
          type: string
          description: Business-managed vocabulary (Sheep Agent, Transport Agent, Feed Agent, ...).
        business_name: { type: string }
        display_name:
          type: string
          description: >-
            Backend-composed name every surface renders verbatim. "Business - Contact" when a
            distinct contact exists, otherwise the business name alone -- a missing contact is
            dropped rather than left as a dangling separator.
        contact_person_name: { type: string, nullable: true }
        phone_number: { type: string, nullable: true }
        breed: { type: string, nullable: true }
        feed: { type: string, nullable: true }
        status: { type: string, enum: [active, inactive, negotiating, banned] }
        status_label:
          type: string
          description: Operator-facing label for status. Rendered verbatim; never re-derived client-side.
        filtered_stock: { type: integer, nullable: true }
        price_per_goat:
          type: string
          nullable: true
          description: Decimal as a string so the amount never round-trips through a float.
        ready_to_filtered: { type: string, nullable: true }
        eta_after_order_days: { type: integer, nullable: true }
        details: { type: string, nullable: true }
        state: { type: string }
        city: { type: string, nullable: true }
        location_display:
          type: string
          description: Backend-composed "City, State", degrading to state alone when no city is recorded.
        bank_name: { type: string, nullable: true }
        account_no: { type: string, nullable: true }
        ifsc_code: { type: string, nullable: true }
        upi_id: { type: string, nullable: true }
        pan_number: { type: string, nullable: true }
        finance_redacted:
          type: boolean
          description: >-
            True when payment fields were WITHHELD for this caller rather than absent. The client
            must render "hidden" in that case -- a blank would read as "no bank details on file".
        comments: { type: string, nullable: true }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
        row_version:
          type: integer
          format: int64
          description: Optimistic-concurrency fence. Echo it back on update.
    ProcurementVendorPage:
      type: object
      required: [vendors, total, limit, offset]
      properties:
        vendors:
          type: array
          items:
            $ref: "#/components/schemas/ProcurementVendor"
        total:
          type: integer
          description: >-
            The WHOLE-FILTER count, not the page length. Pagination changes rows only, never this
            number. The client derives the page count from total and limit.
        limit:
          type: integer
          description: The page size actually applied, after clamping.
        offset:
          type: integer
          description: The offset actually applied. Echoed so the client can render the page number.
    ProcurementVendorWrite:
      type: object
      description: >-
        Create/update body. A REPLACE: an omitted optional field and a cleared one both store NULL,
        so there is no patch-vs-replace ambiguity for a client to get wrong.
      required: [record_type, business_name, status, state]
      properties:
        record_type: { type: string, maxLength: 160 }
        business_name: { type: string, maxLength: 160 }
        contact_person_name: { type: string, maxLength: 160 }
        phone_number: { type: string, maxLength: 64 }
        breed: { type: string, maxLength: 160 }
        feed: { type: string, maxLength: 160 }
        status: { type: string, enum: [active, inactive, negotiating, banned] }
        filtered_stock: { type: integer, minimum: 0, nullable: true }
        price_per_goat: { type: string, nullable: true }
        ready_to_filtered: { type: string, maxLength: 160 }
        eta_after_order_days: { type: integer, minimum: 0, nullable: true }
        details: { type: string, maxLength: 2000 }
        state: { type: string, maxLength: 160 }
        city: { type: string, maxLength: 160 }
        bank_name: { type: string, maxLength: 160 }
        account_no: { type: string, maxLength: 160 }
        ifsc_code: { type: string, maxLength: 160 }
        upi_id: { type: string, maxLength: 160 }
        pan_number: { type: string, maxLength: 160 }
        comments: { type: string, maxLength: 2000 }
        row_version:
          type: integer
          format: int64
          description: Required on update, ignored on create.
    ProcurementVendorCatalogEntry:
      type: object
      required: [value, label, is_active]
      properties:
        value: { type: string }
        label: { type: string }
        is_active:
          type: boolean
          description: >-
            False means retired: still rendered on a vendor that carries it, never offered for a
            new row.
    ProcurementVendorCatalog:
      type: object
      required: [record_types, breeds, states, cities, statuses, feeds]
      properties:
        record_types:
          type: array
          items: { $ref: "#/components/schemas/ProcurementVendorCatalogEntry" }
        breeds:
          type: array
          items: { $ref: "#/components/schemas/ProcurementVendorCatalogEntry" }
        states:
          type: array
          items: { $ref: "#/components/schemas/ProcurementVendorCatalogEntry" }
        cities:
          type: array
          items: { $ref: "#/components/schemas/ProcurementVendorCatalogEntry" }
        statuses:
          type: array
          items: { $ref: "#/components/schemas/ProcurementVendorCatalogEntry" }
        feeds:
          type: array
          items: { $ref: "#/components/schemas/ProcurementVendorCatalogEntry" }
    SalesBuyerLead:
      type: object
      description: One buyer lead in the demand pipeline.
      required: [lead_id, buyer_name, created_at]
      properties:
        lead_id: { type: string, format: uuid }
        recorded_date: { type: string, nullable: true, description: "YYYY-MM-DD when known." }
        farm: { type: string, nullable: true }
        buyer_name: { type: string }
        buyer_place: { type: string, nullable: true }
        animal_type: { type: string, nullable: true }
        breed: { type: string, nullable: true }
        call_status:
          type: string
          nullable: true
          description: Null means not yet called; the overview reports that bucket as "uncontacted".
        created_at: { type: string, format: date-time }
    SalesBuyerLeadPage:
      type: object
      description: One page of the buyer pipeline plus whole-filter total and status vocabulary.
      required: [leads, total, status_options]
      properties:
        leads:
          type: array
          items: { $ref: "#/components/schemas/SalesBuyerLead" }
        total: { type: integer, description: Whole-filter count, never the page length. }
        status_options:
          type: array
          items: { type: string }
          description: The tenant's stored call-status vocabulary, for the status picker.
    SalesBuyerLeadWrite:
      type: object
      description: Records one buyer lead. Optional text sent as "" is stored as not set.
      required: [buyer_name]
      properties:
        recorded_date: { type: string, description: "Optional YYYY-MM-DD." }
        farm: { type: string, enum: ["", CBE, CPT], description: Optional farm. }
        buyer_name: { type: string }
        buyer_place: { type: string }
        animal_type: { type: string }
        breed: { type: string }
        call_status: { type: string, description: Optional; empty means not yet called. }
    SalesFpoLead:
      type: object
      description: One farmer-group (FPO) lead.
      required: [lead_id, fpo_name, created_at]
      properties:
        lead_id: { type: string, format: uuid }
        fpo_name: { type: string }
        crops: { type: string, nullable: true }
        district: { type: string, nullable: true }
        taluk: { type: string, nullable: true }
        state: { type: string, nullable: true }
        call_status: { type: string, nullable: true }
        created_at: { type: string, format: date-time }
    SalesFpoLeadPage:
      type: object
      description: One page of the farmer-group pipeline plus whole-filter total and status vocabulary.
      required: [leads, total, status_options]
      properties:
        leads:
          type: array
          items: { $ref: "#/components/schemas/SalesFpoLead" }
        total: { type: integer }
        status_options:
          type: array
          items: { type: string }
    SalesFpoLeadWrite:
      type: object
      description: Records one farmer-group lead.
      required: [fpo_name]
      properties:
        fpo_name: { type: string }
        crops: { type: string }
        district: { type: string }
        taluk: { type: string }
        state: { type: string }
        call_status: { type: string }
    SalesLeadStatusWrite:
      type: object
      description: Sets one lead's call status; empty clears it back to "not yet called".
      required: [call_status]
      properties:
        call_status: { type: string }
    SalesBenchmarkWrite:
      type: object
      description: Records one comparable market quote.
      required: [breed]
      properties:
        market: { type: string }
        category: { type: string }
        breed: { type: string }
        source: { type: string }
        ex_farm_rate: { type: string }
        transport_rate: { type: string }
        landing_cost_per_kg: { type: number, nullable: true }
        market_price_per_kg: { type: number, nullable: true }
    SalesSoldTagRow:
      type: object
      description: One animal in a handed-over tag list.
      required: [animal_label]
      properties:
        animal_label: { type: string }
        tag_number: { type: string }
        weight_kg: { type: number, nullable: true }
    SalesSoldTagsWrite:
      type: object
      description: Records the tag list handed over at one sale, as one atomic batch.
      required: [rows]
      properties:
        farm: { type: string, enum: ["", CBE, CPT] }
        rows:
          type: array
          minItems: 1
          maxItems: 200
          items: { $ref: "#/components/schemas/SalesSoldTagRow" }
    SalesSoldTagsResult:
      type: object
      required: [recorded]
      properties:
        recorded: { type: integer, description: How many animals landed in the roster. }
    SalesWeightCheckWrite:
      type: object
      description: Records one video-vs-book weight audit row.
      required: [book_weight_kg, video_weight_kg]
      properties:
        tag_number: { type: string }
        book_weight_kg: { type: number }
        video_weight_kg: { type: number }
        farm_born: { type: boolean, default: false }
    SalesRecorded:
      type: object
      required: [recorded]
      properties:
        recorded: { type: boolean }
    SalesDeal:
      type: object
      description: One row of the sales ledger -- one sheet row, or one deal recorded in the app.
      required:
        - deal_id
        - sale_date
        - farm
        - buyer_name
        - product_type
        - breed
        - sales_value
        - status
        - created_at
        - updated_at
      properties:
        deal_id: { type: string, format: uuid }
        sale_date:
          type: string
          format: date
          description: The business date the sale happened, never a timestamp.
        farm: { type: string, enum: [CBE, CPT] }
        source_sales_id:
          type: integer
          nullable: true
          description: The source sheet's own sale reference. It repeats and is never a key.
        source_purchase_id: { type: integer, nullable: true }
        buyer_name: { type: string }
        buyer_place: { type: string, nullable: true }
        product_type: { type: string, enum: [Sheep, Goat, Manure] }
        breed: { type: string }
        animal_count:
          type: number
          nullable: true
          description: Authoritative animal count when recorded; otherwise male_count + female_count applies.
        male_count: { type: number, nullable: true }
        female_count: { type: number, nullable: true }
        total_weight_kg: { type: number, nullable: true }
        advance_amount: { type: number, nullable: true }
        sales_value: { type: number }
        status: { type: string, enum: [Deal Closed, Deal Failed, In Discussion, Advance Paid] }
        feedback: { type: string, nullable: true }
        comments: { type: string, nullable: true }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    SalesDealPage:
      type: object
      required: [deals, total, limit, offset]
      properties:
        deals:
          type: array
          items:
            $ref: "#/components/schemas/SalesDeal"
        total:
          type: integer
          description: >-
            The WHOLE-FILTER count, not the page length. Pagination changes rows only, never this
            number.
        limit:
          type: integer
          description: The page size actually applied, after clamping.
        offset:
          type: integer
          description: The offset actually applied. Echoed so the client can render the page number.
    SalesDealWrite:
      type: object
      description: >-
        Record-sale body. Farm and product type are closed vocabularies validated server-side and
        rejected -- never silently defaulted -- when unrecognised. The recorded deal always lands
        with status `Deal Closed`.
      required: [sale_date, farm, product_type, breed, buyer_name, sales_value]
      properties:
        sale_date: { type: string, format: date }
        farm: { type: string, enum: [CBE, CPT] }
        product_type: { type: string, enum: [Sheep, Goat, Manure] }
        breed: { type: string, maxLength: 160 }
        buyer_name: { type: string, maxLength: 160 }
        buyer_place: { type: string, maxLength: 160 }
        animal_count: { type: number, minimum: 0, nullable: true }
        male_count: { type: number, minimum: 0, nullable: true }
        female_count: { type: number, minimum: 0, nullable: true }
        total_weight_kg: { type: number, minimum: 0, nullable: true }
        sales_value:
          type: number
          exclusiveMinimum: 0
          description: Required and must be more than zero.
        advance_amount: { type: number, minimum: 0, nullable: true }
        comments: { type: string, maxLength: 2000 }
    SalesOverviewSummary:
      type: object
      description: Headline figures over CLOSED deals in the farm scope.
      required:
        - revenue
        - live_revenue
        - deals
        - animals
        - sheep
        - goats
        - live_weight_kg
        - realized_price_per_kg
        - manure_kg
        - manure_revenue
        - period_from
        - period_to
      properties:
        revenue: { type: number }
        live_revenue:
          type: number
          description: Closed Sheep + Goat revenue. Manure is excluded here and carried separately.
        deals: { type: integer }
        animals: { type: number }
        sheep: { type: number }
        goats: { type: number }
        live_weight_kg: { type: number }
        realized_price_per_kg:
          type: number
          description: >-
            Closed live revenue over closed live weight, both ranging over the same deals; 0 when
            no weighed live sales exist.
        manure_kg: { type: number }
        manure_revenue: { type: number }
        period_from:
          type: string
          description: Earliest closed sale date in scope (YYYY-MM-DD); empty when none.
        period_to:
          type: string
          description: Latest closed sale date in scope (YYYY-MM-DD); empty when none.
    SalesOverviewMonthly:
      type: object
      description: One month with at least one closed deal. Months derive from sale_date.
      required: [month, sheep_revenue, goat_revenue, manure_revenue, sheep_count, goat_count, manure_kg]
      properties:
        month: { type: string, description: "YYYY-MM" }
        sheep_revenue: { type: number }
        goat_revenue: { type: number }
        manure_revenue: { type: number }
        sheep_count: { type: number }
        goat_count: { type: number }
        manure_kg: { type: number }
    SalesPriceBand:
      type: object
      description: >-
        Realized price per kg for one (live product type, breed), over closed deals with weight and
        value recorded. Ordered by average price, highest first.
      required: [product_type, breed, deals, animals, weight_kg, revenue, avg_price_per_kg, min_price_per_kg, max_price_per_kg]
      properties:
        product_type: { type: string, enum: [Sheep, Goat] }
        breed: { type: string }
        deals: { type: integer }
        animals: { type: number }
        weight_kg: { type: number }
        revenue: { type: number }
        avg_price_per_kg: { type: number }
        min_price_per_kg: { type: number }
        max_price_per_kg: { type: number }
    SalesBuyer:
      type: object
      description: One buyer's closed-deal history, ordered by revenue. Top 25 buyers only; share_pct is against whole-filter revenue.
      required: [buyer_name, buyer_place, product_types, deals, animals, revenue, share_pct]
      properties:
        buyer_name: { type: string }
        buyer_place: { type: string }
        product_types:
          type: array
          items: { type: string }
          description: Sorted set of product types this buyer has bought.
        deals: { type: integer }
        animals: { type: number }
        revenue: { type: number }
        share_pct: { type: number }
    SalesStatusCount:
      type: object
      required: [status, count]
      properties:
        status:
          type: string
          description: The recorded call status; a lead never contacted reports as "uncontacted".
        count: { type: integer }
    SalesPlaceCount:
      type: object
      required: [place, count]
      properties:
        place: { type: string }
        count: { type: integer }
    SalesBuyerPipeline:
      type: object
      required: [total, statuses, top_places]
      properties:
        total: { type: integer }
        statuses:
          type: array
          items:
            $ref: "#/components/schemas/SalesStatusCount"
        top_places:
          type: array
          items:
            $ref: "#/components/schemas/SalesPlaceCount"
    SalesFPOPipeline:
      type: object
      required: [total, statuses, districts]
      properties:
        total: { type: integer }
        statuses:
          type: array
          items:
            $ref: "#/components/schemas/SalesStatusCount"
        districts:
          type: array
          items:
            $ref: "#/components/schemas/SalesPlaceCount"
    SalesTagTypeCount:
      type: object
      required: [label, count]
      properties:
        label: { type: string }
        count: { type: integer }
    SalesTagRoster:
      type: object
      required: [total, sales_count, by_type]
      properties:
        total: { type: integer }
        sales_count:
          type: integer
          description: Distinct sale references the tag rows point back at, never the row count.
        by_type:
          type: array
          items:
            $ref: "#/components/schemas/SalesTagTypeCount"
    SalesWeightAudit:
      type: object
      description: Video weight vs book weight, bucketed by absolute gap into three disjoint ranges.
      required: [total, within_0_3_kg, within_1_kg, over_1_kg, max_gap_kg]
      properties:
        total: { type: integer }
        within_0_3_kg: { type: integer }
        within_1_kg: { type: integer }
        over_1_kg: { type: integer }
        max_gap_kg: { type: number }
    SalesMarketBenchmark:
      type: object
      required: [breed]
      properties:
        market: { type: string, nullable: true }
        category: { type: string, nullable: true }
        breed: { type: string }
        source: { type: string, nullable: true }
        ex_farm_rate: { type: string, nullable: true }
        transport_rate: { type: string, nullable: true }
        landing_cost_per_kg: { type: number, nullable: true }
        market_price_per_kg:
          type: number
          nullable: true
          description: Parsed out of the market text at import time, never at read time.
    SalesOverview:
      type: object
      description: The whole sales page contract, all blocks whole-filter aggregates.
      required:
        - summary
        - monthly
        - price_bands
        - buyers
        - buyer_pipeline
        - fpo_pipeline
        - tag_roster
        - weight_audit
        - market_benchmarks
      properties:
        summary:
          $ref: "#/components/schemas/SalesOverviewSummary"
        monthly:
          type: array
          items:
            $ref: "#/components/schemas/SalesOverviewMonthly"
        price_bands:
          type: array
          items:
            $ref: "#/components/schemas/SalesPriceBand"
        buyers:
          type: array
          items:
            $ref: "#/components/schemas/SalesBuyer"
        buyer_pipeline:
          $ref: "#/components/schemas/SalesBuyerPipeline"
        fpo_pipeline:
          $ref: "#/components/schemas/SalesFPOPipeline"
        tag_roster:
          $ref: "#/components/schemas/SalesTagRoster"
        weight_audit:
          $ref: "#/components/schemas/SalesWeightAudit"
        market_benchmarks:
          type: array
          items:
            $ref: "#/components/schemas/SalesMarketBenchmark"
    FeedConfigRationGroupPage:
      type: object
      additionalProperties: false
      required:
        - items
        - ration_groups
        - limit
        - offset
        - has_more
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/FeedConfigRationGroup"
        ration_groups:
          type: array
          description: >-
            Every ration-group label that carries an in-force authored rate. This is NOT the set of
            labels reachable through items: items is the BREED map and that table is adult breeds
            only, so the fixed 'Kid' group -- which kids resolve to by age band -- appears in no row
            of it. Build a ration-group filter from this field; build a BREED filter from items.
            Whole-set and unpaginated on purpose -- limit/offset/has_more describe items only.
          items:
            type: string
        limit:
          type: integer
        offset:
          type: integer
        has_more:
          type: boolean
    FeedConfigShedTag:
      type: object
      additionalProperties: false
      required:
        - shed_tag_id
        - shed_tag
        - applies_to
        - display_order
        - status
      properties:
        shed_tag_id:
          type: string
          format: uuid
        shed_tag:
          type: string
        applies_to:
          type: string
          enum: [adult, kid]
        display_order:
          type: integer
        status:
          type: string
          enum: [active, retired]
    FeedConfigShedTagPage:
      type: object
      additionalProperties: false
      required:
        - items
        - limit
        - offset
        - has_more
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/FeedConfigShedTag"
        limit:
          type: integer
        offset:
          type: integer
        has_more:
          type: boolean
    FeedConfigFeedItem:
      type: object
      additionalProperties: false
      required:
        - feed_item_id
        - feed_item
        - display_order
        - status
      properties:
        feed_item_id:
          type: string
          format: uuid
        feed_item:
          type: string
        energy_kcal_per_kg:
          type: string
          description: Exact decimal string. Omitted when unset -- an honest gap, not a zero.
        dry_matter_factor:
          type: string
        wastage_factor:
          type: string
        display_order:
          type: integer
        status:
          type: string
          enum: [active, retired]
    FeedConfigFeedItemPage:
      type: object
      additionalProperties: false
      required:
        - items
        - limit
        - offset
        - has_more
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/FeedConfigFeedItem"
        limit:
          type: integer
        offset:
          type: integer
        has_more:
          type: boolean
    FeedConfigSessionTemplate:
      type: object
      additionalProperties: false
      required:
        - session_template_id
        - park_id
        - session_no
        - session_label
        - split_fraction
        - display_order
        - status
        - items
      properties:
        session_template_id:
          type: string
          format: uuid
        park_id:
          type: string
          format: uuid
        session_no:
          type: integer
        session_label:
          type: string
        split_fraction:
          type: string
          description: Exact decimal string. A park's active sessions are expected to sum to 1.0.
        display_order:
          type: integer
        status:
          type: string
          enum: [active, retired]
        items:
          type: array
          description: >-
            The feeds this session actually serves, in packing order. THIS IS WHAT DECIDES WHETHER A
            FEED REACHES AN ANIMAL: generation walks these slots and looks each one up in the ration
            grid, so a feed with a grid quantity but no slot here is never looked up and is absent
            from the sheet, the summary, the totals and the packing worklist without raising a gap.
            Never null -- a session that declares nothing is an empty list, which is a real and
            blocking state rather than missing data.
          items:
            $ref: "#/components/schemas/FeedConfigSessionTemplateItem"
    FeedConfigSessionTemplateItem:
      type: object
      additionalProperties: false
      required:
        - session_template_item_id
        - session_no
        - slot_no
        - feed_item
      properties:
        session_template_item_id:
          type: string
          format: uuid
        session_no:
          type: integer
        slot_no:
          type: integer
          description: >-
            Packing order within the session. Derived by the backend on declare (appended to the end)
            rather than chosen by the author, so declaring a feed never renumbers slots packers
            already know.
        feed_item:
          type: string
    SetFeedConfigSessionTemplateItemRequest:
      type: object
      additionalProperties: false
      required:
        - park_id
        - session_no
        - feed_item
        - declared
      properties:
        park_id:
          type: string
          format: uuid
        session_no:
          type: integer
          minimum: 1
        feed_item:
          type: string
        declared:
          type: boolean
          description: >-
            true puts the feed on this session's recipe; false withdraws it. Required rather than
            defaulted, because the two directions are opposite feeding decisions and neither is a
            safe guess. There is no quantity field: grams live in the ration grid, keyed by ration
            group and shed tag, because one slot feeds every group in the park at a different rate.
    FeedDirectionItemQuantity:
      type: object
      additionalProperties: false
      required:
        - feed_item
        - status
        - quantity_kg
      properties:
        feed_item:
          type: string
        status:
          type: string
          enum: [resolved, blocked]
          description: >-
            `resolved` means the quantity was DERIVED from authored configuration -- including an
            authored zero, which is a real "feed nothing" instruction. `blocked` means the
            configuration needed to derive it does not exist.
        quantity_kg:
          type: string
          nullable: true
          description: >-
            Rounded, packable quantity in kg as an exact decimal STRING (three decimals), never a
            float -- a float round trip is exactly how a column total ends up reading 12.299999999.


            NULL IF AND ONLY IF `status` is `blocked`. This is the safety contract of this API: an
            unauthored ration has NO number, so it cannot be summed, rendered, or defaulted into a
            plausible-looking 0. An authored zero is the opposite state and is fully numeric
            ("0.000"). A client that coerces null to 0 reintroduces the starvation path this
            distinction exists to prevent.
        grams_per_head:
          type: string
          description: >-
            The authored per-head rate this quantity came from, echoed so an operator can see the
            input without opening the config screen. Absent for blocked cells and for the
            `experiment` workflow, which authors absolute kg and has no per-head rate.
        shed_factor:
          type: string
          description: The per-shed multiplier applied. Absent for blocked and experiment cells.
        blocked_reason:
          $ref: "#/components/schemas/FeedDirectionBlockedReason"
    FeedDirectionBlockedReason:
      type: object
      additionalProperties: false
      required:
        - code
        - detail
      properties:
        code:
          type: string
          enum:
            - no_ration_rate
            - unknown_shed_tag
            - unknown_ration_group
            - no_session_template
          description: Machine-stable reason code, so a UI can group and count authored gaps.
        detail:
          type: string
          description: >-
            The exact missing coordinate in human-readable form. A gap an operator cannot locate is
            a gap they cannot close.
    FeedAnalyticsDirectedDay:
      type: object
      description: One feed day of the normal workflow across every feed item.
      required: [feed_day, directed_kg, head_days, per_head_grams]
      properties:
        feed_day:
          type: string
          format: date
        directed_kg:
          type: string
          description: >-
            Summed resolved quantity as a decimal string. "0" means every resolved cell authored
            zero -- blocked cells contribute nothing and are never coerced to zero.
        head_days:
          type: integer
          format: int64
          description: Distinct pen-grain heads that day; experiment pens excluded.
        per_head_grams:
          type: string
          description: directed_kg x 1000 / head_days, one decimal; empty when head_days is zero.
    FeedAnalyticsDirectedItem:
      type: object
      description: One (feed day, feed item) of the normal workflow.
      required: [feed_day, feed_item_label, feed_item_key, directed_kg, head_days, per_head_grams]
      properties:
        feed_day:
          type: string
          format: date
        feed_item_label:
          type: string
        feed_item_key:
          type: string
        directed_kg:
          type: string
        head_days:
          type: integer
          format: int64
          description: Heads in pens whose sheet carried THIS item that day, pen-grain counted once.
        per_head_grams:
          type: string
    FeedAnalyticsDirectedResponse:
      type: object
      description: >-
        The Feed Analytics directed rollup. DIRECTED kg only -- the sheet's instruction, not a
        measured weight.
      required: [date_from, date_to, days, items]
      properties:
        date_from:
          type: string
          format: date
          description: The served window start after clamping (92-day cap, most recent kept).
        date_to:
          type: string
          format: date
        days:
          type: array
          items:
            $ref: "#/components/schemas/FeedAnalyticsDirectedDay"
        items:
          type: array
          items:
            $ref: "#/components/schemas/FeedAnalyticsDirectedItem"
    FeedAnalyticsExecutionDay:
      type: object
      description: One business date of proof-gated feed execution statuses.
      required:
        [date, packing_verified, packing_awaiting, packing_rework, distribution_verified,
         distribution_awaiting, distribution_rework, transport_completed, transport_open,
         transport_awaiting_verdict, transport_rework]
      properties:
        date:
          type: string
          format: date
        packing_verified:
          type: integer
          format: int64
        packing_awaiting:
          type: integer
          format: int64
        packing_rework:
          type: integer
          format: int64
        distribution_verified:
          type: integer
          format: int64
        distribution_awaiting:
          type: integer
          format: int64
        distribution_rework:
          type: integer
          format: int64
        transport_completed:
          type: integer
          format: int64
        transport_open:
          type: integer
          format: int64
        transport_awaiting_verdict:
          type: integer
          format: int64
        transport_rework:
          type: integer
          format: int64
        median_verify_latency_minutes:
          type: integer
          format: int64
          nullable: true
          description: Median submit-to-verdict latency of verdicts landing that IST date; null when none landed.
    FeedAnalyticsPackingVarianceRow:
      type: object
      description: >-
        One MISMATCH between what the frozen sheet directed a pen-session to pack for one feed item
        and what the verifier entered off the packing video (blind per-item entry, maintainer
        decision 2026-08-21). A row exists only when the difference exceeds the 0.2 kg tolerance --
        a scale read off a video is honest to a couple hundred grams. LEADERSHIP-ONLY: the verifier
        never sees the planned figures, so this comparison must never be rendered on a verifier
        surface.
      required:
        [feed_day, park_label, shed_id, shed_label, operational_location_display, session_no,
         workflow, feed_item_key, feed_item_label, planned_kg, verified_kg, variance_kg]
      properties:
        feed_day:
          type: string
          format: date
        park_label:
          type: string
        shed_id:
          type: string
          format: uuid
        shed_label:
          type: string
        partition_label:
          type: string
          description: The pen ("2", "Part 3"); absent for an undivided shed.
        operational_location_display:
          type: string
          description: Backend-composed shed+pen label; render verbatim, never compose on the client.
        session_no:
          type: integer
          format: int32
        session_label:
          type: string
          description: The sheet's session name ("Morning"); absent when the frozen row is gone.
        workflow:
          type: string
        feed_item_key:
          type: string
        feed_item_label:
          type: string
        planned_kg:
          type: string
          description: >-
            The frozen sheet's summed quantity as a decimal string; EMPTY when the sheet carried no
            resolved quantity for this item -- blank and zero are never conflated.
        verified_kg:
          type: string
          description: The verifier's entered reading. "0" is a real observation.
        variance_kg:
          type: string
          description: verified minus the resolved planned quantity (0 when unresolved), signed.
    FeedAnalyticsExecutionResponse:
      type: object
      required: [date_from, date_to, days, packing_variance]
      properties:
        date_from:
          type: string
          format: date
        date_to:
          type: string
          format: date
        days:
          type: array
          items:
            $ref: "#/components/schemas/FeedAnalyticsExecutionDay"
        packing_variance:
          type: array
          description: >-
            Every intended-vs-entered packing mismatch in the window, newest feed day first. Always
            present; empty when every verified reading matched the sheet.
          items:
            $ref: "#/components/schemas/FeedAnalyticsPackingVarianceRow"
    FeedAnalyticsExperimentItem:
      type: object
      description: One (feed day, feed item) of authored absolute kg across every experiment pen.
      required: [feed_day, feed_item_label, feed_item_key, kg]
      properties:
        feed_day:
          type: string
          format: date
        feed_item_label:
          type: string
        feed_item_key:
          type: string
        kg:
          type: string
          description: Authored TOTAL kg as a decimal string -- never multiplied by heads.
    FeedAnalyticsExperimentResponse:
      type: object
      required: [date_from, date_to, items, wastage_day, wastage_pens]
      properties:
        date_from:
          type: string
          format: date
        date_to:
          type: string
          format: date
        items:
          type: array
          items:
            $ref: "#/components/schemas/FeedAnalyticsExperimentItem"
        wastage_day:
          type: string
          format: date
          description: Echo of the business day the wastage_pens table describes.
        wastage_pens:
          type: array
          description: >-
            The selected day's per-pen leftover-feed table, derived from that day's experiment
            sheet — a pen with no video yet still lists, with blank status and kg. Experiment-only
            by definition — wastage exists on no other workflow.
          items:
            $ref: "#/components/schemas/FeedAnalyticsExperimentWastagePen"
    FeedAnalyticsExperimentWastagePen:
      type: object
      description: >-
        One experiment pen's leftover-feed state for the selected day. wastage_kg is the
        VERIFIER's recorded value — blank until she records one; "0" is a real measurement (an
        empty trough), so blank and zero are never conflated.
      required: [shed_id, park_label, shed_name, partition_label, operational_location_display, lifecycle_status, wastage_kg]
      properties:
        shed_id:
          type: string
          format: uuid
        park_label:
          type: string
          description: Disambiguates the pen under a tenant-wide read — shed names repeat across parks.
        shed_name:
          type: string
        partition_label:
          type: string
          description: The pen ("1", "Part 3"); empty for an undivided shed.
        operational_location_display:
          type: string
          description: Backend-composed shed+pen label ("Castro 1", "Godel 2 - Part 1"); render verbatim.
        lifecycle_status:
          type: string
          description: Blank when no video was submitted yet, else pending_verification | rework | completed.
        wastage_kg:
          type: string
          description: Verifier-recorded leftover kg as a decimal string; blank until recorded.
    FeedAnalyticsStockItem:
      type: object
      description: >-
        One FARM's current stock position for one feed item, off the purchase ledger. Each farm
        keeps its own physical store, so there is deliberately no tenant-wide combined balance
        (maintainer decision 2026-08-21) — under an all-parks scope every card names its farm.
      required: [farm_label, feed_item_label, feed_item_key, balance_kg, avg_daily_kg, latest_batch_no, low_stock]
      properties:
        farm_label:
          type: string
          description: The farm whose store this position describes (e.g. CBE, CPT).
        feed_item_label:
          type: string
        feed_item_key:
          type: string
        balance_kg:
          type: string
          description: May be negative -- the ledger is missing a load, never clamped.
        avg_daily_kg:
          type: string
          description: Average directed kg over the item's 3 most recent locked feed days; empty when never directed.
        days_left:
          type: integer
          format: int64
          nullable: true
        latest_batch_no:
          type: integer
          format: int64
        low_stock:
          type: boolean
    FeedAnalyticsStockFarmItem:
      type: object
      description: >-
        One (farm, Mesha concentrate) row of the Stock tab's per-farm purchase/consumption table.
        Covers the four in-house Mesha concentrate feeds only (maintainer decision 2026-08-21).
        Consumption figures come from LOCKED GoatOS feed sheets, so a bootstrapped item's
        first_directed_day is the ledger cutoff, not the sheet era before it.
      required:
        - farm_label
        - feed_item_label
        - feed_item_key
        - first_purchase_date
        - first_directed_day
        - avg_daily_kg
        - last_load_batch_no
        - last_load_date
        - last_load_quantity_kg
        - last_load_vendor
        - expected_stock_kg
        - ledger_stock_kg
        - stock_variance_kg
        - stock_check_status
      properties:
        farm_label:
          type: string
        feed_item_label:
          type: string
        feed_item_key:
          type: string
        first_purchase_date:
          type: string
          format: date
          description: Earliest load's purchase date for this farm.
        first_directed_day:
          type: string
          description: First locked feed day the item was directed at this farm; empty when never directed.
        avg_daily_kg:
          type: string
          description: >-
            Average directed kg over the farm's 3 most recent locked feed days for the item
            (same semantics as the stock card, scoped to the farm); empty when never directed.
        last_load_batch_no:
          type: integer
          format: int64
        last_load_date:
          type: string
          format: date
        last_load_quantity_kg:
          type: string
        last_load_vendor:
          type: string
        expected_stock_kg:
          type: string
          description: Latest load quantity minus days since first_directed_day through yesterday in IST times avg_daily_kg; negative values show the expected shortage.
        ledger_stock_kg:
          type: string
          description: Displayed current stock from the canonical purchase ledger after consumed-at-import and locked-sheet depletion.
        stock_variance_kg:
          type: string
          description: ledger_stock_kg minus expected_stock_kg.
        stock_check_status:
          type: string
          enum: [ok, mismatch, unavailable]
    FeedAnalyticsExpenditureDay:
      type: object
      required: [feed_day, rupees]
      properties:
        feed_day:
          type: string
          format: date
        rupees:
          type: string
          description: Directed kg priced at each item's most recent load rate on or before the day.
    FeedAnalyticsSpendSummary:
      type: object
      description: >-
        Rupee totals over the standing leadership periods, each ending yesterday and priced like
        the daily series (most recent load rate per item). "0" when nothing priced.
      required: [this_week, this_month, three_months, this_year]
      properties:
        this_week:
          type: string
          description: Monday of the current IST week through yesterday.
        this_month:
          type: string
          description: The 1st of the current IST month through yesterday.
        three_months:
          type: string
          description: Rolling 92 days through yesterday.
        this_year:
          type: string
          description: Jan 1 of the current IST year through yesterday.
    FeedAnalyticsStockResponse:
      type: object
      required: [date_from, date_to, items, farm_items, expenditure, spend]
      properties:
        date_from:
          type: string
          format: date
        date_to:
          type: string
          format: date
        items:
          type: array
          items:
            $ref: "#/components/schemas/FeedAnalyticsStockItem"
        farm_items:
          type: array
          description: Per-farm Mesha-concentrate purchase/consumption rows, ordered by feed item then farm.
          items:
            $ref: "#/components/schemas/FeedAnalyticsStockFarmItem"
        expenditure:
          type: array
          items:
            $ref: "#/components/schemas/FeedAnalyticsExpenditureDay"
        spend:
          $ref: "#/components/schemas/FeedAnalyticsSpendSummary"
    FeedDirectionRow:
      type: object
      additionalProperties: false
      description: >-
        ONE ROW PER OPERATIONAL LOCATION PER SESSION -- one pen, one feeding instruction. A pen
        holding several breeds or management stages is ONE row whose descriptive columns list every
        value present (` + `-joined) and whose quantities are summed, never several rows an operator
        has to re-add at the pen door. The packing worklist is built at the same grain, so a row and
        the bag packed for it always describe the same pen.
      required:
        - park_id
        - park_label
        - shed_id
        - shed_label
        - operational_location_display
        - shed_tag
        - breed
        - ration_group
        - experiment_arm
        - session_no
        - session_label
        - head_count
        - head_count_informational
        - workflow
        - items
        - session_total_kg
        - blocked
        - overdue_pending
        - completed
      properties:
        park_id:
          type: string
          format: uuid
        park_label:
          type: string
        shed_id:
          type: string
          format: uuid
        shed_label:
          type: string
        partition_label:
          type: string
          description: >-
            The row's operational partition within the physical shed ("1", "Part 3"), absent for a
            shed that has none. A feed row's grain is the OPERATIONAL LOCATION, not the shed: one
            shed can run an authored experiment on some partitions while the rest stay on the
            per-head ration grid, so Castro 1 and Castro 2 are two rows sharing one shed_id.
            Clients MUST render shed + partition together ("Castro 1", "Godel 2 - Part 3") and must
            never display the bare shed name for a partitioned row, or two different pens appear as
            two identical lines.
        operational_location_display:
          type: string
          description: Backend-composed physical feed location label. Render verbatim.
        shed_tag:
          type: string
          description: >-
            The AUTHORED tag label the live management stage resolved onto -- the canonical
            spelling, not the raw source text, which carries cosmetic variants. Populated on EVERY
            workflow from the animals actually in the shed: an experiment row has no ration grain,
            but its animals still carry a management stage. It never carries the experiment arm,
            which has its own `experiment_arm` field. A pen holding two stages reports both,
            joined by ` + `, dominant (largest head count) first.
        breed:
          type: string
          description: >-
            The raw live breed label. Reported alongside `ration_group` because they differ in ways
            an operator needs to see: Beetal and Sirohi are two breeds sharing one
            `Beetal/Sirohi` group, and every kid breed collapses to `Kid`. Populated on every
            workflow from the animals in the pen; a multi-breed pen reports every breed joined by
            ` + ` (`Beetal + Sojat`), dominant first, rather than naming one and implying it is the
            only one. THE ROW IS NOT SPLIT PER BREED -- see the row's own grain description.
        ration_group:
          type: string
          description: >-
            What the breed resolved to in the ration grid, joined by ` + ` when the pen holds more
            than one (`Beetal/Sirohi + Kid`). EMPTY on an experiment row, correctly: an absolute
            hand-authored kg never consults the breed -> ration-group map, so there is no group to
            report. That is a real state, not missing data, and must render as a deliberate blank.
        experiment_arm:
          type: string
          description: >-
            The trial group a hand-authored experiment pen is enrolled in (`Sheep M NEW`); empty on
            every normal row. A SEPARATE field from `shed_tag` because the two are different facts:
            a tag is the animals' management stage and selects the ration course, an arm is which
            trial the pen is in and selects nothing, since the quantity is hand-entered. Mirrors
            the `Experiment arm` column Feed Config shows over the same authored value.
        session_no:
          type: integer
        session_label:
          type: string
        head_count:
          type: integer
          format: int64
          description: >-
            Projected head count for this grain on the target date -- live herd plus the approved
            but unexecuted movements. Sex is summed away because it is not part of the ration key.
        head_count_informational:
          type: boolean
          description: >-
            True when `head_count` did NOT drive the quantity, i.e. the `experiment` workflow, whose
            authored kg is already a pen total. Multiplying by head count there would overfeed the
            pen by a factor of its whole population, so this flag exists to stop exactly that.
        workflow:
          type: string
          enum: [normal, experiment]
          description: >-
            Which quantity strategy produced the row. `normal` resolves from the ration grid;
            `experiment` uses hand-authored absolute kg per pen (an undivided shed is one pen).
        items:
          type: array
          items:
            $ref: "#/components/schemas/FeedDirectionItemQuantity"
        session_total_kg:
          type: string
          description: >-
            Sum of the RESOLVED items only, as an exact decimal string. Blocked items contribute
            nothing because they have no number to contribute -- `blocked` is what tells the reader
            this total is partial.
        blocked:
          type: boolean
          description: >-
            True when part of this pen's ration is unauthored, so `session_total_kg` covers only
            part of the pen. The quantities that ARE configured still carry numbers -- the operator
            feeds those animals and records their video -- and `blocked_reasons` names the gap. An
            item where NOTHING resolved has a null `quantity_kg` and is never rendered as 0.
        blocked_reasons:
          type: array
          description: >-
            The distinct gaps behind `blocked`, each naming the exact missing coordinate (park /
            ration group / tag / feed item) so an operator can locate and close it. Absent on a
            fully resolved row. Present because one row covers a whole pen: when only part of the
            pen's ration is authored, the row must say WHICH part is missing.
          items:
            $ref: "#/components/schemas/FeedDirectionBlockedReason"
        overdue_pending:
          type: boolean
          description: >-
            A movement this row's projected head count already assumes came due before the target
            date and still has not been executed.
        completed:
          type: boolean
          description: >-
            True when this pen's session has a recorded completion (`feed.direction.completed`). The
            ration numbers are generated the same way; this reports that the feeding was carried
            out. Completion is recorded per (shed, partition, session, workflow), which is exactly
            this row's grain, so it maps 1:1.
    FeedDirectionFeedItemTotal:
      type: object
      additionalProperties: false
      required:
        - feed_item
        - quantity_kg
        - blocked_cells
      properties:
        feed_item:
          type: string
        quantity_kg:
          type: string
        blocked_cells:
          type: integer
          description: >-
            Cells for this item that could not be resolved. A column total is never read as complete
            when part of it is missing.
    FeedDirectionPreviewSummary:
      type: object
      additionalProperties: false
      required:
        - scope
        - shed_count
        - row_count
        - total_kg_by_feed_item
        - blocked_count
        - blocked_shed_count
      properties:
        scope:
          type: string
          enum: [filtered]
          description: >-
            Always `filtered`: every figure in this summary covers every row matching the request's
            park, target date, shed and session filters -- NOT the returned page -- and is invariant
            to `limit` and `offset`. An explicit field rather than documentation so a client can
            assert the coverage guarantee instead of trusting prose.
        shed_count:
          type: integer
          description: Distinct sheds in the whole filtered scope.
        row_count:
          type: integer
          description: >-
            Generated rows in the whole filtered scope. This is NOT `items.length`, which is the
            page; comparing the two is how a client sees what fraction of the result is visible.
        total_kg_by_feed_item:
          type: array
          description: >-
            Per-item totals across the WHOLE filtered scope, in authored catalog order so a client's
            columns are stable across pages. A blocked cell is absent from the sum and counted in
            `blocked_cells` instead -- never added as zero. These are summed from the same rounded
            per-cell quantities the rows carry, so the total equals what a packer physically packs.
          items:
            $ref: "#/components/schemas/FeedDirectionFeedItemTotal"
        blocked_count:
          type: integer
          description: >-
            Blocked ITEM cells in the whole filtered scope -- the number of authored gaps to close.
        blocked_shed_count:
          type: integer
          description: Distinct sheds carrying at least one blocked cell, i.e. sheds at risk of going unfed.
    FeedDirectionWorkflowLifecycle:
      type: object
      additionalProperties: false
      required:
        - workflow
        - state
        - amendment_count
      properties:
        workflow:
          type: string
          enum: [normal, experiment]
        state:
          type: string
          enum: [issued, amended, locked, pending, not_issued]
          description: >-
            This workflow's issue state. issued/amended/locked describe a FROZEN sheet;
            pending/not_issued mean nothing was issued for this workflow (see `expected_issue_at`).
        issued_at:
          type: string
          format: date-time
          description: When the sheet was issued (Asia/Kolkata). Absent until issued.
        amended_at:
          type: string
          format: date-time
        locked_at:
          type: string
          format: date-time
        amendment_count:
          type: integer
        expected_issue_at:
          type: string
          format: date-time
          description: >-
            Set on pending/not_issued: the instant this workflow's sheet is (or was) due to be
            issued -- the direction_time on the day before the feed day. Lets the UI say "issued
            <D-1> at <time> IST" instead of showing a speculative number.
    FeedDirectionLifecycle:
      type: object
      additionalProperties: false
      required:
        - state
        - amendment_count
        - workflows
      properties:
        state:
          type: string
          enum: [issued, amended, locked, pending, not_issued, preview, beyond_horizon, draft]
          description: >-
            The aggregate lifecycle of the served park-day, the LEAST-ADVANCED state among its
            workflows (issued < amended < locked): a park-day is never "locked" while a workflow is
            still merely issued. `pending` = the workflow's dispatch clock has NOT fired yet, so the
            day serves NO ROWS and `message` names when the sheet arrives (normal at direction_time
            07:00, experiment at 14:00, both on D-1). At/after that instant the FIRST read generates
            AND FREEZES the sheet, so the state becomes `issued` and every later read serves those
            frozen rows -- the kilograms a packing crew sees cannot move under them (maintainer
            decision 2026-08-08, superseding the 2026-07-20 always-generate `preview`). A mixed
            morning is normal: at 08:00 the aggregate is `issued` for normal while the workflows[]
            entry for experiment is still `pending` with its 14:00 `expected_issue_at`.
            `beyond_horizon` = the day has no issued sheet AND falls outside the [today, tomorrow]
            projection window, so NO rows were generated (generating would fabricate a sheet from
            today's herd); the `message` names the horizon, and this check runs BEFORE the gate so a
            past day is never freeze-on-read. An already-issued sheet for any date still serves its
            frozen rows regardless of the window. `draft` = a live what-if (draft=true), never a
            frozen sheet, and the only remaining path that live-computes an un-issued day.
            `preview` is retained in the enum for older clients only; the serve path no longer
            emits it.
        issued_at:
          type: string
          format: date-time
        amended_at:
          type: string
          format: date-time
        locked_at:
          type: string
          format: date-time
        amendment_count:
          type: integer
          description: Total amendments across the park-day's workflows.
        message:
          type: string
          description: Operator sentence, most useful for pending/not_issued.
        workflows:
          type: array
          items:
            $ref: "#/components/schemas/FeedDirectionWorkflowLifecycle"
    FeedDirectionFilterOptions:
      type: object
      additionalProperties: false
      description: >-
        Backend-owned park/shed filter vocabulary for the feed screens, plus the park this response
        was actually generated for. A client renders its farm/shed pickers from this and holds no
        location list of its own. Bounded by physical infrastructure (parks and one park's shed
        catalog), never by herd size. served_park_id equals the requested park_id, or — when the
        request omitted park_id — the default park the server selected from the caller's own
        authorized parks.
      required:
        - served_park_id
        - parks
        - sheds
        - sessions
      properties:
        served_park_id:
          type: string
          description: The park id this response was generated for (the requested park, or the caller's defaulted first authorized park).
        parks:
          type: array
          description: >-
            The parks THIS CALLER may open, not the tenant catalog. A park-scoped operator receives
            only their own park; a tenant-wide principal receives every active park. The route
            refuses a park outside the caller's scope (403 park_scope_forbidden), so an option that
            is not listed here could never have been selected successfully.
          items:
            $ref: "#/components/schemas/FeedDirectionFilterPark"
        sheds:
          type: array
          items:
            $ref: "#/components/schemas/FeedDirectionFilterShed"
        sessions:
          type: array
          description: >-
            The served park's active feeding-session split (session 1, session 2, …), in display
            order — the backend-owned vocabulary the client renders its session picker from. The
            client sends session_no back as the `session` query param.
          items:
            $ref: "#/components/schemas/FeedDirectionFilterSession"
    FeedDirectionFilterSession:
      type: object
      additionalProperties: false
      required: [session_no, label]
      properties:
        session_no:
          type: integer
          description: The session number sent back as the `session` filter param.
        label:
          type: string
          description: The authored session label (e.g. Morning / Evening).
    FeedDirectionFilterPark:
      type: object
      additionalProperties: false
      required: [park_id, label]
      properties:
        park_id:
          type: string
        label:
          type: string
    FeedDirectionFilterShed:
      type: object
      additionalProperties: false
      required: [shed_id, label, park_id]
      properties:
        shed_id:
          type: string
        label:
          type: string
        park_id:
          type: string
    FeedDirectionPreviewPage:
      type: object
      additionalProperties: false
      required:
        - items
        - summary
        - lifecycle
        - draft
        - filters
        - target_date
        - limit
        - offset
        - has_more
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/FeedDirectionRow"
        summary:
          $ref: "#/components/schemas/FeedDirectionPreviewSummary"
        lifecycle:
          $ref: "#/components/schemas/FeedDirectionLifecycle"
        draft:
          type: boolean
          description: >-
            True only for the deliberate live-compute escape hatch (draft=true). An issued, amended,
            locked, pending or not_issued response is never draft.
        filters:
          $ref: "#/components/schemas/FeedDirectionFilterOptions"
        target_date:
          type: string
          format: date
        limit:
          type: integer
          description: Page size in SHEDS.
        offset:
          type: integer
        has_more:
          type: boolean
    FeedPackingRow:
      type: object
      additionalProperties: false
      required:
        - park_id
        - park_label
        - shed_id
        - shed_label
        - operational_location_display
        - session_no
        - session_label
        - workflow
        - experiment_arm
        - head_count
        - items
        - total_kg
        - status
        - completed
        - lifecycle_status
      properties:
        park_id:
          type: string
          format: uuid
        park_label:
          type: string
        shed_id:
          type: string
          format: uuid
        shed_label:
          type: string
        partition_label:
          type: string
          description: >-
            The operational partition this bag is for ("1", "Part 3"), absent for a shed with no
            partitions. A packing line is grouped at the same operational-location grain as the
            direction row it is built from, so Castro 1 and Castro 2 are two separate bags. Clients
            MUST render shed + partition together: two "Castro" lines with no partition leave the
            packer unable to tell which pen either bag is for, and one shed's partitions can carry
            very different quantities when some run an authored experiment and the rest the grid.
        operational_location_display:
          type: string
          description: Backend-composed physical feed location label. Render verbatim.
        session_no:
          type: integer
          description: >-
            The feeding session this bag is for. Part of the line's identity: a pen appears once per
            session, and one video proves one session.
        session_label:
          type: string
          description: The authored session name ("Morning"). Farm copy; render verbatim.
        workflow:
          type: string
          enum: [normal, experiment]
        experiment_arm:
          type: string
          description: >-
            The trial group of a hand-authored experiment pen, empty on normal lines. Carried here
            as well as on the direction row so a packer knows which trial a bag belongs to without
            cross-referencing the direction sheet. Never a shed tag.
        head_count:
          type: integer
          format: int64
          description: >-
            The operational pen's projected or informational head count -- the DENOMINATOR this
            session's ration was computed from, not a quantity. The same animals are fed morning and
            evening, so the figure repeats across a pen's sessions and must never be summed over
            them. An undivided shed is its single pen; never interpret a partitioned row as the
            building-wide count.
        items:
          type: array
          description: >-
            This session's expected quantity per feed item, with the ration grains already summed
            within the pen, because a packer fills one bag per item per operational location. The
            same blocked-vs-zero contract as the preview applies: a blocked item has
            `quantity_kg: null`, which is a different statement from a resolved `0.000` and must not
            be confused with one.
          items:
            $ref: "#/components/schemas/FeedDirectionItemQuantity"
        total_kg:
          type: string
          description: Sum of this session's resolved items, as an exact decimal string.
        status:
          type: string
          enum: [ready, blocked, empty]
          description: >-
            The line's state, DERIVED from the generation result and never stored. `ready` when every
            item resolved; `blocked` when any item has no authored ration -- the line must not be
            packed from the resolved remainder, which would send the pen short; `empty` when the pen
            holds no projected animals, which is not a configuration gap and must not be confused
            with one.
        completed:
          type: boolean
          description: >-
            True when this SHED-SESSION has a verifier-approved packing completion
            (`feed.packing.completed`). Orthogonal to `status`: a completed line was still
            ready/blocked/empty underneath, so a client can show a "completed" badge without losing
            the packing state.
        lifecycle_status:
          type: string
          enum: [pending, pending_verification, completed]
          description: >-
            The shed-session's verification-lifecycle bucket -- the finer state `completed`
            collapses. `rework` merges into `pending`, because a bounced line is the operator's to act
            on again. Orthogonal to `status` (the ration state): a line can be blocked underneath and
            still be awaiting a verdict.
        rework_reason:
          type: string
          description: >-
            Why this line came back to the packer, present only while it is in rework (which surfaces
            above as `lifecycle_status: pending`). Two very different things put a line there and the
            status alone cannot tell them apart: a verifier rejected the video, or the afternoon feed
            correction changed how many animals the pen feeds, so the recorded video no longer proves
            the right quantity and the bag must be repacked to the new amounts.


            A correction reopens BOTH of a pen's sessions, because head count scales both rations, so
            expect this on each of the pen's lines.


            Backend-composed farm copy. Render verbatim; never substitute a client-side sentence and
            never derive one from the status.
        blocked_reasons:
          type: array
          description: The distinct gaps behind a blocked status.
          items:
            $ref: "#/components/schemas/FeedDirectionBlockedReason"
    FeedDirectionCompleteRequest:
      type: object
      additionalProperties: false
      required:
        - shed_id
        - session_no
        - target_date
        - workflow
      properties:
        park_id:
          type: string
          format: uuid
          description: >-
            The park the shed belongs to. Optional: when omitted the server resolves the tenant's
            default park, matching the read routes. Exactly one park per completion.
        shed_id:
          type: string
          format: uuid
          description: The shed whose feeding session was carried out.
        session_no:
          type: integer
          minimum: 1
          description: >-
            The feeding session that was completed. A concrete session (>= 1); session 0 ("every
            session") is a read filter, never a completion target.
        target_date:
          type: string
          format: date
          description: The feed day, as an India business-calendar date (Asia/Kolkata). A date, never an instant.
        workflow:
          type: string
          enum: [normal, experiment]
          description: >-
            The dispatch workflow completed. REQUIRED (unlike the read filter, where empty means
            "both"): a completion records ONE concrete workflow.
        proof_refs:
          type: array
          description: >-
            OPTIONAL video proof. May be empty (completed with no video). Each reference carries a
            server-minted `proof_id` from `/app/proofs/*`; the bytes live in GCS.
          items:
            $ref: "#/components/schemas/FeedDirectionProofRef"
    FeedDirectionProofRef:
      type: object
      additionalProperties: false
      required:
        - proof_id
      properties:
        proof_id:
          type: string
          description: The server-minted proof id of a completed upload.
        proof_type:
          type: string
        subject_type:
          type: string
        subject_id:
          type: string
        upload_state:
          type: string
    FeedDirectionCompleteResponse:
      type: object
      additionalProperties: false
      required:
        - completion_id
        - status
        - applied
      properties:
        completion_id:
          type: string
          format: uuid
        status:
          type: string
          enum: [completed]
        applied:
          type: boolean
          description: >-
            False on an idempotent replay or when the shed-session was already completed by an
            earlier request -- the original completion is returned and no new side effects ran.
    FeedDistributionCapturesResponse:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items:
          type: array
          description: >-
            At most one entry per proof slot -- the slot's CURRENT proof. A slot re-recorded several
            times reports only its latest upload, never one entry per take.
          items:
            $ref: "#/components/schemas/FeedDistributionCapturedSlot"
    FeedDistributionCapturedSlot:
      type: object
      additionalProperties: false
      required: [field_key, proof_ref, captured_at]
      properties:
        field_key:
          type: string
          description: The slot this proof fills.
          enum:
            - feed_distribution_feed_weight_photo
            - feed_distribution_video
            - feed_distribution_water_video
        proof_ref:
          type: string
          format: uuid
          description: >-
            The SERVER proof id, not a device-local reference -- this is what lets a phone that did
            not shoot the proof name it when submitting the completion.
        captured_at:
          type: string
          format: date-time
          description: When the upload completed.
        mime_type:
          type: string
      description: >-
        One already-recorded proof slot. Carries no media url and no uploader name by design: the
        operator's need is "this slot is done, and here is the reference I can submit with", and the
        footage itself stays a verifier surface.
    FeedDistributionCompleteRequest:
      type: object
      additionalProperties: false
      required:
        - shed_id
        - session_no
        - target_date
        - workflow
        - feed_weight_proof_ref
        - distribution_proof_ref
        - water_proof_ref
      properties:
        park_id:
          type: string
          format: uuid
          description: >-
            The park the shed belongs to. Optional: when omitted the server resolves the tenant's
            default park, matching the read routes. Exactly one park per completion.
        shed_id:
          type: string
          format: uuid
          description: The shed whose feeding session was distributed.
        partition_label:
          type: string
          description: >-
            The PEN inside the shed that was worked ("2", "Part 3"). Omit or send "" for an
            undivided shed. This is part of the completion's IDENTITY, not a label: a partitioned
            shed has one completion PER PEN, so omitting it on a partitioned shed records the work
            against the shed as a whole and one video stands as proof for every pen (the defect
            reported on STG 2026-08-08). See migration 000137.
        session_no:
          type: integer
          minimum: 1
          description: >-
            The feeding session that was distributed. A concrete session (>= 1); session 0 is a read
            filter, never a completion target.
        target_date:
          type: string
          format: date
          description: The feed day, as an India business-calendar date (Asia/Kolkata). A date, never an instant.
        workflow:
          type: string
          enum: [normal, experiment]
          description: The dispatch workflow distributed. REQUIRED -- a completion records ONE concrete workflow.
        feed_weight_proof_ref:
          type: string
          minLength: 1
          description: >-
            MANDATORY. The server-minted `proof_id` of the feed-weight PHOTO, captured BEFORE the
            feed is given out. A blank value is rejected `422 proof_required`.


            It must be a PHOTO: the server checks both the artifact's `proof_type` and its stored
            mime, and a video here is rejected `422 proof_required` naming this step. It is captured
            first because it must be taken while the feed is still on the scale -- after
            distribution there is nothing left to weigh.


            Why it exists: the distribution video proves the feed reached the animals but cannot
            prove HOW MUCH did. This is the only capture a verifier can check against the expected
            ration shown on the item.
        distribution_proof_ref:
          type: string
          minLength: 1
          description: >-
            MANDATORY. The server-minted `proof_id` of the feed-distribution VIDEO. A blank value is
            rejected `422 proof_required`. The bytes live in GCS; only the reference is recorded.
            Must be a VIDEO -- `proof_type` and mime are both checked.
        water_proof_ref:
          type: string
          minLength: 1
          description: >-
            MANDATORY. The server-minted `proof_id` of the water-distribution VIDEO. A blank value
            is rejected `422 proof_required`.


            VIDEO-ONLY since 2026-08-11 (it previously accepted a photo OR a video): a photo of a
            full trough proves a trough is full, not that this operator filled it today. A photo
            here is rejected `422 proof_required`.
    FeedDistributionCompleteResponse:
      type: object
      additionalProperties: false
      required:
        - completion_id
        - status
        - newly_pending
      properties:
        completion_id:
          type: string
          format: uuid
        status:
          type: string
          enum: [pending_verification, completed]
          description: >-
            `pending_verification` on a fresh submit or a rework re-submit (awaiting the verifier);
            `completed` when the shed-session had already been verifier-approved. Never `completed` on
            a first submit -- distribution is done only at verifier approval.
        newly_pending:
          type: boolean
          description: >-
            True when this call flipped the session into pending_verification and enqueued a
            verification item. False on an idempotent replay or an already-pending/already-completed
            no-op.
    FeedPackingCompleteRequest:
      type: object
      # additionalProperties: false is load-bearing rather than tidy. On a write that consumes an
      # operator's video, a field this server does not understand means the client disagrees with the
      # contract about what it just submitted, and the honest answer is a 400 they can see rather
      # than a 200 that silently drops half of it.
      additionalProperties: false
      required:
        - shed_id
        - session_no
        - target_date
        - workflow
        - packing_proof_ref
      properties:
        park_id:
          type: string
          format: uuid
          description: >-
            The park the shed belongs to. Optional: when omitted the server resolves the tenant's
            default park, matching the read routes. Exactly one park per completion.
        shed_id:
          type: string
          format: uuid
          description: The shed whose packing day was carried out.
        partition_label:
          type: string
          description: >-
            The PEN inside the shed that was packed ("2", "Part 3"). Omit or send "" for an
            undivided shed. Part of the completion's IDENTITY: a partitioned shed has one
            completion PER PEN, so omitting it records the work against the whole shed and one
            video stands as proof for every pen. See FeedDistributionCompleteRequest and
            migration 000137.


        session_no:
          type: integer
          minimum: 1
          description: >-
            The feeding session that was packed and filmed. REQUIRED, and part of the completion's
            IDENTITY (maintainer decision 2026-08-11, reverting the 2026-08-10 pen-day grain): a pen
            produces one bag and one video per session, so a completion that does not name its
            session cannot be recorded against the right line. `0` is rejected -- it is not "the
            whole day", it is a value no worklist line matches, and accepting it would leave the
            operator's bag still showing as owed.
        target_date:
          type: string
          format: date
          description: The feed day, as an India business-calendar date (Asia/Kolkata). A date, never an instant.
        workflow:
          type: string
          enum: [normal, experiment]
          description: The dispatch workflow packed. REQUIRED -- a completion records ONE concrete workflow.
        packing_proof_ref:
          type: string
          minLength: 1
          description: >-
            MANDATORY. The server-minted `proof_id` of the packing VIDEO. A blank value is rejected
            `422 proof_required`. The bytes live in GCS; only the reference is recorded.
    FeedPackingCompleteResponse:
      type: object
      additionalProperties: false
      required:
        - completion_id
        - status
        - newly_pending
      properties:
        completion_id:
          type: string
          format: uuid
        status:
          type: string
          enum: [pending_verification, completed]
          description: >-
            `pending_verification` on a fresh submit or a rework re-submit (awaiting the verifier);
            `completed` when the pen-day had already been verifier-approved. Never `completed` on
            a first submit -- packing is done only at verifier approval.
        newly_pending:
          type: boolean
          description: >-
            True when this call flipped the session into pending_verification and enqueued a
            verification item. False on an idempotent replay or an already-pending/already-completed
            no-op.
    FeedTransportTask:
      type: object
      additionalProperties: false
      required: [task_id, park_id, park_label, shed_id, shed_label, business_date, status, scheduled_at, partition_label, operational_location_display]
      properties:
        task_id: { type: string, format: uuid }
        park_id: { type: string, format: uuid }
        park_label: { type: string }
        shed_id: { type: string, format: uuid }
        shed_label: { type: string }
        partition_label:
          type: string
          description: >-
            Empty on every task created since the shed-grain repair. Transport is one task per
            physical shed -- the feed for all of a shed's pens leaves on one trip -- so there is no
            pen to name. Older rows created while the task was briefly per-pen keep their label so
            their recorded video still says where it was filmed.
        operational_location_display: { type: string }
        business_date: { type: string, format: date }
        status: { type: string, enum: [due, verification_due, rework, completed] }
        operator_id: { type: string, format: uuid }
        rework_reason: { type: string }
        scheduled_at: { type: string, format: date-time }
    FeedTransportTaskPage:
      type: object
      additionalProperties: false
      required: [items, filters]
      properties:
        items: { type: array, items: { $ref: "#/components/schemas/FeedTransportTask" } }
        next_cursor: { type: string }
        filters: { $ref: "#/components/schemas/FeedTransportFilterOptions" }
    FeedTransportFilterOption:
      type: object
      additionalProperties: false
      required: [id, label]
      properties:
        id:
          type: string
          description: >-
            A park UUID for a park option, a shed UUID for a shed option. Never a composite pen
            key: transport is one task per physical shed, so one shed is exactly one option.
        label: { type: string }
        partition_label:
          type: string
          description: Always empty for transport options; kept so older clients stay decodable.
    FeedTransportFilterOptions:
      type: object
      additionalProperties: false
      required: [parks, sheds]
      properties:
        parks: { type: array, items: { $ref: "#/components/schemas/FeedTransportFilterOption" } }
        sheds: { type: array, items: { $ref: "#/components/schemas/FeedTransportFilterOption" } }
    FeedTransportSubmitRequest:
      type: object
      additionalProperties: false
      required: [proof_ref]
      properties:
        proof_ref: { type: string, minLength: 1, description: Server-minted proof id for one fresh in-app camera video. }
    FeedTransportSubmitResponse:
      type: object
      additionalProperties: false
      required: [attempt_id, status, attempt_no, newly_pending]
      properties:
        attempt_id: { type: string, format: uuid }
        status: { type: string, enum: [verification_due] }
        attempt_no: { type: integer, minimum: 1 }
        newly_pending: { type: boolean }
    FeedPackingWorklistSummary:
      type: object
      additionalProperties: false
      required:
        - scope
        - shed_count
        - line_count
        - total_kg_by_feed_item
        - blocked_count
        - blocked_shed_count
        - blocked_line_count
      properties:
        scope:
          type: string
          enum: [filtered]
          description: >-
            Always `filtered`: covers every line matching the request's filters, not the returned
            page, and is invariant to `limit` and `offset`. This is the figure an operator draws
            from the store before walking the park, so a page-scoped total would send them out with
            a fraction of the load.
        shed_count:
          type: integer
          description: Distinct sheds in the whole filtered scope.
        line_count:
          type: integer
          description: >-
            PEN-DAY packing LINES in the whole filtered scope. A packer's unit of work is the bag,
            so this counts lines rather than the preview's ration grains. A line is a pen-DAY since
            2026-08-10, so on a two-session park this is half what it used to be for the same
            physical work.
        total_kg_by_feed_item:
          type: array
          description: >-
            Per-item store draw across the WHOLE filtered scope, in authored catalog order. Blocked
            cells are counted, never summed as zero.


            Counts EVERY SESSION of every pen -- the packer carries out the morning bag AND the
            evening bag, so this figure did NOT halve when the lines above did.
          items:
            $ref: "#/components/schemas/FeedDirectionFeedItemTotal"
        blocked_count:
          type: integer
          description: Blocked item cells in the whole filtered scope.
        blocked_shed_count:
          type: integer
          description: Distinct sheds carrying at least one blocked cell.
        blocked_line_count:
          type: integer
          description: Packing lines that cannot be packed as printed.
    FeedPackingWorklistPage:
      type: object
      additionalProperties: false
      required:
        - items
        - summary
        - lifecycle
        - draft
        - filters
        - target_date
        - limit
        - offset
        - has_more
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/FeedPackingRow"
        summary:
          $ref: "#/components/schemas/FeedPackingWorklistSummary"
        lifecycle:
          $ref: "#/components/schemas/FeedDirectionLifecycle"
        draft:
          type: boolean
          description: True only for the deliberate live-compute escape hatch (draft=true).
        filters:
          $ref: "#/components/schemas/FeedDirectionFilterOptions"
        target_date:
          type: string
          format: date
        limit:
          type: integer
        offset:
          type: integer
        has_more:
          type: boolean
    FeedWastageRow:
      type: object
      additionalProperties: false
      required:
        - park_id
        - park_label
        - shed_id
        - shed_label
        - operational_location_display
        - workflow
        - experiment_arm
        - head_count
        - lifecycle_status
        - completed
      properties:
        park_id:
          type: string
          format: uuid
        park_label:
          type: string
        shed_id:
          type: string
          format: uuid
        shed_label:
          type: string
        partition_label:
          type: string
          description: >-
            The pen this wastage task is for ("1", "Part 3"), absent for an undivided shed. Part of
            the row's identity: one pen's video must never close another pen's task. Clients MUST
            render shed + partition together.
        operational_location_display:
          type: string
          description: Backend-composed physical location label. Render verbatim.
        workflow:
          type: string
          enum: [experiment]
          description: Always `experiment` — wastage exists only for experiment pens.
        experiment_arm:
          type: string
          description: The pen's authored trial group. Farm copy; render verbatim.
        head_count:
          type: integer
          format: int64
          description: The pen's projected head count — context, never a gate or a quantity.
        lifecycle_status:
          type: string
          enum: [pending, pending_verification, completed]
          description: >-
            The pen-day's verification-lifecycle bucket. `rework` merges into `pending`, because a
            bounced pen is the operator's to act on again.
        completed:
          type: boolean
        rework_reason:
          type: string
          description: >-
            Why this pen came back to the operator, present only while it is in rework (surfacing
            above as `lifecycle_status: pending`). Backend-composed farm copy; render verbatim.
        wastage_kg:
          type: string
          description: >-
            The VERIFIER'S recorded leftover weight in kg, as an exact decimal string, present only
            once she has recorded one. "0" is a real measurement (an empty trough), a different
            statement from the field being absent (not yet measured).
    FeedWastageWorklistSummary:
      type: object
      additionalProperties: false
      required:
        - total_pens
        - pending_pens
        - in_review_pens
        - completed_pens
      properties:
        total_pens:
          type: integer
          description: >-
            Experiment pens owing a wastage video on this feed day, across the WHOLE filtered
            scope — invariant to `limit`/`offset`. The three buckets below are disjoint and sum to
            this figure.
        pending_pens:
          type: integer
          description: Pens with no submitted video yet, or bounced back for a re-shoot.
        in_review_pens:
          type: integer
          description: Pens with a submitted video awaiting the verifier.
        completed_pens:
          type: integer
          description: Verifier-approved pens.
    PCCareCategory:
      type: string
      description: A PC Care work category (maintainer decision 2026-08-21).
      enum: [deworming, ticks_removal, hoof_trimming, hair_trimming]
    PCCareSlot:
      type: object
      description: >-
        One expected proof slot for a task's category — the BACKEND-OWNED slot contract. The
        min_duration_hint_seconds on the trimming "during" clip is recorder guidance, never a
        client-enforced cap.
      properties:
        field_key: { type: string, enum: [video, before_video, during_video, after_video] }
        label: { type: string }
        description:
          type: string
          description: Backend-owned farm copy saying what this video must show, rendered verbatim.
        min_duration_hint_seconds: { type: integer }
      required: [field_key, label]
    PCCareTask:
      type: object
      properties:
        task_id: { type: string, format: uuid }
        category: { $ref: "#/components/schemas/PCCareCategory" }
        park_id: { type: string, format: uuid }
        park_label: { type: string }
        shed_id: { type: string, format: uuid }
        shed_label: { type: string }
        partition_label: { type: string }
        operational_location_display:
          type: string
          description: Backend-composed pen display ("Castro - 2"), rendered verbatim.
        planned_business_date: { type: string, format: date }
        due_business_date: { type: string, format: date }
        work_state: { type: string, enum: [scheduled, delayed, completed, closed, canceled] }
        status: { type: string, enum: [open, pending_verification, completed, rework] }
        rework_reason:
          type: string
          description: The verifier's rejection sentence, rendered verbatim (backend-owned copy).
        row_version: { type: integer }
        submitted_at: { type: string, format: date-time }
        assignee_user_ids:
          type: array
          items: { type: string, format: uuid }
        assignee_names:
          type: array
          items: { type: string }
        animal_count: { type: integer }
        capture_mode:
          type: string
          enum: [scan_record, roster_pick]
          description: >-
            Backend-owned capture flow for this task's category. scan_record — scanning a tag
            opens the video recorder immediately. roster_pick — the screen lists the pen's
            resident RFIDs (GET .../roster) and tapping one records that animal.
        expected_slots:
          type: array
          items: { $ref: "#/components/schemas/PCCareSlot" }
      required:
        - task_id
        - category
        - park_id
        - park_label
        - shed_id
        - shed_label
        - operational_location_display
        - planned_business_date
        - due_business_date
        - work_state
        - status
        - row_version
        - assignee_user_ids
        - assignee_names
        - animal_count
        - capture_mode
        - expected_slots
    PCCareTaskRoster:
      type: object
      description: >-
        One keyset page of the RFIDs of animals currently resident in a task's pen — the
        roster-pick capture mode's tap list. Identifiers are verbatim; the list never gates a
        scan.
      properties:
        identifiers:
          type: array
          items: { type: string }
        next_cursor: { type: string }
      required: [identifiers]
    PCCareTaskPage:
      type: object
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/PCCareTask" }
        next_cursor: { type: string }
      required: [items, next_cursor]
    PCCareAnimalSlot:
      type: object
      properties:
        field_key: { type: string }
        proof_ref: { type: string }
        captured_by: { type: string, format: uuid }
        captured_by_name: { type: string }
        captured_at: { type: string, format: date-time }
      required: [field_key]
    PCCareAnimalRow:
      type: object
      properties:
        animal_row_id: { type: string, format: uuid }
        scanned_identifier: { type: string }
        scanned_by: { type: string, format: uuid }
        scanned_by_name: { type: string }
        scanned_at: { type: string, format: date-time }
        slots:
          type: array
          items: { $ref: "#/components/schemas/PCCareAnimalSlot" }
      required: [animal_row_id, scanned_identifier, scanned_at, slots]
    PCCareCapturesResponse:
      type: object
      properties:
        animals:
          type: array
          items: { $ref: "#/components/schemas/PCCareAnimalRow" }
        next_cursor: { type: string }
      required: [animals]
    PCCarePlannerCatalog:
      type: object
      properties:
        parks:
          type: array
          items:
            type: object
            properties:
              park_id: { type: string, format: uuid }
              park_label: { type: string }
            required: [park_id, park_label]
        operators:
          type: array
          items:
            type: object
            properties:
              user_id: { type: string, format: uuid }
              display_name: { type: string }
              park_ids:
                type: array
                description: Empty means every park (a cross-park director).
                items: { type: string, format: uuid }
            required: [user_id, display_name, park_ids]
        categories:
          type: array
          items:
            type: object
            properties:
              key: { $ref: "#/components/schemas/PCCareCategory" }
              label: { type: string }
            required: [key, label]
      required: [parks, operators, categories]
    PCCarePlannerSheds:
      type: object
      properties:
        sheds:
          type: array
          items:
            type: object
            properties:
              shed_id: { type: string, format: uuid }
              shed_label: { type: string }
              partition_label: { type: string }
              operational_location_display:
                type: string
                description: Backend-composed pen display, rendered verbatim.
              existing_task_id:
                type: string
                description: Non-empty when a live task already covers this pen for the chosen category+date.
            required: [shed_id, shed_label, operational_location_display]
        next_cursor: { type: string }
      required: [sheds]
    PCCareCreateTaskRequest:
      type: object
      properties:
        category: { $ref: "#/components/schemas/PCCareCategory" }
        park_id: { type: string, format: uuid }
        shed_id: { type: string, format: uuid }
        partition_label: { type: string }
        planned_business_date: { type: string, format: date }
        assignee_user_ids:
          type: array
          minItems: 1
          items: { type: string, format: uuid }
      required: [category, park_id, shed_id, planned_business_date, assignee_user_ids]
    PCCareScanRequest:
      type: object
      properties:
        scanned_identifier:
          type: string
          description: The tag exactly as scanned. Stored verbatim; never resolved against the herd.
      required: [scanned_identifier]
    PCCareScanResponse:
      type: object
      properties:
        animal_row_id: { type: string, format: uuid }
      required: [animal_row_id]
    PCCareSlotProofRequest:
      type: object
      properties:
        proof_ref:
          type: string
          description: The server proof id from the /app/proofs pipeline (completed, in-app-camera video).
      required: [proof_ref]
    PCCareSubmitResponse:
      type: object
      properties:
        task_id: { type: string, format: uuid }
        status: { type: string, enum: [open, pending_verification, completed, rework] }
        row_version: { type: integer }
        animal_count: { type: integer }
      required: [task_id, status, row_version, animal_count]
    FeedWastageWorklistPage:
      type: object
      additionalProperties: false
      required:
        - items
        - summary
        - lifecycle
        - filters
        - target_date
        - limit
        - offset
        - has_more
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/FeedWastageRow"
        summary:
          $ref: "#/components/schemas/FeedWastageWorklistSummary"
        lifecycle:
          $ref: "#/components/schemas/FeedDirectionLifecycle"
        filters:
          $ref: "#/components/schemas/FeedDirectionFilterOptions"
        target_date:
          type: string
          format: date
        limit:
          type: integer
        offset:
          type: integer
        has_more:
          type: boolean
    FeedWastageCompleteRequest:
      type: object
      # additionalProperties: false is load-bearing on a write that consumes an operator's video —
      # a field this server does not understand is a 400 they can see, never a 200 that silently
      # drops half the submission.
      additionalProperties: false
      required:
        - shed_id
        - target_date
        - wastage_proof_ref
      properties:
        park_id:
          type: string
          format: uuid
          description: >-
            The park the shed belongs to. Optional: when omitted the server resolves the tenant's
            default park, matching the read routes. Exactly one park per completion.
        shed_id:
          type: string
          format: uuid
        partition_label:
          type: string
          description: >-
            The PEN whose leftover was filmed ("2", "Part 3"). Omit or send "" for an undivided
            shed. Part of the completion's IDENTITY: a partitioned shed has one wastage task PER
            PEN.
        target_date:
          type: string
          format: date
          description: The feed day, as an India business-calendar date (Asia/Kolkata). A date, never an instant.
        wastage_proof_ref:
          type: string
          minLength: 1
          description: >-
            MANDATORY. The server-minted `proof_id` of the wastage VIDEO. A blank value is rejected
            `422 proof_required`. The bytes live in GCS; only the reference is recorded.
    FeedWastageCompleteResponse:
      type: object
      additionalProperties: false
      required:
        - completion_id
        - status
        - newly_pending
      properties:
        completion_id:
          type: string
          format: uuid
        status:
          type: string
          enum: [pending_verification, completed]
          description: >-
            `pending_verification` on a fresh submit or a rework re-submit; `completed` when the
            pen-day had already been verifier-approved. Never `completed` on a first submit.
        newly_pending:
          type: boolean
          description: >-
            True when this call flipped the pen-day into pending_verification and enqueued a
            verification item. False on an idempotent replay or an already-pending/already-completed
            no-op.
    FeedWastageMeasurementRequest:
      type: object
      additionalProperties: false
      required:
        - wastage_kg
      properties:
        wastage_kg:
          type: number
          format: double
          minimum: 0
          maximum: 10000
          description: >-
            The leftover feed weight the verifier reads off the video, in kg. ZERO IS VALID (an
            empty trough). Clients must never coerce a blank field into 0 — blank means she has not
            typed a value, and is refused as `missing_wastage`, never sent as a number she never
            entered.
        idempotency_key:
          type: string
          description: >-
            Derived, not random, so a double-tap is ONE write. Include the value in the key —
            recording 3 kg and then 3.5 kg are two different acts. The `Idempotency-Key` header is
            the fallback when this field is absent.
    FeedWastageMeasurementResult:
      type: object
      additionalProperties: false
      required:
        - completion_id
        - wastage_kg
        - recorded_by
        - recorded_at
        - subject_label
      properties:
        completion_id:
          type: string
          format: uuid
        wastage_kg:
          type: number
          format: double
          description: The value AFTER this entry.
        previous_wastage_kg:
          type: number
          format: double
          description: >-
            What the row held immediately before this entry. Absent on a first entry — a wastage
            measurement is born on the verifier's screen, so there is no operator original to
            preserve.
        recorded_by:
          type: string
          format: uuid
        recorded_at:
          type: string
          format: date-time
        subject_label:
          type: string
          description: >-
            The recomposed verifier-facing label for this pen-day, carrying the recorded value. The
            server also pushes it onto the verification item, so the queue stops advertising the
            bare pen.
    FeedWastageMeasurementResponse:
      type: object
      additionalProperties: false
      required:
        - wastage_measurement
      properties:
        wastage_measurement:
          $ref: "#/components/schemas/FeedWastageMeasurementResult"
    FeedConfigSessionTemplatePage:
      type: object
      additionalProperties: false
      required:
        - items
        - limit
        - offset
        - has_more
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/FeedConfigSessionTemplate"
        limit:
          type: integer
        offset:
          type: integer
        has_more:
          type: boolean
    FeedConfigSchedule:
      type: object
      additionalProperties: false
      required:
        - schedule_config_id
        - park_id
        - workflow
        - direction_time
        - correction_time
        - valid_from
      properties:
        schedule_config_id:
          type: string
          format: uuid
        park_id:
          type: string
          format: uuid
        workflow:
          type: string
          enum: [normal, experiment]
          description: >-
            Which dispatch workflow this clock governs. Part of the natural key: normal and experiment
            genuinely run on different cutoffs.
        direction_time:
          type: string
          description: >-
            LOCAL Asia/Kolkata wall-clock time (HH:MM:SS) the day's packing direction is issued. No
            UTC offset -- a recurring business-calendar rule, not an instant.
          examples: ["07:00:00"]
        correction_time:
          type: string
          description: >-
            LOCAL Asia/Kolkata wall-clock time at which approved emergency-shifting corrections are
            BATCHED and the amended direction is reissued. Fixed rather than fire-on-approval, so a
            shed gets at most one amended direction per day.
          examples: ["14:00:00"]
        transport_time:
          type: string
          description: >-
            LOCAL Asia/Kolkata wall-clock deadline after which a correction can no longer reach the
            shed. OMITTED when the park has declared no cutoff -- read that as UNKNOWN, never as "no
            deadline".
          examples: ["15:45:00"]
        valid_from:
          type: string
          format: date
        valid_to:
          type: string
          format: date
          nullable: true
    FeedConfigSchedulePage:
      type: object
      additionalProperties: false
      required:
        - items
        - limit
        - offset
        - has_more
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/FeedConfigSchedule"
        limit:
          type: integer
        offset:
          type: integer
        has_more:
          type: boolean
    FeedConfigShedFactor:
      type: object
      additionalProperties: false
      required:
        - shed_factor_id
        - park_id
        - shed_id
        - feed_item
        - multiplier
        - valid_from
      properties:
        shed_factor_id:
          type: string
          format: uuid
        park_id:
          type: string
          format: uuid
        shed_id:
          type: string
          format: uuid
        feed_item:
          type: string
        multiplier:
          type: string
          description: Exact decimal string. A shed with NO row is treated as 1.0 by the read path.
        valid_from:
          type: string
          format: date
        valid_to:
          type: string
          format: date
          nullable: true
    FeedConfigShedFactorPage:
      type: object
      additionalProperties: false
      required:
        - items
        - limit
        - offset
        - has_more
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/FeedConfigShedFactor"
        limit:
          type: integer
        offset:
          type: integer
        has_more:
          type: boolean
    FeedConfigExperiment:
      type: object
      additionalProperties: false
      required:
        - experiment_config_id
        - park_id
        - park_name
        - shed_id
        - shed_name
        - operational_location_display
        - feed_item
        - absolute_kg
        - experiment_category
        - status
      properties:
        experiment_config_id:
          type: string
          format: uuid
        park_id:
          type: string
          format: uuid
        park_name:
          type: string
          description: >-
            The park this cell belongs to. Carried because the list may span BOTH parks; the shed name
            cannot stand in for it, since Castro, Gandhi and Yashoda each exist in both parks.
        shed_id:
          type: string
          format: uuid
        shed_name:
          type: string
          description: Display name of the physical shed, without the pen.
        partition_label:
          type: string
          nullable: true
          description: >-
            The HUMAN pen label ('Part 3', '2'), never the normalized matching key ('3'). Absent or
            null means an undivided shed. A partitioned shed authors ONE CELL PER PEN, so shed_id
            alone does not identify a row -- send this back on edit or the request is rejected.
        operational_location_display:
          type: string
          description: >-
            Backend-composed ground location ("Mandela 1 - Part 3", or just "Yashoda" when
            undivided). Render verbatim; never rejoin shed_name and partition_label client-side.
        feed_item:
          type: string
        absolute_kg:
          type: string
          description: >-
            Exact decimal string. A PEN TOTAL in kg, never a per-head rate -- it is already
            inclusive of however many animals are in the pen. Never multiply it by head_count.
        head_count:
          type: integer
          nullable: true
          description: >-
            INFORMATIONAL only: the population the hand-entered quantity was authored against. It is
            never a multiplier. Null means the population was not recorded, which is NOT the same as
            0 -- rendering null as 0 would state the pen is empty.
        experiment_category:
          type: string
          description: >-
            The experiment ARM (e.g. "Sheep M NEW"). It stands in for the shed tag on the direction
            sheet, because an experiment pen has no ration grain and so no authored tag to report.
        status:
          type: string
          enum: [active, retired]
          description: >-
            The workflow switch, not a visibility flag. "active" = this pen is fed the absolute kg
            authored here. "retired" = it is fed from the normal per-head ration grid instead.
    FeedConfigExperimentPage:
      type: object
      additionalProperties: false
      required:
        - items
        - limit
        - offset
        - has_more
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/FeedConfigExperiment"
        limit:
          type: integer
        offset:
          type: integer
        has_more:
          type: boolean
    FeedConfigPen:
      type: object
      additionalProperties: false
      required:
        - park_id
        - shed_id
        - shed_name
        - operational_location_display
        - has_experiment_config
      properties:
        park_id:
          type: string
          format: uuid
        shed_id:
          type: string
          format: uuid
        shed_name:
          type: string
        partition_label:
          type: string
          description: >-
            The pen's HUMAN label ('Part 3', '2'). Absent means the shed is undivided. Never the
            normalized matching key ('3') and never the 'whole' sentinel.
        operational_location_display:
          type: string
          description: >-
            Backend-composed location label ("Mandela 1 - Part 3"). Rendered verbatim; clients never
            rejoin the shed name and partition themselves.
        has_experiment_config:
          type: boolean
          description: >-
            Whether this pen already carries at least one authored experiment cell. Computed against
            the same (shed_id, partition) natural key the experiment table is unique on, so the
            enroller's candidate filter cannot disagree with what a write would land on.
    FeedConfigPenPage:
      type: object
      additionalProperties: false
      required:
        - items
        - limit
        - offset
        - has_more
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/FeedConfigPen"
        limit:
          type: integer
        offset:
          type: integer
        has_more:
          type: boolean
    UpsertFeedConfigExperimentBatchRequest:
      type: object
      additionalProperties: false
      required:
        - park_id
        - shed_id
        - experiment_category
        - items
      properties:
        park_id:
          type: string
          format: uuid
        shed_id:
          type: string
          format: uuid
        partition_label:
          type: string
          nullable: true
          description: >-
            WHICH PEN is being authored. Part of the row's identity -- omitting it on a partitioned
            shed is rejected rather than creating a phantom whole-shed row. Absent means an
            undivided shed.
        experiment_category:
          type: string
          minLength: 1
          description: >-
            The experiment ARM, carried ONCE for the pen rather than per item. Per-item copies would
            let one pen hold two arms, with the display picking whichever row sorted first.
        head_count:
          type: integer
          minimum: 0
          nullable: true
          description: >-
            INFORMATIONAL population for the pen. Never multiplied into any absolute_kg. Carried once
            for the pen. Null records "not recorded", which stays distinct from an authored 0.
        items:
          type: array
          minItems: 1
          description: >-
            The authored cells. At least one -- an empty batch would enrol the pen onto the
            experiment workflow with nothing authored, which the planner reads as "fed nothing".
          items:
            $ref: "#/components/schemas/UpsertFeedConfigExperimentBatchItem"
    UpsertFeedConfigExperimentBatchItem:
      type: object
      additionalProperties: false
      required:
        - feed_item
        - absolute_kg
      properties:
        feed_item:
          type: string
          minLength: 1
        absolute_kg:
          type: number
          minimum: 0
          multipleOf: 0.001
          description: >-
            Authored ABSOLUTE kg for the whole pen of this item. An explicit 0 is accepted (an arm
            that deliberately gets none of it); a negative or over-precise value is rejected with a
            field error. An item the author cleared must be omitted from the array entirely.
    SetFeedConfigFeedItemStatusRequest:
      type: object
      additionalProperties: false
      required:
        - feed_item_id
        - status
      properties:
        feed_item_id:
          type: string
          format: uuid
          description: >-
            The catalog row being retired or restored. Keyed on the id rather than the label so the
            write cannot become ambiguous if an item is ever renamed.
        status:
          type: string
          enum: [active, retired]
          description: >-
            "retired" removes the item from every future feed sheet and from the ration grid;
            "active" restores it along with its untouched authored rates. Required, with no default
            -- see the endpoint description.
    CreateFeedConfigFeedItemRequest:
      type: object
      additionalProperties: false
      required:
        - feed_item
      properties:
        feed_item:
          type: string
          minLength: 1
          description: >-
            The item's name, as it will appear on the ration grid, the shed factors, the experiment
            sheds and the generated feed sheet. Compared against the catalog on the same
            normalization the storage key uses, so a name differing only in case or surrounding
            whitespace is the SAME item and is rejected as a duplicate rather than added twice.
        energy_kcal_per_kg:
          type: number
          minimum: 0
          multipleOf: 0.001
          nullable: true
          description: >-
            OPTIONAL. Omit it when nobody has measured the item's energy -- it is stored as NULL,
            which is an honest gap: a missing energy value blocks a rollup, never a feeding decision.
            An explicit 0 is kept as a measured zero and is a different statement from omitting it.
        dry_matter_factor:
          type: number
          exclusiveMinimum: 0
          maximum: 1
          multipleOf: 0.0001
          nullable: true
          description: >-
            OPTIONAL fraction of the item that is dry matter, greater than 0 and at most 1. Both
            bounds are rejections, never clamps: 0 would say the item is entirely water and a value
            above 1 would say it is more than 100% dry matter.
        wastage_factor:
          type: number
          minimum: 0
          exclusiveMaximum: 1
          multipleOf: 0.0001
          nullable: true
          description: >-
            OPTIONAL expected wastage fraction, at least 0 and less than 1. An authored 0 is legal
            (no expected wastage); 1 is not, because it would say the entire quantity is lost and
            nothing reaches the animals.
        display_order:
          type: integer
          minimum: 0
          nullable: true
          description: >-
            OPTIONAL sort position within the catalog. Omitted appends the item to the END rather
            than taking the column default of 0, which would place every new item first in every
            dropdown. This is the one derived value on this write, and it is derivable only because
            it is a presentation position no feeding decision reads.
    UpsertFeedConfigExperimentRequest:
      type: object
      additionalProperties: false
      required:
        - park_id
        - shed_id
        - feed_item
        - absolute_kg
        - experiment_category
      properties:
        park_id:
          type: string
          format: uuid
        shed_id:
          type: string
          format: uuid
        partition_label:
          type: string
          nullable: true
          description: >-
            WHICH PEN of the shed is being authored -- echo back the partition_label the row was
            rendered with. Part of the row's identity: the natural key is
            (tenant, park, shed, partition, feed_item), so omitting it on a partitioned shed is
            rejected rather than targeting a different place. Absent means an undivided shed.
        feed_item:
          type: string
          minLength: 1
        absolute_kg:
          type: number
          minimum: 0
          multipleOf: 0.001
          description: >-
            Authored ABSOLUTE kg for the addressed pen (or undivided shed). REQUIRED -- it must never be omitted and filled
            in as 0. The failure mode of an absent value is quieter than on the ration grid and worse
            for it: a missing ration rate BLOCKS the shed loudly, while a missing experiment row
            silently drops the shed back onto the per-head grid and prints a complete-looking sheet
            with roughly twice the authored quantity. An explicit 0 is accepted; a negative or
            over-precise value is rejected with a field error rather than clamped. A cleared input in
            the UI must NOT be sent as 0.
        head_count:
          type: integer
          minimum: 0
          nullable: true
          description: >-
            INFORMATIONAL population count. Never multiplied into absolute_kg. Optional: null records
            "not recorded", which stays distinct from an authored 0.
        experiment_category:
          type: string
          minLength: 1
          description: >-
            The experiment arm. REQUIRED rather than defaulted because it is what the direction sheet
            prints in the shed-tag column for an experiment pen -- the operator's only cue that these
            numbers are hand-entered rather than computed.
    SetFeedConfigExperimentShedStatusRequest:
      type: object
      additionalProperties: false
      required:
        - park_id
        - shed_id
        - status
      properties:
        park_id:
          type: string
          format: uuid
        shed_id:
          type: string
          format: uuid
        partition_label:
          type: string
          description: >-
            The PEN being switched, spelled as the shed's own catalog spells it ("2", "Part 3").
            Required for a subdivided shed; omitted or blank for an undivided one, and validated
            against shed_partitions either way. Without it this switch was shed-wide while the
            screen above it was already pen-grouped, so a control captioned "Return Godel 1 - Part
            3" retired all ten Godel 1 pens and dropped nine of them back to the per-head grid.
        status:
          type: string
          enum: [active, retired]
          description: >-
            REQUIRED with no default. This field decides which planner feeds the pen, so an absent
            value cannot be filled in: "active" enrols it onto authored absolute kg and "retired"
            returns it to head count x grams per head. Both are changes to what its animals eat.
    UpsertFeedConfigRationRateRequest:
      type: object
      additionalProperties: false
      required:
        - park_id
        - ration_group
        - shed_tag
        - feed_item
        - grams_per_head
      properties:
        park_id:
          type: string
          format: uuid
        ration_group:
          type: string
          minLength: 1
        shed_tag:
          type: string
          minLength: 1
        feed_item:
          type: string
          minLength: 1
        grams_per_head:
          type: number
          minimum: 0
          multipleOf: 0.001
          description: >-
            Authored grams per head per day. REQUIRED -- it must never be omitted and filled in as 0,
            because absence of a rate means "not configured" (blocking) while 0 means "feed nothing"
            (correct for milk-fed kids). An explicit 0 is accepted; a negative or over-precise value
            is rejected with a field error rather than clamped or rounded. A cleared input in the UI
            must NOT be sent as 0.
    UpsertFeedConfigShedFactorRequest:
      type: object
      additionalProperties: false
      required:
        - park_id
        - shed_id
        - feed_item
        - multiplier
      properties:
        park_id:
          type: string
          format: uuid
        shed_id:
          type: string
          format: uuid
        feed_item:
          type: string
          minLength: 1
        multiplier:
          type: number
          minimum: 0
          multipleOf: 0.0001
          description: >-
            Authored multiplier. REQUIRED: an absent row reads as the safe 1.0, so omitting the field
            and having the server invent one would author a value nobody entered. An explicit 0 is
            accepted; a negative value is rejected.
    UpsertFeedConfigScheduleRequest:
      type: object
      additionalProperties: false
      required:
        - park_id
        - workflow
        - direction_time
        - correction_time
      properties:
        park_id:
          type: string
          format: uuid
        workflow:
          type: string
          enum: [normal, experiment]
        direction_time:
          type: string
          description: LOCAL Asia/Kolkata wall-clock time, "HH:MM" or "HH:MM:SS". No UTC offset.
          examples: ["07:00"]
        correction_time:
          type: string
          description: >-
            LOCAL Asia/Kolkata wall-clock time corrections are batched and reissued at. May EQUAL
            direction_time (the live experiment case) but may not precede it.
          examples: ["14:00"]
        transport_time:
          type: string
          nullable: true
          description: >-
            LOCAL Asia/Kolkata transport cutoff. May be sent as null to record "no declared cutoff",
            but a present-but-malformed value is REJECTED rather than dropped to null -- turning a
            typo into null would tell the dispatcher this park has no transport deadline at all. May
            not precede correction_time.
          examples: ["15:45"]
    FeedConfigWriteResult:
      type: object
      additionalProperties: false
      required:
        - write_id
        - kind
        - outcome
        - effective_from
        - idempotent_replay
      properties:
        write_id:
          type: string
          format: uuid
          description: The ledger entry recording this edit.
        kind:
          type: string
          enum: [ration_rate, shed_factor, schedule_config, experiment_config]
        outcome:
          type: string
          enum: [inserted, superseded, corrected, unchanged]
          description: >-
            What the edit actually did. "inserted" = first authored value for the key. "superseded" =
            an earlier day's row was CLOSED and a new one opened, preserving history. "corrected" = a
            row authored on the SAME business day was corrected in place, because a window closed on
            the day it opened cannot satisfy valid_to > valid_from. "unchanged" = the value already
            matched and no row was written. An experiment_config write never returns "superseded":
            feed_experiment_config is not effective-dated, so an existing row is always corrected in
            place.
        result_row_id:
          type: string
          format: uuid
          description: The configuration row now in force. Absent only for an "unchanged" write.
        superseded_row_id:
          type: string
          format: uuid
          description: The row this edit closed. Present only for "superseded".
        effective_from:
          type: string
          format: date
          description: >-
            The Asia/Kolkata business date the edit takes effect on. Server-derived from the India
            business calendar, never from the client and never from the database clock.
        idempotent_replay:
          type: boolean
          description: >-
            True when this response is the ORIGINAL result of an earlier identical request, returned
            without re-running any side effect.
    ValidationIssue:
      type: object
      additionalProperties: false
      required:
        - field
        - code
        - message
      properties:
        field:
          type: string
        code:
          type: string
        message:
          type: string
    ValidationReport:
      type: object
      additionalProperties: false
      required:
        - valid
        - errors
        - warnings
      properties:
        valid:
          type: boolean
        errors:
          type: array
          items:
            $ref: "#/components/schemas/ValidationIssue"
        warnings:
          type: array
          items:
            $ref: "#/components/schemas/ValidationIssue"
    SOPVersion:
      type: object
      additionalProperties: false
      required:
        - sop_version_id
        - tenant_id
        - sop_id
        - sop_code
        - version
        - version_label
        - status
        - form_dsl
        - proof_policy
        - compatibility
        - validation_report
        - row_version
        - created_at
        - updated_at
      properties:
        sop_version_id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        sop_id:
          type: string
          format: uuid
        sop_code:
          type: string
        version:
          type: integer
          minimum: 1
        version_label:
          type: string
        status:
          type: string
          enum:
            - draft
            - published
            - retired
        form_dsl:
          type: object
          additionalProperties: true
        proof_policy:
          type: object
          additionalProperties: true
        compatibility:
          type: object
          additionalProperties: true
        validation_report:
          $ref: "#/components/schemas/ValidationReport"
        published_at:
          type:
            - string
            - "null"
          format: date-time
        retired_at:
          type:
            - string
            - "null"
          format: date-time
        row_version:
          type: integer
          minimum: 1
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    TaskPresentation:
      type: object
      additionalProperties: false
      required:
        - eyebrow
        - title
        - summary_items
      properties:
        eyebrow:
          type: string
          description: Locale-aware task category label for app display.
        title:
          type: string
          description: Locale-aware task title safe for user-visible app display; never an internal id.
        summary_items:
          type: array
          items:
            $ref: "#/components/schemas/TaskPresentationItem"
    TaskPresentationItem:
      type: object
      additionalProperties: false
      required:
        - key
        - label
        - value
      properties:
        key:
          type: string
        label:
          type: string
        value:
          type: string
    SOPVersionResponse:
      type: object
      additionalProperties: false
      required:
        - version
        - trace_id
      properties:
        version:
          $ref: "#/components/schemas/SOPVersion"
        trace_id:
          type: string
    TaskSummary:
      type: object
      additionalProperties: false
      required:
        - task_id
        - tenant_id
        - sop_id
        - sop_version_id
        - sop_code
        - task_type
        - title
        - description
        - state
        - scope_type
        - scope_id
        - priority
        - context
        - row_version
        - created_at
        - updated_at
      properties:
        task_id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        sop_id:
          type: string
          format: uuid
        sop_version_id:
          type: string
          format: uuid
        sop_code:
          type: string
        task_type:
          type: string
        title:
          type: string
        description:
          type: string
        state:
          type: string
          enum:
            - queued
            - assigned
            - in_progress
            - submitted
            - accepted
            - needs_review
            - rework_requested
            - rejected
            - canceled
        assigned_to:
          type:
            - string
            - "null"
          format: uuid
        scope_type:
          type: string
        scope_id:
          type: string
          format: uuid
        priority:
          type: string
        due_at:
          type:
            - string
            - "null"
          format: date-time
        context:
          type: object
          additionalProperties: true
        row_version:
          type: integer
          minimum: 1
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        presentation:
          $ref: "#/components/schemas/TaskPresentation"
    TaskListResponse:
      type: object
      additionalProperties: false
      required:
        - items
        - total
        - has_more
        - next_cursor
        - trace_id
      properties:
        items:
          type: array
          maxItems: 20
          items:
            $ref: "#/components/schemas/TaskSummary"
        total:
          type: integer
          format: int64
          minimum: 0
          description: Total authorized tasks matching the filters, independent of this page.
        has_more:
          type: boolean
          description: True exactly when another keyset page is available.
        next_cursor:
          type:
            - string
            - "null"
          maxLength: 512
        trace_id:
          type: string
    ProofReference:
      type: object
      additionalProperties: false
      required:
        - proof_id
        - proof_type
        - subject_type
        - upload_state
        - metadata
      properties:
        proof_id:
          type: string
        proof_type:
          type: string
          enum:
            - photo
            - video
            - attachment
        subject_type:
          type: string
          enum:
            - batch
            - goat
            - shed
            - task
            - vial_lot
            - administration
            - other
        subject_id:
          type:
            - string
            - "null"
        upload_state:
          type: string
          enum:
            - pending
            - uploading
            - completed
            - failed
        metadata:
          type: object
          additionalProperties: true
    CreateProofUploadRequest:
      type: object
      additionalProperties: false
      required:
        - proof_type
        - mime_type
        - scope_type
        - scope_id
        - subject_type
      properties:
        proof_type:
          type: string
          enum:
            - photo
            - video
            - attachment
        mime_type:
          type: string
        scope_type:
          type: string
          enum:
            - tenant
            - farm
            - park
            - shed
            - cohort
            - batch
            - task
            - goat
        scope_id:
          type: string
          format: uuid
        subject_type:
          type: string
          enum:
            - batch
            - goat
            - shed
            - task
            - vial_lot
            - administration
            - other
        subject_id:
          type:
            - string
            - "null"
          format: uuid
        metadata:
          type: object
          additionalProperties: true
    CompleteProofUploadRequest:
      type: object
      additionalProperties: false
      properties:
        content_hash:
          type: string
        mime_type:
          type: string
        size_bytes:
          type: integer
          format: int64
          minimum: 0
        duration_ms:
          type:
            - integer
            - "null"
          format: int64
          minimum: 0
        metadata:
          type: object
          additionalProperties: true
    ProofArtifact:
      type: object
      additionalProperties: false
      required:
        - proof_id
        - storage_provider
        - proof_type
        - subject_type
        - upload_state
        - mime_type
        - size_bytes
        - content_hash
        - metadata
        - created_at
      properties:
        proof_id:
          type: string
          format: uuid
        storage_provider:
          type: string
          enum:
            - local
            - gcs
        proof_type:
          type: string
          enum:
            - photo
            - video
            - attachment
        subject_type:
          type: string
        subject_id:
          type:
            - string
            - "null"
          format: uuid
        upload_state:
          type: string
          enum:
            - pending
            - uploading
            - completed
            - failed
        mime_type:
          type: string
        size_bytes:
          type: integer
          format: int64
        duration_ms:
          type:
            - integer
            - "null"
          format: int64
        content_hash:
          type: string
        metadata:
          type: object
          additionalProperties: true
        created_at:
          type: string
          format: date-time
        uploaded_at:
          type:
            - string
            - "null"
          format: date-time
    CreateProofUploadResponse:
      type: object
      additionalProperties: false
      required:
        - proof
        - upload_url
        - upload_method
        - headers
        - expires_at
        - upload_protocol
      properties:
        proof:
          $ref: "#/components/schemas/ProofArtifact"
        upload_url:
          type: string
        upload_method:
          type: string
          enum:
            - PUT
            - POST
        headers:
          type: object
          additionalProperties:
            type: string
        expires_at:
          type: string
          format: date-time
        upload_protocol:
          type: string
          enum:
            - simple_put
            - gcs_resumable_v1
        chunk_size_bytes:
          type: integer
          format: int64
          minimum: 262144
    ProofResponse:
      type: object
      additionalProperties: false
      required:
        - proof
      properties:
        proof:
          $ref: "#/components/schemas/ProofArtifact"
    DownloadProofResponse:
      type: object
      additionalProperties: false
      required:
        - download_url
      properties:
        download_url:
          type: string
    SubmissionItem:
      type: object
      additionalProperties: false
      required:
        - item_id
        - item_key
        - state
        - result
      properties:
        item_id:
          type: string
          format: uuid
        goat_id:
          type:
            - string
            - "null"
          format: uuid
        item_key:
          type: string
        state:
          type: string
          enum:
            - accepted
            - needs_review
            - rejected
            - skipped
        result:
          type: object
          additionalProperties: true
    SubmissionSummary:
      type: object
      additionalProperties: false
      required:
        - submission_id
        - task_id
        - sop_version_id
        - submitted_by
        - idempotency_key
        - answers
        - proof_refs
        - state
        - validation_report
        - items
        - submitted_at
        - row_version
      properties:
        submission_id:
          type: string
          format: uuid
        task_id:
          type: string
          format: uuid
        sop_version_id:
          type: string
          format: uuid
        submitted_by:
          type: string
          format: uuid
        idempotency_key:
          type: string
        answers:
          type: object
          additionalProperties: true
        proof_refs:
          type: array
          items:
            $ref: "#/components/schemas/ProofReference"
        state:
          type: string
          enum:
            - submitted
            - accepted
            - needs_review
            - rejected
            - voided
        validation_report:
          $ref: "#/components/schemas/ValidationReport"
        items:
          type: array
          items:
            $ref: "#/components/schemas/SubmissionItem"
        submitted_at:
          type: string
          format: date-time
        accepted_at:
          type:
            - string
            - "null"
          format: date-time
        row_version:
          type: integer
          minimum: 1
    TaskResponse:
      type: object
      additionalProperties: false
      required:
        - task
        - trace_id
      properties:
        task:
          $ref: "#/components/schemas/TaskSummary"
        sop_version:
          oneOf:
            - $ref: "#/components/schemas/SOPVersion"
            - type: "null"
        submissions:
          type: array
          items:
            $ref: "#/components/schemas/SubmissionSummary"
        trace_id:
          type: string
    ShedCompletionVaccineBreakdownItem:
      type: object
      additionalProperties: false
      required:
        - vaccine
        - count
      properties:
        vaccine:
          type: string
          description: Human display name of the vaccine, never a raw id/code.
        count:
          type: integer
    ShedCompletionSummary:
      type: object
      additionalProperties: false
      description: >-
        FROZEN read-only contract for the vaccination shed-completion / submit screen. Shed
        completion is an acknowledgement, not a manual medical form: the operator already scanned
        every animal. Proof grain is SOP-controlled: either per-goat video or shed-level video.
        shed_name / drive_name / vaccine names are always human, never raw UUIDs.
      required:
        - task_id
        - shed_name
        - drive_name
        - expected_count
        - handled_count
        - proof_ready_count
        - proof_mode
        - vaccine_breakdown
        - submit_enabled
        - blocking_reason
        - submit_state
        - round_submitted
        - round_id
      properties:
        task_id:
          type: string
          format: uuid
        shed_name:
          type: string
        partition_label:
          type:
            - string
            - "null"
        source_shed_name:
          type:
            - string
            - "null"
          description: >-
            Original partition-bearing source name (e.g. "Castro 1"), kept for
            traceability only. Not a display field.
        operational_location_display:
          type: string
          description: >-
            User-facing location label. No partition -> bare shed name
            ("Yashoda"); numeric convention -> "Castro 2"; prefixed convention
            -> "Godel 1 - Part 3".
        drive_name:
          type: string
        expected_count:
          type: integer
        handled_count:
          type: integer
        proof_ready_count:
          type: integer
        proof_mode:
          type: string
          enum:
            - per_goat_video
            - shed_level_video
        vaccine_breakdown:
          type: array
          items:
            $ref: "#/components/schemas/ShedCompletionVaccineBreakdownItem"
        submit_enabled:
          type: boolean
        blocking_reason:
          oneOf:
            - type: string
            - type: "null"
        submit_state:
          type: string
          enum: [draft, submitted, verified, closed]
        round_submitted:
          type: boolean
          description: >-
            True only when a live or accepted submission trail exists for THIS shed's CURRENT
            round of eligible obligations. False whenever the shed has open, unsubmitted
            obligations for this round -- including immediately after a verifier rejection
            reopens an obligation, even if submit_state still reads a stale terminal word from
            an earlier round.
        round_id:
          type: string
          description: >-
            Deterministic fingerprint of this shed's current obligation-round state. Changes
            value whenever an obligation in this shed submits or is reopened by a verifier
            rejection. Opaque -- not a UUID, not stable across schema changes -- for round-
            identity comparisons only, not for display.
    RetryReviewFanoutsRequest:
      type: object
      additionalProperties: false
      properties:
        limit:
          type: integer
          minimum: 1
          maximum: 100
          default: 50
    RetryReviewFanoutsResponse:
      type: object
      additionalProperties: false
      required:
        - applied
        - trace_id
      properties:
        applied:
          type: integer
          minimum: 0
        trace_id:
          type: string
    SubmitTaskRequest:
      type: object
      additionalProperties: false
      required:
        - sop_version_id
        - idempotency_key
        - answers
        - proof_refs
      properties:
        sop_version_id:
          type: string
          format: uuid
        idempotency_key:
          type: string
          minLength: 8
          maxLength: 200
        partition_label:
          type: string
          description: Raw partition label for a partition-scoped shed submission. Omit for unpartitioned sheds.
        answers:
          type: object
          additionalProperties: true
        proof_refs:
          type: array
          items:
            $ref: "#/components/schemas/ProofReference"
    SubmissionResponse:
      type: object
      additionalProperties: false
      required:
        - submission
        - task
        - trace_id
      properties:
        submission:
          $ref: "#/components/schemas/SubmissionSummary"
        task:
          $ref: "#/components/schemas/TaskSummary"
        trace_id:
          type: string
    ErrorEnvelope:
      type: object
      additionalProperties: false
      required:
        - code
        - message
        - field_errors
        - trace_id
        - retryable
      properties:
        code:
          type: string
          minLength: 1
          maxLength: 120
        message:
          type: string
          minLength: 1
          maxLength: 500
        field_errors:
          type: array
          items:
            $ref: "#/components/schemas/FieldError"
        trace_id:
          type: string
          minLength: 1
          maxLength: 200
        retryable:
          type: boolean
    FieldError:
      type: object
      additionalProperties: false
      required:
        - field
        - code
        - message
      properties:
        field:
          type: string
          minLength: 1
          maxLength: 200
        code:
          type: string
          minLength: 1
          maxLength: 120
        message:
          type: string
          minLength: 1
          maxLength: 500
    OperatorProfile:
      type: object
      additionalProperties: false
      required:
        - operator_id
        - user_id
        - display_code
        - display_name
        - status
        - primary_role_hint
        - primary_location_id
        - primary_location
        - grant_count
        - capability_count
        - active_device_count
        - metadata
        - row_version
        - created_at
        - updated_at
      properties:
        operator_id:
          type: string
          format: uuid
        user_id:
          type:
            - string
            - "null"
          format: uuid
        display_code:
          type: string
          minLength: 1
          maxLength: 80
        display_name:
          type: string
          minLength: 1
          maxLength: 160
        status:
          type: string
          enum:
            - candidate
            - active
            - inactive
            - suspended
            - left
        primary_role_hint:
          type: string
          enum:
            - operator
            - park_head
            - verifier
            - supervisor
            - admin
            - other
        primary_location_id:
          type:
            - string
            - "null"
          format: uuid
        primary_location:
          type:
            - string
            - "null"
          maxLength: 200
        grant_count:
          type: integer
          minimum: 0
        capability_count:
          type: integer
          minimum: 0
        active_device_count:
          type: integer
          minimum: 0
        metadata:
          type: object
          additionalProperties: true
        row_version:
          type: integer
          minimum: 1
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    GrantSummary:
      type: object
      additionalProperties: false
      required:
        - grant_id
        - user_id
        - role
        - scope_type
        - scope_id
        - status
        - valid_from
        - valid_to
        - created_at
        - created_by
      properties:
        grant_id:
          type: string
          format: uuid
        user_id:
          type: string
          format: uuid
        role:
          type: string
          enum:
            - admin
            - park_head
            - operator
            - verifier
            - ceo_internal
        scope_type:
          type: string
          enum:
            - tenant
            - custodian_party
            - farm
            - park
            - shed
            - cohort
        scope_id:
          type: string
          format: uuid
        status:
          type: string
          enum:
            - active
            - inactive
            - revoked
        valid_from:
          type: string
          format: date-time
        valid_to:
          type:
            - string
            - "null"
          format: date-time
        created_at:
          type: string
          format: date-time
        created_by:
          type:
            - string
            - "null"
          format: uuid
    CapabilityAssignment:
      type: object
      additionalProperties: false
      required:
        - member_capability_id
        - capability_id
        - capability_code
        - description
        - scope_type
        - scope_id
        - status
        - valid_from
        - valid_to
        - created_at
      properties:
        member_capability_id:
          type: string
          format: uuid
        capability_id:
          type: string
          format: uuid
        capability_code:
          type: string
          minLength: 1
          maxLength: 120
        description:
          type: string
          maxLength: 500
        scope_type:
          type: string
          enum:
            - tenant
            - custodian_party
            - farm
            - park
            - shed
            - cohort
        scope_id:
          type: string
          format: uuid
        status:
          type: string
          enum:
            - active
            - inactive
            - revoked
        valid_from:
          type: string
          format: date-time
        valid_to:
          type:
            - string
            - "null"
          format: date-time
        created_at:
          type: string
          format: date-time
    DeviceSummary:
      type: object
      additionalProperties: false
      required:
        - device_id
        - operator_id
        - platform
        - app_install_id
        - device_public_key_hash
        - push_token_hash
        - app_version
        - os_version
        - status
        - last_seen_at
        - registered_at
        - revoked_at
        - metadata
        - row_version
      properties:
        device_id:
          type: string
          format: uuid
        operator_id:
          type: string
          format: uuid
        platform:
          type: string
          const: android
        app_install_id:
          type: string
          minLength: 1
          maxLength: 200
        device_public_key_hash:
          type:
            - string
            - "null"
          maxLength: 200
        push_token_hash:
          type:
            - string
            - "null"
          maxLength: 200
        notifications_enabled:
          type: boolean
          description: >-
            Last phone-reported state of the OS notification switch. Present only
            once the phone has reported it; false means push-muted (the device is
            not addressed and a dropped push is not counted as delivered).
        app_version:
          type: string
          minLength: 1
          maxLength: 80
        os_version:
          type: string
          maxLength: 80
        status:
          type: string
          enum:
            - active
            - revoked
            - lost
            - retired
        last_seen_at:
          type: string
          format: date-time
        registered_at:
          type: string
          format: date-time
        revoked_at:
          type:
            - string
            - "null"
          format: date-time
        metadata:
          type: object
          additionalProperties: true
        row_version:
          type: integer
          minimum: 1
    AppMeResponse:
      type: object
      additionalProperties: false
      required:
        - actor_id
        - operator_profile
        - grants
        - trace_id
      properties:
        actor_id:
          type: string
          format: uuid
        operator_profile:
          oneOf:
            - $ref: "#/components/schemas/OperatorProfile"
            - type: "null"
        grants:
          type: array
          items:
            $ref: "#/components/schemas/GrantSummary"
        trace_id:
          type: string
          minLength: 1
          maxLength: 200
    RegisterDeviceRequest:
      type: object
      additionalProperties: false
      required:
        - app_install_id
        - app_version
      properties:
        app_install_id:
          type: string
          minLength: 1
          maxLength: 200
        device_public_key_hash:
          type:
            - string
            - "null"
          maxLength: 200
        push_token_hash:
          type:
            - string
            - "null"
          maxLength: 200
        fcm_token:
          type:
            - string
            - "null"
          maxLength: 4096
          description: >-
            Raw FCM registration token used as the push delivery address
            (message.token). push_token_hash stays the identity/dedup hash.
        notifications_enabled:
          type:
            - boolean
            - "null"
          description: >-
            Whether this phone will actually show what is sent to it (the OS
            notification switch). false marks the device push-muted: FCM would
            accept the send and the OS would drop it, so the device is not
            addressed and the drop is never counted as a delivery. Omitted or
            null by an app build that predates this field, which stays reachable.
        app_version:
          type: string
          minLength: 1
          maxLength: 80
        os_version:
          type: string
          maxLength: 80
        metadata:
          type: object
          additionalProperties: true
    HeartbeatDeviceRequest:
      type: object
      additionalProperties: false
      properties:
        app_version:
          type: string
          maxLength: 80
        os_version:
          type: string
          maxLength: 80
        push_token_hash:
          type:
            - string
            - "null"
          maxLength: 200
        fcm_token:
          type:
            - string
            - "null"
          maxLength: 4096
          description: >-
            Raw FCM registration token used as the push delivery address
            (message.token). push_token_hash stays the identity/dedup hash.
        notifications_enabled:
          type:
            - boolean
            - "null"
          description: >-
            Whether this phone will actually show what is sent to it (the OS
            notification switch). false marks the device push-muted: FCM would
            accept the send and the OS would drop it, so the device is not
            addressed and the drop is never counted as a delivery. Omitted or
            null by an app build that predates this field, which stays reachable.
        metadata:
          type: object
          additionalProperties: true
    DeviceResponse:
      type: object
      additionalProperties: false
      required:
        - device
        - trace_id
      properties:
        device:
          $ref: "#/components/schemas/DeviceSummary"
        trace_id:
          type: string
          minLength: 1
          maxLength: 200
    BootstrapResponse:
      type: object
      additionalProperties: false
      required:
        - actor
        - operator_profile
        - roles_and_scopes
        - capabilities
        - device_state
        - app_min_supported_version
        - feature_flags
        - visible_navigation
        - nav_chrome
        - task_queue_descriptors
        - pinned_sop_versions
        - supported_field_types
        - supported_rule_operators
        - supported_proof_actions
        - option_source_descriptors
        - sync_policy
        - server_time
        - trace_id
      properties:
        actor:
          $ref: "#/components/schemas/BootstrapActor"
        operator_profile:
          $ref: "#/components/schemas/OperatorProfile"
        roles_and_scopes:
          type: array
          items:
            $ref: "#/components/schemas/GrantSummary"
        capabilities:
          type: array
          items:
            $ref: "#/components/schemas/CapabilityAssignment"
        device_state:
          $ref: "#/components/schemas/BootstrapDeviceState"
        app_min_supported_version:
          type: string
          minLength: 1
          maxLength: 80
        feature_flags:
          type: object
          additionalProperties:
            type: boolean
        visible_navigation:
          type: array
          items:
            $ref: "#/components/schemas/BootstrapNavigationItem"
        nav_chrome:
          $ref: "#/components/schemas/NavChrome"
        task_queue_descriptors:
          type: array
          items:
            $ref: "#/components/schemas/BootstrapTaskQueue"
        pinned_sop_versions:
          type: array
          items:
            $ref: "#/components/schemas/BootstrapSOPVersion"
        supported_field_types:
          type: array
          items:
            type: string
        supported_rule_operators:
          type: array
          items:
            type: string
        supported_proof_actions:
          type: array
          items:
            type: string
        option_source_descriptors:
          type: array
          items:
            $ref: "#/components/schemas/BootstrapOptionSource"
        sync_policy:
          $ref: "#/components/schemas/BootstrapSyncPolicy"
        server_time:
          type: string
          format: date-time
        trace_id:
          type: string
          minLength: 1
          maxLength: 200
    AdminWebBootstrapResponse:
      type: object
      additionalProperties: false
      required:
        - source
        - schema_version
        - contract_revision
        - family_hashes
        - cache_policy
        - navigation
        - nav_chrome
        - route_labels
        - top_bar
        - role_lenses
        - pages
        - copy
        - display_rules
      properties:
        source:
          type: string
          const: api
        schema_version:
          type: string
        contract_revision:
          type: string
          minLength: 1
        family_hashes:
          type: object
          additionalProperties:
            type: string
            minLength: 1
        cache_policy:
          $ref: "#/components/schemas/AdminWebContractCachePolicy"
        navigation:
          $ref: "#/components/schemas/AdminWebNavigationContract"
        nav_chrome:
          $ref: "#/components/schemas/NavChrome"
        route_labels:
          type: array
          items:
            $ref: "#/components/schemas/AdminWebRouteLabelRule"
        top_bar:
          $ref: "#/components/schemas/AdminWebTopBarContract"
        role_lenses:
          type: array
          items:
            $ref: "#/components/schemas/AdminWebRoleLens"
        pages:
          type: array
          items:
            $ref: "#/components/schemas/AdminWebPageContract"
        copy:
          type: object
          additionalProperties:
            type: string
        display_rules:
          type: array
          items:
            $ref: "#/components/schemas/AdminWebDisplayRule"
    AdminWebContractCachePolicy:
      type: object
      additionalProperties: false
      required: [etag, in_process_ttl_sec, redis_ttl_hint_sec, revision_source]
      properties:
        etag:
          type: string
          minLength: 1
        in_process_ttl_sec:
          type: integer
          minimum: 0
        redis_ttl_hint_sec:
          type: integer
          minimum: 0
        revision_source:
          type: string
          minLength: 1
    AdminWebNavigationContract:
      type: object
      additionalProperties: false
      required: [primary, groups, footer]
      properties:
        primary:
          type: array
          items:
            $ref: "#/components/schemas/AdminWebNavigationItem"
        groups:
          type: array
          items:
            $ref: "#/components/schemas/AdminWebNavigationGroup"
        footer:
          type: string
    AdminWebNavigationItem:
      type: object
      additionalProperties: false
      required: [id, label, href, icon, badge_key, enabled, disabled_reason, domain, extra]
      properties:
        id:
          type: string
        label:
          type: string
        href:
          type: string
        icon:
          type: string
        badge_key:
          type: string
        enabled:
          type: boolean
        disabled_reason:
          type: string
        domain:
          type: string
        extra:
          type: object
          additionalProperties:
            type: string
    AdminWebNavigationGroup:
      type: object
      additionalProperties: false
      required: [id, label, icon, default_open, badge_key, leaves]
      properties:
        id:
          type: string
        label:
          type: string
        icon:
          type: string
        default_open:
          type: boolean
        badge_key:
          type: string
        leaves:
          type: array
          items:
            $ref: "#/components/schemas/AdminWebNavigationItem"
    AdminWebRouteLabelRule:
      type: object
      additionalProperties: false
      required: [pattern, label, match]
      properties:
        pattern:
          type: string
        label:
          type: string
        match:
          type: string
          enum: [exact, prefix, pattern]
    AdminWebTopBarContract:
      type: object
      additionalProperties: false
      required: [product_name, logo_text, scope_mode_toggle, park_selector, date_range_selector, notifications, role_preview]
      properties:
        product_name:
          type: string
        logo_text:
          type: string
        scope_mode_toggle:
          type: array
          items:
            $ref: "#/components/schemas/AdminWebTopBarOption"
        park_selector:
          $ref: "#/components/schemas/AdminWebTopBarControl"
        date_range_selector:
          $ref: "#/components/schemas/AdminWebTopBarControl"
        notifications:
          $ref: "#/components/schemas/AdminWebTopBarControl"
        role_preview:
          $ref: "#/components/schemas/AdminWebRolePreviewActor"
    AdminWebTopBarOption:
      type: object
      additionalProperties: false
      required: [key, label, title, enabled, disabled_reason]
      properties:
        key:
          type: string
        label:
          type: string
        title:
          type: string
        enabled:
          type: boolean
        disabled_reason:
          type: string
    AdminWebTopBarControl:
      type: object
      additionalProperties: false
      required: [label, enabled, disabled_reason, hint, options]
      properties:
        label:
          type: string
        enabled:
          type: boolean
        disabled_reason:
          type: string
        hint:
          type: string
        options:
          type: array
          items:
            $ref: "#/components/schemas/AdminWebTopBarOption"
    AdminWebRolePreviewActor:
      type: object
      additionalProperties: false
      required: [display_name, initials, subtitle]
      properties:
        display_name:
          type: string
        initials:
          type: string
        subtitle:
          type: string
    AdminWebRoleLens:
      type: object
      additionalProperties: false
      required: [id, name, audit_short, scope, description, full_access]
      properties:
        id:
          type: string
        name:
          type: string
        audit_short:
          type: string
        scope:
          type: string
        description:
          type: string
        full_access:
          type: boolean
    AdminWebPageContract:
      type: object
      additionalProperties: false
      required:
        - route_id
        - href
        - path_pattern
        - title
        - subtitle
        - surface_kind
        - source_scope
        - sections
        - tables
        - drawers
        - controls
        - copy
        - option_groups
        - migration_status
        - validation_notes
      properties:
        route_id:
          type: string
        href:
          type: string
        path_pattern:
          type: string
        title:
          type: string
        subtitle:
          type: string
        surface_kind:
          type: string
        source_scope:
          type: array
          items:
            type: string
        sections:
          type: array
          items:
            $ref: "#/components/schemas/AdminWebSectionContract"
        tables:
          type: array
          items:
            $ref: "#/components/schemas/AdminWebTableContract"
        drawers:
          type: array
          items:
            $ref: "#/components/schemas/AdminWebDrawerContract"
        controls:
          type: array
          items:
            $ref: "#/components/schemas/AdminWebControl"
        copy:
          type: object
          additionalProperties:
            type: string
        option_groups:
          type: array
          items:
            $ref: "#/components/schemas/AdminWebOptionGroup"
        migration_status:
          type: string
        validation_notes:
          type: array
          items:
            type: string
    AdminWebSectionContract:
      type: object
      additionalProperties: false
      required: [id, title, kind, chip_keys]
      properties:
        id:
          type: string
        title:
          type: string
        kind:
          type: string
        chip_keys:
          type: array
          items:
            type: string
    AdminWebTableContract:
      type: object
      additionalProperties: false
      required:
        - id
        - title
        - data_source
        - columns
        - filters
        - sort_keys
        - page_size_options
        - row_click
        - summary_fields
        - detail_fields
      properties:
        id:
          type: string
        title:
          type: string
        data_source:
          type: string
        columns:
          type: array
          items:
            $ref: "#/components/schemas/AdminWebColumn"
        filters:
          type: array
          items:
            $ref: "#/components/schemas/AdminWebFilter"
        sort_keys:
          type: array
          items:
            $ref: "#/components/schemas/AdminWebSortKey"
        page_size_options:
          type: array
          items:
            type: integer
        row_click:
          $ref: "#/components/schemas/AdminWebRowClickRule"
        summary_fields:
          type: array
          items:
            type: string
        detail_fields:
          type: array
          items:
            type: string
    AdminWebColumn:
      type: object
      additionalProperties: false
      required: [key, label, sortable, visible]
      properties:
        key:
          type: string
        label:
          type: string
        sortable:
          type: boolean
        visible:
          type: boolean
    AdminWebFilter:
      type: object
      additionalProperties: false
      required: [key, label, kind, enabled, disabled_reason]
      properties:
        key:
          type: string
        label:
          type: string
        kind:
          type: string
        enabled:
          type: boolean
        disabled_reason:
          type: string
    AdminWebSortKey:
      type: object
      additionalProperties: false
      required: [key, label, direction]
      properties:
        key:
          type: string
        label:
          type: string
        direction:
          type: string
    AdminWebRowClickRule:
      type: object
      additionalProperties: false
      required: [enabled, param, target_drawer, summary_fields, detail_fields]
      properties:
        enabled:
          type: boolean
        param:
          type: string
        target_drawer:
          type: string
        summary_fields:
          type: array
          items:
            type: string
        detail_fields:
          type: array
          items:
            type: string
    AdminWebDrawerContract:
      type: object
      additionalProperties: false
      required: [id, title_source, trigger_param, data_source, anatomy, summary_fields, detail_fields, footer_actions]
      properties:
        id:
          type: string
        title_source:
          type: string
        trigger_param:
          type: string
        data_source:
          type: string
        anatomy:
          type: string
        summary_fields:
          type: array
          items:
            type: string
        detail_fields:
          type: array
          items:
            type: string
        footer_actions:
          type: array
          items:
            $ref: "#/components/schemas/AdminWebControl"
    AdminWebControl:
      type: object
      additionalProperties: false
      required: [id, label, kind, enabled, disabled_reason, action]
      properties:
        id:
          type: string
        label:
          type: string
        kind:
          type: string
        enabled:
          type: boolean
        disabled_reason:
          type: string
        action:
          type: string
    AdminWebOptionGroup:
      type: object
      additionalProperties: false
      required: [id, options]
      properties:
        id:
          type: string
        options:
          type: array
          items:
            $ref: "#/components/schemas/AdminWebOption"
    AdminWebOption:
      type: object
      additionalProperties: false
      required: [key, label, title, enabled, disabled_reason, tone]
      properties:
        key:
          type: string
        label:
          type: string
        title:
          type: string
        enabled:
          type: boolean
        disabled_reason:
          type: string
        tone:
          type: string
    AdminWebDisplayRule:
      type: object
      additionalProperties: false
      required: [id, applies_to, summary, frontend_owns, backend_owns]
      properties:
        id:
          type: string
        applies_to:
          type: array
          items:
            type: string
        summary:
          type: string
        frontend_owns:
          type: array
          items:
            type: string
        backend_owns:
          type: array
          items:
            type: string
    BootstrapActor:
      type: object
      additionalProperties: false
      required:
        - actor_id
        - tenant_id
      properties:
        actor_id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
    BootstrapDeviceState:
      type: object
      additionalProperties: false
      required:
        - required
        - device
        - status
        - reason
      properties:
        required:
          type: boolean
        device:
          oneOf:
            - $ref: "#/components/schemas/DeviceSummary"
            - type: "null"
        status:
          type: string
          minLength: 1
          maxLength: 80
        reason:
          type:
            - string
            - "null"
          maxLength: 500
    BootstrapNavigationItem:
      type: object
      additionalProperties: false
      required:
        - key
        - label
        - href
      properties:
        key:
          type: string
          minLength: 1
          maxLength: 80
        label:
          type: string
          minLength: 1
          maxLength: 120
        href:
          type: string
          minLength: 1
          maxLength: 120
    NavChrome:
      type: string
      description: >-
        Backend-computed navigation chrome, shared by both bootstraps. The
        client renders this value; it never counts modules or checks role to
        decide chrome.
      enum:
        - expanded
        - minimal
    BootstrapTaskQueue:
      type: object
      additionalProperties: false
      required:
        - key
        - label
        - required_capabilities
      properties:
        key:
          type: string
          minLength: 1
          maxLength: 80
        label:
          type: string
          minLength: 1
          maxLength: 120
        required_capabilities:
          type: array
          items:
            type: string
            minLength: 1
            maxLength: 120
    BootstrapSOPVersion:
      type: object
      additionalProperties: false
      required:
        - sop_code
        - sop_version_id
        - status
        - compatible
        - reason
      properties:
        sop_code:
          type: string
          minLength: 1
          maxLength: 120
        sop_version_id:
          type:
            - string
            - "null"
          format: uuid
        status:
          type: string
          minLength: 1
          maxLength: 80
        compatible:
          type: boolean
        reason:
          type:
            - string
            - "null"
          maxLength: 500
    BootstrapOptionSource:
      type: object
      additionalProperties: false
      required:
        - key
        - scope_type
        - scope_id
        - ttl_seconds
      properties:
        key:
          type: string
          minLength: 1
          maxLength: 120
        scope_type:
          type: string
          enum:
            - tenant
            - custodian_party
            - farm
            - park
            - shed
            - cohort
        scope_id:
          type: string
          format: uuid
        ttl_seconds:
          type: integer
          minimum: 1
    BootstrapSyncPolicy:
      type: object
      additionalProperties: false
      required:
        - offline_draft_ttl_hours
        - heartbeat_interval_seconds
        - max_retry_backoff_seconds
      properties:
        offline_draft_ttl_hours:
          type: integer
          minimum: 1
        heartbeat_interval_seconds:
          type: integer
          minimum: 1
        max_retry_backoff_seconds:
          type: integer
          minimum: 1
    IdentifierType:
      type: string
      enum:
        - animal_identifier_1
        - animal_identifier_2
    IdentifierStatus:
      type: string
      enum:
        - active
        - retired
        - disputed
        - duplicate
        - invalid
    ResolutionState:
      type: string
      enum:
        - single_match
        - multiple_matches
        - no_match
        - needs_review
        - merged_redirect
    Warning:
      type: object
      additionalProperties: false
      required:
        - code
        - message
      properties:
        code:
          type: string
          minLength: 1
          maxLength: 120
        message:
          type: string
          minLength: 1
          maxLength: 500
        original_goat_id:
          type:
            - string
            - "null"
          format: uuid
        redirect_goat_id:
          type:
            - string
            - "null"
          format: uuid
    LocationPath:
      type: object
      additionalProperties: false
      required:
        - operational_location_display
      properties:
        farm_id:
          type:
            - string
            - "null"
          format: uuid
        farm_code:
          type:
            - string
            - "null"
          maxLength: 80
        farm_name:
          type:
            - string
            - "null"
          maxLength: 200
        park_id:
          type:
            - string
            - "null"
          format: uuid
        park_code:
          type:
            - string
            - "null"
          maxLength: 80
        park_name:
          type:
            - string
            - "null"
          maxLength: 200
        shed_id:
          type:
            - string
            - "null"
          format: uuid
        shed_code:
          type:
            - string
            - "null"
          maxLength: 80
        shed_name:
          type:
            - string
            - "null"
          maxLength: 200
        cohort_id:
          type:
            - string
            - "null"
          format: uuid
        cohort_code:
          type:
            - string
            - "null"
          maxLength: 80
        cohort_name:
          type:
            - string
            - "null"
          maxLength: 200
        partition_label:
          type:
            - string
            - "null"
          maxLength: 80
          description: >-
            Raw stored partition label for the shed ('1', 'Part 3'). Null or
            absent means the shed is non-partitioned. Never the literal string
            "whole".
        source_shed_name:
          type:
            - string
            - "null"
          maxLength: 200
          description: >-
            Original partition-bearing source name (e.g. "Castro 1"), kept for
            traceability only. Not a display field.
        operational_location_display:
          type: string
          maxLength: 200
          description: >-
            User-facing location label. No partition -> bare shed name
            ("Yashoda"); numeric convention -> "Castro 2"; prefixed convention
            -> "Godel 1 - Part 3".
    EvidenceRef:
      type: object
      additionalProperties: false
      required:
        - evidence_type
        - evidence_id
      properties:
        evidence_type:
          type: string
          enum:
            - source_record
            - identifier
            - goat
            - event
            - media
            - decision
            - import_run
            - conflict
            - location
            - actor
        evidence_id:
          type: string
          minLength: 1
          maxLength: 200
        source_system:
          type:
            - string
            - "null"
          minLength: 1
          maxLength: 120
        description:
          type:
            - string
            - "null"
          maxLength: 500
    GoatSummary:
      type: object
      additionalProperties: false
      required:
        - goat_id
        - display_id
        - breed
        - sex
        - age_band
        - lifecycle_status
        - reproductive_status
        - growth_cohort_tag
        - management_stage
        - health_status
        - animal_identifier_1
        - location_path
        - row_version
        - warnings
      properties:
        goat_id:
          type: string
          format: uuid
        display_id:
          type: string
          pattern: ^G-[0-9]{6,}$
        row_version:
          description: >-
            Current optimistic-concurrency token for this animal. An operator flow that mutates the
            goat (e.g. recording a death from a tag scan) sends this back verbatim so a stale
            in-hand record is rejected instead of clobbering a newer change. Resolved server-side
            from the scan/search; the operator never types it.
          type: integer
          format: int32
          minimum: 1
        animal_identifier_1:
          type: string
          maxLength: 200
        animal_identifier_2:
          type:
            - string
            - "null"
          maxLength: 200
        breed:
          type:
            - string
            - "null"
          maxLength: 120
        sex:
          type: string
          enum:
            - female
            - male
        age_band:
          type:
            - string
            - "null"
          maxLength: 80
        lifecycle_status:
          type: string
          minLength: 1
          maxLength: 80
        reproductive_status:
          type:
            - string
            - "null"
          maxLength: 80
        growth_cohort_tag:
          type:
            - string
            - "null"
          maxLength: 80
        management_stage:
          type:
            - string
            - "null"
          maxLength: 80
        health_status:
          type:
            - string
            - "null"
          maxLength: 80
        location_path:
          $ref: "#/components/schemas/LocationPath"
        weight_kg:
          type:
            - number
            - "null"
          minimum: 0
        warnings:
          type: array
          items:
            $ref: "#/components/schemas/Warning"
    GoatIdentifier:
      type: object
      additionalProperties: false
      required:
        - identifier_id
        - identifier_type
        - identifier_value
        - scope_key
        - status
        - is_primary_for_goat
        - valid_from
      properties:
        identifier_id:
          type: string
          format: uuid
        identifier_type:
          $ref: "#/components/schemas/IdentifierType"
        identifier_value:
          type: string
          minLength: 1
          maxLength: 200
        scope_key:
          type: string
          minLength: 1
          maxLength: 120
        status:
          $ref: "#/components/schemas/IdentifierStatus"
        is_primary_for_goat:
          type: boolean
        valid_from:
          type: string
          format: date-time
        valid_to:
          type:
            - string
            - "null"
          format: date-time
        source_system:
          type:
            - string
            - "null"
          maxLength: 120
        source_record_id:
          type:
            - string
            - "null"
          maxLength: 200
        confidence:
          type:
            - number
            - "null"
          minimum: 0
          maximum: 1
    GoatPassport:
      type: object
      additionalProperties: false
      required:
        - goat_id
        - display_id
        - species
        - summary
        - identifiers
        - evidence_refs
        - row_version
      properties:
        goat_id:
          type: string
          format: uuid
        display_id:
          type: string
          pattern: ^G-[0-9]{6,}$
        species:
          type: string
          enum:
            - goat
            - sheep
        summary:
          $ref: "#/components/schemas/GoatSummary"
        identifiers:
          type: array
          items:
            $ref: "#/components/schemas/GoatIdentifier"
        evidence_refs:
          type: array
          items:
            $ref: "#/components/schemas/EvidenceRef"
        family_refs:
          type: array
          items:
            type: object
            additionalProperties: false
            required:
              - relation
              - goat_id
            properties:
              relation:
                type: string
                minLength: 1
                maxLength: 80
              goat_id:
                type: string
                format: uuid
        merged_into_goat_id:
          type:
            - string
            - "null"
          format: uuid
        row_version:
          type: integer
          minimum: 1
    GoatSearchResponse:
      type: object
      additionalProperties: false
      required:
        - items
        - next_cursor
        - trace_id
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/GoatSummary"
        next_cursor:
          type:
            - string
            - "null"
          maxLength: 500
        trace_id:
          type: string
          minLength: 1
          maxLength: 200
    GoatPassportResponse:
      type: object
      additionalProperties: false
      required:
        - goat
        - warnings
        - trace_id
      properties:
        goat:
          $ref: "#/components/schemas/GoatPassport"
        warnings:
          type: array
          items:
            $ref: "#/components/schemas/Warning"
        trace_id:
          type: string
          minLength: 1
          maxLength: 200
    GoatTimelineEvent:
      type: object
      additionalProperties: false
      required:
        - event_id
        - event_type
        - occurred_at
        - recorded_at
        - actor_type
        - evidence_refs
      properties:
        event_id:
          type: string
          format: uuid
        event_type:
          type: string
          minLength: 1
          maxLength: 120
        occurred_at:
          type: string
          format: date-time
        recorded_at:
          type: string
          format: date-time
        actor_type:
          type: string
          minLength: 1
          maxLength: 80
        evidence_refs:
          type: array
          items:
            $ref: "#/components/schemas/EvidenceRef"
        decision_id:
          type:
            - string
            - "null"
          format: uuid
    GoatTimelineResponse:
      type: object
      additionalProperties: false
      required:
        - items
        - next_cursor
        - trace_id
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/GoatTimelineEvent"
        next_cursor:
          type:
            - string
            - "null"
          maxLength: 500
        trace_id:
          type: string
          minLength: 1
          maxLength: 200
    ResolveIdentifierResponse:
      type: object
      additionalProperties: false
      required:
        - resolution_state
        - candidate_goats
        - warnings
        - trace_id
      properties:
        resolution_state:
          $ref: "#/components/schemas/ResolutionState"
        goat_summary:
          oneOf:
            - $ref: "#/components/schemas/GoatSummary"
            - type: "null"
        candidate_goats:
          type: array
          items:
            $ref: "#/components/schemas/GoatSummary"
        warnings:
          type: array
          items:
            $ref: "#/components/schemas/Warning"
        conflict_id:
          type:
            - string
            - "null"
          format: uuid
        redirect_goat_id:
          type:
            - string
            - "null"
          format: uuid
        trace_id:
          type: string
          minLength: 1
          maxLength: 200
    IdempotencyMeta:
      type: object
      additionalProperties: false
      required:
        - idempotency_key
        - replayed
      properties:
        idempotency_key:
          type: string
          minLength: 8
          maxLength: 200
        replayed:
          type: boolean
        first_result_id:
          type:
            - string
            - "null"
          format: uuid
    CalendarOwnerFilter:
      type: string
      enum:
        - all
        - pc
        - inventory
        - admin_data_ops
    CalendarOwnerKey:
      type: string
      enum:
        - pc
        - inventory
        - admin_data_ops
    CalendarStatus:
      type: string
      enum:
        - scheduled
        - due
        - overdue
        - missed
        - in_progress
        - proof_pending
        - verification_pending
        - rejected
        - rework_due
        - deferred
        - blocked
        - completed
        - canceled
    CalendarSeverity:
      type: string
      enum:
        - info
        - warning
        - critical
    CalendarEventType:
      type: string
      enum:
        - vaccination_dose_due
        - vaccination_drive
        - vaccination_history
        - vaccination_campaign
        - vaccination_booster_due
        - vaccination_defer_review
        - vaccination_evidence_review
        - vaccination_proof_verification
        - vaccination_rework_due
        - vaccine_stock_readiness
        - vaccine_cold_chain_check
        - vaccine_reorder_expiry_grn
        - pc_stock_anti_misuse
        - vaccination_config_activation_review
    CalendarJSONBlock:
      type: object
      additionalProperties: true
    CalendarEventLinks:
      type: object
      additionalProperties:
        oneOf:
          - type: string
          - type: boolean
          - type: "null"
    CalendarEvent:
      type: object
      additionalProperties: false
      required:
        - event_id
        - event_type
        - owner_key
        - title
        - subtitle
        - aggregated
        - all_day
        - summary_primary
        - summary_secondary
        - summary_tertiary
        - status
        - severity
        - due_at
        - window_start
        - window_end
        - timezone
        - timezone_source
        - park_id
        - park_code
        - shed_id
        - shed_name
        - cohort_id
        - cohort_name
        - target_type
        - target_count
        - shed_count
        - vaccine_count
        - drive_count
        - catch_up_count
        - scheduled_count
        - deferred_count
        - review_count
        - shed_labels
        - shed_partition_labels
        - vaccine_labels
        - protocol_id
        - protocol_version_id
        - rule_id
        - vaccine_name
        - dose_code
        - source_backed
        - source_label
        - assignee_label
        - executor_role
        - verifier_label
        - reminder_state
        - primary_notification_channel
        - escalation_state
        - system
        - cross_cutting
        - links
      properties:
        event_id:
          type: string
          minLength: 1
          maxLength: 256
        event_type:
          $ref: "#/components/schemas/CalendarEventType"
        owner_key:
          $ref: "#/components/schemas/CalendarOwnerKey"
        title:
          type: string
        subtitle:
          type: string
        aggregated:
          type: boolean
        all_day:
          type: boolean
        summary_primary:
          type: string
        summary_secondary:
          type: string
        summary_tertiary:
          type: string
        status:
          $ref: "#/components/schemas/CalendarStatus"
        severity:
          $ref: "#/components/schemas/CalendarSeverity"
        due_at:
          type: string
          format: date-time
        window_start:
          type:
            - string
            - "null"
          format: date-time
        window_end:
          type:
            - string
            - "null"
          format: date-time
        timezone:
          type: string
        timezone_source:
          type: string
        park_id:
          type:
            - string
            - "null"
          format: uuid
        park_code:
          type:
            - string
            - "null"
        shed_id:
          type:
            - string
            - "null"
          format: uuid
        shed_name:
          type:
            - string
            - "null"
        partition_label:
          type:
            - string
            - "null"
          description: >-
            Raw stored partition label for shed_id ('1', 'Part 3'). Null or
            absent means the shed is non-partitioned. Never the literal
            string "whole".
        source_shed_name:
          type:
            - string
            - "null"
          description: >-
            Original partition-bearing source name (e.g. "Castro 1"), kept for
            traceability only. Not a display field.
        operational_location_display:
          type:
            - string
            - "null"
          description: >-
            User-facing location label for shed_id. No partition -> bare shed
            name ("Yashoda"); numeric convention -> "Castro 2"; prefixed
            convention -> "Godel 1 - Part 3".
        cohort_id:
          type:
            - string
            - "null"
          format: uuid
        cohort_name:
          type:
            - string
            - "null"
        target_type:
          type: string
        target_count:
          type: integer
          minimum: 0
        shed_count:
          type: integer
          minimum: 0
        vaccine_count:
          type: integer
          minimum: 0
        drive_count:
          type: integer
          minimum: 0
        catch_up_count:
          type: integer
          minimum: 0
        scheduled_count:
          type: integer
          minimum: 0
        deferred_count:
          type: integer
          minimum: 0
        review_count:
          type: integer
          minimum: 0
        shed_labels:
          type: array
          items:
            type: string
        shed_partition_labels:
          type: array
          items:
            type:
              - string
              - "null"
          description: >-
            Index-parallel to shed_labels. The raw stored partition label is emitted when the entry is
            partitioned; an empty string means the physical shed is not partitioned. Repeated shed_labels
            with different partition labels are distinct operational locations and count separately in
            shed_count.
        vaccine_labels:
          type: array
          items:
            type: string
        protocol_id:
          type:
            - string
            - "null"
          format: uuid
        protocol_version_id:
          type:
            - string
            - "null"
          format: uuid
        rule_id:
          type:
            - string
            - "null"
          format: uuid
        vaccine_name:
          type:
            - string
            - "null"
        dose_code:
          type:
            - string
            - "null"
        source_backed:
          type: boolean
        source_label:
          type: string
        assignee_label:
          type:
            - string
            - "null"
        executor_role:
          type:
            - string
            - "null"
        verifier_label:
          type:
            - string
            - "null"
        reminder_state:
          type: string
        primary_notification_channel:
          type: string
        escalation_state:
          type: string
        system:
          type: boolean
        cross_cutting:
          type: boolean
        links:
          $ref: "#/components/schemas/CalendarEventLinks"
        drive_summary:
          allOf:
            - $ref: "#/components/schemas/DriveSummary"
          nullable: true
    DriveSummary:
      type: object
      additionalProperties: false
      description: >-
        Park-level drive progress. Invariant: total_count = completed_count + submitted_count +
        due_count + overdue_count + deferred_count (5 disjoint obligation buckets); remaining_count =
        due_count + overdue_count + deferred_count = total_count - completed_count - submitted_count
        (work still owed by the OPERATOR; it excludes submitted work so it can never contradict
        progress_pct on the same payload). Distinct-animal coverage: total_animals = distinct goats in this drive;
        completed_animals = goats where ALL drive obligations are completed (fully covered);
        submitted_animals = goats with recorded mobile completion pending verification.
        Cross-surface progress: progress_completed / progress_total / progress_pct on the
        progress_basis grain are the ONE authoritative drive-progress figure. Admin-web and the
        Android app MUST render them verbatim and MUST NOT derive their own numerator from any
        other field -- two clients each picking their own fields is how the same drive showed two
        different completion numbers and ring percentages. The numerator is FIELD WORK DONE =
        completed + submitted (maintainer decision 2026-08-03): an operator who vaccinated every
        animal and submitted proof sees 100% and "4 of 4 sheds done", and the outstanding verifier
        review is carried by the verification_pending status/chip and submitted_count /
        submitted_animals, never by holding the ring below 100%. remaining_count follows the same
        numerator, so a fully submitted drive reports progress_pct 100 AND remaining_count 0 --
        one payload never carries two answers to "how much is left".
        Stock/execution "blocked" visibility is deliberately out of scope -- stock is not a built
        product feature yet (owner decision 2026-07-14); revisit when the stock module ships.
      required:
        - park_name
        - drive_name
        - drive_total
        - due_date
        - shed_count
        - sheds_completed
        - sheds
        - vaccine_labels
        - total_count
        - completed_count
        - submitted_count
        - remaining_count
        - due_count
        - overdue_count
        - deferred_count
        - rejected_count
        - total_animals
        - completed_animals
        - submitted_animals
        - progress_basis
        - progress_completed
        - progress_total
        - progress_pct
        - owner_label
      properties:
        park_name:
          type: string
          description: Park name or code
        drive_name:
          type: string
          description: Shared logical drive name across every operator-day in the same vaccine campaign
        drive_total:
          type: integer
          minimum: 0
          description: Total distinct animals across all operator-days carrying this logical drive name
        due_date:
          type: string
          format: date
          description: Drive due date
        shed_count:
          type: integer
          minimum: 0
          description: Number of unique operational locations (physical shed plus optional partition) in the drive
        sheds_completed:
          type: integer
          minimum: 0
          description: Number of operational locations where all obligations are completed or submitted
        sheds:
          type: array
          description: Per-operational-location animal counts for this drive.
          items:
            $ref: "#/components/schemas/DriveShedSummary"
        vaccine_labels:
          type: array
          items:
            type: string
          description: List of vaccine names involved in the drive
        total_count:
          type: integer
          minimum: 0
          description: Total obligations in the drive (completed + due + overdue + deferred)
        completed_count:
          type: integer
          minimum: 0
          description: Completed obligations
        submitted_count:
          type: integer
          minimum: 0
          description: Recorded mobile completions pending verification
        remaining_count:
          type: integer
          minimum: 0
          description: >-
            Work still owed by the OPERATOR = due_count + overdue_count + deferred_count, i.e.
            total_count - completed_count - submitted_count. Deliberately EXCLUDES
            submitted-but-unverified work, exactly like the progress numerator, so a fully
            submitted drive reports remaining_count 0 beside progress_pct 100. It was
            total_count - completed_count, which rendered "20 left" next to a full ring.
        due_count:
          type: integer
          minimum: 0
          description: Active open obligations
        overdue_count:
          type: integer
          minimum: 0
          description: Overdue/missed obligations
        deferred_count:
          type: integer
          minimum: 0
          description: Deferred obligations
        rejected_count:
          type: integer
          minimum: 0
          description: >-
            INFORMATIONAL ONLY subset already counted inside due_count/overdue_count above -- never
            an additional partition on top of the five-bucket total. Obligations whose most recent
            verification verdict for this drive is a rejection that has not since been superseded
            by an accepted (or newly recorded) completion for the same obligation. Explains why the
            completed/progress numerator dropped after a verifier rejects proof: the obligation
            leaves completed/submitted and reopens as due/overdue work. Clears to 0 for an
            obligation the moment it is rescanned and accepted -- this is CURRENT rejected state,
            not lifetime rejection history.
        total_animals:
          type: integer
          minimum: 0
          description: Total distinct animals in the drive
        completed_animals:
          type: integer
          minimum: 0
          description: Distinct animals where all drive obligations are completed
        submitted_animals:
          type: integer
          minimum: 0
          description: Distinct animals with recorded completion pending verification
        progress_basis:
          type: string
          enum: [animals, doses]
          description: >-
            Grain that progress_completed/progress_total are counted on. "animals" whenever the
            drive has animals (a goat due several vaccines the same day is ONE animal), else the
            obligation/dose grain.
        progress_completed:
          type: integer
          minimum: 0
          description: >-
            Authoritative progress numerator on progress_basis. Verified completion only -- clients
            render this verbatim and never substitute submitted counts.
        progress_total:
          type: integer
          minimum: 0
          description: Authoritative progress denominator on progress_basis.
        progress_pct:
          type: integer
          minimum: 0
          maximum: 100
          description: >-
            Backend-rounded (half-up) progress percentage. Clients render this verbatim instead of
            re-deriving it, so the ring reads identically on every surface.
        owner_label:
          type: string
          description: Owner/team label
    DriveShedSummary:
      type: object
      additionalProperties: false
      required:
        - shed_id
        - shed_name
        - operational_location_display
        - total_animals
      properties:
        shed_id:
          type: string
          format: uuid
        shed_name:
          type: string
        partition_label:
          type:
            - string
            - "null"
          description: >-
            Raw stored partition label for this shed ('1', 'Part 3'). Null or
            absent means the shed is non-partitioned. Never the literal
            string "whole".
        source_shed_name:
          type:
            - string
            - "null"
          description: >-
            Original partition-bearing source name (e.g. "Castro 1"), kept for
            traceability only. Not a display field.
        operational_location_display:
          type: string
          description: >-
            User-facing location label. No partition -> bare shed name
            ("Yashoda"); numeric convention -> "Castro 2"; prefixed convention
            -> "Godel 1 - Part 3".
        total_animals:
          type: integer
          minimum: 0
    CalendarPresentationQuery:
      type: object
      additionalProperties:
        type: string
    CalendarPresentationTab:
      type: object
      additionalProperties: false
      required:
        - key
        - label
        - active
        - enabled
        - disabled_reason
        - query
      properties:
        key:
          type: string
        label:
          type: string
        active:
          type: boolean
        enabled:
          type: boolean
        disabled_reason:
          type: string
        query:
          $ref: "#/components/schemas/CalendarPresentationQuery"
    CalendarOwnerPresentationTab:
      type: object
      additionalProperties: false
      required:
        - key
        - label
        - scope_label
        - color
        - active
        - enabled
        - disabled_reason
        - query
      properties:
        key:
          type: string
        label:
          type: string
        scope_label:
          type: string
        color:
          type: string
        active:
          type: boolean
        enabled:
          type: boolean
        disabled_reason:
          type: string
        query:
          $ref: "#/components/schemas/CalendarPresentationQuery"
    CalendarRhythmDay:
      type: object
      additionalProperties: false
      required:
        - day
        - label
        - tone
        - enabled
        - query
      properties:
        day:
          type: string
        label:
          type: string
        tone:
          type: string
        enabled:
          type: boolean
        query:
          $ref: "#/components/schemas/CalendarPresentationQuery"
    CalendarRhythmPresentation:
      type: object
      additionalProperties: false
      required:
        - title
        - note
        - days
      properties:
        title:
          type: string
        note:
          type: string
        days:
          type: array
          items:
            $ref: "#/components/schemas/CalendarRhythmDay"
    CalendarViewPresentation:
      type: object
      additionalProperties: false
      required:
        - title
        - scope_label
        - scope_only_message
        - clear_scope_label
        - whole_period_message
        - all_days_selected_label
        - clear_day_label
        - empty_message
        - reminder_title
        - reminder_empty_message
        - reminder_note
        - as_of_hint
        - cell_note
      properties:
        title:
          type: string
        scope_label:
          type: string
        scope_only_message:
          type: string
        clear_scope_label:
          type: string
        whole_period_message:
          type: string
        all_days_selected_label:
          type: string
        clear_day_label:
          type: string
        empty_message:
          type: string
        reminder_title:
          type: string
        reminder_empty_message:
          type: string
        reminder_note:
          type: string
        as_of_hint:
          type: string
        cell_note:
          type: string
    CalendarActionPresentation:
      type: object
      additionalProperties: false
      required:
        - label
        - enabled
        - disabled_reason
      properties:
        label:
          type: string
        enabled:
          type: boolean
        disabled_reason:
          type: string
    CalendarEmptyStatePresentation:
      type: object
      additionalProperties: false
      required:
        - ok_message
        - error_message
        - primary_label
        - secondary_label
      properties:
        ok_message:
          type: string
        error_message:
          type: string
        primary_label:
          type: string
        secondary_label:
          type: string
    CalendarKeyLabel:
      type: object
      additionalProperties: false
      required:
        - key
        - label
      properties:
        key:
          type: string
        label:
          type: string
    CalendarPresentation:
      type: object
      additionalProperties: false
      required:
        - page_title
        - page_subtitle
        - view_tabs
        - owner_tabs
        - workstream_tabs
        - rhythm
        - week
        - month
        - new_event
        - empty_state
        - event_types
        - active_owner_key
        - active_owner_label
        - active_owner_scope_label
        - active_owner_color
        - all_owners_selected_label
      properties:
        page_title:
          type: string
        page_subtitle:
          type: string
        view_tabs:
          type: array
          items:
            $ref: "#/components/schemas/CalendarPresentationTab"
        owner_tabs:
          type: array
          items:
            $ref: "#/components/schemas/CalendarOwnerPresentationTab"
        workstream_tabs:
          type: array
          items:
            $ref: "#/components/schemas/CalendarPresentationTab"
        rhythm:
          $ref: "#/components/schemas/CalendarRhythmPresentation"
        week:
          $ref: "#/components/schemas/CalendarViewPresentation"
        month:
          $ref: "#/components/schemas/CalendarViewPresentation"
        new_event:
          $ref: "#/components/schemas/CalendarActionPresentation"
        empty_state:
          $ref: "#/components/schemas/CalendarEmptyStatePresentation"
        event_types:
          type: array
          items:
            $ref: "#/components/schemas/CalendarKeyLabel"
        active_owner_key:
          type: string
        active_owner_label:
          type: string
        active_owner_scope_label:
          type: string
        active_owner_color:
          type: string
        all_owners_selected_label:
          type: string
    CalendarProjectionMetadata:
      type: object
      additionalProperties: false
      required:
        - projection_version
        - projected_at
        - freshness_status
        - serving_state
        - stale
      properties:
        projection_version:
          type: integer
          format: int64
          minimum: 1
        projected_at:
          type: string
          format: date-time
        freshness_status:
          type: string
          enum: [green, yellow, red, unknown]
        serving_state:
          type: string
          enum: [never_synced, fresh, stale, rebuilding, failed]
        stale:
          type: boolean
        partial_coverage:
          type: boolean
          description: "True when the requested date window extends beyond the projection's build window coverage."
    CalendarEventListResponse:
      type: object
      additionalProperties: false
      required:
        - source
        - presentation
        - items
        - date_markers
        - next_cursor
        - projection
      properties:
        source:
          type: string
          const: api
        presentation:
          $ref: "#/components/schemas/CalendarPresentation"
        items:
          type: array
          items:
            $ref: "#/components/schemas/CalendarEvent"
        date_markers:
          type: array
          items:
            $ref: "#/components/schemas/CalendarDateMarker"
        filter_options:
          $ref: "#/components/schemas/CalendarFilterOptions"
        next_cursor:
          type:
            - string
            - "null"
        projection:
          $ref: "#/components/schemas/CalendarProjectionMetadata"
        history_projection:
          $ref: "#/components/schemas/CalendarProjectionMetadata"
          description: "Freshness and coverage metadata for the completed-history projection, populated only when this read consulted the history CTE (explicit status=completed filter or date-marker inclusion). Null otherwise."
          nullable: true
        reminder_rail:
          $ref: "#/components/schemas/CalendarReminderRail"
          description: "Backend-computed, whole-filtered-week reminder/escalation rail. Populated only when include_reminder_rail is set (the week view request shape); null otherwise so a plain paged list fetch does not pay for it."
          nullable: true
    CalendarFilterOption:
      type: object
      additionalProperties: false
      required:
        - value
        - label
      properties:
        value:
          type: string
        label:
          type: string
        parent_value:
          type:
            - string
            - "null"
    CalendarFilterOptions:
      type: object
      additionalProperties: false
      required:
        - parks
        - management_stages
        - sheds
        - vaccines
        - statuses
        - months
        - years
      properties:
        parks:
          type: array
          items:
            $ref: "#/components/schemas/CalendarFilterOption"
        sheds:
          type: array
          items:
            $ref: "#/components/schemas/CalendarFilterOption"
        vaccines:
          type: array
          items:
            $ref: "#/components/schemas/CalendarFilterOption"
        statuses:
          type: array
          items:
            $ref: "#/components/schemas/CalendarKeyLabel"
        months:
          type: array
          items:
            $ref: "#/components/schemas/CalendarKeyLabel"
        years:
          type: array
          items:
            $ref: "#/components/schemas/CalendarKeyLabel"
    CalendarReminderRail:
      type: object
      additionalProperties: false
      required:
        - count
        - empty_message
        - items
      properties:
        count:
          type: integer
          minimum: 0
          description: "Total active reminder/escalation events in the full filtered week window, independent of the bounded items preview below."
        empty_message:
          type: string
        items:
          type: array
          items:
            $ref: "#/components/schemas/CalendarReminderRailItem"
          description: "Bounded (~20) keyset-ordered preview of active reminder/escalation events."
    CalendarReminderRailItem:
      type: object
      additionalProperties: false
      required:
        - event_id
        - title
        - subtitle
        - reminder_label
        - escalation_label
        - channels
      properties:
        event_id:
          type: string
        title:
          type: string
        subtitle:
          type: string
        reminder_label:
          type: string
        escalation_label:
          type: string
        channels:
          type: array
          items:
            type: string
    CalendarDateMarker:
      type: object
      additionalProperties: false
      required:
        - date
        - event_count
        - completed_count
        - open_count
        - drive_count
        - due_count
        - overdue_count
        - deferred_count
      properties:
        date:
          type: string
          format: date
        event_count:
          type: integer
          minimum: 0
        completed_count:
          type: integer
          minimum: 0
        open_count:
          type: integer
          minimum: 0
        drive_count:
          type: integer
          minimum: 0
        due_count:
          type: integer
          minimum: 0
        overdue_count:
          type: integer
          minimum: 0
        deferred_count:
          type: integer
          minimum: 0
    CalendarHistoryItem:
      type: object
      additionalProperties: false
      required:
        - history_id
        - event_type
        - status
        - title
        - actor_label
        - occurred_at
        - channel
        - reason
        - trace_id
        - source_table
        - details
      properties:
        history_id:
          type: string
        event_type:
          type: string
        status:
          type: string
        title:
          type: string
        actor_label:
          type:
            - string
            - "null"
        occurred_at:
          type: string
          format: date-time
        channel:
          type:
            - string
            - "null"
        reason:
          type:
            - string
            - "null"
        trace_id:
          type:
            - string
            - "null"
        source_table:
          type: string
        details:
          $ref: "#/components/schemas/CalendarJSONBlock"
    CalendarHistoryResponse:
      type: object
      additionalProperties: false
      required:
        - source
        - event_id
        - items
        - next_cursor
      properties:
        source:
          type: string
          const: api
        event_id:
          type: string
        items:
          type: array
          items:
            $ref: "#/components/schemas/CalendarHistoryItem"
        next_cursor:
          type:
            - string
            - "null"
    CalendarEventDetail:
      type: object
      additionalProperties: false
      required:
        - event
        - summary
        - source_and_rule
        - execution
        - stock
        - proof
        - verification
        - notification_channels
        - notification_policy
        - links
        - recent_actions
      properties:
        event:
          $ref: "#/components/schemas/CalendarEvent"
        summary:
          $ref: "#/components/schemas/CalendarJSONBlock"
        source_and_rule:
          $ref: "#/components/schemas/CalendarJSONBlock"
        execution:
          $ref: "#/components/schemas/CalendarJSONBlock"
        stock:
          $ref: "#/components/schemas/CalendarJSONBlock"
        proof:
          $ref: "#/components/schemas/CalendarJSONBlock"
        verification:
          $ref: "#/components/schemas/CalendarJSONBlock"
        notification_channels:
          type: array
          items:
            type: string
        notification_policy:
          $ref: "#/components/schemas/CalendarJSONBlock"
        links:
          $ref: "#/components/schemas/CalendarEventLinks"
        recent_actions:
          type: array
          items:
            $ref: "#/components/schemas/CalendarHistoryItem"
        history_projection:
          $ref: "#/components/schemas/CalendarProjectionMetadata"
          description: "Freshness and coverage metadata for the completed-history projection, populated only when this detail was served from the history-projection fallback path. Null otherwise."
          nullable: true
    CalendarDriveTarget:
      type: object
      additionalProperties: false
      required:
        - obligation_id
        - animal_id
        - display_id
        - animal_identifier_1
        - animal_identifier_2
        - status
        - scheduled_at
      properties:
        obligation_id:
          type: string
          format: uuid
        animal_id:
          type: string
          format: uuid
        display_id:
          type: string
        animal_identifier_1:
          type:
            - string
            - "null"
        animal_identifier_2:
          type:
            - string
            - "null"
        shed_name:
          type:
            - string
            - "null"
        partition_label:
          type:
            - string
            - "null"
          description: >-
            Raw stored partition label for this target's shed ('1', 'Part 3').
            Null or absent means the shed is non-partitioned. Never the
            literal string "whole".
        source_shed_name:
          type:
            - string
            - "null"
          description: >-
            Original partition-bearing source name (e.g. "Castro 1"), kept for
            traceability only. Not a display field.
        operational_location_display:
          type:
            - string
            - "null"
          description: >-
            User-facing location label. No partition -> bare shed name
            ("Yashoda"); numeric convention -> "Castro 2"; prefixed convention
            -> "Godel 1 - Part 3".
        stage:
          type:
            - string
            - "null"
        lifecycle_status:
          type:
            - string
            - "null"
        health_status:
          type:
            - string
            - "null"
        exit_reason:
          type:
            - string
            - "null"
        defer_reason:
          type:
            - string
            - "null"
        status:
          type: string
        scheduled_at:
          type: string
          format: date-time
    CalendarDriveTargetListResponse:
      type: object
      additionalProperties: false
      required:
        - source
        - event_id
        - items
        - next_cursor
      properties:
        source:
          type: string
          const: api
        event_id:
          type: string
        items:
          type: array
          items:
            $ref: "#/components/schemas/CalendarDriveTarget"
        next_cursor:
          type:
            - string
            - "null"
    CalendarNudgeRequest:
      type: object
      additionalProperties: false
      properties:
        channel:
          type: string
          enum:
            - local-stub
            - push_fcm
            - slack
            - email
            - webhook
        message:
          type: string
          maxLength: 2000
        reason:
          type: string
          maxLength: 1000
    CalendarSnoozeRequest:
      type: object
      additionalProperties: false
      required:
        - snooze_until
        - reason
      properties:
        snooze_until:
          type: string
          format: date-time
        reason:
          type: string
          minLength: 1
          maxLength: 1000
        replace_existing:
          type: boolean
          default: false
    CalendarEscalationAcknowledgeRequest:
      type: object
      additionalProperties: false
      properties:
        reason:
          type: string
          maxLength: 1000
    CalendarEscalationResolveRequest:
      type: object
      additionalProperties: false
      required:
        - reason
      properties:
        reason:
          type: string
          minLength: 1
          maxLength: 1000
    CalendarActionResponse:
      type: object
      additionalProperties: false
      required:
        - action_id
        - event_id
        - action_type
        - status
        - channel
        - snooze_until
        - idempotent_replay
      properties:
        action_id:
          type: string
          format: uuid
        event_id:
          type: string
        action_type:
          type: string
          enum:
            - reminder
            - nudge
            - snooze
            - escalation
            - acknowledge_escalation
            - resolve_escalation
        status:
          type: string
        channel:
          type:
            - string
            - "null"
        snooze_until:
          type:
            - string
            - "null"
          format: date-time
        idempotent_replay:
          type: boolean
    WorkState:
      type: string
      enum:
        - scheduled
        - due
        - overdue
        - in_progress
        - proof_pending
        - verification_pending
        - rejected
        - deferred
        - missed
        - blocked
        - completed
    ProcessIntegritySeverity:
      type: string
      enum:
        - ok
        - watch
        - at_risk
        - broken
    ProcessIntegrityOwnerState:
      type: string
      enum:
        - assigned
        - missing
    ProcessIntegritySOPState:
      type: string
      enum:
        - not_started
        - in_progress
        - submitted
        - accepted
        - rework
    ProcessIntegrityProofState:
      type: string
      enum:
        - not_required
        - missing
        - uploaded
        - accepted
        - rejected
    ProcessIntegrityVerificationState:
      type: string
      enum:
        - not_ready
        - pending
        - accepted
        - rejected
    ProcessIntegrityOwner:
      type: object
      additionalProperties: false
      properties:
        operator_id:
          type: string
          format: uuid
        operator_name:
          type: string
        park_head_id:
          type: string
          format: uuid
        park_head_name:
          type: string
        verifier_id:
          type: string
          format: uuid
        verifier_name:
          type: string
        escalation_owner_id:
          type: string
          format: uuid
        escalation_owner_name:
          type: string
    ProcessIntegrityEvidence:
      type: object
      additionalProperties: false
      required:
        - proof_ids
        - evidence_count
      properties:
        proof_ids:
          type: array
          items:
            type: string
            format: uuid
        evidence_count:
          type: integer
          minimum: 0
        latest_evidence_at:
          type: string
          format: date-time
        latest_rejection_reason:
          type: string
        audit_ref:
          type: string
    DriveCapacityState:
      type: string
      description: >-
        Operator-drive capacity state from the canonical vaccination drive planner.
        Capacity is unique animals per available operator per day, never vaccine
        doses or obligation cells.
      enum:
        - not_planned
        - within_cap
        - over_cap_required
        - medical_defer
        - terminal_animal_closed
    CountByWorkState:
      type: object
      additionalProperties: false
      required:
        - work_state
        - count
      properties:
        work_state:
          $ref: "#/components/schemas/WorkState"
        count:
          type: integer
          minimum: 0
    ActionCenterObligation:
      type: object
      additionalProperties: false
      required:
        - category
        - row_id
        - process_key
        - obligation_id
        - protocol_version_id
        - rule_id
        - park_id
        - park_name
        - shed_id
        - shed_name
        - operational_location_display
        - animal_stage
        - protocol_id
        - protocol_name
        - dose_code
        - proof_policy
        - due_at
        - expected_count
        - obligation_status
        - sop_task_state
        - proof_state
        - verification_state
        - completed_count
        - proof_count
        - rejected_count
        - deferred_count
        - work_state
        - gap_type
        - severity
        - owner_state
        - next_action
        - process_intact
        - owner
        - evidence
      properties:
        category:
          type: string
          enum:
            - vaccination
            - feed_direction
        row_id:
          type: string
        process_key:
          type: string
        obligation_id:
          type: string
          format: uuid
        batch_id:
          type: string
          format: uuid
        sop_task_id:
          type: string
          format: uuid
        sop_task_row_version:
          type: integer
        sop_submission_id:
          type: string
          format: uuid
        completion_id:
          type: string
          format: uuid
        park_id:
          type: string
          format: uuid
        park_name:
          type: string
        shed_id:
          type: string
          format: uuid
        partition_label:
          type: string
          nullable: true
          description: >-
            Raw stored partition label for the shed ('1', 'Part 3'). Null or absent
            means the shed is non-partitioned. Never the literal string 'whole' --
            that is a matching key, not user copy.
        operational_location_display:
          type: string
          description: >-
            User-facing location label composed by the backend (oploc.Display()).
            No partition -> bare shed name; partitioned -> 'Godel 1 - Part 3'.
            Clients RENDER this; they must not re-compose it.
        shed_name:
          type: string
        cohort_id:
          type: string
          format: uuid
        goat_id:
          type: string
          format: uuid
        animal_stage:
          type: string
        protocol_id:
          type: string
        protocol_version_id:
          type: string
        rule_id:
          type: string
        protocol_name:
          type: string
        dose_code:
          type: string
        drive_name:
          type: string
        sop_version_id:
          type: string
          format: uuid
        proof_policy:
          type: string
        due_at:
          type: string
          format: date-time
        window_start:
          type: string
          format: date-time
        window_end:
          type: string
          format: date-time
        expected_count:
          type: integer
          minimum: 0
        drive_capacity_state:
          $ref: "#/components/schemas/DriveCapacityState"
        drive_animals_required:
          type: integer
          minimum: 0
        drive_animals_assigned:
          type: integer
          minimum: 0
        drive_operator_cap:
          type: integer
          minimum: 0
        drive_available_operators:
          type: integer
          minimum: 0
        drive_latest_safe_date:
          type: string
          format: date-time
        drive_medical_defer_reason:
          type: string
        obligation_status:
          type: string
        batch_status:
          type: string
        sop_task_state:
          $ref: "#/components/schemas/ProcessIntegritySOPState"
        submission_state:
          type: string
        proof_state:
          $ref: "#/components/schemas/ProcessIntegrityProofState"
        verification_state:
          $ref: "#/components/schemas/ProcessIntegrityVerificationState"
        completion_state:
          type: string
        completed_count:
          type: integer
          minimum: 0
        proof_count:
          type: integer
          minimum: 0
        rejected_count:
          type: integer
          minimum: 0
        deferred_count:
          type: integer
          minimum: 0
        work_state:
          $ref: "#/components/schemas/WorkState"
        gap_type:
          type: string
        severity:
          $ref: "#/components/schemas/ProcessIntegritySeverity"
        blocker_reason:
          type: string
        owner_state:
          $ref: "#/components/schemas/ProcessIntegrityOwnerState"
        next_action:
          type: string
        process_intact:
          type: boolean
        owner:
          $ref: "#/components/schemas/ProcessIntegrityOwner"
        evidence:
          $ref: "#/components/schemas/ProcessIntegrityEvidence"
    ProcessIntegrityProjectionMetadata:
      type: object
      additionalProperties: false
      required:
        - projection_version
        - projected_at
        - as_of
        - freshness_status
        - serving_state
        - stale
      properties:
        projection_version:
          type: integer
          format: int64
          minimum: 1
        projected_at:
          type: string
          format: date-time
        as_of:
          type: string
          format: date-time
        freshness_status:
          type: string
          enum: [green, yellow, red, unknown]
        serving_state:
          type: string
          enum: [never_synced, fresh, stale, rebuilding, failed]
        stale:
          type: boolean
    ActionCenterResponse:
      type: object
      additionalProperties: false
      required:
        - source
        - items
        - counts_by_work_state
        - total_count
        - projection
      properties:
        source:
          type: string
          enum:
            - api
        items:
          type: array
          items:
            $ref: "#/components/schemas/ActionCenterObligation"
        counts_by_work_state:
          type: array
          items:
            $ref: "#/components/schemas/CountByWorkState"
        total_count:
          type: integer
          minimum: 0
        next_cursor:
          type: string
        projection:
          $ref: "#/components/schemas/ProcessIntegrityProjectionMetadata"
    ActionCenterCountsResponse:
      type: object
      additionalProperties: false
      required:
        - source
        - counts_by_work_state
        - total_count
        - projection
      properties:
        source:
          type: string
          enum:
            - api
        counts_by_work_state:
          type: array
          items:
            $ref: "#/components/schemas/CountByWorkState"
        total_count:
          type: integer
          minimum: 0
        projection:
          $ref: "#/components/schemas/ProcessIntegrityProjectionMetadata"
    AdherenceSummary:
      type: object
      additionalProperties: false
      required:
        - expected_count
        - completed_count
        - open_gap_count
        - deferred_count
        - process_intact_count
        - adherence_percent
      properties:
        expected_count:
          type: integer
          minimum: 0
        completed_count:
          type: integer
          minimum: 0
        open_gap_count:
          type: integer
          minimum: 0
        deferred_count:
          type: integer
          minimum: 0
        process_intact_count:
          type: integer
          minimum: 0
        adherence_percent:
          type: number
    AdherenceRow:
      type: object
      additionalProperties: false
      required:
        - row_id
        - shed_name
        - operational_location_display
        - expected
        - actual
        - gap
        - severity
        - owner
        - next_action
        - evidence
        - work_state
      properties:
        row_id:
          type: string
        shed_name:
          type: string
        partition_label:
          type:
            - string
            - "null"
          description: >-
            EXISTING field, kept as-is for existing consumers. Historically
            populated with "whole" for unsplit sheds by some producers; new
            code should not rely on that and should treat "whole"/""/null as
            non-partitioned. Nullable, matching that stated contract: the
            previous non-nullable typing contradicted this very description
            and forced clients to model an impossible shape.
        source_shed_name:
          type:
            - string
            - "null"
          description: >-
            Original partition-bearing source name (e.g. "Castro 1"), kept for
            traceability only. Not a display field.
        operational_location_display:
          type: string
          description: >-
            User-facing location label. No partition -> bare shed name
            ("Yashoda"); numeric convention -> "Castro 2"; prefixed convention
            -> "Godel 1 - Part 3".
        expected:
          type: string
        actual:
          type: string
        gap:
          type: string
        severity:
          $ref: "#/components/schemas/ProcessIntegritySeverity"
        owner:
          $ref: "#/components/schemas/ProcessIntegrityOwner"
        next_action:
          type: string
        evidence:
          $ref: "#/components/schemas/ProcessIntegrityEvidence"
        work_state:
          $ref: "#/components/schemas/WorkState"
        drive_capacity_state:
          $ref: "#/components/schemas/DriveCapacityState"
        drive_animals_required:
          type: integer
          minimum: 0
        drive_animals_assigned:
          type: integer
          minimum: 0
        drive_operator_cap:
          type: integer
          minimum: 0
        drive_available_operators:
          type: integer
          minimum: 0
        drive_latest_safe_date:
          type: string
          format: date-time
        drive_medical_defer_reason:
          type: string
    ProtocolAdherenceResponse:
      type: object
      additionalProperties: false
      required:
        - source
        - summary
        - rows
        - total_count
        - projection
      properties:
        source:
          type: string
          enum:
            - api
        summary:
          $ref: "#/components/schemas/AdherenceSummary"
        rows:
          type: array
          items:
            $ref: "#/components/schemas/AdherenceRow"
        total_count:
          type: integer
          minimum: 0
        next_cursor:
          type: string
        projection:
          $ref: "#/components/schemas/ProcessIntegrityProjectionMetadata"
    ControlTowerSummary:
      type: object
      additionalProperties: false
      required:
        - process_intact
        - critical_count
        - warning_count
        - open_gap_count
        - verification_backlog
        - config_or_sop_blockers
      properties:
        process_intact:
          type: boolean
        critical_count:
          type: integer
          minimum: 0
        warning_count:
          type: integer
          minimum: 0
        open_gap_count:
          type: integer
          minimum: 0
        verification_backlog:
          type: integer
          minimum: 0
        config_or_sop_blockers:
          type: integer
          minimum: 0
    ControlTowerAlert:
      type: object
      additionalProperties: false
      required:
        - row_id
        - severity
        - work_state
        - title
        - detail
        - scope_label
        - evidence_summary
        - proof_summary
        - proof_state
        - verification_state
        - park_id
        - park_name
        - shed_id
        - shed_name
        - operational_location_display
        - owner
        - next_action
        - evidence_link
        - obligation_id
      properties:
        row_id:
          type: string
        severity:
          $ref: "#/components/schemas/ProcessIntegritySeverity"
        work_state:
          $ref: "#/components/schemas/WorkState"
        title:
          type: string
        detail:
          type: string
        scope_label:
          type: string
        evidence_summary:
          type: string
        proof_summary:
          type: string
        proof_state:
          $ref: "#/components/schemas/ProcessIntegrityProofState"
        verification_state:
          $ref: "#/components/schemas/ProcessIntegrityVerificationState"
        park_id:
          type: string
          format: uuid
        park_name:
          type: string
        shed_id:
          type: string
          format: uuid
        operational_location_display:
          type: string
          description: >-
            User-facing location label composed by the backend (oploc.Display()).
            No partition -> bare shed name; partitioned -> 'Godel 1 - Part 3'.
            Clients RENDER this; they must not re-compose it.
        shed_name:
          type: string
        partition_label:
          type:
            - string
            - "null"
        drive_name:
          type: string
        owner:
          $ref: "#/components/schemas/ProcessIntegrityOwner"
        next_action:
          type: string
        evidence_link:
          type: string
        drive_capacity_state:
          $ref: "#/components/schemas/DriveCapacityState"
        drive_animals_required:
          type: integer
          minimum: 0
        drive_animals_assigned:
          type: integer
          minimum: 0
        drive_operator_cap:
          type: integer
          minimum: 0
        drive_available_operators:
          type: integer
          minimum: 0
        drive_latest_safe_date:
          type: string
          format: date-time
        drive_medical_defer_reason:
          type: string
        obligation_id:
          type: string
          format: uuid
          description: >-
            The obligation this alert is about. Lets the mobile app target
            POST /app/vaccination/obligations/{obligation_id}/reschedule with a real id.
    ControlTowerResponse:
      type: object
      additionalProperties: false
      required:
        - source
        - summary
        - alerts
        - total_count
        - projection
      properties:
        source:
          type: string
          enum:
            - api
        summary:
          $ref: "#/components/schemas/ControlTowerSummary"
        alerts:
          type: array
          items:
            $ref: "#/components/schemas/ControlTowerAlert"
        total_count:
          type: integer
          minimum: 0
        next_cursor:
          type: string
        projection:
          $ref: "#/components/schemas/ProcessIntegrityProjectionMetadata"
    WorkflowNode:
      type: object
      additionalProperties: false
      required:
        - key
        - label
        - state
      properties:
        key:
          type: string
        label:
          type: string
        state:
          type: string
        timestamp:
          type: string
          format: date-time
        actor:
          type: string
        owner:
          type: string
        evidence:
          type: string
        blocker:
          type: string
    WorkflowDrilldownResponse:
      type: object
      additionalProperties: false
      required:
        - source
        - row
        - nodes
      properties:
        source:
          type: string
          enum:
            - api
        row:
          $ref: "#/components/schemas/ActionCenterObligation"
        nodes:
          type: array
          items:
            $ref: "#/components/schemas/WorkflowNode"
    VaccinationExecutionWorkState:
      type: string
      enum:
        - due
        - overdue
        - scheduled
        - in_progress
        - proof_pending
        - verification_pending
        - rejected
        - deferred
        - missed
        - blocked
        - completed
    VaccinationExecutionSeverity:
      type: string
      enum:
        - ok
        - watch
        - at_risk
        - broken
    VaccinationExecutionSOPStatus:
      type: string
      enum:
        - not_started
        - in_progress
        - submitted
        - accepted
        - rework
    VaccinationExecutionProofStatus:
      type: string
      enum:
        - not_required
        - missing
        - uploaded
        - rejected
        - accepted
    VaccinationExecutionVerificationStatus:
      type: string
      enum:
        - not_ready
        - pending
        - verified
        - rejected
    VaccinationExecutionOwner:
      type: object
      additionalProperties: false
      properties:
        operatorName:
          type: string
        parkHeadName:
          type: string
        verifierName:
          type: string
    VaccinationExecutionRow:
      type: object
      additionalProperties: false
      required:
        - parkId
        - parkName
        - shedId
        - shedName
        - partition_label
        - operational_location_display
        - animalStage
        - targetCount
        - openCount
        - doneCount
        - acceptedCount
        - reviewCount
        - workState
        - severity
        - sopStatus
        - proofStatus
        - verificationStatus
        - nextAction
        - primaryActionKey
        - operatorCanContinue
        - operatorLockedReason
      properties:
        parkId:
          type: string
          format: uuid
        parkName:
          type: string
        shedId:
          type: string
          format: uuid
        shedName:
          type: string
        physicalShed:
          type: string
          description: Normalized physical shed/building name. Partition suffixes such as "Gandhi 1" are exposed separately as partition.
        partition:
          type: string
          description: >-
            DEPRECATED ALIAS — kept for existing consumers; "whole" for unsplit
            sheds. New consumers must use partition_label instead, which is
            null (never the string "whole") when the shed is non-partitioned.
        partition_label:
          type:
            - string
            - "null"
          description: >-
            Raw stored partition label for the shed ('1', 'Part 3'). Null or
            absent means the shed is non-partitioned. Never the literal string
            "whole".
        source_shed_name:
          type:
            - string
            - "null"
          description: >-
            Original partition-bearing source name (e.g. "Castro 1"), kept for
            traceability only. Not a display field.
        operational_location_display:
          type: string
          description: >-
            User-facing location label. No partition -> bare shed name
            ("Yashoda"); numeric convention -> "Castro 2"; prefixed convention
            -> "Godel 1 - Part 3".
        animalStage:
          type: string
        targetCount:
          type: integer
          minimum: 0
          description: Number of distinct current-drive animals represented by this aggregated execution row.
        openCount:
          type: integer
          minimum: 0
          description: Distinct current-drive animals still requiring field execution.
        doneCount:
          type: integer
          minimum: 0
          description: Distinct current-drive animals with completion evidence recorded.
        acceptedCount:
          type: integer
          minimum: 0
          description: Distinct current-drive animals accepted by verification inside this row.
        reviewCount:
          type: integer
          minimum: 0
          description: Distinct current-drive animals with recorded/rejected completion evidence not accepted inside this row.
        driveId:
          type: string
          format: uuid
        driveName:
          type: string
        vaccineLabels:
          type: array
          description: >-
            Vaccine chips for this actual shed card. Counts on the card remain animal-grain:
            if one animal needs multiple vaccines, one proof/video satisfies all listed labels.
          items:
            type: string
        dueDate:
          type: string
          format: date
        workState:
          $ref: "#/components/schemas/VaccinationExecutionWorkState"
        severity:
          $ref: "#/components/schemas/VaccinationExecutionSeverity"
        owner:
          $ref: "#/components/schemas/VaccinationExecutionOwner"
        blockerReason:
          type: string
        sopStatus:
          $ref: "#/components/schemas/VaccinationExecutionSOPStatus"
        proofStatus:
          $ref: "#/components/schemas/VaccinationExecutionProofStatus"
        verificationStatus:
          $ref: "#/components/schemas/VaccinationExecutionVerificationStatus"
        nextAction:
          type: string
        primaryActionKey:
          type: string
          enum:
            - scan
            - none
          description: Backend-owned primary row action. Clients may open Scan only for "scan"; "none" keeps the row on the current vaccination surface unless a backend-owned review/detail action is added.
        obligationId:
          type: string
          format: uuid
        batchId:
          type: string
          format: uuid
        sopTaskId:
          type: string
          format: uuid
        sopVersionId:
          type: string
          format: uuid
        sopTaskRowVersion:
          type: integer
        completionId:
          type: string
          format: uuid
        operatorCanContinue:
          type: boolean
          description: >-
            Backend-owned gate: true iff tapping this card may open the scan/capture flow.
            CORE INVARIANT — only a FINAL SUBMIT locks the card (false); partial
            review/proof/verification state never locks while openCount > 0.
        operatorLockedReason:
          type: string
          enum:
            - none
            - final_submitted
            - assigned_elsewhere
            - scheduled_later
          description: Why operatorCanContinue is false, or "none" when it is true.
    VaccinationProjectionFreshness:
      type: object
      additionalProperties: false
      required:
        - projectionVersion
        - projectedAt
        - asOf
        - status
        - lagSeconds
      properties:
        projectionVersion:
          type: integer
          format: int64
        projectedAt:
          type: string
          format: date-time
        asOf:
          type: string
          format: date-time
        status:
          type: string
        lagSeconds:
          type: integer
          format: int64
          minimum: 0
        servingState:
          type: string
        stale:
          type: boolean
        rebuildRequired:
          type: boolean
        rowCount:
          type: integer
          minimum: 0
    VaccinationExecutionResponse:
      type: object
      additionalProperties: false
      required:
        - source
        - rows
        - totalCount
      properties:
        source:
          type: string
          enum:
            - api
        rows:
          type: array
          items:
            $ref: "#/components/schemas/VaccinationExecutionRow"
        totalCount:
          type: integer
          format: int64
          minimum: 0
        nextCursor:
          type: string
        freshness:
          $ref: "#/components/schemas/VaccinationProjectionFreshness"
        cardSummaries:
          type: object
          additionalProperties:
            $ref: "#/components/schemas/ShedCardSummary"
          description: >-
            Authoritative per-card summaries (status, counts, vaccine groups)
            keyed by execution card ID. Page-independent: computed from all rows
            matching the query, not from paginated subsets. Ensures correct status
            when rows straddle page boundaries.
    VaccineGroupSummary:
      type: object
      additionalProperties: false
      required:
        - label
        - full
      properties:
        label:
          type: string
          description: Display label for the vaccine group
        full:
          type: boolean
          description: True if all animals done and none pending redo
    ShedCardSummary:
      type: object
      additionalProperties: false
      required:
        - shedId
        - status
        - doneCount
        - targetCount
        - openCount
        - needsRedo
        - vaccineGroups
      properties:
        shedId:
          type: string
          format: uuid
        partitionLabel:
          type: string
          nullable: true
          description: Partition identifier if present (null for "whole" shed)
        taskId:
          type: string
          format: uuid
          nullable: true
        batchId:
          type: string
          format: uuid
          nullable: true
        driveId:
          type: string
          format: uuid
          nullable: true
        status:
          type: string
          enum:
            - due
            - overdue
            - missed
            - blocked
            - rejected
            - in_progress
            - proof_pending
            - verification_pending
            - completed
          description: >-
            Authoritative card status computed from all rows.
            Client maps: rejected→SENT_BACK, overdue→DELAYED, completed→DONE, due→PENDING
        doneCount:
          type: integer
          description: Max doneCount across all rows for this card
        targetCount:
          type: integer
          description: Max targetCount across all rows for this card
        openCount:
          type: integer
          description: Max openCount across all rows for this card
        needsRedo:
          type: boolean
          description: True if any row has work sent back (rejected/deferred)
        vaccineGroups:
          type: array
          items:
            $ref: "#/components/schemas/VaccineGroupSummary"
          description: Per-vaccine group summaries for this card
    VaccinationOperationsProtocol:
      type: object
      additionalProperties: false
      required:
        - protocolId
        - name
      properties:
        protocolId:
          type: string
          format: uuid
        name:
          type: string
    VaccinationOperationsCounts:
      type: object
      additionalProperties: false
      description: >-
        Obligation/completion tallies for a cohort × protocol cell (or a cohort rollup), computed as-of
        OperationsQuery.as_of. proofPending = completions recorded and awaiting verification;
        rejected = rework (rejected completions); accepted = verified doses.
      required:
        - overdue
        - due
        - inProgress
        - scheduled
        - missed
        - deferred
        - accepted
        - proofPending
        - rejected
        - total
      properties:
        overdue:
          type: integer
        due:
          type: integer
        inProgress:
          type: integer
        scheduled:
          type: integer
        missed:
          type: integer
        deferred:
          type: integer
        accepted:
          type: integer
        proofPending:
          type: integer
        rejected:
          type: integer
        total:
          type: integer
    VaccinationOperationsCell:
      type: object
      additionalProperties: false
      required:
        - protocolId
        - workState
        - vaccineNames
        - counts
      properties:
        protocolId:
          type: string
          format: uuid
        workState:
          $ref: "#/components/schemas/VaccinationExecutionWorkState"
        lastDose:
          type: string
          format: date-time
        nextDue:
          type: string
          format: date-time
        vaccineNames:
          type: array
          items:
            type: string
        counts:
          $ref: "#/components/schemas/VaccinationOperationsCounts"
    VaccinationOperationsCohort:
      type: object
      additionalProperties: false
      required:
        - parkId
        - parkName
        - shedId
        - shedName
        - stage
        - animals
        - workState
        - counts
        - cells
      properties:
        parkId:
          type: string
          format: uuid
        parkName:
          type: string
        shedId:
          type: string
          format: uuid
        partitionLabel:
          type: string
          nullable: true
          description: >-
            Raw stored partition label for the shed ('1', 'Part 3'). Null or absent
            means the shed is non-partitioned. Never the literal string 'whole' --
            that is a matching key, not user copy.
        operationalLocationDisplay:
          type: string
          description: >-
            User-facing location label composed by the backend (oploc.Display()).
            No partition -> bare shed name; partitioned -> 'Godel 1 - Part 3'.
            Clients RENDER this; they must not re-compose it.
        shedName:
          type: string
        stage:
          type: string
        ageBand:
          type: string
        animals:
          type: integer
        lastDose:
          type: string
          format: date-time
        nextDue:
          type: string
          format: date-time
        workState:
          $ref: "#/components/schemas/VaccinationExecutionWorkState"
        counts:
          $ref: "#/components/schemas/VaccinationOperationsCounts"
        cells:
          type: array
          items:
            $ref: "#/components/schemas/VaccinationOperationsCell"
    VaccinationOperationsResponse:
      type: object
      additionalProperties: false
      required:
        - source
        - protocols
        - cohorts
      properties:
        source:
          type: string
          enum:
            - api
        protocols:
          type: array
          items:
            $ref: "#/components/schemas/VaccinationOperationsProtocol"
        cohorts:
          type: array
          items:
            $ref: "#/components/schemas/VaccinationOperationsCohort"
        next_cursor:
          type: string
        freshness:
          $ref: "#/components/schemas/VaccinationProjectionFreshness"
    VaccinationCommandBoardKPI:
      type: object
      additionalProperties: false
      description: >-
        The board's headline row, at ANIMAL grain. targets counts DISTINCT animals in scope, and
        the six counts below it are a DISJOINT and EXHAUSTIVE partition of targets, so
        missedNotGiven + dosesVerified + awaitingVerification + overdueNotGiven + scheduledAhead +
        closedWithoutDose == targets always. Each animal is placed in exactly one bucket by the
        priority chain missed > verified > awaiting > overdue > scheduled > closedWithoutDose:
        a missed dose wins outright, and below that its most-progressed dose wins. Apart from
        missed the tiles therefore answer "how far has this animal got", not "how much work is
        outstanding"; the outstanding-work question is answered at dose grain by cohortMatrix and
        verificationQueue.
        Every due-date comparison is on the Asia/Kolkata BUSINESS DATE, never an instant, so a
        dose due today never reads overdue merely because as-of is later the same day.
      required:
        - targets
        - missedNotGiven
        - dosesVerified
        - awaitingVerification
        - overdueNotGiven
        - scheduledAhead
        - closedWithoutDose
      properties:
        targets:
          type: integer
          description: >-
            Distinct ANIMALS in scope (the selected drive, or all history when no drive is
            selected). This is the roster size the tiles below partition — not an obligation
            count, so a multi-dose animal counts once.
        missedNotGiven:
          type: integer
          description: >-
            Animals holding at least one obligation in status 'missed' WITH NO COMPLETION AGAINST IT.
            Evaluated FIRST, ahead of dosesVerified, and gated on the absence of a completion — both
            deliberate.
            Leading the chain is necessary because it folds to one row per animal, so while verified
            led it a single accepted dose anywhere in an animal's history swallowed every missed dose
            it also held.
            The no-completion gate is necessary because an obligation swept to 'missed' that carries
            a recorded completion was DOSED. On the live tenant all 137 such obligations were
            administered on the exact day they were due and are waiting on a verifier; counting them
            here reported 137 vaccinated animals as unvaccinated while awaitingVerification
            simultaneously read 0. missedNotGiven means no dose reached the animal. Proof waiting in
            the verification queue is a desk backlog and surfaces as the shed x vaccine matrix's
            'verifying' state, never here. A missed dose is the failure this
            board exists to report, so it outranks every state an animal can simultaneously be in.
        dosesVerified:
          type: integer
          description: >-
            Animals with at least one verifier-accepted completion and NO missed obligation.
        awaitingVerification:
          type: integer
          description: >-
            Animals with a recorded completion not yet verifier-accepted (status=recorded,
            verified_at=null) and no accepted completion.
        overdueNotGiven:
          type: integer
          description: >-
            Animals with no completion at all whose earliest open obligation was due before the
            as-of IST business date.
        scheduledAhead:
          type: integer
          description: >-
            Animals with no completion at all whose open obligations are all due on or after the
            as-of IST business date.
        closedWithoutDose:
          type: integer
          description: >-
            Animals whose every obligation closed with no completion recorded against it
            (canceled, waived, superseded). They belong to the drive's roster, so they count in
            targets, but no dose was given and none is outstanding. Named explicitly because
            without it the tiles summed to LESS than targets and a reader could not tell whether
            the gap was a bug, missing data, or real outstanding work. Defined as the residual of
            the other four, so the partition stays exhaustive as statuses change.
    VaccinationCommandBoardCohort:
      type: object
      additionalProperties: false
      required:
        - parkId
        - parkName
        - managementStage
        - sex
        - animalCount
      properties:
        parkId:
          type: string
          description: >-
            Farm (park) this cohort sits on. The matrix is read farmwise, so the same cohort on
            two farms stays two cells. Empty when the obligation's shed has no resolvable parent.
        parkName:
          type: string
          description: Farm display name, from canonical locations.
        managementStage:
          type: string
          description: Management stage from goat classification (e.g., Buck, Non-Pregnant Female).
        sex:
          type: string
          enum:
            - male
            - female
        animalCount:
          type: integer
    VaccinationCommandBoardCohortCell:
      type: object
      additionalProperties: false
      description: >-
        One farm × cohort × dose-qualified-vaccine cell, at OBLIGATION grain
        (COUNT(DISTINCT obligation_id)). pendingCount, submittedCount and verifiedCount are a
        DISJOINT partition of the cell's obligations along "who owes the next move": the operator,
        the verifier, nobody. pendingCount previously fused the first two, because obligation
        status advances only on VERIFICATION and never on submission — a park whose every animal
        had been vaccinated and submitted rendered byte-identically to a park nobody had touched,
        and the page showed "40 awaiting verification" in the KPI row above "40 pending" in this
        matrix with no column reconciling them. submittedCount is that reconciling column.
        GRAIN NOTE: these counts are obligation grain while VaccinationCommandBoardKPI is animal
        grain; the two agree exactly at one-obligation-per-animal-per-vaccine, the grain every live
        drive uses, and the matrix stays obligation grain by design so a multi-vaccine animal is
        visible once per vaccine.
      required:
        - cohort
        - vaccineLabel
        - pendingCount
        - submittedCount
        - verifiedCount
        # Always emitted by the server (no omitempty on the Go field): zero exceptions is the
        # answer "this cohort is clean", not "the server did not say". Optional here would make
        # the generated client type it `number | undefined` and invite a client-side default.
        - missingPriorDoseCount
      properties:
        cohort:
          $ref: "#/components/schemas/VaccinationCommandBoardCohort"
        vaccineLabel:
          type: string
          description: Human-readable vaccine label (e.g., ET+TT, PPR · Booster).
        pendingCount:
          type: integer
          description: >-
            Field work the OPERATOR still owes: obligations in this cohort/vaccine that are open
            (scheduled, due, in_progress, deferred, missed), due on or before the as-of IST
            business date, and carry NO recorded completion. Does NOT include submitted work —
            counting submitted animals here is what made a fully vaccinated park look untouched.
        submittedCount:
          type: integer
          description: >-
            Field work DONE and awaiting a verifier: obligations with a recorded completion that
            is not yet verifier-accepted. Reconciles with
            VaccinationCommandBoardKPI.awaitingVerification (same predicate, animal grain there).
            Disjoint from pendingCount and verifiedCount.
        verifiedCount:
          type: integer
          description: >-
            Obligations in this cohort whose dose for this vaccine is verifier-accepted. Disjoint
            from pendingCount and submittedCount — an accepted obligation is neither
            open-unrecorded nor recorded-but-unverified — so the three may be displayed side by
            side without double counting.
        minAdministeredDate:
          type: string
          format: date-time
          description: Earliest actual operator-administered date among accepted vaccinations in this cohort cell.
        maxAdministeredDate:
          type: string
          format: date-time
          description: Latest actual operator-administered date among accepted vaccinations in this cohort cell.
        administeredDays:
          type: array
          items:
            $ref: "#/components/schemas/VaccinationCommandBoardCohortDay"
          description: >-
            Per-IST-business-day split of this cell's verified doses, ascending. A
            minAdministeredDate..maxAdministeredDate span of "30 Jun-1 Jul" hides that 84 animals
            were dosed on the first day and 237 on the second; this array carries that split so
            leadership reads the actual operator story rather than a range.
        missingPriorDoseCount:
          type: integer
          description: >-
            Dose-sequence EXCEPTION count for this cell: animals of this cohort holding an accepted
            LATER dose of the same vaccine course while THIS dose has no accepted completion (for
            example an accepted ET+TT Dose 2 with no accepted Dose 1). Whole-cohort truth, never
            capped. Cohort scope and key set are identical to verifiedCount, so "321 verified · 3
            exceptions" compares like with like.
        missingPriorDoseGoats:
          type: array
          items:
            $ref: "#/components/schemas/VaccinationCommandBoardCohortAnimal"
          description: >-
            The animals behind missingPriorDoseCount, capped at 25 per cell so a cell can never
            return an unbounded list. missingPriorDoseCount remains the full count when capped.
    VaccinationCommandBoardClosedWithoutDoseAnimal:
      type: object
      additionalProperties: false
      description: >-
        One animal behind the Closed, No Dose tile, with the identity and ground location a park
        head needs to act on it.
      required:
        - goatId
        - displayId
        - operational_location_display
        - parkName
        - shedName
        - reason
        - vaccineLabel
      properties:
        goatId:
          type: string
          format: uuid
        displayId:
          type: string
        tag1:
          type: string
          description: >-
            The animal's physical tag (animal_identifier_1). This is the identity the FARM uses;
            displayId is an internal Goat OS id and is only a fallback label.
        tag2:
          type: string
          description: Second physical tag (animal_identifier_2) when the animal carries two.
        operational_location_display:
          type: string
          description: >-
            Farm-readable OPERATIONAL location — park, physical shed, and partition when the shed
            has one ("Castro 2", "Godel 1 - Part 3"). Never the bare parent shed name for an animal
            standing in a partition, and never the "whole" matching sentinel. Backend-composed via
            oploc.Display(); clients render verbatim.
        parkName:
          type: string
        shedName:
          type: string
          description: Physical shed name. Not a ground location on its own when a partition exists.
        partitionLabel:
          type: string
          description: Raw stored partition label, absent for a non-partitioned shed.
        reason:
          type: string
          description: >-
            Why the work closed with no dose, in farm language (Cancelled, Waived, Superseded,
            Deferred for recovery). Never a raw obligation status token.
        vaccineLabel:
          type: string
          description: Human dose label whose obligation closed (e.g. "ET+TT · Dose 2").
    VaccinationCommandBoardCohortDay:
      type: object
      additionalProperties: false
      description: One business day of accepted administration inside a cohort x dose cell.
      required:
        - date
        - animalCount
      properties:
        date:
          type: string
          format: date
          description: IST business date the dose actually went in.
        animalCount:
          type: integer
          description: DISTINCT animals dosed on that date in this cell.
    VaccinationCommandBoardCohortAnimal:
      type: object
      additionalProperties: false
      description: One animal behind a cohort cell exception, in farm-readable identity.
      required:
        - goatId
        - displayId
      properties:
        goatId:
          type: string
          format: uuid
        displayId:
          type: string
        tag:
          type: string
          description: Primary visible tag when the animal has one.
    ShedDoseMatrixCell:
      type: object
      additionalProperties: false
      required:
        - shedId
        - shedName
        - doseRule
        - state
        - animalCount
      properties:
        shedId:
          type: string
          format: uuid
        shedName:
          type: string
        partition_label:
          type:
            - string
            - "null"
          description: >-
            Raw stored partition label for shedId ('1', 'Part 3'). Null or
            absent means the shed is non-partitioned. Never the literal
            string "whole".
        source_shed_name:
          type:
            - string
            - "null"
          description: >-
            Original partition-bearing source name (e.g. "Castro 1"), kept for
            traceability only. Not a display field.
        operational_location_display:
          type: string
          description: >-
            User-facing location label. No partition -> bare shed name
            ("Yashoda"); numeric convention -> "Castro 2"; prefixed convention
            -> "Godel 1 - Part 3".
        doseRule:
          type: string
          description: Dose rule identifier (e.g., et_tt_adult_w1) or human label.
        state:
          type: string
          enum:
            - verified
            - awaiting
            - overdue
            - scheduled
          description: State of completion (verified), awaiting (recorded-unverified), overdue, or scheduled.
        animalCount:
          type: integer
          description: Number of unique animals with this state in this shed for this dose.
        minAdministeredDate:
          type: string
          format: date-time
          description: Earliest administered_at date for verified/awaiting completions.
        maxAdministeredDate:
          type: string
          format: date-time
          description: Latest administered_at date for verified/awaiting completions.
        minDueDate:
          type: string
          format: date-time
          description: Earliest due_at date for scheduled obligations.
        maxDueDate:
          type: string
          format: date-time
          description: Latest due_at date for scheduled obligations.
    WeeklyGivenRow:
      type: object
      additionalProperties: false
      required:
        - isoYear
        - isoWeek
        - vaccineLabel
        - completionStatus
        - count
        - minAdministeredAt
        - maxAdministeredAt
      properties:
        isoYear:
          type: integer
          description: ISO 8601 week year.
        isoWeek:
          type: integer
          description: ISO 8601 week number (1-53).
        vaccineLabel:
          type: string
          description: Human-readable vaccine label.
        completionStatus:
          type: string
          enum:
            - accepted
            - recorded
          description: Completion status (accepted=verified, recorded=recorded-unverified).
        count:
          type: integer
          description: Number of completions this week for this vaccine and status.
        minAdministeredAt:
          type: string
          format: date-time
          description: Earliest administered_at this week.
        maxAdministeredAt:
          type: string
          format: date-time
          description: Latest administered_at this week.
    VerificationQueueRow:
      type: object
      additionalProperties: false
      required:
        - shedId
        - shedName
        - doseRule
        - awaitingCount
        - totalCount
      properties:
        shedId:
          type: string
          format: uuid
        shedName:
          type: string
        partition_label:
          type:
            - string
            - "null"
          description: >-
            Raw stored partition label for shedId ('1', 'Part 3'). Null or
            absent means the shed is non-partitioned. Never the literal
            string "whole".
        source_shed_name:
          type:
            - string
            - "null"
          description: >-
            Original partition-bearing source name (e.g. "Castro 1"), kept for
            traceability only. Not a display field.
        operational_location_display:
          type: string
          description: >-
            User-facing location label. No partition -> bare shed name
            ("Yashoda"); numeric convention -> "Castro 2"; prefixed convention
            -> "Godel 1 - Part 3".
        doseRule:
          type: string
          description: Dose rule identifier or human label.
        awaitingCount:
          type: integer
          description: Count of completions awaiting verification (status=recorded, verified_at=null).
        totalCount:
          type: integer
          description: Total count of obligations for this shed × dose.
        lastGivenOnDate:
          type: string
          format: date-time
          description: Latest administered_at date for awaiting completions.
        daysInQueue:
          type: integer
          description: Business days since earliest administered_at.
    VaccinationCommandBoardDriveOption:
      type: object
      additionalProperties: false
      required:
        - driveBatchId
        - driveName
        - label
        - status
        - targetCount
        - doseCount
        - shedNames
      properties:
        driveBatchId:
          type: string
          format: uuid
          description: Stable drive identity — the obligation batch. A rule id alone is not a drive selector.
        parkId:
          type: string
          description: >-
            Park the drive's work is in. Row grain is (batch, park), not batch alone: on an
            all-parks board two same-vaccine, same-window drives in different parks are two
            operator days and must be offered — and counted — separately. Empty when the
            drive's obligations resolve to no park.
        parkName:
          type: string
          description: Display name of parkId, so the selector can label a drive without a second lookup.
        driveName:
          type: string
          description: Vaccine-name-only logical drive label, with initial/repeat rule rows collapsed.
        label:
          type: string
          description: >-
            Operator-facing executable-day label: vaccine names, planned date, distinct animal count,
            and status. Never a raw config token such as et_tt_adult_w2.
        status:
          type: string
        plannedDate:
          type: string
          format: date-time
        windowStart:
          type: string
          format: date-time
        windowEnd:
          type: string
          format: date-time
        targetCount:
          type: integer
          description: Distinct animals assigned to this executable operator day.
        doseCount:
          type: integer
          description: Distinct vaccine-dose obligations assigned to this executable operator day.
        operatorDays:
          type: array
          items:
            type: object
            additionalProperties: false
            required: [date, targetCount, doseCount]
            properties:
              date:
                type: string
              targetCount:
                type: integer
              doseCount:
                type: integer
          description: Per-business-day execution totals for completed or in-progress drives.
        shedNames:
          type: array
          items:
            type: string
          description: Whole physical sheds assigned to this executable operator day.
        shedIds:
          type: array
          items:
            type: string
            format: uuid
          description: Location IDs of the physical sheds assigned to this executable operator day.
        shedLocations:
          type: array
          description: Operational shed locations assigned to this executable operator day. Partitioned sheds appear once per real partition, using shedId + partition_label as identity and operational_location_display for UI labels.
          items:
            type: object
            additionalProperties: false
            required: [shedId, shedName, operational_location_display]
            properties:
              shedId:
                type: string
                format: uuid
              shedName:
                type: string
              partition_label:
                type: string
              operational_location_display:
                type: string
    CommandBoardVaccineColumn:
      type: object
      additionalProperties: false
      description: One column of the shed x vaccine matrix.
      required:
        - code
        - label
      properties:
        code:
          type: string
          description: The protocol vaccine code the cells are keyed by (ET_TT, SHEEP_POX).
        label:
          type: string
          description: >-
            The header text. Empty when the canonical labeller does not recognise the code — clients
            should then fall back to showing the code, since a catalogue vaccine the label table has
            not caught up with is a gap worth seeing, not one worth hiding.
    CommandBoardShedVaccineCell:
      type: object
      additionalProperties: false
      description: >-
        One shed x vaccine cell with every dose of that vaccine collapsed into a single flag. The
        question it answers is the one a park head asks walking into a shed: "is anything behind
        here, for this vaccine, today" — not how many, not which dose, not what is scheduled next
        quarter. behindAnimals exists only so the flag can be explained on hover; the cell's meaning
        is state.
      required:
        - shedId
        - shedName
        - vaccineCode
        - state
        - behindAnimals
        - totalAnimals
      properties:
        shedId:
          type: string
          description: >-
            The cell's IDENTITY. Clients MUST group rows on this, never on shedName. The live tenant
            runs 175 sheds under 99 distinct names ("Godel 1" exists in two parks), so grouping by
            name merges two parks' sheds into one row and attributes one park's red cell to the
            other park's shed.
        shedName:
          type: string
          description: A display label, not an identity. See shedId.
        partition_label:
          type: string
          description: Raw stored partition label for shedId. Omitted for unpartitioned sheds.
        operational_location_display:
          type: string
          description: Backend-owned display label for shed plus partition.
        parkName:
          type: string
          description: >-
            The shed's park, carried so two same-named sheds in different parks can be told apart on
            screen. Clients should show it whenever a shed name is not unique in the payload.
        vaccineCode:
          type: string
        state:
          type: string
          enum:
            - behind
            - verifying
            - ok
            - not_planned
          description: >-
            behind — at least one animal in this shed holds a dose of this vaccine that is 'missed',
            or is still open with its due IST business date already past, and has no accepted
            completion. RED.
            ok — this vaccine is scheduled in this shed and nothing is behind. GREEN.
            not_planned — this shed has no obligation for this vaccine at all. GREY, and named
            rather than omitted: a blank cell told the reader nothing about whether the vaccine was
            clean, un-generated, or genuinely out of protocol for that shed.
            'behind' is deliberately broader than the KPI row's missedNotGiven bucket. An operator
            standing in the shed cannot act on the difference between "the sweeper has flipped this
            to missed" and "the sweeper has not run yet" — both mean the animal is unvaccinated past
            its window.
        verifyingAnimals:
          type: integer
          description: >-
            Animals whose dose WAS GIVEN, whose proof is recorded, and whose verifier has not
            accepted it yet. Never added to behindAnimals: one is a herd problem and the other is a
            desk problem. Merging them reported 76 vaccinated goats in Sumathi 1 as unvaccinated,
            because every obligation swept to 'missed' there had in fact been dosed on the day it
            was due.
        behindAnimals:
          type: integer
          description: >-
            DISTINCT animals behind for this shed and vaccine, and the number rendered ON the red
            cell. "How many are missing, each vaccine, shed wise" is half the ask this matrix
            answers; a bare colour answers only "is anything wrong" and forces a second question
            before the row is actionable. Because it counts DISTINCT ANIMALS rather than summing
            doses it can never exceed the shed's head count. Clients must still key the cell's
            COLOUR off state, not off this number being non-zero.
        totalAnimals:
          type: integer
          description: >-
            DISTINCT animals in this shed carrying any obligation for this vaccine. behindAnimals is
            a subset of the same key set, so behindAnimals <= totalAnimals always.
        proofVideos:
          type: array
          items:
            $ref: "#/components/schemas/CommandBoardShedVideo"
          description: >-
            The shed's vaccination clips for the day these doses were recorded. SHED-and-day grain,
            not per animal: proof is filmed per shed for the operator day (Sumathi 1 has five clips
            covering 76 goats), so attaching one to every animal repeats a single link 76 times and
            implies per-goat footage that does not exist. Absent when nothing was filmed, which is a
            finding — a verification queue with nothing to watch cannot be drained.
        flaggedAnimals:
          type: array
          items:
            $ref: "#/components/schemas/CommandBoardShedVaccineAnimal"
          description: >-
            The animals behind BOTH flagged states — genuinely behind AND waiting on a verifier —
            capped across all flagged cells in one board read. Each row carries
            awaitingVerification, which is what tells the two apart; clients must read that rather
            than infer it from the cell's state.
            Previously named behindAnimalsList and documented as behind-only while it already
            carried verifier-backlog rows, so the field name asserted the opposite of the payload.
            Evidence for the flag, not the flag itself: behindAnimals and verifyingAnimals stay
            whole-scope truth, so when this list is shorter than their sum the client must say the
            list is partial rather than present it as complete.
    CommandBoardShedVideo:
      type: object
      additionalProperties: false
      description: One proof clip a verifier has to watch, at shed-and-day grain.
      required:
        - path
      properties:
        path:
          type: string
          description: >-
            Playback path, never a bare id — the signed GCS URL is minted per request by the proof
            service.
        uploadedAt:
          type: string
          format: date-time
        durationMs:
          type: integer
          format: int64
    CommandBoardShedVaccineAnimal:
      type: object
      additionalProperties: false
      description: One animal behind a shed x vaccine cell.
      required:
        - goatId
        - displayId
        - tag
        - status
        - awaitingVerification
        - locationDisplay
      properties:
        locationDisplay:
          type: string
          description: >-
            The animal's GROUND location — park, physical shed, and partition when the shed has one
            ("Godel 1 - Part 3"). The shed name alone is not a location on a partitioned shed: it
            sends a person to the wrong pen.
        partitionLabel:
          type: string
          description: >-
            Present only when the shed is genuinely partitioned. Absent on an unpartitioned shed —
            the "whole" sentinel is a grouping key, never display copy.
        awaitingVerification:
          type: boolean
          description: >-
            The dose was GIVEN and its proof is queued for a verifier. Clients must say so rather
            than render the raw obligation status: an animal dosed on its due date still reads
            status 'missed' when the sweeper closed the obligation before a verifier looked at the
            proof, and showing that word accuses an operator who did the work on time.
        recordedAt:
          type: string
          format: date-time
          description: When the operator recorded the dose.
        goatId:
          type: string
        displayId:
          type: string
          description: >-
            The INTERNAL Goat OS id. A fallback label only, never the animal's identity — an
            operator sent to a shed cannot act on a UUID.
        tag:
          type: string
          description: >-
            The animal's first EAR TAG — what identifies it standing in the shed. Empty only when the
            animal carries no active identifier at all.
        tag2:
          type: string
          description: >-
            The second ear tag. Most of this herd carries two (1004 of 1670 goats, and 172 carry
            three), so a client showing only `tag` leaves an operator reading the other ear unable to
            match the animal to the row.
        status:
          type: string
          description: >-
            The obligation status behind the flag. 'missed' and a still-open past-due status are the
            same red to a park head but different facts to whoever has to fix it.
        dueAt:
          type: string
          format: date-time
    VaccinationLiveTrackerParkCount:
      type: object
      additionalProperties: false
      required: [park_id, park_name, park_code, count]
      properties:
        park_id: { type: string, format: uuid }
        park_name: { type: string }
        park_code:
          type: string
          description: >-
            Compact location code ("CBE"/"CPT"). Empty when the park has no location_code seeded;
            consumers fall back to park_name.
        count:
          type: integer
          description: Scheduled ADMINISTRATIONS in this park on the drive day.
    VaccinationLiveTrackerKPIs:
      type: object
      additionalProperties: false
      required:
        - scheduled_administrations
        - scheduled_by_park
        - proof_videos_received
        - closed_administrations
        - awaiting_close
        - scan_captures
        - remaining
        - combo_animals
        - attention_count
        - active_parks
      properties:
        scheduled_administrations:
          type: integer
          description: >-
            ADMINISTRATION grain — one obligation is one administration, counted over
            protocol-category 'vaccination' obligations only, excluding the dead statuses
            canceled/superseded/waived.

            Aggregated over the WHOLE drive day, independent of the row caps below: it is computed by
            a window aggregate over the untruncated rollup, not folded from the returned rows, so it
            stays exact when sheds_truncated / operators_truncated / cells_truncated are true. (Under
            an explicit status filter it describes the filtered subset instead, because a status
            filter is a narrowing of the day.)

            Reconciliation: equals the sum of the SHED board's scheduled column for the same filter
            set, provided sheds_truncated and cells_truncated are both false. The OPERATOR board's
            scheduled column additionally omits unassigned_administrations — administrations in a
            shed/partition with no vaccination_drive_assignments row for the day have no operator to
            be attributed to — so operators sum + unassigned_administrations = this tile. The page
            renders that residual as a visible note under the operator table.
        scheduled_by_park:
          type: array
          items: { $ref: "#/components/schemas/VaccinationLiveTrackerParkCount" }
        proof_videos_received:
          type: integer
          description: >-
            ADMINISTRATION grain — administrations whose animal has a completed vaccination video
            proof on the drive day. On a combo day ONE uploaded video covers two obligations and
            contributes 2 here, which is why the tile's caption reads "administrations proofed" and
            not "videos landed".
        closed_administrations:
          type: integer
          description: >-
            ADMINISTRATION grain — administrations whose obligation_instances row reached
            status='completed'. Proof ARRIVAL and obligation CLOSURE are different facts and both are
            reported: in stg on 2026-08-12 all 298 proof videos had landed while 9 of 298 obligations
            were completed, so a board deriving progress from proof arrival read as a finished drive
            on a drive that was still open.
        awaiting_close:
          type: integer
          description: proof_videos_received minus closed_administrations, floored at zero.
        scan_captures:
          type: integer
          description: >-
            ADMINISTRATION grain — administrations whose animal has at least one RFID scan capture on
            the drive day. This is NOT a count of sop_task_scan_captures rows: an animal scanned three
            times contributes 1, and a combo animal scanned once contributes 2. The tile is labelled
            "RFID confirmed / administrations with a scan" for exactly that reason; the operator
            board's scans column is the physical capture count and is a different figure by design.
        remaining:
          type: integer
          description: >-
            scheduled_administrations minus closed_administrations, floored at zero — what the drive
            still owes. It is NOT scheduled minus proofed: that measured proof arrival and let the
            page read "Remaining 0" while the obligations were still open.
        combo_animals:
          type: integer
          description: ANIMAL grain — animals carrying two or more distinct same-day obligations.
        attention_count:
          type: integer
          description: Exactly len(attention). Never a constant.
        active_parks:
          type: integer
          description: Distinct parks that produced proof or scan evidence on the drive day.
    VaccinationLiveTrackerOperatorRow:
      type: object
      additionalProperties: false
      required:
        - operator_id
        - operator_name
        - operator_display_code
        - identity_resolved
        - park_id
        - park_name
        - park_code
        - current_shed_id
        - current_shed_label
        - current_partition_label
        - current_vaccine_label
        - scheduled_administrations
        - proof_videos
        - scan_captures
        - closed_administrations
        - remaining
        - last_activity_at
        - idle_minutes
        - state
      properties:
        operator_id: { type: string }
        operator_name: { type: string }
        operator_display_code:
          type: string
          description: >-
            Seeded workforce display code. May be an "auth:<uuid>" placeholder in environments where
            operator identity is only partially seeded; identity_resolved reports which.
        identity_resolved: { type: boolean }
        park_id: { type: string }
        park_name: { type: string }
        park_code: { type: string }
        current_shed_id: { type: string }
        current_shed_label: { type: string }
        current_partition_label: { type: string }
        current_vaccine_label: { type: string }
        scheduled_administrations: { type: integer }
        proof_videos:
          type: integer
          description: >-
            PHYSICAL count of proof_artifacts rows this person uploaded today — display only. On a
            combo day one video covers two obligations, so this is deliberately NOT the same grain as
            scheduled_administrations and must never be subtracted from it.
        scan_captures:
          type: integer
          description: PHYSICAL count of sop_task_scan_captures rows this person captured today.
        closed_administrations:
          type: integer
          description: >-
            This operator's assigned administrations that reached status='completed' — the same
            OBLIGATION grain as scheduled_administrations, which is what makes remaining a
            subtraction of like from like.
        remaining:
          type: integer
          description: scheduled_administrations minus closed_administrations, floored at zero.
        last_activity_at: { type: string, format: date-time, nullable: true }
        idle_minutes: { type: integer, nullable: true }
        state:
          type: string
          enum: [active, done, not_started, idle]
    VaccinationLiveTrackerShedRow:
      type: object
      additionalProperties: false
      required:
        - shed_id
        - shed_name
        - physical_shed
        - partition_label
        - shed_label
        - operational_location_display
        - park_id
        - park_name
        - vaccine_code
        - vaccine_label
        - operator_id
        - operator_name
        - scheduled_administrations
        - closed_administrations
        - proof_videos_received
        - remaining
        - last_proof_at
        - extra_attempt_count
        - state
      properties:
        shed_id: { type: string }
        shed_name: { type: string }
        physical_shed: { type: string }
        partition_label: { type: string }
        shed_label: { type: string }
        operational_location_display: { type: string }
        park_id: { type: string }
        park_name: { type: string }
        vaccine_code: { type: string }
        vaccine_label: { type: string }
        operator_id: { type: string }
        operator_name: { type: string }
        scheduled_administrations: { type: integer }
        closed_administrations:
          type: integer
          description: Administrations in this cell whose obligation reached status='completed'.
        proof_videos_received:
          type: integer
          description: >-
            Administrations whose animal has a completed video proof today. Proof ARRIVAL, not
            closure — remaining is derived from closed_administrations.
        remaining:
          type: integer
          description: scheduled_administrations minus closed_administrations, floored at zero.
        last_proof_at: { type: string, format: date-time, nullable: true }
        extra_attempt_count:
          type: integer
          description: Duplicate / not-due / unknown scan attempts recorded against this shed's animals today.
        state:
          type: string
          enum: [receiving, slow, done, not_started, review]
    VaccinationLiveTrackerComboDose:
      type: object
      additionalProperties: false
      required: [obligation_id, vaccine_code, vaccine_label, state]
      properties:
        obligation_id: { type: string }
        vaccine_code: { type: string }
        vaccine_label: { type: string }
        state:
          type: string
          description: >-
            A pure function of the OBLIGATION's own status, with proof arrival reported separately.
            verification_pending means a proof landed for this animal today and the obligation is not
            closed yet — the same fact the sibling execution read model already calls
            verification_pending. It is never inferred that a proof closed the obligation.
          enum: [closed, verification_pending, awaiting_proof, scheduled]
    VaccinationLiveTrackerComboRow:
      type: object
      additionalProperties: false
      required:
        - goat_id
        - display_id
        - primary_tag
        - secondary_tag
        - shed_label
        - proof_state
        - proof_count
        - doses
      properties:
        goat_id: { type: string }
        display_id: { type: string }
        primary_tag:
          type: string
          description: The animal's REAL active primary identifier. No synthetic id is ever generated.
        secondary_tag:
          type: string
          description: Second active tag where the animal is dual-tagged; empty otherwise.
        shed_label: { type: string }
        proof_state:
          type: string
          enum: [video, uploading, none]
        proof_count: { type: integer }
        doses:
          type: array
          items: { $ref: "#/components/schemas/VaccinationLiveTrackerComboDose" }
    VaccinationLiveTrackerCombo:
      type: object
      additionalProperties: false
      required: [animal_count, vaccine_labels, rows, rows_truncated]
      properties:
        animal_count:
          type: integer
          description: Exact total combo ANIMALS, computed before the row cap.
        vaccine_labels:
          type: array
          items: { type: string }
        rows:
          type: array
          items: { $ref: "#/components/schemas/VaccinationLiveTrackerComboRow" }
        rows_truncated: { type: boolean }
    VaccinationLiveTrackerActivityItem:
      type: object
      additionalProperties: false
      required:
        - event_id
        - occurred_at
        - kind
        - actor_id
        - actor_name
        - park_name
        - shed_label
        - vaccine_label
        - goat_id
        - goat_display_id
        - scanned_identifier
        - detail_code
      properties:
        event_id:
          type: string
          description: "<kind>:<primary key> — stable dedupe key across polls."
        occurred_at: { type: string, format: date-time }
        kind:
          type: string
          description: >-
            obligation_closed is one row per obligation_status_events row with event_type='completed'
            — PER-ANIMAL obligation closure. It was previously labelled shed_submitted, a shed-level
            name on an animal-grain count; a real shed submission lives in sop_submissions and is not
            read by this feed.
          enum: [proof_video, scan_capture, scan_duplicate, scan_unknown, administration, obligation_closed]
        actor_id: { type: string }
        actor_name: { type: string }
        park_name: { type: string }
        shed_label: { type: string }
        vaccine_label:
          type: string
          description: >-
            EVERY same-day dose the animal is on, joined with " + ". A combo animal's single video
            covers both antigens, so naming only one of them silently dropped the other from the row
            that reports it.
        goat_id: { type: string }
        goat_display_id: { type: string }
        scanned_identifier:
          type: string
          description: The tag actually scanned, or the animal's active primary identifier. Never generated.
        detail_code: { type: string }
    VaccinationLiveTrackerActivity:
      type: object
      additionalProperties: false
      required: [items, next_cursor, next_cursor_event_id, observed_per_min, window_minutes]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/VaccinationLiveTrackerActivityItem" }
        next_cursor:
          type: string
          format: date-time
          nullable: true
          description: >-
            Timestamp half of the next page's keyset cursor. Must be sent back together with
            next_cursor_event_id; the feed's sort key is (occurred_at DESC, event_id DESC) and
            paging on the timestamp alone loses every event tied with this boundary.
        next_cursor_event_id:
          type: string
          nullable: true
          description: Tiebreaker half of the next page's keyset cursor. Null exactly when next_cursor is null.
        observed_per_min:
          type: number
          nullable: true
          description: >-
            Events per minute OBSERVED over the returned window. Null when fewer than two events were
            returned — the rate is measured, never assumed.
        window_minutes: { type: integer }
    VaccinationLiveTrackerAttentionRow:
      type: object
      additionalProperties: false
      required:
        - kind
        - subject_label
        - operator_id
        - shed_id
        - partition_label
        - operational_location_display
        - metric_count
        - total_count
        - elapsed_minutes
        - since_at
        - severity
      properties:
        kind:
          type: string
          enum: [extra_attempts, idle_operator, slow_shed]
        subject_label: { type: string }
        operator_id: { type: string }
        shed_id: { type: string }
        partition_label: { type: string }
        operational_location_display: { type: string }
        metric_count: { type: integer }
        total_count: { type: integer }
        elapsed_minutes: { type: integer }
        since_at: { type: string, format: date-time, nullable: true }
        severity:
          type: string
          enum: [warn, dng]
    VaccinationLiveTrackerVerification:
      type: object
      additionalProperties: false
      required:
        - awaiting_review_items
        - awaiting_review_sheds
        - verified_today_items
        - verified_today_sheds
        - rework_requested
      properties:
        awaiting_review_items: { type: integer }
        awaiting_review_sheds: { type: integer }
        verified_today_items: { type: integer }
        verified_today_sheds: { type: integer }
        rework_requested: { type: integer }
    VaccinationLiveTrackerFilterOption:
      type: object
      additionalProperties: false
      required: [id, code, partition_label, label]
      properties:
        id: { type: string }
        code: { type: string }
        partition_label: { type: string }
        label: { type: string }
    VaccinationLiveTrackerFilterOptions:
      type: object
      additionalProperties: false
      required: [parks, vaccines, operators, sheds, truncated]
      description: >-
        The filter bar's vocabulary, compiled from the day's own rows with only the AUTHORIZATION park
        scope applied (the caller's own park selection is deliberately dropped so the park control
        cannot self-collapse). Because it comes from real administrations, an option that matches zero
        rows cannot be offered, and choosing one operator does not collapse the operator list.

        Each kind carries its own server-side cap. A single shared cap across the union was spent in
        kind-name order, which destroyed the vaccine list entirely before any other list lost a row.
      properties:
        parks:
          type: array
          items: { $ref: "#/components/schemas/VaccinationLiveTrackerFilterOption" }
        vaccines:
          type: array
          items: { $ref: "#/components/schemas/VaccinationLiveTrackerFilterOption" }
        operators:
          type: array
          items: { $ref: "#/components/schemas/VaccinationLiveTrackerFilterOption" }
        sheds:
          type: array
          items: { $ref: "#/components/schemas/VaccinationLiveTrackerFilterOption" }
        truncated:
          type: boolean
          description: True when any option kind hit its own cap, so the filter bar is incomplete.
    VaccinationLiveTrackerResponse:
      type: object
      additionalProperties: false
      required:
        - business_date
        - generated_at
        - is_live_day
        - kpis
        - operators
        - sheds
        - combo
        - operators_total
        - operators_truncated
        - sheds_total
        - sheds_truncated
        - cells_truncated
        - unassigned_administrations
        - activity
        - attention
        - attention_total
        - attention_truncated
        - verification
        - filter_options
      properties:
        business_date: { type: string, format: date }
        generated_at: { type: string, format: date-time }
        is_live_day:
          type: boolean
          description: >-
            True only when business_date is today in business time. A past drive day is a legitimate
            read on this route, and consumers must not render it under a live badge or keep polling it.
        freshness: { $ref: "#/components/schemas/VaccinationProjectionFreshness" }
        kpis: { $ref: "#/components/schemas/VaccinationLiveTrackerKPIs" }
        operators:
          type: array
          items: { $ref: "#/components/schemas/VaccinationLiveTrackerOperatorRow" }
        sheds:
          type: array
          items: { $ref: "#/components/schemas/VaccinationLiveTrackerShedRow" }
        combo: { $ref: "#/components/schemas/VaccinationLiveTrackerCombo" }
        operators_total:
          type: integer
          description: Operator rows BEFORE the server-side cap. Equals operators.length when operators_truncated is false.
        operators_truncated:
          type: boolean
          description: >-
            True when the operator board was cut to its server-side cap. The KPI tiles are aggregated
            over the whole day, so while this is true the Scheduled tile intentionally exceeds the sum
            of the visible operator table's scheduled column.
        sheds_total:
          type: integer
          description: Shed × partition rows BEFORE the server-side cap. Equals sheds.length when sheds_truncated is false.
        sheds_truncated:
          type: boolean
          description: >-
            True when the shed board was cut to its server-side cap. While true the Scheduled tile
            intentionally exceeds the sum of the visible shed table's scheduled column.
        cells_truncated:
          type: boolean
          description: >-
            True when the underlying park × shed × partition × vaccine × operator rollup itself hit
            its per-read cap, so the two tables list only part of the day. The KPI tiles stay exact —
            they come from window aggregates over the untruncated rollup, not from the returned rows.
        unassigned_administrations:
          type: integer
          description: >-
            Scheduled administrations that resolved to NO drive assignment for the day. They are
            counted in the tiles and rendered in the shed board, but have no operator to be attributed
            to and appear in no operator row, so the operator table's scheduled column sums to
            scheduled_administrations minus this figure. On a partially-planned stg day (2026-08-14)
            that residual is 95 of 199.
        activity: { $ref: "#/components/schemas/VaccinationLiveTrackerActivity" }
        attention:
          type: array
          items: { $ref: "#/components/schemas/VaccinationLiveTrackerAttentionRow" }
        attention_total:
          type: integer
          description: Attention rows BEFORE the server-side cap. kpis.attention_count equals this.
        attention_truncated:
          type: boolean
          description: True when the attention rail was cut to its server-side cap.
        verification: { $ref: "#/components/schemas/VaccinationLiveTrackerVerification" }
        filter_options: { $ref: "#/components/schemas/VaccinationLiveTrackerFilterOptions" }
    VaccinationCommandBoardResponse:
      type: object
      additionalProperties: false
      required:
        - source
        - kpis
        - cohortMatrix
        # Always emitted (the Go field carries no omitempty and the repository initialises it), so
        # an empty residual bucket is the answer "no animals closed without a dose", never a field
        # the server declined to send.
        - closedWithoutDoseAnimals
        - shedDoseMatrix
        - shedVaccineMatrix
        - shedVaccineColumns
        - weeklyGiven
        - verificationQueue
        - driveOptions
        - driveOptionsTruncated
      properties:
        shedVaccineMatrix:
          type: array
          items:
            $ref: "#/components/schemas/CommandBoardShedVaccineCell"
          description: >-
            Shed x VACCINE, dose collapsed, reported as a flag. DENSE: every shed in view carries a
            cell for every code in shedVaccineCodes, so a gap in this array is never the answer
            "clean" — an unplanned vaccine is an explicit not_planned cell.
            This does NOT supersede shedDoseMatrix, which stays dose-qualified. An earlier attempt
            to collapse doses in that matrix was reverted because it SUMMED Dose 1 + Dose 2 +
            Revaccination into a figure that exceeded the cohort head count. This array carries no
            sum: its cell state is a boolean OR over the shed's doses, so collapsing cannot
            over-count by construction. The two matrices answer different questions — "how much of
            each dose" and "is anything behind at all" — and neither is derivable from the other on
            the client without losing a guarantee.
        shedVaccineColumns:
          type: array
          items:
            $ref: "#/components/schemas/CommandBoardVaccineColumn"
          description: >-
            Column order for shedVaccineMatrix: the tenant's FULL protocol vaccine catalogue, not
            the distinct codes present in the cells. Clients must render columns from this list.
            Deriving columns from the cells drops any vaccine that generated zero obligations
            anywhere — the live tenant configures BLUE_TONGUE across eight rule dimensions and has
            produced no obligations for it at all, so a cells-derived header omits it silently and
            the matrix reads complete while a whole vaccine is unaccounted for. A column that is
            entirely not_planned is a finding, not an empty column.
            Each entry carries the header LABEL as well as the code, because visible copy must
            originate server-side; a client-side code-to-label map is a second, drifting copy of the
            canonical vaccine label table.
        source:
          type: string
          enum:
            - api
        kpis:
          $ref: "#/components/schemas/VaccinationCommandBoardKPI"
        driveOptions:
          type: array
          items:
            $ref: "#/components/schemas/VaccinationCommandBoardDriveOption"
          description: >-
            Drives the board can be narrowed to, newest executable day first, park-scoped and
            bounded to 200 rows. Not filtered by the currently selected drive, so the selector can
            still offer the others. One row is one (batch, park): a drive whose work spans two
            parks is two operator days in two places and is offered as two choices, so it spends
            two of the 200 rows. When the bound is reached, driveOptionsTruncated is true and the
            list is incomplete — surface that, do not present the list as the full programme.
        driveOptionsTruncated:
          type: boolean
          description: >-
            True when driveOptions hit its bound and drives were left out. The list has always
            been bounded, but it used to stop silently, so a scheduled drive past the bound was
            indistinguishable from a drive that was never planned. Clients must show that more
            drives exist (e.g. "narrow by park") rather than presenting a truncated picker as
            complete.
        cohortMatrix:
          type: array
          items:
            $ref: "#/components/schemas/VaccinationCommandBoardCohortCell"
          description: Cohort (management_stage × sex) × vaccine matrix; rows are cohort+vaccine cells.
        closedWithoutDoseAnimals:
          type: array
          items:
            $ref: "#/components/schemas/VaccinationCommandBoardClosedWithoutDoseAnimal"
          description: >-
            The animals behind kpis.closedWithoutDose, capped at 50. The tile answers "how many",
            which is where the question starts: the next one is always "which animals, and why did
            their work close with no dose". Selected by the SAME per-animal residual predicate the
            tile counts with, so the list and the number can never describe different animals. The
            tile's count stays whole-scope truth when this list is capped.
        shedDoseMatrix:
          type: array
          items:
            $ref: "#/components/schemas/ShedDoseMatrixCell"
          description: Shed × dose rule state matrix; each row is a shed+dose combination with state and date range.
        weeklyGiven:
          type: array
          items:
            $ref: "#/components/schemas/WeeklyGivenRow"
          description: >-
            Weekly aggregation of doses given (ISO week × vaccine × completion status).
            Ordered by week descending.
        verificationQueue:
          type: array
          items:
            $ref: "#/components/schemas/VerificationQueueRow"
          description: >-
            Per-shed × dose rows awaiting verification. Includes animal count, last-given date,
            and days in queue.
        freshness:
          $ref: "#/components/schemas/VaccinationProjectionFreshness"
    VaccinationExecutionDriveSummary:
      type: object
      additionalProperties: false
      required:
        - workState
        - severity
      properties:
        driveId:
          type: string
        driveName:
          type: string
        workState:
          $ref: "#/components/schemas/VaccinationExecutionWorkState"
        severity:
          $ref: "#/components/schemas/VaccinationExecutionSeverity"
    VaccinationExecutionShedSummary:
      type: object
      additionalProperties: false
      required:
        - total
        - due
        - overdue
        - proofPending
        - verificationPending
        - rejected
        - deferred
        - missed
        - blocked
        - completed
      properties:
        total:
          type: integer
        due:
          type: integer
        overdue:
          type: integer
        proofPending:
          type: integer
        verificationPending:
          type: integer
        rejected:
          type: integer
        deferred:
          type: integer
        missed:
          type: integer
        blocked:
          type: integer
        completed:
          type: integer
    VaccinationExecutionShedDrilldown:
      type: object
      additionalProperties: false
      required:
        - parkId
        - parkName
        - shedId
        - shedName
        - partitionLabel
        - operationalLocationDisplay
        - animalStages
        - drives
        - rows
        - summary
      properties:
        parkId:
          type: string
          format: uuid
        parkName:
          type: string
        shedId:
          type: string
          format: uuid
        partitionLabel:
          type: string
          nullable: true
          description: >-
            Raw stored partition label for the shed ('1', 'Part 3'). Null or absent
            means the shed is non-partitioned. Never the literal string 'whole' --
            that is a matching key, not user copy.
        operationalLocationDisplay:
          type: string
          description: >-
            User-facing location label composed by the backend (oploc.Display()).
            No partition -> bare shed name; partitioned -> 'Godel 1 - Part 3'.
            Clients RENDER this; they must not re-compose it.
        shedName:
          type: string
        animalStages:
          type: array
          items:
            type: string
        drives:
          type: array
          items:
            $ref: "#/components/schemas/VaccinationExecutionDriveSummary"
        rows:
          type: array
          items:
            $ref: "#/components/schemas/VaccinationExecutionRow"
        summary:
          $ref: "#/components/schemas/VaccinationExecutionShedSummary"
    VaccinationQueueItem:
      type: object
      additionalProperties: false
      required:
        - completion_id
        - obligation_id
        - goat_id
        - administered_at
        - doses
        - work_state
      properties:
        completion_id:
          type: string
        obligation_id:
          type: string
        goat_id:
          type: string
        batch_id:
          type: string
        sop_task_id:
          type: string
        sop_task_row_version:
          type: integer
          minimum: 1
        administered_at:
          type: string
          format: date-time
        doses:
          type: integer
        route_site:
          type: string
        work_state:
          $ref: "#/components/schemas/WorkState"
    VaccinationQueueResponse:
      type: object
      additionalProperties: false
      required:
        - items
        - total_count
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/VaccinationQueueItem"
        total_count:
          type: integer
          format: int64
        next_cursor:
          type: string
    ImpactPreviewInput:
      type: object
      additionalProperties: false
      description: >-
        Config impact-preview filter. Empty filter dims mean "any". Preview is aggregate-only over the
        eligibility rollup, so there is no per-goat dose count and no time-relative warmup on this path.
      properties:
        species:
          type: string
        stage:
          type: string
        sex:
          type: string
        breed:
          type: string
        health:
          type: string
        park_id:
          type: string
        vaccine_item_id:
          type: string
        location_id:
          type: string
        dose_rows:
          type: integer
          minimum: 0
          description: Number of selected dose/schedule rows (vaccination cells per eligible animal).
        daily_cap:
          type: integer
          minimum: 1
          description: >-
            Draft operator animal cap (unique animals per available operator per day) authored in the
            rule editor, used to compute estimated operator-days BEFORE publish. Omit to fall back to
            the published/operational cap.
        max_buffer_days:
          type: integer
          minimum: 0
          description: >-
            Draft safe-window buffer (days) authored in the rule editor. Omit to fall back to the
            business default. Drives the within_cap / over_cap (split) / capacity_breach classification.
        horizon_days:
          type: integer
          minimum: 0
    ImpactPreviewResult:
      type: object
      additionalProperties: false
      description: >-
        Aggregate-only config impact preview. Every number is read from the precomputed vaccination
        eligibility rollup (read model); the request path never scans goats. Use the business names
        only (eligible_animals / vaccination_cells / affected_sheds / estimated_days / daily_cap).
        vaccination_cells remains a dose-row volume metric for stock planning; drive capacity is based on
        unique eligible animals per available operator per day.
      required:
        - eligible_animals
        - vaccination_cells
        - affected_sheds
        - estimated_days
        - daily_cap
        - planned_sessions
        - source_revision
        - warnings
      properties:
        eligible_animals:
          type: integer
          description: SUM(animal_count) over usable animals matching the filter.
        vaccination_cells:
          type: integer
          description: eligible_animals × selected dose rows; stock/dose volume only, not operator capacity.
        affected_sheds:
          type: integer
          description: Distinct sheds holding usable animals in scope.
        estimated_days:
          type: integer
          description: Estimated operator-days from unique eligible animals and daily_cap.
        daily_cap:
          type: integer
          description: Configured unique animals per available operator per day used for estimated_days.
        capacity_status:
          type: string
          enum: [within_cap, over_cap, capacity_breach]
          description: >-
            Planner classification of the draft under daily_cap + the buffer window: within_cap (fits one
            operator-day), over_cap (Split — fits the safe window), capacity_breach (Capacity action —
            add operators or finish over cap inside the safe window). Omitted when there is no animal work to plan.
        planned_sessions:
          type: array
          description: >-
            Pre-publish operator-day estimate under the draft daily cap. Bounded for very large herds;
            estimated_days remains the full duration when the preview is truncated.
          items:
            $ref: "#/components/schemas/VaccinationPlannedSession"
        doses_available:
          type: string
          description: Optional cheap stock check; present only when a vaccine item is set.
        earliest_expiry:
          type: string
          format: date-time
        source_revision:
          type: integer
          format: int64
          description: Read-model recompute stamp behind these numbers (0 when the rollup has no data for the scope yet).
        recomputed_at:
          type: string
          format: date-time
          description: When the read-model grain behind these numbers was last rebuilt.
        warnings:
          type: array
          items:
            type: string
    CreateProtocolDefinitionRequest:
      type: object
      additionalProperties: false
      required:
        - code
        - name
        - category
      properties:
        code:
          type: string
        name:
          type: string
        category:
          type: string
    CreateProtocolDefinitionResponse:
      type: object
      additionalProperties: false
      required:
        - protocol_id
      properties:
        protocol_id:
          type: string
    CreateProtocolVersionRequest:
      type: object
      additionalProperties: false
      required:
        - scope_type
        - effective_from
        - rule_dsl
      properties:
        scope_type:
          type: string
        scope_id:
          type: string
          nullable: true
        version:
          type: integer
          description: Optional compatibility field. When omitted, the backend allocates the next scoped protocol version atomically.
        version_label:
          type: string
        effective_from:
          type: string
          format: date-time
        effective_to:
          type: string
          format: date-time
          nullable: true
        rule_dsl:
          type: object
          additionalProperties: true
          description: Protocol rule DSL.
        proof_policy:
          type: object
          additionalProperties: true
          description: Proof policy DSL.
        sop_version_id:
          type: string
          nullable: true
    CreateProtocolVersionResponse:
      type: object
      additionalProperties: false
      required:
        - protocol_version_id
      properties:
        protocol_version_id:
          type: string
    AddProtocolRuleRequest:
      type: object
      additionalProperties: false
      required:
        - dose_code
        - sequence
        - trigger_type
        - repeat
        - catch_up
        - eligibility_json
      properties:
        dose_code:
          type: string
        sequence:
          type: integer
        trigger_type:
          type: string
        offset_days:
          type: integer
        due_window_days:
          type: integer
        min_gap_days:
          type: integer
        repeat:
          type: string
          enum:
            - none
            - every_n_days
            - yearly
        repeat_until_after_age:
          type: string
        catch_up:
          type: string
        eligibility_json:
          type: object
          additionalProperties: true
          description: Eligibility predicate JSON.
        proof_policy:
          description: Row-level proof policy JSON. Accepts an object policy or a bare array of proof tokens.
          oneOf:
            - type: object
              additionalProperties: true
            - type: array
              items:
                type: string
        withdrawal_days:
          type: integer
          nullable: true
        sort_order:
          type: integer
    AddProtocolRuleResponse:
      type: object
      additionalProperties: false
      required:
        - rule_id
      properties:
        rule_id:
          type: string
    ProtocolVersionResponse:
      type: object
      additionalProperties: false
      required:
        - protocol_version_id
        - protocol_id
        - scope_type
        - scope_id
        - version
        - status
        - rule_dsl
        - proof_policy
        - row_version
      properties:
        protocol_version_id:
          type: string
        protocol_id:
          type: string
        scope_type:
          type: string
        scope_id:
          type: string
        version:
          type: integer
        status:
          type: string
        effective_from:
          type: string
          format: date-time
          nullable: true
        effective_to:
          type: string
          format: date-time
          nullable: true
        rule_dsl:
          type: object
          additionalProperties: true
          description: Protocol rule DSL.
        proof_policy:
          type: object
          additionalProperties: true
          description: Proof policy DSL.
        sop_version_id:
          type: string
        row_version:
          type: integer
    ProtocolConfigListResponse:
      type: object
      additionalProperties: false
      required:
        - category
        - items
      properties:
        category:
          type: string
        items:
          type: array
          items:
            $ref: "#/components/schemas/ProtocolConfigItem"
    ProtocolConfigItem:
      type: object
      additionalProperties: false
      required:
        - protocol_id
        - code
        - name
        - category
        - protocol_version_id
        - version
        - version_label
        - scope_type
        - scope_label
        - status
        - rule_count
      properties:
        protocol_id:
          type: string
        code:
          type: string
        name:
          type: string
        category:
          type: string
        protocol_version_id:
          type: string
        version:
          type: integer
        version_label:
          type: string
        scope_type:
          type: string
        scope_id:
          type: string
        scope_label:
          type: string
        status:
          type: string
          description: draft | published | retired
        effective_from:
          type: string
          format: date-time
          nullable: true
        effective_to:
          type: string
          format: date-time
          nullable: true
        sop_version_id:
          type: string
        published_by:
          type: string
        published_at:
          type: string
          format: date-time
          nullable: true
        updated_at:
          type: string
          format: date-time
          nullable: true
        source_system:
          type: string
          deprecated: true
          description: Legacy rule_dsl.source metadata retained only for old rows. Not a publish gate and not shown in the V1 Config UI.
        source_ref:
          type: string
          deprecated: true
        review_status:
          type: string
          deprecated: true
        approved_by:
          type: string
          deprecated: true
        approved_at:
          type: string
          deprecated: true
        rule_count:
          type: integer
    AnimalStageListResponse:
      type: object
      additionalProperties: false
      required:
        - items
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/AnimalStageItem"
    AnimalStageItem:
      type: object
      additionalProperties: false
      required:
        - animal_stage_id
        - stage_code
        - name
        - age_band
        - assignable_as_cohort
        - sort_order
      properties:
        animal_stage_id:
          type: string
        stage_code:
          type: string
          description: Stable stage code (e.g. K1, K2) authored against in rule_dsl.eligibility.animal_stage.
        name:
          type: string
        age_band:
          type: string
          description: >-
            "kid" or "adult", or "" for a tag the farm has not classified. The band is a property OF
            the tag rather than of the animal's birthday, so an animal INHERITS it when its pen is
            retagged: moving a pen from a kid cohort to an adult one makes those animals adults. A
            picker offering this vocabulary should show the band, because that consequence is not
            obvious from the tag name alone.
        assignable_as_cohort:
          type: boolean
          description: >-
            False for a CLINICAL tag (ICU, Quarantine). Those describe an animal's medical state,
            belong to the clinical flows, and are rejected by every write that assigns a cohort — so
            an assigning picker must not offer them and then fail. Always emitted: an absent field
            would read as "assignable", which is the wrong default for a safety-bearing value.
        min_age_days:
          type: integer
          nullable: true
        max_age_days:
          type: integer
          nullable: true
        sort_order:
          type: integer
    FeedDirectionReadinessStatus:
      type: string
      enum:
        - ready
        - blocked
        - pending
    FeedDirectionGenerationPreviewResponse:
      type: object
      additionalProperties: false
      required:
        - preview
        - trace_id
      properties:
        preview:
          $ref: "#/components/schemas/FeedDirectionGenerationPreview"
        trace_id:
          type: string
    FeedDirectionGenerationPreview:
      type: object
      additionalProperties: false
      required:
        - tenant_id
        - park_id
        - target_date
        - status
        - generation_allowed
        - blocker_reason
        - snapshot_id
        - projection_status
        - source_contract_version
        - source_hash
        - base_anchor_ids_hash
        - shifting_event_ids_hash
        - exception_count
        - total_row_count
        - rows
        - shed_breed_totals
        - blockers
      properties:
        tenant_id:
          type: string
        park_id:
          type: string
          format: uuid
        target_date:
          type: string
          format: date-time
        status:
          $ref: "#/components/schemas/FeedDirectionReadinessStatus"
        generation_allowed:
          type: boolean
          description: True only when this projection preview page carries no blockers.
        blocker_reason:
          type: string
        snapshot_id:
          type: string
        projection_status:
          type: string
          description: Counts/Shifting projection status for the returned immutable snapshot/page.
        source_contract_version:
          type: string
        source_hash:
          type: string
        base_anchor_ids_hash:
          type: string
        shifting_event_ids_hash:
          type: string
        exception_count:
          type: integer
          format: int64
        total_row_count:
          type: integer
          format: int64
        rows:
          type: array
          items:
            $ref: "#/components/schemas/FeedDirectionGenerationPreviewRow"
        shed_breed_totals:
          type: array
          items:
            $ref: "#/components/schemas/FeedDirectionGenerationPreviewTotal"
        blockers:
          type: array
          items:
            $ref: "#/components/schemas/FeedDirectionGenerationPreviewBlocker"
        next_cursor:
          type: string
    FeedDirectionGenerationPreviewRow:
      type: object
      additionalProperties: false
      required:
        - projection_row_id
        - park_id
        - shed_id
        - target_date
        - grain_key
        - base_count_anchor_id
        - included_shifting_event_ids_hash
        - breed_key
        - breed_label
        - head_count
        - pregnant_count
        - lactating_count
        - warmup_count
        - ration_context_resolution_state
        - source_row_hash
      properties:
        projection_row_id:
          type: string
        park_id:
          type: string
          format: uuid
        shed_id:
          type: string
          format: uuid
        target_date:
          type: string
          format: date-time
        grain_key:
          type: string
        base_count_anchor_id:
          type: string
        included_shifting_event_ids_hash:
          type: string
        breed_id:
          type: string
          nullable: true
        breed_key:
          type: string
        breed_label:
          type: string
        stage_tag:
          type: string
          nullable: true
        age_class:
          type: string
          nullable: true
        sex:
          type: string
          nullable: true
        head_count:
          type: integer
        pregnant_count:
          type: integer
        lactating_count:
          type: integer
        warmup_count:
          type: integer
        ration_context_resolution_state:
          type: string
        ration_context_ref:
          type: string
          nullable: true
        blocker_reason:
          type: string
          nullable: true
        source_row_hash:
          type: string
    FeedDirectionGenerationPreviewTotal:
      type: object
      additionalProperties: false
      required:
        - park_id
        - shed_id
        - breed_key
        - breed_label
        - head_count
        - pregnant_count
        - lactating_count
        - warmup_count
        - ration_context_resolution_state
      properties:
        park_id:
          type: string
          format: uuid
        shed_id:
          type: string
          format: uuid
        breed_key:
          type: string
        breed_label:
          type: string
        head_count:
          type: integer
        pregnant_count:
          type: integer
        lactating_count:
          type: integer
        warmup_count:
          type: integer
        ration_context_resolution_state:
          type: string
    FeedDirectionGenerationPreviewBlocker:
      type: object
      additionalProperties: false
      required:
        - source
        - type
        - source_key
        - grain_key
        - severity
        - blocker_reason
      properties:
        source:
          type: string
          enum:
            - projection
            - counts_projection
            - counts_projection_exception
            - projection_row
            - shed_breed_total
        type:
          type: string
        source_key:
          type: string
        grain_key:
          type: string
        severity:
          type: string
        blocker_reason:
          type: string
    FeedDirectionCountsProjectionExceptionListResponse:
      type: object
      additionalProperties: false
      required:
        - items
        - trace_id
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/FeedDirectionCountsProjectionException"
        next_cursor:
          type: string
        trace_id:
          type: string
    FeedDirectionCountsProjectionException:
      type: object
      additionalProperties: false
      required:
        - projection_exception_id
        - exception_type
        - source_key
        - grain_key
        - severity
        - status
        - work_type
        - work_state
        - due_at
        - next_action
        - evidence_link
        - blocker_reason
        - evidence_json
        - created_at
        - updated_at
      properties:
        projection_exception_id:
          type: string
          format: uuid
        projection_snapshot_id:
          type: string
          format: uuid
        exception_type:
          type: string
          enum:
            - missing_base_count
            - missing_structured_impact
            - unreported_shifting
            - count_mismatch
            - alias_conflict
            - ration_context_unresolved
            - destination_shortage
            - unsafe_surplus
            - query_plan_unproven
        source_key:
          type: string
          minLength: 1
        grain_key:
          type: string
          minLength: 1
        park_id:
          type: string
          format: uuid
        shed_id:
          type: string
          format: uuid
        breed_key:
          type: string
        stage_tag:
          type: string
        severity:
          type: string
          enum:
            - warning
            - blocking
            - critical
        status:
          type: string
          enum:
            - open
            - resolved
            - dismissed
        owner_ref:
          type: string
        work_type:
          type: string
          enum:
            - counts_projection_exception
        work_state:
          type: string
          enum:
            - blocked
            - resolved
            - dismissed
        due_at:
          type: string
          format: date-time
        next_action:
          type: string
          minLength: 1
        evidence_link:
          type: string
          minLength: 1
        blocker_reason:
          type: string
          minLength: 1
        evidence_json:
          type: object
          additionalProperties: true
        resolution_id:
          type: string
          format: uuid
        resolved_by_ref:
          type: string
        resolution_reason:
          type: string
        resolution_ref:
          type: string
        resolved_at:
          type: string
          format: date-time
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    FeedDirectionCountsProjectionExceptionActionRequest:
      type: object
      additionalProperties: false
      required:
        - resolution_reason
      properties:
        resolution_reason:
          type: string
          minLength: 1
          maxLength: 2000
          description: Reviewed reason for closing the Counts/Shifting projection exception.
        resolution_ref:
          type: string
          nullable: true
          maxLength: 500
          description: Optional source, ticket, shifting-event, proof, or owner-review reference.
    FeedDirectionCountsProjectionExceptionActionResponse:
      type: object
      additionalProperties: false
      required:
        - resolution
        - trace_id
      properties:
        resolution:
          $ref: "#/components/schemas/FeedDirectionCountsProjectionExceptionResolution"
        trace_id:
          type: string
    FeedDirectionCountsProjectionExceptionResolution:
      type: object
      additionalProperties: false
      required:
        - resolution_id
        - projection_exception_id
        - action
        - status
        - work_state
        - resolved_by_ref
        - resolution_reason
        - resolved_at
        - replayed
      properties:
        resolution_id:
          type: string
          format: uuid
        projection_exception_id:
          type: string
          format: uuid
        action:
          type: string
          enum:
            - resolve
            - dismiss
        status:
          type: string
          enum:
            - resolved
            - dismissed
        work_state:
          type: string
          enum:
            - resolved
            - dismissed
        resolved_by_ref:
          type: string
          minLength: 1
        resolution_reason:
          type: string
          minLength: 1
        resolution_ref:
          type: string
          nullable: true
        resolved_at:
          type: string
          format: date-time
        replayed:
          type: boolean
    VaccinationCapacityStatus:
      type: string
      enum:
        - within_cap
        - over_cap
        - capacity_breach
      description: |
        Machine vocabulary for capacity/session-splitting state:
        - within_cap: fits in a single day (sessions <= 1)
        - over_cap: safely split across multiple days within the safe window
        - capacity_breach: cannot fit within the safe window at authored cap; add operators or finish over cap

        NEVER show raw tokens in CEO UI. Frontend renders provided labels:
        within_cap -> "Within cap", over_cap -> "Split", capacity_breach -> "Capacity action"
    VaccinationPlannedSession:
      type: object
      additionalProperties: false
      required:
        - date
        - vaccinations
        - dailyLimit
        - capacity
      properties:
        date:
          type: string
          description: Asia/Kolkata business date (YYYY-MM-DD)
        vaccinations:
          type: integer
          description: Animal slots planned for this day
        dailyLimit:
          type: integer
          description: The configured daily animals/operator cap
        capacity:
          $ref: "#/components/schemas/VaccinationCapacityStatus"
    VaccinationCapacityConfig:
      type: object
      additionalProperties: false
      description: |
        Tenant vaccination drive capacity config published from the versioned vaccination rule DSL. The cap
        counts unique animals per available operator per business date, not vaccine doses or obligation
        cells. One animal receiving a same-day vaccine bundle still consumes one operator slot.
      required:
        - maxPerDay
        - capacityScope
        - maxBufferDays
        - overflowPolicy
        - rowVersion
      properties:
        maxPerDay:
          type: integer
          minimum: 1
          maximum: 100000
          description: Max unique animals one available operator can handle per business date.
        capacityScope:
          type: string
          enum:
            - tenant
            - center
            - shed
          description: Where the cap applies. Only 'tenant' is honored by the planner today.
        maxBufferDays:
          type: integer
          minimum: 0
          maximum: 60
          description: Extra safe-window days past the first due day before added operators or over-cap completion is required.
        overflowPolicy:
          type: string
          enum:
            - split_within_safe_window_last_safe_may_exceed_cap
          description: What the planner does when due work exceeds the cap.
        rowVersion:
          type: integer
          description: Published capacity config row version.
        maxShotsPerAnimalPerDrive:
          type: integer
          minimum: 1
          nullable: true
          description: >-
            Admin-editable override of the same-day per-animal shot cap enforced by the obligation
            sweeper. null means "no override" -- the planner falls back to the published rule_dsl
            drive_policy value / code default.
    VaccinationOperatorShift:
      type: object
      additionalProperties: false
      description: |
        Phase 1 CONFIG-ONLY: one operator's authored shift window + week-off for a park. Start/end are
        minutes-of-day (0-1439) so a non-hour-aligned start (e.g. 08:30) is exact.
      required:
        - operatorId
        - displayName
        - shiftLabel
        - shiftStartMinute
        - shiftEndMinute
      properties:
        operatorId:
          type: string
          format: uuid
        displayName:
          type: string
          description: Backend-owned operator display name (workforce_members.display_name).
        shiftLabel:
          type: string
          enum:
            - am
            - pm
            - rover
        shiftStartMinute:
          type: integer
          minimum: 0
          maximum: 1439
        shiftEndMinute:
          type: integer
          minimum: 0
          maximum: 1439
        weekOffWeekday:
          type: string
          enum:
            - monday
            - tuesday
            - wednesday
            - thursday
            - friday
            - saturday
            - sunday
          description: Lowercase weekday name; absent when the operator has no week-off.
    VaccinationParkScopeOption:
      type: object
      additionalProperties: false
      description: >-
        One selectable park in the backend-owned park-scope vocabulary. Ids and labels are canonical
        Postgres `locations` rows compiled by the backend; clients render them and send parkId back.
      required:
        - parkId
        - code
        - name
      properties:
        parkId:
          type: string
          format: uuid
        code:
          type: string
          description: Short park code (may be empty when the park has none).
        name:
          type: string
          description: Display name for the park option.
    VaccinationParkScopeAmbiguous:
      type: object
      additionalProperties: false
      description: >-
        409 body for a read whose park scope the caller must choose. availableParks is the exact set
        the caller is authorized for; an empty array means the caller has no active park scope at all
        (a grant problem), which is a different situation from having several.
      required:
        - code
        - message
        - trace_id
        - availableParks
      properties:
        code:
          type: string
          enum: [park_scope_ambiguous]
        message:
          type: string
          description: Backend-owned user-facing reason. Clients render this text; they do not compose their own.
        trace_id:
          type: string
        availableParks:
          type: array
          items:
            $ref: "#/components/schemas/VaccinationParkScopeOption"
    VaccinationOperatorAssignmentConfig:
      type: object
      additionalProperties: false
      description: |
        Phase 1 CONFIG-ONLY: a park's N-active-operators-per-day + CEO-set default operator, plus every
        operator's authored shift. Not yet consumed by the drive scheduler (Phase 5).
      required:
        - parkId
        - activeOperatorsPerDay
        - defaultOperatorId
        - rowVersion
        - shifts
      properties:
        parkId:
          type: string
          format: uuid
          description: >-
            The park this config belongs to, as RESOLVED by the backend (echoing an explicit park_id,
            or the caller's single authorized park scope). Clients scope their roster/capacity reads
            to this value instead of inferring a park from returned rows.
        activeOperatorsPerDay:
          type: integer
          minimum: 1
          maximum: 3
          description: N operators active per business day for this park. CPT runs N=1 today.
        defaultOperatorId:
          type: string
          format: uuid
          description: The CEO-set default operator. Required whenever a config row exists (validate-or-reject, never a silent default).
        selectedOperatorIds:
          type: array
          description: Explicit operators selected for parallel mode. When empty, the backend falls back to roster order.
          items:
            type: string
            format: uuid
          maxItems: 3
        rowVersion:
          type: integer
          format: int64
          description: Optimistic-concurrency token; required on PUT to avoid clobbering a concurrent admin edit.
        shifts:
          type: array
          items:
            $ref: "#/components/schemas/VaccinationOperatorShift"
    UpdateVaccinationOperatorAssignmentConfigRequest:
      type: object
      additionalProperties: false
      required:
        - parkId
        - activeOperatorsPerDay
        - defaultOperatorId
        - rowVersion
      properties:
        parkId:
          type: string
          format: uuid
        activeOperatorsPerDay:
          type: integer
          minimum: 1
          maximum: 3
        defaultOperatorId:
          type: string
          format: uuid
        selectedOperatorIds:
          type: array
          items:
            type: string
            format: uuid
          maxItems: 3
        rowVersion:
          type: integer
          format: int64
          description: The rowVersion last read by the admin; 0 when authoring a config for this park for the first time.
    UpdateVaccinationCapacityConfigRequest:
      type: object
      additionalProperties: false
      required:
        - maxPerDay
        - rowVersion
      properties:
        maxPerDay:
          type: integer
          minimum: 1
          maximum: 100000
        maxShotsPerAnimalPerDrive:
          type: integer
          minimum: 1
          nullable: true
          description: Omit or set null to clear the override and fall back to the published rule_dsl value / code default.
        rowVersion:
          type: integer
          description: The rowVersion last read by the admin.
    VaccinationShedStatus:
      type: string
      enum:
        - needs_review
        - overdue
        - split
        - due
        - scheduled
        - on_track
      description: |
        Merged CEO headline folding capacity and vaccination state by priority (highest first):
        needs_review > overdue > split > due > scheduled > on_track

        This is the INTERNAL machine vocabulary. The CEO UI renders the backend-provided label
        (never the raw token, never the word "state").
    VaccinationShedSortKey:
      type: string
      enum:
        - park_shed
        - due_desc
        - animals_desc
        - next_due
    VaccinationPageInfo:
      type: object
      additionalProperties: false
      required:
        - total
        - limit
        - offset
      properties:
        total:
          type: integer
        limit:
          type: integer
        offset:
          type: integer
    VaccinationShedOwner:
      type: object
      additionalProperties: false
      required:
        - workforceMemberId
        - displayName
      properties:
        workforceMemberId:
          type: string
        displayName:
          type: string
    VaccinationShedSummaryRow:
      type: object
      additionalProperties: false
      required:
        - parkId
        - parkName
        - shedId
        - shedName
        - animals
        - due
        - done
        - sessions
        - capacity
        - status
      properties:
        parkId:
          type: string
          format: uuid
        parkName:
          type: string
        shedId:
          type: string
          format: uuid
        partitionLabel:
          type: string
          nullable: true
          description: >-
            Raw stored partition label for the shed ('1', 'Part 3'). Null or absent
            means the shed is non-partitioned. Never the literal string 'whole' --
            that is a matching key, not user copy.
        operationalLocationDisplay:
          type: string
          description: >-
            User-facing location label composed by the backend (oploc.Display()).
            No partition -> bare shed name; partitioned -> 'Godel 1 - Part 3'.
            Clients RENDER this; they must not re-compose it.
        shedName:
          type: string
        animals:
          type: integer
        due:
          type: integer
        done:
          type: integer
        sessions:
          type: integer
          description: Planned vaccination visits/days for this shed. Usually 1; >1 when the daily cap forces a split.
        lastDone:
          type:
            - string
            - "null"
        nextDue:
          type:
            - string
            - "null"
        manager:
          allOf:
            - $ref: "#/components/schemas/VaccinationShedOwner"
            - nullable: true
        backup:
          allOf:
            - $ref: "#/components/schemas/VaccinationShedOwner"
            - nullable: true
        driveOperatorNames:
          type: array
          description: Actual vaccination drive operators assigned by the operator-cap planner for this physical shed.
          items:
            type: string
        capacity:
          $ref: "#/components/schemas/VaccinationCapacityStatus"
        status:
          $ref: "#/components/schemas/VaccinationShedStatus"
    VaccinationDriveAssignmentRow:
      type: object
      additionalProperties: false
      required:
        - plannedDate
        - originalPlannedDate
        - vaccineOriginalDates
        - operatorId
        - operatorName
        - parkId
        - parkName
        - physicalShed
        - partitionLabel
        - animals
        - dueAnimals
        - doneAnimals
        - deferredAnimals
        - overdueAnimals
        - vaccineNames
        - vaccineCodes
        - totalDoses
        - capacity
      properties:
        plannedDate:
          type: string
          format: date
        originalPlannedDate:
          type: string
          format: date
          description: Persisted source drive date before any vaccine-level date override is applied.
        vaccineOriginalDates:
          type: object
          additionalProperties:
            type: string
            format: date
          description: Original drive date keyed by vaccine code, used when a mixed-vaccine operator row is moved one vaccine at a time.
        operatorId:
          type: string
          format: uuid
        operatorName:
          type: string
        parkId:
          type: string
          format: uuid
        parkName:
          type: string
        shedId:
          type:
            - string
            - "null"
          format: uuid
        physicalShed:
          type: string
        partitionLabel:
          type: string
          description: >-
            EXISTING camelCase field, kept for existing consumers. New
            consumers should prefer partition_label below (nullable, never
            "whole").
        partition_label:
          type:
            - string
            - "null"
          description: >-
            Raw stored partition label for the shed ('1', 'Part 3'). Null or
            absent means the shed is non-partitioned. Never the literal string
            "whole".
        source_shed_name:
          type:
            - string
            - "null"
          description: >-
            Original partition-bearing source name (e.g. "Castro 1"), kept for
            traceability only. Not a display field.
        operational_location_display:
          type: string
          description: >-
            User-facing location label. No partition -> bare shed name
            ("Yashoda"); numeric convention -> "Castro 2"; prefixed convention
            -> "Godel 1 - Part 3".
        animals:
          type: integer
          minimum: 0
          description: Assigned operator-capacity animals for this operator/date/shed/partition row.
        dueAnimals:
          type: integer
          minimum: 0
          description: Assigned animals still planned or in progress and not yet overdue.
        doneAnimals:
          type: integer
          minimum: 0
          description: Assigned animals whose drive batch is completed.
        deferredAnimals:
          type: integer
          minimum: 0
          description: Assigned animals held/deferred from the drive.
        overdueAnimals:
          type: integer
          minimum: 0
          description: Assigned animals still open after the planned drive date.
        vaccineNames:
          type: array
          description: Backend-owned vaccine display labels attached to this operator assignment row.
          items:
            type: string
        vaccineCodes:
          type: array
          description: Backend-owned vaccine identity codes used for drive-date overrides.
          items:
            type: string
        totalDoses:
          type: integer
          description: Number of vaccine tasks/doses represented by this assignment row.
          minimum: 0
        capacity:
          $ref: "#/components/schemas/VaccinationCapacityStatus"
    VaccinationDriveAssignmentResponse:
      type: object
      additionalProperties: false
      required:
        - source
        - rows
      properties:
        source:
          type: string
          enum:
            - api
        rows:
          type: array
          items:
            $ref: "#/components/schemas/VaccinationDriveAssignmentRow"
    WeighingCampaignStatus:
      type: string
      enum: [draft, published, in_progress, delayed, completed, closed, canceled]
    WeighingCampaignShedStatus:
      type: string
      enum: [pending, in_progress, completed, closed, canceled]
    WeighingCategory:
      type: string
      enum: [individual_animal, per_shed_partition]
    WeighingAvailabilityStatus:
      type: string
      enum: [expected_shed, moved_other_shed, icu, quarantine, dead, culled, sold_transferred, exited, unknown_review]
    WeighingProgress:
      type: object
      additionalProperties: false
      required:
        - individual_expected_count
        - individual_completed_count
        - per_scope_expected_count
        - per_scope_completed_count
        - wrong_shed_count
        - missing_count
        - remaining_count
      properties:
        individual_expected_count:
          type: integer
          minimum: 0
        individual_completed_count:
          type: integer
          minimum: 0
        per_scope_expected_count:
          type: integer
          minimum: 0
        per_scope_completed_count:
          type: integer
          minimum: 0
        wrong_shed_count:
          type: integer
          minimum: 0
        missing_count:
          type: integer
          minimum: 0
        remaining_count:
          type: integer
          minimum: 0
    WeighingCampaignShed:
      type: object
      additionalProperties: false
      required:
        - campaign_shed_id
        - campaign_id
        - location_id
        - location_type
        - display_name
        - expected_animal_count
        - weighing_category
        - operator_user_id
        - operator_display_name
        - status
        - pending_verification_count
        - ready_to_close
        - animals_weighed_count
        - animals_submitted_count
      properties:
        campaign_shed_id:
          type: string
          format: uuid
        campaign_id:
          type: string
          format: uuid
        location_id:
          type: string
          format: uuid
        location_type:
          type: string
          enum: [shed, cohort, pen]
        display_name:
          type: string
        parent_shed_name:
          type:
            - string
            - "null"
          description: >-
            Base shed name extracted from display_name when a partition label is present.
            Null or absent when display_name carries no partition suffix.
        partition_label:
          type:
            - string
            - "null"
          description: >-
            Raw stored partition label for this shed ('1', 'Part 3'). Null or
            absent means the shed is non-partitioned. Never the literal
            string "whole". NOT the same field as location_type, which is an
            enum (shed/cohort/pen) and carries no partition information.
        source_shed_name:
          type:
            - string
            - "null"
          description: >-
            Original partition-bearing source name (e.g. "Castro 1"), kept for
            traceability only. Not a display field.
        operational_location_display:
          type: string
          description: >-
            User-facing location label. No partition -> bare shed name
            ("Yashoda"); numeric convention -> "Castro 2"; prefixed convention
            -> "Godel 1 - Part 3".
        operator_display_name:
          type: string
          description: >-
            Backend-resolved assignee name, carried ON the bucket so a client never has to join it
            against a separately paged operator vocabulary. Empty WITH a non-empty
            operator_user_id is a roster gap, not "not assigned".
        expected_animal_count:
          type: integer
          minimum: 0
        weighing_category:
          $ref: "#/components/schemas/WeighingCategory"
        operator_user_id:
          type: string
          format: uuid
        status:
          $ref: "#/components/schemas/WeighingCampaignShedStatus"
        planned_business_date:
          type: string
          format: date
          description: Business date originally planned for this operator bucket.
        due_business_date:
          type: string
          format: date
          description: Current due business date after roll-forward.
        pending_verification_count:
          type: integer
          minimum: 0
          description: >-
            Exact count of this bucket's SUBMITTED observations (proof already
            uploaded) whose verification_status is not yet 'verified'. This is a
            count of evidence that actually exists -- there is deliberately no
            expected-animal denominator or ratio here.
        rework_count:
          type: integer
          minimum: 0
          description: >-
            Work a verifier actively bounced back to the operator and the
            operator still owes. Individual rework counts submitted animal rows.
            Shed-grain rework is a bucket signal: a withdrawn rejected lump-sum
            proof counts as 1 until an open replacement proof exists, so this is
            not always a subset of pending_verification_count.
        latest_rework_reason:
          type: string
          description: Most recent verifier-provided rework reason for this bucket, when available.
        ready_to_close:
          type: boolean
          description: >-
            True only when the bucket is submitted (status=completed), holds at
            least one submitted observation, and none of its observations have a
            verification_status other than 'verified'. An outstanding 'rework'
            observation also makes this false.
        animals_weighed_count:
          type: integer
          minimum: 0
          description: >-
            FACT 1 of 2. How many ANIMALS this bucket has a RECORDED weight for,
            submitted or not: one per individual observation, plus the recorded
            head count of the standing (non-withdrawn) shed proof for a lump-sum
            bucket. ANIMAL grain, not record grain -- it replaces captured_count,
            which counted the lump-sum proof ROW and so read as 1 for a 40-animal
            shed proof while the per-operator roll-up said 40 for the same work.
            Identical predicate to WeighingOperatorSummary.animals_weighed_count,
            so no two surfaces can disagree.
            A plain count, NEVER a numerator: weighing is free-flow, there is no
            expected-animal roster, expected_animal_count is a fixed bucket-grain
            1, and dividing weighings by it would render a share of a total that
            does not exist. Clients report this number as-is and must never turn
            it into a percentage or a progress bar fill.
        animals_submitted_count:
          type: integer
          minimum: 0
          description: >-
            FACT 2 of 2. The subset of animals_weighed_count that has been
            SUBMITTED for verification. An individual observation counts only once
            submitted; a lump-sum shed proof IS the submission, so a standing one
            counts as soon as it exists. Identical predicate to
            WeighingOperatorSummary.animals_submitted_count.
            Clients render the PAIR, in this order and these words:
            "N weighed · N submitted". When work exists and this is zero they show a
            "Not submitted" chip -- the same word as the operator's Submit button.
            Never render either number bare, and never divide one by the other.
    WeighingCampaign:
      type: object
      additionalProperties: false
      required:
        - campaign_id
        - tenant_id
        - park_id
        - period_start_date
        - period_end_date
        - start_business_date
        - status
        - planned_cap_per_day
        - operator_user_id
        - created_by
        - created_at
        - updated_at
        - row_version
        - progress
      properties:
        campaign_id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        park_id:
          type: string
          format: uuid
        period_start_date:
          type: string
          format: date
        period_end_date:
          type: string
          format: date
        start_business_date:
          type: string
          format: date
        status:
          $ref: "#/components/schemas/WeighingCampaignStatus"
        planned_cap_per_day:
          type: integer
          minimum: 1
        operator_user_id:
          type: string
          format: uuid
        created_by:
          type: string
          format: uuid
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        row_version:
          type: integer
          minimum: 1
        sheds:
          type: array
          items:
            $ref: "#/components/schemas/WeighingCampaignShed"
        progress:
          $ref: "#/components/schemas/WeighingProgress"
    WeighingCampaignListResponse:
      type: object
      additionalProperties: false
      required:
        - items
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/WeighingCampaign"
        next_cursor:
          type: string
        counts:
          $ref: "#/components/schemas/WeighingCampaignCounts"
        capabilities:
          allOf:
            - $ref: "#/components/schemas/WeighingCampaignCapabilities"
          description: >-
            SURFACE-grain, not row-grain. The envelope covers a page whose rows may span several
            parks, so this is an upper bound ("the caller holds this permission somewhere on this
            surface") and must NOT be used to gate a per-row button. The single-task read answers
            at row grain.
        operator_summaries:
          type: array
          description: >-
            OPERATOR-grain roll-up behind the weighing oversight surface: one row per person
            holding weighing work in this scope. Unlike `counts` it IS narrowed by `park_id`,
            because the park chip is that screen's own filter. Served whole (capped at 50), not
            paged: a roll-up that pages cannot answer "who did what".
          items:
            $ref: "#/components/schemas/WeighingOperatorSummary"
        trace_id:
          type: string
    WeighingCampaignCapabilities:
      type: object
      additionalProperties: false
      description: >-
        Which task-level writes the caller may attempt. Publish is `weighing.plan` while ending
        and reopening are `weighing.monitor`, so a client that gates buttons on task status alone
        renders a live button that fails. On the single-task read these are answered for the
        task's OWN park, matching the park scope the corresponding writes enforce -- a caller who
        monitors another park gets false rather than a button whose tap answers 404.
      required:
        - can_publish
        - can_end
        - can_reopen
      properties:
        can_publish:
          type: boolean
        can_end:
          type: boolean
        can_reopen:
          type: boolean
    WeighingOperatorSummary:
      type: object
      additionalProperties: false
      description: >-
        What ONE person's weighing work adds up to. GRAIN: one row per operator_user_id over
        that person's non-canceled shed buckets in scope. Every field is a PLAIN COUNT and none
        is ever a numerator -- weighing is free-flow, there is no expected-animal roster, so no
        share or percentage can honestly be rendered from any of these. not_started_count +
        capturing_count + submitted_count + accepted_count == shed_count exactly (the four are
        disjoint and exhaustive over the live bucket statuses), so a client may lay them out as
        a discrete state ladder but must never draw a part-filled fraction.
      required:
        - operator_user_id
        - operator_display_name
        - shed_count
        - not_started_count
        - capturing_count
        - submitted_count
        - accepted_count
        - rework_count
        - animals_weighed_count
        - animals_submitted_count
      properties:
        operator_user_id:
          type: string
          description: >-
            Empty on the "nobody is assigned yet" row, which is real work leadership must see.
        operator_display_name:
          type: string
          description: >-
            Backend-resolved name. Blank WITH a non-blank operator_user_id is a roster gap, not
            "unassigned"; clients render the gap and never fall back to the user id.
        shed_count:
          type: integer
          minimum: 0
        not_started_count:
          type: integer
          minimum: 0
          description: Buckets holding nothing captured yet.
        capturing_count:
          type: integer
          minimum: 0
          description: Buckets where weighing is under way but nothing is submitted.
        submitted_count:
          type: integer
          minimum: 0
          description: Buckets submitted and waiting for a verifier.
        accepted_count:
          type: integer
          minimum: 0
          description: Buckets verified and closed.
        rework_count:
          type: integer
          minimum: 0
          description: >-
            Buckets a verifier bounced back. OVERLAPS the four state counts on purpose (a
            bounced bucket is still in one of them) and is never added to them.
        animals_weighed_count:
          type: integer
          minimum: 0
          description: >-
            FACT 1 of 2. How many ANIMALS this person has RECORDED a weight for, submitted or
            not: one per individual observation plus the recorded head count of a standing
            lump-sum weighing. Whole-filter aggregate computed by the backend over every bucket
            the person holds in scope -- never grouped client-side from a page of rows. Same
            predicate as WeighingCampaignShed.animals_weighed_count. A plain total of work
            done; never a numerator.
        animals_submitted_count:
          type: integer
          minimum: 0
          description: >-
            FACT 2 of 2. The subset of animals_weighed_count this person has SUBMITTED for
            verification. Whole-filter aggregate, same predicate as
            WeighingCampaignShed.animals_submitted_count.
            Clients render the PAIR, in this order and these words: "N weighed · N submitted".
            When work exists and this is zero they show a "Not submitted" chip -- the same word
            as the operator's Submit button. This pair is what makes visible the mid-shift state
            where an operator has weighed animals and walked away without submitting them.
    WeighingCampaignCounts:
      type: object
      additionalProperties: false
      description: >-
        Whole-filter task tally behind the Active / Completed tabs. GRAIN: one task = one park
        on one weigh date. Computed over the entire scope the caller may see, never from the
        returned page and never narrowed by `park_id`. `completed` is status completed or
        closed; `active` is every other live status. A canceled task is in neither.
      required:
        - active
        - completed
      properties:
        active:
          type: integer
          minimum: 0
        completed:
          type: integer
          minimum: 0
    WeighingCampaignResponse:
      type: object
      additionalProperties: false
      required:
        - campaign
      properties:
        campaign:
          $ref: "#/components/schemas/WeighingCampaign"
        capabilities:
          allOf:
            - $ref: "#/components/schemas/WeighingCampaignCapabilities"
          description: >-
            Answered for THIS task's park. The single-task read is what a deep-linked task screen
            gates its buttons on, so the answer has to match what the write would allow: end and
            reopen are park-scoped and refuse an unauthorized park, and a park-blind answer here
            put a live Close button on a task whose write returns 404.
        trace_id:
          type: string
    WeighingParkListResponse:
      type: object
      additionalProperties: false
      required:
        - parks
      properties:
        parks:
          type: array
          items:
            $ref: "#/components/schemas/WeighingPark"
        trace_id:
          type: string
    GrowthDirectorWeightsResponse:
      type: object
      additionalProperties: false
      description: >-
        The Growth Director section of the Weights screen: six widgets over weighing,
        herd-register and feed-direction data. Every widget carries its own
        denominators — there is no expected-animal roster, so every count is an
        actual-scan/identity count, never "of expected".
      required:
        - period
        - parks
        - road_to_sale
        - fair_fight
        - slow_growth
        - feed_vs_growth
        - feed_problems
        - trust
      properties:
        period:
          $ref: "#/components/schemas/GrowthDirectorPeriod"
        parks:
          type: array
          description: Park filter vocabulary, limited to the caller's authorized scope.
          items:
            $ref: "#/components/schemas/WeighingPark"
        road_to_sale:
          $ref: "#/components/schemas/GrowthDirectorRoadToSale"
        fair_fight:
          $ref: "#/components/schemas/GrowthDirectorFairFight"
        slow_growth:
          $ref: "#/components/schemas/GrowthDirectorSlowGrowth"
        feed_vs_growth:
          $ref: "#/components/schemas/GrowthDirectorFeedVsGrowth"
        feed_problems:
          $ref: "#/components/schemas/GrowthDirectorFeedProblems"
        trust:
          $ref: "#/components/schemas/GrowthDirectorTrust"
    GrowthDirectorPeriod:
      type: object
      additionalProperties: false
      description: >-
        The resolved reporting window. Weighing data is selected by CAMPAIGN-WEEK
        OVERLAP (campaigns are week-grain, so the window pulls in every overlapping
        week in full); feed rows use the exact day range. `resolution` discloses this.
      required: [start, end, resolution]
      properties:
        start:
          type: string
          format: date
        end:
          type: string
          format: date
        resolution:
          type: string
          description: Currently always `campaign_week`.
    GrowthDirectorRoadToSale:
      type: object
      additionalProperties: false
      description: >-
        Where every kid sits on the way to sale weight, counted from each tag
        identity's latest weigh. Unmatched identities stay in the bands — a scale
        reading is a scale reading — but are counted separately. Rework-status
        captures are excluded.
      required:
        - total_identities
        - matched_identities
        - unmatched_identities
        - bands
        - movement
      properties:
        total_identities:
          type: integer
          description: Distinct tag identities weighed in the period. The denominator for the bands.
        matched_identities:
          type: integer
          description: Identities whose tag resolves in the herd register.
        unmatched_identities:
          type: integer
        bands:
          type: array
          description: Always all six bands (<15, 15-20, 20-25, 25-30, 30-35, 35+), ascending.
          items:
            $ref: "#/components/schemas/GrowthDirectorWeightBand"
        movement:
          $ref: "#/components/schemas/GrowthDirectorBandMovement"
    GrowthDirectorWeightBand:
      type: object
      additionalProperties: false
      required: [band, identity_count]
      properties:
        band:
          type: string
          enum: ["<15", "15-20", "20-25", "25-30", "30-35", "35+"]
        identity_count:
          type: integer
    GrowthDirectorBandMovement:
      type: object
      additionalProperties: false
      description: >-
        Band movement between an identity's previous and latest campaign rounds.
        `pair_identities` is the honest denominator: a kid has to be weighed in two
        rounds before it can move a band.
      required: [pair_identities, moved_up, held, moved_down]
      properties:
        pair_identities:
          type: integer
        moved_up:
          type: integer
        held:
          type: integer
        moved_down:
          type: integer
    GrowthDirectorFairFight:
      type: object
      additionalProperties: false
      description: >-
        Same breed, same sex, different sheds — a fairer comparison that points at
        shed-level causes. A cohort renders only when at least two sheds each field
        at least three pair-identities. Breed and sex come from the herd register
        only, never from shed names or feed-sheet strings.
      required: [cohorts]
      properties:
        cohorts:
          type: array
          items:
            $ref: "#/components/schemas/GrowthDirectorFairFightCohort"
    GrowthDirectorFairFightCohort:
      type: object
      additionalProperties: false
      required: [breed, sex, sheds]
      properties:
        breed:
          type: string
        sex:
          type: string
        sheds:
          type: array
          description: Strongest median first.
          items:
            $ref: "#/components/schemas/GrowthDirectorFairFightShed"
    GrowthDirectorFairFightShed:
      type: object
      additionalProperties: false
      required: [location_id, operational_key, shed_display_name, pair_identities, median_adg_g_per_day]
      properties:
        location_id:
          type: string
          format: uuid
        operational_key:
          type: string
          description: Stable operational-location identity — parent shed uuid plus normalized partition (partition is the operational shed).
        shed_display_name:
          type: string
        pair_identities:
          type: integer
          description: The n behind the median.
        median_adg_g_per_day:
          type: number
          format: double
    GrowthDirectorSlowGrowth:
      type: object
      additionalProperties: false
      description: >-
        Every (shed, breed, sex) group with at least three plausible pair-identities,
        slowest first, judged against the disclosed target. Changes within 3% of body
        weight are scored as flat (gut fill); losses over 0.30 kg/day are treated as
        bad scans and excluded.
      required: [target_g_per_day, groups]
      properties:
        target_g_per_day:
          type: number
          format: double
          description: The ops rule-of-thumb daily gain target (200), disclosed rather than buried.
        groups:
          type: array
          items:
            $ref: "#/components/schemas/GrowthDirectorSlowGrowthGroup"
    GrowthDirectorSlowGrowthGroup:
      type: object
      additionalProperties: false
      required:
        - location_id
        - operational_key
        - shed_display_name
        - breed
        - sex
        - pair_identities
        - median_adg_g_per_day
        - week_over_week_delta_g
        - status
      properties:
        location_id:
          type: string
          format: uuid
        operational_key:
          type: string
          description: Stable operational-location identity — parent shed uuid plus normalized partition (partition is the operational shed).
        shed_display_name:
          type: string
        breed:
          type: string
        sex:
          type: string
        pair_identities:
          type: integer
        median_adg_g_per_day:
          type: number
          format: double
        week_over_week_delta_g:
          type: number
          format: double
          nullable: true
          description: >-
            This week's median consecutive-round gain minus last week's, for this
            group. Null until the group has pairs in two distinct campaign weeks —
            two weeks of weighing are needed before a trend exists.
        status:
          type: string
          enum: [on_track, below_target, losing]
          description: >-
            Judged on the noise-adjusted median, so one noisy scale never brands a
            shed as shrinking.
    GrowthDirectorFeedVsGrowth:
      type: object
      additionalProperties: false
      description: >-
        Feed DIRECTED against growth measured, per shed. `estimate` is always true:
        the feed figure is what the sheet told the team to give, not what the kids
        finished — leftovers are not measured yet.
      required: [sheds, estimate]
      properties:
        sheds:
          type: array
          items:
            $ref: "#/components/schemas/GrowthDirectorFeedVsGrowthShed"
        estimate:
          type: boolean
    GrowthDirectorFeedVsGrowthShed:
      type: object
      additionalProperties: false
      description: >-
        Null never means zero on this row: it means "not computable", with the
        reason readable from the other fields. Blocked feed cells (quantity NULL)
        stay out of the feed sum; authored zeros stay in. Experiment /
        informational-headcount kg is never divided by heads.
      required:
        - location_id
        - partition_label
        - shed_display_name
        - feed_g_per_head_per_day
        - adg_g_per_day
        - kg_feed_per_kg_gain
        - basis
        - is_experiment
        - pair_identities
      properties:
        location_id:
          type: string
          format: uuid
        partition_label:
          type: string
          description: >-
            The PEN within location_id, blank for an undivided shed. Half of this row's identity,
            not decoration: the row grain is one pen, so a partitioned shed returns up to ten rows
            under ONE location_id and location_id alone identifies none of them.
        shed_display_name:
          type: string
        feed_g_per_head_per_day:
          type: number
          format: double
          nullable: true
        adg_g_per_day:
          type: number
          format: double
          nullable: true
        kg_feed_per_kg_gain:
          type: number
          format: double
          nullable: true
          description: Null when either side is missing or gain is non-positive — never 0, never infinity.
        basis:
          type: string
          enum: [per_animal, whole_shed]
          description: >-
            per_animal = median of per-kid gains (at least three pairs);
            whole_shed = the shed's average movement over thinner data.
        is_experiment:
          type: boolean
        pair_identities:
          type: integer
    GrowthDirectorFeedProblems:
      type: object
      additionalProperties: false
      description: >-
        Feed-sheet cells that could not be filled in. Blocked is structural
        (quantity NULL iff a reason code exists); an authored zero is a real
        instruction, counted separately and never listed as a problem.
      required:
        - blocked_rows_latest_day
        - blocked_rows_history
        - authored_zero_latest_day
        - authored_zero_history
        - items
      properties:
        blocked_rows_latest_day:
          type: integer
        blocked_rows_history:
          type: integer
        authored_zero_latest_day:
          type: integer
        authored_zero_history:
          type: integer
        items:
          type: array
          description: Most recently blocked first.
          items:
            $ref: "#/components/schemas/GrowthDirectorFeedProblemItem"
    GrowthDirectorFeedProblemItem:
      type: object
      additionalProperties: false
      required: [shed_label, feed_item_label, blocked_days, latest_reason_code]
      properties:
        shed_label:
          type: string
        feed_item_label:
          type: string
        blocked_days:
          type: integer
        latest_reason_code:
          type: string
    GrowthDirectorTrust:
      type: object
      additionalProperties: false
      description: >-
        The honest-denominator panel behind every other widget. Unlike the growth
        widgets it INCLUDES rework captures — trust reports the raw stream — and
        breaks out the rework count so the two views reconcile.
      required:
        - scans_total
        - scans_matched
        - scans_unmatched
        - identities_total
        - identities_with_pair
        - identities_once_only
        - whole_shed_observations
        - scans_pending_verification
        - scans_rework
      properties:
        scans_total:
          type: integer
        scans_matched:
          type: integer
        scans_unmatched:
          type: integer
        identities_total:
          type: integer
        identities_with_pair:
          type: integer
          description: Identities weighed in at least two campaign rounds — the only ones a gain exists for.
        identities_once_only:
          type: integer
        whole_shed_observations:
          type: integer
          description: Live lump-sum weighings in the period (withdrawn rows excluded).
        scans_pending_verification:
          type: integer
        scans_rework:
          type: integer
    WeighingPark:
      type: object
      additionalProperties: false
      description: >-
        One park the caller may filter weighing by. Identity only -- anything date-scoped or
        count-bearing belongs on the planner catalog, which is a different grain and a
        different gate.
      required:
        - park_id
        - name
      properties:
        park_id:
          type: string
          format: uuid
        name:
          type: string
    WeighingPlannerCampaignSummary:
      type: object
      additionalProperties: false
      required:
        - campaign_id
        - status
        - period_start_date
        - period_end_date
        - start_business_date
        - operator_user_id
        - shed_count
      properties:
        campaign_id:
          type: string
          format: uuid
        status:
          $ref: "#/components/schemas/WeighingCampaignStatus"
        period_start_date:
          type: string
          format: date
        period_end_date:
          type: string
          format: date
        start_business_date:
          type: string
          format: date
        operator_user_id:
          type: string
          format: uuid
        shed_count:
          type: integer
          minimum: 0
    WeighingPlannerShed:
      type: object
      additionalProperties: false
      required:
        - location_id
        - name
        - kid_count
      properties:
        location_id:
          type: string
          format: uuid
        name:
          type: string
        partition_label:
          type:
            - string
            - "null"
          description: >-
            Raw stored partition label for this shed ('1', 'Part 3'). Null or
            absent means the shed is non-partitioned. Never the literal
            string "whole".
        source_shed_name:
          type:
            - string
            - "null"
          description: >-
            Original partition-bearing source name (e.g. "Castro 1"), kept for
            traceability only. Not a display field.
        operational_location_display:
          type: string
          description: >-
            User-facing label for this shed option. No partition -> bare shed
            name ("Yashoda"); numeric convention -> "Castro 2"; prefixed
            convention -> "Godel 1 - Part 3".
        kid_count:
          type: integer
          minimum: 0
        scheduled:
          type: boolean
          description: >-
            True when an open weighing task already claims this shed on the requested weigh
            date. Absent/false means the shed is free on that date.
        scheduled_campaign_id:
          type: string
          format: uuid
        scheduled_status:
          $ref: "#/components/schemas/WeighingCampaignShedStatus"
        scheduled_operator_user_id:
          type: string
          format: uuid
        scheduled_operator_display_name:
          type: string
        scheduled_weighing_category:
          $ref: "#/components/schemas/WeighingCategory"
    WeighingPlannerPark:
      type: object
      additionalProperties: false
      required:
        - park_id
        - name
        - kid_count
        - shed_count
      properties:
        park_id:
          type: string
          format: uuid
        name:
          type: string
        kid_count:
          type: integer
          minimum: 0
        shed_count:
          type: integer
          minimum: 0
          description: >-
            PARK-GRAIN count of this park's active sheds, computed over the park's own
            children. It is not a count of rows on any page: this response carries no shed
            rows, and a bucket page carries only ~20. Read the sheds themselves from
            /app/weighing/planner/parks/{park_id}/buckets.
        existing_campaign:
          $ref: "#/components/schemas/WeighingPlannerCampaignSummary"
        existing_campaign_count:
          type: integer
          minimum: 0
          description: >-
            How many non-canceled weighing tasks this park holds on the requested week.
            A park-week may legitimately hold SEVERAL tasks: the capture category
            (individual vs lump-sum) is a per-BUCKET property, so leadership plans some
            sheds now and the park's leftover sheds as a separate task. existing_campaign
            summarizes only the MOST RECENT of them, so treat this count -- not the
            presence of existing_campaign -- as the answer to "how much is already
            scheduled here". Never use either field to block creating another task:
            per-shed availability is reported by the buckets endpoint.
    WeighingPlannerOperator:
      type: object
      additionalProperties: false
      required:
        - user_id
        - display_name
        - display_code
      properties:
        user_id:
          type: string
          format: uuid
        display_name:
          type: string
        display_code:
          type: string
    WeighingPlannerParkBucketsResponse:
      type: object
      additionalProperties: false
      required:
        - park_id
        - sheds
      properties:
        park_id:
          type: string
          format: uuid
          description: The park these buckets belong to; echoes the path parameter.
        sheds:
          type: array
          items:
            $ref: "#/components/schemas/WeighingPlannerShed"
        next_cursor:
          type: string
          description: >-
            Keyset cursor for this park's next bucket page. Absent/empty means the last page.
            The cursor is scoped to the park in the path, so a page can never carry rows from
            another park.
        trace_id:
          type: string
    WeighingPlannerCatalogResponse:
      type: object
      additionalProperties: false
      required:
        - parks
        - operators
      properties:
        parks:
          type: array
          items:
            $ref: "#/components/schemas/WeighingPlannerPark"
        operators:
          type: array
          items:
            $ref: "#/components/schemas/WeighingPlannerOperator"
        trace_id:
          type: string
    WeighingRosterRow:
      type: object
      additionalProperties: false
      required:
        - campaign_id
        - campaign_shed_id
        - animal_id
        - display_animal_id
        - expected_location_id
        - expected_location_label
        - status
        - availability_status
        - seq
      properties:
        campaign_id:
          type: string
          format: uuid
        campaign_shed_id:
          type: string
          format: uuid
        animal_id:
          type: string
          format: uuid
        display_animal_id:
          type: string
        primary_identifier:
          type: string
        secondary_identifier:
          type: string
        expected_location_id:
          type: string
          format: uuid
        expected_location_label:
          type: string
        expected_location_partition_label:
          type:
            - string
            - "null"
          description: >-
            Raw stored partition label for expected_location_id ('1',
            'Part 3'). Null or absent means non-partitioned.
        expected_operational_location_display:
          type: string
          description: >-
            User-facing label for expected_location_id. No partition -> bare
            shed name; numeric convention -> "Castro 2"; prefixed convention
            -> "Godel 1 - Part 3".
        status:
          type: string
          enum: [pending, weighed, unavailable, missed, canceled, closed_by_override]
        availability_status:
          type: string
          enum: [expected_shed, moved_other_shed, icu, quarantine, dead, culled, sold_transferred, exited, unknown_review]
        current_location_id:
          type: string
          format: uuid
        current_location_label:
          type: string
        current_location_partition_label:
          type:
            - string
            - "null"
          description: >-
            Raw stored partition label for current_location_id ('1',
            'Part 3'). Null or absent means non-partitioned.
        current_operational_location_display:
          type: string
          description: >-
            User-facing label for current_location_id. No partition -> bare
            shed name; numeric convention -> "Castro 2"; prefixed convention
            -> "Godel 1 - Part 3".
        current_lifecycle_status:
          type: string
        seq:
          type: integer
          minimum: 1
    WeighingCampaignShedPageResponse:
      type: object
      additionalProperties: false
      required:
        - campaign_id
        - items
        - total_count
      properties:
        campaign_id:
          type: string
          format: uuid
        items:
          type: array
          items:
            $ref: "#/components/schemas/WeighingCampaignShed"
        next_cursor:
          type: string
          description: Keyset cursor for the next page. Absent/empty means the last page.
        total_count:
          type: integer
          minimum: 0
          description: >-
            WHOLE-TASK bucket count over the same scope the rows range over — never the length
            of this page, so the header does not change as the user scrolls.
        trace_id:
          type: string
    WeighingRosterResponse:
      type: object
      additionalProperties: false
      required:
        - items
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/WeighingRosterRow"
        observations:
          type: array
          items:
            $ref: "#/components/schemas/WeighingObservation"
        next_cursor:
          type: string
        next_observations_cursor:
          type: string
          description: >-
            Opaque cursor for the next page of observations, keyset-paginated
            on (accepted_at, observation_id) within this campaign_shed_id.
            Empty/absent means no further observations for this request.
        trace_id:
          type: string
    CreateWeighingCampaignShed:
      type: object
      additionalProperties: false
      required:
        - location_id
        - location_type
        - display_name
        - weighing_category
      properties:
        location_id:
          type: string
          format: uuid
        location_type:
          type: string
          enum: [shed, cohort, pen]
        display_name:
          type: string
          minLength: 1
        partition_label:
          type: string
          description: >-
            Raw operational partition label for this shed bucket, e.g. "1" or
            "Part 3". Required when location_id names a partitioned physical
            shed; omitted/empty only for truly unpartitioned sheds. The backend
            validates it against the active shed partition catalog and stores
            null for unpartitioned sheds.
        weighing_category:
          $ref: "#/components/schemas/WeighingCategory"
        operator_user_id:
          type: string
          format: uuid
    CreateWeighingCampaignRequest:
      type: object
      additionalProperties: false
      required:
        - park_id
        - period_start_date
        - period_end_date
        - start_business_date
        - planned_cap_per_day
        - operator_user_id
        - sheds
      properties:
        park_id:
          type: string
          format: uuid
        period_start_date:
          type: string
          format: date
        period_end_date:
          type: string
          format: date
        start_business_date:
          type: string
          format: date
        planned_cap_per_day:
          type: integer
          minimum: 1
        operator_user_id:
          type: string
          format: uuid
        sheds:
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/CreateWeighingCampaignShed"
    RecordWeighingAnimalObservationRequest:
      type: object
      additionalProperties: false
      required:
        - campaign_shed_id
        - scanned_identifier
        - weight_kg
        - proof_artifact_id
        - actual_location_id
      properties:
        campaign_shed_id:
          type: string
          format: uuid
        # Weighing is free-flow: the raw scanned identifier is the required
        # identity. There is deliberately no animal_id field here at all --
        # weighing never resolves a scan to herd identity (backend
        # domain.RecordAnimalObservation carries no such field either), and the
        # backing column was dropped by
        # 000078_weighing_observations_drop_animal_id.sql.
        scanned_identifier:
          type: string
        weight_kg:
          type: number
          exclusiveMinimum: 0
        proof_artifact_id:
          type: string
          format: uuid
        actual_location_id:
          type: string
          format: uuid
    RecordWeighingShedObservationRequest:
      type: object
      additionalProperties: false
      required:
        - campaign_shed_id
        - weight_kg
        - animal_count
      properties:
        campaign_shed_id:
          type: string
          format: uuid
        weight_kg:
          type: number
          exclusiveMinimum: 0
        average_weight_kg:
          type: number
          exclusiveMinimum: 0
        animal_count:
          type: integer
          minimum: 1
        proof_artifact_id:
          type: string
          format: uuid
        proof_artifact_ids:
          type: array
          items:
            type: string
            format: uuid
    WeighingObservation:
      type: object
      additionalProperties: false
      required:
        - observation_id
        - campaign_id
        - weight_kg
        - proof_artifact_id
        - accepted_at
      properties:
        observation_id:
          type: string
          format: uuid
        campaign_id:
          type: string
          format: uuid
        campaign_shed_id:
          type: string
          format: uuid
        # The raw scanned identifier (RFID/ear tag) the scanner read. Weighing is
        # free-flow: this is the ONLY identity an observation carries. There is
        # deliberately no animal_id here -- weighing never resolves a scan to
        # herd identity, and the backing column was dropped by
        # 000078_weighing_observations_drop_animal_id.sql. NOT a uuid: a real
        # RFID is not UUID-shaped.
        scanned_identifier:
          type: string
        weight_kg:
          type: number
        average_weight_kg:
          type: number
        animal_count:
          type: integer
        proof_artifact_id:
          type: string
          format: uuid
        proof_artifact_ids:
          type: array
          items:
            type: string
            format: uuid
        media:
          type: array
          items:
            $ref: "#/components/schemas/WeighingProofMedia"
        expected_location_id:
          type: string
          format: uuid
        actual_location_id:
          type: string
          format: uuid
        actual_location_label:
          type: string
        accepted_at:
          type: string
          format: date-time
    WeighingProofMedia:
      type: object
      additionalProperties: false
      required:
        - proof_id
        - download_url
      properties:
        proof_id:
          type: string
          format: uuid
        download_url:
          type: string
          format: uri
        mime_type:
          type: string
    WeighingShedVideos:
      type: object
      additionalProperties: false
      required:
        - campaign_id
        - campaign_shed_id
        - shed_name
        - park_name
        - operational_location_display
        - weigh_date
        - operator_user_id
        - operator_display_name
        - weighing_category
        - status
        - estimated_animal_count
        - max_shed_videos
        - individual
      properties:
        campaign_id:
          type: string
          format: uuid
        campaign_shed_id:
          type: string
          format: uuid
        shed_name:
          type: string
        park_name:
          type: string
          description: The park this bucket's task belongs to. Rendered as the screen eyebrow.
        partition_label:
          type: string
          nullable: true
          description: >-
            Partition label for subdivided sheds (e.g., "Part 1", "Part 2"), or null for
            undivided sheds. Must be displayed alongside shed_name to form operational_location_display.
        operational_location_display:
          type: string
          description: >-
            Backend-composed display string for the physical location: shed_name alone for
            undivided sheds, or "shed_name - partition_label" for subdivided ones (e.g., "Castro 1 - Part 1").
            This is the ONLY display-safe location identifier and must be rendered verbatim.
        weigh_date:
          type: string
          format: date
          description: The task's Asia/Kolkata business DATE. Never a timestamp.
        operator_user_id:
          type: string
          description: >-
            Who owns this bucket. An EMPTY value is the only thing that entitles a client to
            say the bucket is not assigned.
        operator_display_name:
          type: string
          description: >-
            Backend-resolved assignee name. Empty WITH a non-empty operator_user_id means the
            assignee has no active workforce record — a roster gap, not "not assigned".
        estimated_animal_count:
          type: integer
          minimum: 0
          description: >-
            Planning-time herd estimate for the shed. A coverage hint, never a completeness
            denominator — weighing is free-flow and has no expected roster.
        max_shed_videos:
          type: integer
          minimum: 1
          description: Group-video allowance for a lump-sum submission, so "N of M" reads off policy.
        next_individual_cursor:
          type: string
          description: >-
            Keyset cursor for the next page of `individual`, on (accepted_at, observation_id).
            Absent/empty means the last page.
        weighing_category:
          $ref: "#/components/schemas/WeighingCategory"
        status:
          type: string
        individual:
          type: array
          items:
            $ref: "#/components/schemas/WeighingObservation"
        lump_sum:
          $ref: "#/components/schemas/WeighingObservation"
        period_label:
          type: string
          description: >-
            Backend-owned sentence for the weigh period this bucket belongs to. A client must not
            build this label by concatenating dates itself.
    WeighingAlert:
      type: object
      additionalProperties: false
      required:
        - alert_id
        - kind
        - direction
        - title
        - body
        - severity
        - target
        - occurred_at
      description: >-
        ONE weighing work-state transition that was routed to the caller. GRAIN: one row =
        one transition for one recipient, collapsed across that person's devices. Every
        human-visible string is authored by the backend. No goat, herd identity, or
        expected-roster denominator appears here: weighing is free-flow and fully isolated.
      properties:
        alert_id:
          type: string
        kind:
          type: string
          description: >-
            Producer's transition type, e.g. weighing_campaign_published,
            weighing_shed_submitted, weighing_shed_reopened, weighing_rework,
            weighing_shed_closed. Clients may branch on it for iconography; they must NOT
            rebuild the sentence from it.
        direction:
          type: string
          enum: [downstream, upstream]
          description: >-
            Which way this alert travelled FOR THIS RECIPIENT. downstream = it landed on the
            person who must do the work; upstream = on the person who oversees it. Derived
            from the recipient role the producer stamped, not from the event type, because
            one transition can travel both ways at once (a rework goes down to the operator
            and up to the director).
        title:
          type: string
          description: Backend-owned headline. Render verbatim.
        body:
          type: string
          description: Backend-owned sentence naming the sheds and the reason. Render verbatim.
        severity:
          type: string
          enum: [normal, high]
        target:
          type: string
          description: In-app destination this row opens, chosen by the producer (e.g. /weighing).
        shed_label:
          type: string
          description: Shed or shed list this transition concerns, as the producer named it.
        campaign_id:
          type: string
        park_id:
          type: string
        occurred_at:
          type: string
          format: date-time
    WeighingAlertPageResponse:
      type: object
      additionalProperties: false
      required:
        - items
        - title
        - empty_message
      description: >-
        ONE keyset page of the caller's weighing alerts, newest first.
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/WeighingAlert"
        next_cursor:
          type: string
          description: >-
            Keyset cursor on (occurred_at DESC, alert_id DESC). Absent/empty means the last
            page.
        title:
          type: string
          description: >-
            Backend-owned screen title. The client MUST render this rather than hardcode a
            weighing string, so the surface can be renamed without an app release.
        empty_message:
          type: string
          description: >-
            Backend-owned empty-state sentence, shown when items is empty. Also client-owned
            never.
        trace_id:
          type: string
    WeighingLeadershipShedPageResponse:
      type: object
      additionalProperties: false
      required:
        - items
      description: >-
        ONE keyset page of shed buckets across tasks, each carrying its own context and its FIRST
        page of captured evidence. GRAIN: one row = one bucket = one shed on one task. There is no
        denominator; each bucket states its own evidence cursor.
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/WeighingShedVideos"
        next_cursor:
          type: string
          description: >-
            Keyset cursor on (period_start_date, created_at, campaign_id, campaign_shed_id).
            Absent/empty means the last page.
        trace_id:
          type: string
    WeighingShedVideosResponse:
      type: object
      additionalProperties: false
      required:
        - shed
      properties:
        shed:
          $ref: "#/components/schemas/WeighingShedVideos"
        trace_id:
          type: string
    SubmitWeighingIndividualScopeRequest:
      type: object
      additionalProperties: false
      required:
        - scanned_identifiers
      properties:
        scanned_identifiers:
          type: array
          minItems: 1
          items:
            type: string
            minLength: 1
    WeighingSubmitResponse:
      type: object
      additionalProperties: false
      required:
        - status
      properties:
        status:
          type: string
          enum: [completed]
        trace_id:
          type: string
    WeighingCloseRequest:
      type: object
      additionalProperties: false
      required:
        - reason
      properties:
        reason:
          type: string
          minLength: 1
        idempotency_key:
          type: string
    WeighingCloseResult:
      type: object
      additionalProperties: false
      required:
        - campaign_id
        - closed_at
        - not_accepted_count
      properties:
        campaign_id:
          type: string
          format: uuid
        campaign_shed_id:
          type: string
          format: uuid
        closed_by:
          type: string
          format: uuid
        closed_at:
          type: string
          format: date-time
        not_accepted_count:
          type: integer
          minimum: 0
        not_accepted:
          type: array
          items:
            type: string
    WeighingCloseResponse:
      type: object
      additionalProperties: false
      required:
        - close
      properties:
        close:
          $ref: "#/components/schemas/WeighingCloseResult"
        trace_id:
          type: string
    WeighingWeightCorrectionRequest:
      type: object
      additionalProperties: false
      required:
        - ref_type
        - weight_kg
      properties:
        ref_type:
          type: string
          description: >-
            The observation grain, echoed from the verification item's `source.ref_type`.
          enum:
            - weighing_observation
            - weighing_shed_observation
        weight_kg:
          type: number
          format: double
          description: >-
            The corrected weight in kg. One animal's weight for an individual capture, the
            whole shed total for a lump-sum one. Rounded to three decimals, the scale the
            column stores.
          exclusiveMinimum: 0
          maximum: 100000
        animal_count:
          type: integer
          description: >-
            The corrected head count. LUMP-SUM ONLY -- omitted or 0 leaves the recorded count
            alone, which is the normal case. Sending it on an individual capture is refused
            (422 `animal_count_not_applicable`) rather than ignored, because an individual
            observation weighs exactly one animal and silently dropping it would report a
            correction that never happened.
          minimum: 0
          maximum: 100000
        reason:
          type: string
          description: The verifier's own words. Optional -- the video is the evidence.
        idempotency_key:
          type: string
          description: >-
            Falls back to the `Idempotency-Key` header. An exact replay returns the original
            result with no second write; the same key carrying a different weight is refused.
    WeighingWeightCorrectionResponse:
      type: object
      additionalProperties: false
      required:
        - weight_correction
      properties:
        weight_correction:
          $ref: "#/components/schemas/WeighingWeightCorrectionResult"
        trace_id:
          type: string
    WeighingWeightCorrectionResult:
      type: object
      additionalProperties: false
      description: >-
        The readback of one correction, carrying BOTH the new values and what they replaced:
        the client that just sent it needs the new number to render, and the audit trail needs
        the old one to be legible without a second read.
      required:
        - observation_id
        - ref_type
        - campaign_id
        - campaign_shed_id
        - weight_kg
        - previous_weight_kg
        - operator_weight_kg
        - subject_label
        - corrected_by
        - corrected_at
      properties:
        observation_id:
          type: string
          format: uuid
        ref_type:
          type: string
          enum:
            - weighing_observation
            - weighing_shed_observation
        campaign_id:
          type: string
          format: uuid
        campaign_shed_id:
          type: string
          description: The bucket to refresh once the correction lands.
        weight_kg:
          type: number
          format: double
          description: The weight AFTER the correction.
        animal_count:
          type: integer
          description: The head count after the correction. Lump-sum only.
        average_weight_kg:
          type: number
          format: double
          description: >-
            Recomputed from the corrected total and count, so the shed's average cannot
            disagree with the shed's own total. Lump-sum only.
        previous_weight_kg:
          type: number
          format: double
          description: What the row held immediately before this correction.
        previous_animal_count:
          type: integer
        operator_weight_kg:
          type: number
          format: double
          description: >-
            What the OPERATOR originally recorded, preserved across every later correction.
            Equal to `previous_weight_kg` on a first correction only.
        operator_animal_count:
          type: integer
        reason:
          type: string
        corrected_by:
          type: string
          format: uuid
        subject_label:
          type: string
          description: >-
            The recomposed verifier-facing sentence carrying the corrected weight. The backend
            pushes it onto the verification item so the queue stops advertising the number that
            was just replaced; clients render it verbatim.
        corrected_at:
          type: string
          format: date-time
    WeighingProcessStateDayMarker:
      type: object
      additionalProperties: false
      required:
        - business_date
        - open_count
        - delayed_count
      properties:
        business_date:
          type: string
          format: date
        open_count:
          type: integer
          minimum: 0
        delayed_count:
          type: integer
          minimum: 0
    WeighingProcessStateSummary:
      type: object
      additionalProperties: false
      required:
        - scheduled
        - delayed
        - completed
        - closed
        - canceled
        - open_total
        - total
      properties:
        scheduled:
          type: integer
          minimum: 0
        delayed:
          type: integer
          minimum: 0
        completed:
          type: integer
          minimum: 0
        closed:
          type: integer
          minimum: 0
        canceled:
          type: integer
          minimum: 0
        open_total:
          type: integer
          minimum: 0
        total:
          type: integer
          minimum: 0
    WeighingProcessState:
      type: object
      additionalProperties: false
      required:
        - grain
        - from_business_date
        - to_business_date
        - day_markers
        - summary
      properties:
        grain:
          type: string
          enum: [weighing_work_item]
        from_business_date:
          type: string
          format: date
        to_business_date:
          type: string
          format: date
        day_markers:
          type: array
          items:
            $ref: "#/components/schemas/WeighingProcessStateDayMarker"
        summary:
          $ref: "#/components/schemas/WeighingProcessStateSummary"
    WeighingObservationResponse:
      type: object
      additionalProperties: false
      required:
        - observation
      properties:
        observation:
          $ref: "#/components/schemas/WeighingObservation"
        trace_id:
          type: string
    WeighingParkShed:
      type: object
      additionalProperties: false
      description: A shed within a park, for the weight-history filter vocabulary.
      required:
        - campaign_shed_id
        - display_name
        - park_id
        - location_id
      properties:
        campaign_shed_id:
          type: string
          format: uuid
        display_name:
          type: string
        partition_label:
          type:
            - string
            - "null"
          description: >-
            Raw stored partition label for this shed ('1', 'Part 3'). Null or
            absent means the shed is non-partitioned. Never the literal
            string "whole".
        source_shed_name:
          type:
            - string
            - "null"
          description: >-
            Original partition-bearing source name (e.g. "Castro 1"), kept for
            traceability only. Not a display field.
        operational_location_display:
          type: string
          description: >-
            User-facing label for this shed option. No partition -> bare shed
            name ("Yashoda"); numeric convention -> "Castro 2"; prefixed
            convention -> "Godel 1 - Part 3".
        park_id:
          type: string
          format: uuid
        location_id:
          type: string
          format: uuid
    WeighingWeightHistoryPoint:
      type: object
      additionalProperties: false
      description: >-
        One observation: (scanned_identifier, weigh_date, weight_kg or shed totals). The
        individual-only fields (`scanned_identifier`, `weight_kg`) and the lump-sum-only fields
        (`total_weight_kg`, `average_weight_kg`, `animal_count`) are mutually exclusive per
        `capture_kind` and are omitted (not zero) on the side that does not apply.
      required:
        - capture_kind
        - weigh_date
        - campaign_shed_id
        - shed_display_name
      properties:
        capture_kind:
          type: string
          enum: [individual, lump_sum]
        scanned_identifier:
          type: string
          description: RFID/tag read by the scanner. Individual only; absent for lump_sum.
        weigh_date:
          type: string
          format: date
          description: Asia/Kolkata business date the observation was accepted.
        weight_kg:
          type: number
          description: Recorded weight. Individual only; absent for lump_sum.
        total_weight_kg:
          type: number
          description: Total weight of the shed. Lump-sum only.
        average_weight_kg:
          type: number
          description: Average weight per animal. Lump-sum only.
        animal_count:
          type: integer
          description: Number of animals in the lump-sum measurement. Lump-sum only.
        verification_status:
          type: string
          description: "pending, verified, or rework."
        campaign_shed_id:
          type: string
          format: uuid
        shed_display_name:
          type: string
    WeighingWeightHistorySeries:
      type: object
      additionalProperties: false
      description: >-
        The time series for one RFID tag (individual) or shed (lump_sum) across multiple weigh
        days.
      required:
        - capture_kind
        - points
      properties:
        scanned_identifier:
          type: string
          description: The series key for individual captures. Absent for lump_sum.
        campaign_shed_id:
          type: string
          format: uuid
          description: The shed id, for lump_sum series.
        shed_display_name:
          type: string
          description: For lump_sum series.
        capture_kind:
          type: string
          enum: [individual, lump_sum]
        points:
          type: array
          description: Ordered by weigh_date, oldest first.
          items:
            $ref: "#/components/schemas/WeighingWeightHistoryPoint"
    WeighingWeightHistoryResponse:
      type: object
      additionalProperties: false
      description: >-
        CEO-tier weight history for a park and optional shed scope. Parks and sheds are only
        those actually represented in the result, so the client can render filter chips with
        backend-owned vocabulary.
      required:
        - parks
        - sheds
        - series
        - truncated
      properties:
        parks:
          type: array
          items:
            $ref: "#/components/schemas/WeighingPark"
        sheds:
          type: array
          items:
            $ref: "#/components/schemas/WeighingParkShed"
        series:
          type: array
          items:
            $ref: "#/components/schemas/WeighingWeightHistorySeries"
        truncated:
          type: boolean
          description: >-
            True if the result was capped (too many unique tags, too many weigh days, or too many
            points). When true, `capped_at` names which limit was hit.
        capped_at:
          type: string
          enum: [max_unique_tags, max_weigh_days, max_points]
    WeighingGrowthHeadline:
      type: object
      additionalProperties: false
      description: >-
        Herd-level ADG (Average Daily Gain) summary for the requested period, scoped to a park.
        `status: "insufficient_data"` means no animal was weighed twice in the period, and every
        nullable field below is null rather than a fabricated 0.
      required:
        - status
        - previous_status
        - negative_adg_count
        - losing_animal_count
        - pair_count
        - rejected_observation_count
        - unverified_observation_count
      properties:
        status:
          type: string
          enum: [ok, insufficient_data]
        median_adg_g_per_day:
          type: number
          nullable: true
        previous_median_adg_g_per_day:
          type: number
          nullable: true
        previous_status:
          type: string
          enum: [ok, insufficient_data]
        delta_g_per_day:
          type: number
          nullable: true
          description: median_adg_g_per_day - previous_median_adg_g_per_day. Null whenever either side is null.
        positive_adg_percent:
          type: number
          nullable: true
        negative_adg_count:
          type: integer
          description: Count of qualifying PAIRS with ADG < 0 across the whole period.
        losing_animal_count:
          type: integer
          description: Count of ANIMALS whose most recent pair is negative.
        pair_count:
          type: integer
        rejected_observation_count:
          type: integer
        unverified_observation_count:
          type: integer
    WeighingGrowthEligibility:
      type: object
      additionalProperties: false
      required:
        - animals_with_two_plus_weighs
        - total_animals_weighed
      properties:
        animals_with_two_plus_weighs:
          type: integer
        total_animals_weighed:
          type: integer
    WeighingGrowthTrendPoint:
      type: object
      additionalProperties: false
      required:
        - week_start
        - median_adg_g_per_day
        - pair_count
      properties:
        week_start:
          type: string
          format: date
        median_adg_g_per_day:
          type: number
        pair_count:
          type: integer
    WeighingGrowthShedLeaderboardRow:
      type: object
      additionalProperties: false
      required:
        - location_id
        - display_name
        - operational_location_display
        - park_name
        - n
        - median_weight_kg
        - median_adg_g_per_day
        - adg_pair_count
      properties:
        location_id:
          type: string
          format: uuid
        display_name:
          type: string
        partition_label:
          type: string
        operational_location_display:
          type: string
        park_name:
          type: string
          description: >-
            The park's SHORT CODE (CBE, CPT) when it has one, falling back to its full name --
            the same convention the shed-weights rows use, so both series of the gain chart name
            a park identically. Required rather than optional: 39 shed names exist in BOTH parks,
            so a row without it names two different sheds at once.
        n:
          type: integer
          description: Animal count.
        median_weight_kg:
          type: number
        median_adg_g_per_day:
          type: number
        adg_pair_count:
          type: integer
          description: Can be less than `n` -- a shed can have animals weighed once (no pair yet).
    WeighingGrowthDistributionBucket:
      type: object
      additionalProperties: false
      description: >-
        One bin of the ADG histogram. Bins are fixed at 25 g/day width, plus one explicit
        negative bucket.
      required:
        - label
        - count
      properties:
        label:
          type: string
          description: 'Human-readable bucket name, e.g. "negative", "0-25", "300+".'
        min_g_per_day:
          type: number
          description: Inclusive lower edge. Absent for the negative bucket, which has no floor.
        max_g_per_day:
          type: number
          description: Exclusive upper edge. Absent for the top overflow bucket.
        count:
          type: integer
    WeighingGrowthSaleReadiness:
      type: object
      additionalProperties: false
      description: >-
        Counts animals against fixed weight thresholds using each animal's latest weight ever
        recorded (not bounded to the requested period).
      required:
        - at_or_above_30kg
        - at_or_above_35kg
        - animals_considered
      properties:
        at_or_above_30kg:
          type: integer
        at_or_above_35kg:
          type: integer
        animals_considered:
          type: integer
          description: Distinct resolved animals with at least one accepted weigh ever recorded.
    WeighingGrowthLumpSumShedTrendPoint:
      type: object
      additionalProperties: false
      description: >-
        One shed's one week of population-level (not per-animal) average weight. Kept entirely
        separate from the individual ADG numbers: shed population changes between weighs.
      required:
        - location_id
        - display_name
        - operational_location_display
        - week_start
        - average_weight_kg
        - head_count
      properties:
        location_id:
          type: string
          format: uuid
        display_name:
          type: string
        partition_label:
          type: string
        operational_location_display:
          type: string
        week_start:
          type: string
          format: date
        average_weight_kg:
          type: number
        head_count:
          type: integer
    WeighingGrowthLumpSum:
      type: object
      additionalProperties: false
      required:
        - shed_week_trend
      properties:
        shed_week_trend:
          type: array
          items:
            $ref: "#/components/schemas/WeighingGrowthLumpSumShedTrendPoint"
    WeighingGrowthLosingAnimal:
      type: object
      additionalProperties: false
      description: >-
        An animal that lost weight between its two most recent weighs, identified by its raw
        scanned tag only -- weighing never resolves a tag to a goat.
      required:
        - scanned_identifier
        - shed_display_name
        - operational_location_display
        - previous_weight_kg
        - latest_weight_kg
        - adg_g_per_day
        - days_between
        - latest_weigh_date
      properties:
        scanned_identifier:
          type: string
        shed_display_name:
          type: string
        operational_location_display:
          type: string
        previous_weight_kg:
          type: number
        latest_weight_kg:
          type: number
        adg_g_per_day:
          type: number
        days_between:
          type: number
        latest_weigh_date:
          type: string
          format: date
    WeighingWeightDemographicBucket:
      type: object
      additionalProperties: false
      required: [label, animals, average_weight_kg]
      properties:
        label: {type: string, description: "Value as stored; clients render it and do not re-map it."}
        animals: {type: integer, description: "Distinct animals behind the average."}
        average_weight_kg:
          type: number
          format: double
          description: "Mean of each animal's LATEST weight in the window -- a weighted mean over animals."
    WeighingWeightGainBucket:
      type: object
      additionalProperties: false
      required: [label, animals, median_gain_g_per_day]
      properties:
        label: {type: string}
        animals:
          type: integer
          description: "Animals with a computable gain -- always fewer than the weight bucket's count."
        median_gain_g_per_day:
          type: number
          format: double
          description: "Median across those animals, so one scale misread cannot swing a breed."
    WeighingShedCompositionChip:
      type: object
      additionalProperties: false
      required: [animals]
      properties:
        breed:
          type: string
          description: Breed label from the herd register, or Unknown breed when unresolved.
        sex:
          type: string
          description: Sex label from the herd register, or unknown sex when unresolved.
        animals:
          type: integer
          description: Animals in this breed+sex cohort.
    WeighingShedComposition:
      type: object
      additionalProperties: false
      required: [location_id, source, total_animals, chips]
      properties:
        location_id:
          type: string
          format: uuid
        partition_label:
          type: string
        source:
          type: string
          enum: [scanned_tags, live_shed_cohort]
          description: Whether chips came from the actual scanned tags or the live shed cohort for a whole-shed weigh.
        total_animals:
          type: integer
        chips:
          type: array
          items: {$ref: "#/components/schemas/WeighingShedCompositionChip"}
    WeighingWeightDemographicsResponse:
      type: object
      additionalProperties: false
      required: [by_breed, by_sex, by_stage, gain_by_breed, gain_by_sex, gain_by_stage, resolved_animals, unresolved_animals, lump_sum_animals, lump_sum_unattributed_animals, shed_composition]
      properties:
        by_breed:
          type: array
          description: "Per-animal weighs only; a whole-shed total cannot be split by breed."
          items: {$ref: "#/components/schemas/WeighingWeightDemographicBucket"}
        by_sex:
          type: array
          description: "Per-animal weighs only."
          items: {$ref: "#/components/schemas/WeighingWeightDemographicBucket"}
        by_stage:
          type: array
          description: "Per-animal weighs plus whole-shed weighs attributed to their shed's cohort."
          items: {$ref: "#/components/schemas/WeighingWeightDemographicBucket"}
        gain_by_breed:
          type: array
          description: "Same dimension as daily gain. Scanned animals only -- a whole-shed weigh yields no per-animal gain."
          items: {$ref: "#/components/schemas/WeighingWeightGainBucket"}
        gain_by_sex:
          type: array
          items: {$ref: "#/components/schemas/WeighingWeightGainBucket"}
        gain_by_stage:
          type: array
          items: {$ref: "#/components/schemas/WeighingWeightGainBucket"}
        resolved_animals: {type: integer}
        unresolved_animals:
          type: integer
          description: "Scanned tags with no animal in the herd register. Real weighs, reported not dropped."
        lump_sum_animals: {type: integer}
        lump_sum_unattributed_animals:
          type: integer
          description: "Animals in whole-shed weighs whose shed holds more than one stage, so no stage row claims them."
        shed_composition:
          type: array
          description: "Breed+sex chips keyed by location_id + partition_label for the weighed shed rows. Mixed sheds are listed as multiple chips; clients must not split one whole-shed average across them."
          items: {$ref: "#/components/schemas/WeighingShedComposition"}
    WeighingShedWeightsRow:
      type: object
      additionalProperties: false
      description: >-
        ONE operational shed's most recent weigh. Grain is the physical shed partition, not the campaign bucket.
      required:
        - location_id
        - park_id
        - park_name
        - shed_display_name
        - operational_location_display
        - weighing_category
        - animals_weighed
        - average_weight_kg
        - total_weight_kg
        - bucket_status
      properties:
        location_id:
          type: string
          format: uuid
        park_id:
          type: string
          format: uuid
        park_name:
          type: string
        shed_display_name:
          type: string
          description: >-
            Canonical shed name from the location record, never the free-text name typed
            when the task was planned.
        partition_label:
          type: string
        operational_location_display:
          type: string
          description: Backend-composed physical weighing location label. Render verbatim.
        weighing_category:
          type: string
          enum: [individual_animal, per_shed_partition]
        animals_weighed:
          type: integer
          description: >-
            Count of ANIMALS, not captures: distinct scanned tags for a per-animal shed, the
            declared head count for a whole-shed weigh.
        average_weight_kg:
          type: number
          format: double
        total_weight_kg:
          type: number
          format: double
          description: >-
            Weight of the animals actually weighed. NOT the shed's total weight -- free-flow
            weighing has no roster, so nothing knows which animals were missed.
        last_weighed_date:
          type: string
          format: date
          description: >-
            Business date of the most recent accepted weigh. Absent when the shed is planned
            but not yet weighed, which is a fact and not a fault.
        bucket_status:
          type: string
          enum: [pending, in_progress, completed, canceled]
        shed_average_gain_g_per_day:
          type: number
          format: double
          description: >-
            How fast this shed's AVERAGE weight is moving, for whole-shed sheds weighed
            more than once in the window. NOT per-animal growth: a shed's population
            changes between weighs, so if the lightest animals leave the average rises
            while no animal gained a gram. Measured from the first accepted weigh date
            in the selected window to the latest accepted weigh date in that same window.
        gain_span_days:
          type: integer
          description: >-
            The span that gain was measured over, so a short-span figure can be
            discounted rather than hidden.
    WeighingShedWeightsSummary:
      type: object
      additionalProperties: false
      description: >-
        WHOLE-FILTER rollup behind the KPI cards. Paging changes rows only, never these numbers.
      required:
        - sheds_weighed
        - sheds_in_scope
        - animals_weighed
        - total_weight_kg
        - at_or_above_30kg
        - at_or_above_35kg
        - threshold_basis_animals
      properties:
        sheds_weighed:
          type: integer
        sheds_in_scope:
          type: integer
        animals_weighed:
          type: integer
        total_weight_kg:
          type: number
          format: double
        average_weight_kg:
          type: number
          format: double
          nullable: true
          description: >-
            Weighted mean over ANIMALS (total / animals), never the mean of per-shed averages.
            Null when nothing was weighed, so a client never renders 0.0 kg for "no data".
        at_or_above_30kg:
          type: integer
        at_or_above_35kg:
          type: integer
        threshold_basis_animals:
          type: integer
          description: >-
            The real denominator for the two threshold counts: animals in PER-ANIMAL sheds only.
            A whole-shed weigh reports one average and cannot say how many of its animals cleared
            a threshold, so it contributes nothing here. Rendering the counts against
            animals_weighed instead would understate the share.
    WeighingShedWeightsResponse:
      type: object
      additionalProperties: false
      required:
        - parks
        - summary
        - rows
        - by_load
        - load_unattributed_sheds
        - period_start
        - period_end
      properties:
        parks:
          type: array
          description: Park filter vocabulary, limited to the caller's authorized scope.
          items:
            $ref: "#/components/schemas/WeighingPark"
        summary:
          $ref: "#/components/schemas/WeighingShedWeightsSummary"
        rows:
          type: array
          items:
            $ref: "#/components/schemas/WeighingShedWeightsRow"
        by_load:
          type: array
          description: >-
            Growth per procurement load, strongest grower first. Empty until the
            shed-to-load mapping is authored for the tenant.
          items:
            $ref: "#/components/schemas/WeighingLoadGainBucket"
        load_unattributed_sheds:
          type: integer
          description: >-
            Weighed sheds carrying no load tag, or more than one. Returned so the
            gap between the load chart and the shed table reads as unmapped rather
            than as missing weighing data.
        period_start:
          type: string
          format: date
        period_end:
          type: string
          format: date
    WeighingLoadGainBucket:
      type: object
      additionalProperties: false
      description: >-
        One procurement load's growth, blended across the sheds it was placed into.
        Attribution is SHED-level: a shed carrying two loads is excluded from every
        load and counted in load_unattributed_sheds instead, because one shed
        average cannot be split between two suppliers.
      required:
        - load_ref
        - sheds
        - animals
        - average_weight_kg
        - placements
      properties:
        load_ref:
          type: string
          description: The farm's own load number, rendered verbatim.
        owner_name:
          type: string
          description: Supplier the load was bought from. Absent when unrecorded.
        sheds:
          type: integer
          description: Tagged sheds behind this load that carry a weigh.
        animals:
          type: integer
          description: >-
            Head count at each contributing shed's latest weigh, summed. This is the
            denominator both figures below are weighted by.
        average_weight_kg:
          type: number
          format: double
          description: Weighted mean over animals, never a mean of per-shed averages.
        gain_g_per_day:
          type: number
          format: double
          description: >-
            Each shed's selected-window movement, blended by head count. Absent when
            no shed in the load was weighed twice inside the selected date range --
            a load with a single visible weigh has a weight but no growth, and 0
            would read as flat. This is shed-average movement, NOT per-animal
            growth: a shed's population changes between weighs, so if the lightest
            animals leave the average rises while no animal gained a gram.
        gain_span_days:
          type: integer
          description: >-
            Widest span any contributing shed was measured over, so a figure drawn
            from two days can be discounted on sight rather than hidden.
        placements:
          type: array
          description: >-
            Where this load's weighed animals actually are -- one entry per
            contributing operational shed row, park included. Aggregated over the
            same key set the figures above blend, so the animal counts sum to
            `animals` and the entry count equals `sheds`. Ordered park, then shed.
          items:
            $ref: "#/components/schemas/WeighingLoadPlacement"
    WeighingLoadPlacement:
      type: object
      additionalProperties: false
      description: >-
        One operational shed a procurement load's weighed animals sit in. The grain is
        (shed, partition) -- the measured row -- while the load TAG itself is authored
        at physical-shed grain, so a partition here says where the weighed animals are,
        never that the tag was authored per pen.
      required:
        - park_name
        - shed_display_name
        - operational_location_display
        - animals
      properties:
        park_name:
          type: string
          description: >-
            Park short code (CBE, CPT) when it has one, falling back to the full name --
            the same rule the shed table's park_name follows.
        shed_display_name:
          type: string
          description: Shed name from the locations register, never the weighing bucket's planning label.
        partition_label:
          type: string
          description: Human partition label ('Part 3'), absent for an undivided shed.
        operational_location_display:
          type: string
          description: Backend-composed park-local shed label; clients render it verbatim.
        animals:
          type: integer
          description: >-
            Head count at this shed row's latest weigh -- the same figure that weights
            it inside the load's blended average.
    WeighingGrowthADGResponse:
      type: object
      additionalProperties: false
      description: >-
        CEO-tier ADG / growth read model for a park or the herd. Weighing is free-flow: there is
        no weighing cadence rule, so no field here reports an "overdue" or "missed" weigh, and no
        target/benchmark ADG value is included anywhere.
      required:
        - park_ids
        - parks
        - losing_animals
        - period_start
        - period_end
        - headline
        - eligibility
        - trend
        - shed_leaderboard
        - distribution
        - sale_readiness
        - lump_sum
      properties:
        park_id:
          type: string
          format: uuid
          description: >-
            The single requested park. Absent when the caller omitted park_id -- this response
            then aggregates across every park in `park_ids`.
        park_ids:
          type: array
          items:
            type: string
            format: uuid
          description: Every park this response aggregates across.
        parks:
          type: array
          items:
            $ref: "#/components/schemas/WeighingPark"
          description: Names the ids in park_ids so a client can offer a park selector.
        losing_animals:
          type: array
          items:
            $ref: "#/components/schemas/WeighingGrowthLosingAnimal"
        period_start:
          type: string
          format: date
        period_end:
          type: string
          format: date
        headline:
          $ref: "#/components/schemas/WeighingGrowthHeadline"
        eligibility:
          $ref: "#/components/schemas/WeighingGrowthEligibility"
        trend:
          type: array
          items:
            $ref: "#/components/schemas/WeighingGrowthTrendPoint"
        shed_leaderboard:
          type: array
          items:
            $ref: "#/components/schemas/WeighingGrowthShedLeaderboardRow"
        distribution:
          type: array
          items:
            $ref: "#/components/schemas/WeighingGrowthDistributionBucket"
        sale_readiness:
          $ref: "#/components/schemas/WeighingGrowthSaleReadiness"
        lump_sum:
          $ref: "#/components/schemas/WeighingGrowthLumpSum"
    VaccinationShedVaccineRow:
      type: object
      additionalProperties: false
      required:
        - protocolId
        - name
        - workState
        - counts
      properties:
        protocolId:
          type: string
        name:
          type: string
        workState:
          $ref: "#/components/schemas/WorkState"
        lastDose:
          type:
            - string
            - "null"
        nextDue:
          type:
            - string
            - "null"
        counts:
          $ref: "#/components/schemas/VaccinationOperationsCounts"
    VaccinationShedSummaryResponse:
      type: object
      additionalProperties: false
      required:
        - source
        - rows
        - page
      properties:
        source:
          type: string
          enum:
            - api
        rows:
          type: array
          items:
            $ref: "#/components/schemas/VaccinationShedSummaryRow"
        page:
          $ref: "#/components/schemas/VaccinationPageInfo"
        freshness:
          $ref: "#/components/schemas/VaccinationProjectionFreshness"
    VaccinationShedDetail:
      type: object
      additionalProperties: false
      required:
        - source
        - parkId
        - parkName
        - shedId
        - shedName
        - animals
        - due
        - done
        - sessions
        - capacity
        - plannedSessions
        - status
        - vaccines
      properties:
        source:
          type: string
          enum:
            - api
        parkId:
          type: string
          format: uuid
        parkName:
          type: string
        shedId:
          type: string
          format: uuid
        shedName:
          type: string
        partition_label:
          type:
            - string
            - "null"
          description: >-
            Raw stored partition label for this shed ('1', 'Part 3'). Null or
            absent means the shed is non-partitioned. Never the literal
            string "whole".
        source_shed_name:
          type:
            - string
            - "null"
          description: >-
            Original partition-bearing source name (e.g. "Castro 1"), kept for
            traceability only. Not a display field.
        operational_location_display:
          type: string
          description: >-
            User-facing location label. No partition -> bare shed name
            ("Yashoda"); numeric convention -> "Castro 2"; prefixed convention
            -> "Godel 1 - Part 3".
        animals:
          type: integer
        due:
          type: integer
        done:
          type: integer
        sessions:
          type: integer
          description: Planned vaccination visits/days for this shed. Usually 1; >1 when the daily cap forces a split.
        manager:
          allOf:
            - $ref: "#/components/schemas/VaccinationShedOwner"
            - nullable: true
        backup:
          allOf:
            - $ref: "#/components/schemas/VaccinationShedOwner"
            - nullable: true
        capacity:
          $ref: "#/components/schemas/VaccinationCapacityStatus"
        plannedSessions:
          type: array
          description: Planned vaccination days for this shed, with daily cell and capacity breakdown.
          items:
            $ref: "#/components/schemas/VaccinationPlannedSession"
        status:
          $ref: "#/components/schemas/VaccinationShedStatus"
        vaccines:
          type: array
          items:
            $ref: "#/components/schemas/VaccinationShedVaccineRow"
    VaccinationShedAnimalRow:
      type: object
      additionalProperties: false
      required:
        - goatId
        - displayId
        - sex
        - lifecycleStatus
        - status
      properties:
        goatId:
          type: string
        displayId:
          type: string
        tag1:
          type:
            - string
            - "null"
        tag2:
          type:
            - string
            - "null"
        breed:
          type:
            - string
            - "null"
        sex:
          type: string
          enum:
            - female
            - male
        age:
          type:
            - string
            - "null"
        lifecycleStatus:
          type: string
        healthStatus:
          type:
            - string
            - "null"
        lastDose:
          type:
            - string
            - "null"
        nextDue:
          type:
            - string
            - "null"
        status:
          type: string
    VaccinationShedAnimalPage:
      type: object
      additionalProperties: false
      required:
        - rows
      properties:
        rows:
          type: array
          items:
            $ref: "#/components/schemas/VaccinationShedAnimalRow"
        nextCursor:
          type:
            - string
            - "null"
    VaccinationPassportDue:
      type: object
      additionalProperties: false
      required:
        - obligation_id
        - protocol_version_id
        - rule_id
        - workflow_row_id
        - status
        - due_at
        - clinical_due_at
        - sequence
        - dose_code
        - vaccine_label
        - display_label
        - shed_id
        - shed_name
        - partition_label
        - operational_location_display
      properties:
        obligation_id:
          type: string
        protocol_version_id:
          type: string
        rule_id:
          type: string
        batch_id:
          type: string
        workflow_row_id:
          type: string
        status:
          type: string
        due_at:
          type: string
          format: date-time
        clinical_due_at:
          type: string
          format: date-time
        scheduled_for:
          type: string
          format: date-time
        dose_code:
          type: string
        vaccine_label:
          type: string
        display_label:
          type: string
        sequence:
          type: integer
        shed_id:
          type: string
          description: UUID of the shed this obligation is scoped to, or empty if not shed-scoped.
        shed_name:
          type: string
          description: Display name of the shed, or empty if not shed-scoped.
        partition_label:
          type: string
          description: Raw partition label ('1', 'Part 3'), or empty string for non-partitioned sheds. Never the literal string 'whole'.
        operational_location_display:
          type: string
          description: Backend-composed display label combining shed name and partition ("Castro 2" or "Godel 1 - Part 3"). Empty if not shed-scoped.
    VaccinationPassportHistoryItem:
      type: object
      additionalProperties: false
      required:
        - completion_id
        - obligation_id
        - status
        - administered_at
        - doses
        - dose_code
        - vaccine_label
        - display_label
        - adverse_reaction
        - shed_id
        - shed_name
        - partition_label
        - operational_location_display
      properties:
        completion_id:
          type: string
        obligation_id:
          type: string
        batch_id:
          type: string
        status:
          type: string
        route_site:
          type: string
        administered_at:
          type: string
          format: date-time
        doses:
          type: integer
        dose_code:
          type: string
        vaccine_label:
          type: string
        display_label:
          type: string
        adverse_reaction:
          type: boolean
        withdrawal_until:
          type: string
          format: date-time
          nullable: true
        shed_id:
          type: string
          description: UUID of the shed where this vaccination occurred, or empty if not available.
        shed_name:
          type: string
          description: Display name of the shed, or empty if not available.
        partition_label:
          type: string
          description: Raw partition label ('1', 'Part 3'), or empty string for non-partitioned sheds. Never the literal string 'whole'.
        operational_location_display:
          type: string
          description: Backend-composed display label combining shed name and partition ("Castro 2" or "Godel 1 - Part 3"). Empty if not available.
    LastAcceptedVaccinationDose:
      type: object
      additionalProperties: false
      required:
        - completion_id
        - obligation_id
        - administered_at
      properties:
        completion_id:
          type: string
        obligation_id:
          type: string
        administered_at:
          type: string
          format: date-time
    VaccinationPassport:
      type: object
      additionalProperties: false
      required:
        - goat_id
        - next_due
        - open_obligations
        - last_accepted
        - vaccination_history
      properties:
        goat_id:
          type: string
        park_id:
          type: string
          description: UUID of the park the goat is currently assigned to. Empty if location unknown.
        park_name:
          type: string
          description: Display name of the park. Empty if location unknown.
        shed_id:
          type: string
          description: UUID of the physical shed. Empty if location unknown.
        shed_name:
          type: string
          description: Display name of the physical shed. Empty if location unknown.
        partition_label:
          type: string
          description: Raw partition label ('1', 'Part 3'), or empty string for non-partitioned sheds. Never the literal string 'whole'.
        operational_location_display:
          type: string
          description: Backend-composed display label combining shed name and partition ("Castro 2" or "Godel 1 - Part 3"). Bare shed name for non-partitioned sheds. Empty if location unknown.
        next_due:
          anyOf:
            - $ref: "#/components/schemas/VaccinationPassportDue"
            - type: "null"
        open_obligations:
          type: array
          items:
            $ref: "#/components/schemas/VaccinationPassportDue"
        last_accepted:
          anyOf:
            - $ref: "#/components/schemas/LastAcceptedVaccinationDose"
            - type: "null"
        vaccination_history:
          type: array
          items:
            $ref: "#/components/schemas/VaccinationPassportHistoryItem"
    EnrichedPosition:
      type: object
      additionalProperties: false
      required:
        - position_id
        - position_code
        - position_tier
        - scope_type
        - scope_id
        - status
      properties:
        position_id:
          type: string
          format: uuid
        workforce_member_id:
          type: string
          format: uuid
        position_code:
          type: string
        position_title:
          type:
            - string
            - "null"
        position_tier:
          type: string
        scope_type:
          type: string
          enum:
            - tenant
            - center
        scope_id:
          type: string
          format: uuid
        center_label:
          type:
            - string
            - "null"
        person_display_name:
          type:
            - string
            - "null"
        hr_designation_grade:
          type:
            - string
            - "null"
        tier:
          type:
            - string
            - "null"
        week_off:
          type:
            - string
            - "null"
        backup_group:
          type:
            - string
            - "null"
        is_backup_slot:
          type: boolean
        status:
          type: string
        valid_from:
          type: string
          format: date-time
        valid_to:
          type:
            - string
            - "null"
          format: date-time
    MyCoverage:
      type: object
      additionalProperties: false
      required:
        - has_coverage
        - timezone
      properties:
        has_coverage:
          type: boolean
        position_id:
          type:
            - string
            - "null"
          format: uuid
        covering_person_name:
          type:
            - string
            - "null"
        covering_position_title:
          type:
            - string
            - "null"
        window_start:
          type:
            - string
            - "null"
          format: date-time
        window_end:
          type:
            - string
            - "null"
          format: date-time
        banner_text:
          type:
            - string
            - "null"
        timezone:
          type: string
    MyCoverageResponse:
      type: object
      additionalProperties: false
      required:
        - coverage
        - trace_id
      properties:
        coverage:
          $ref: "#/components/schemas/MyCoverage"
        trace_id:
          type: string
          minLength: 1
          maxLength: 200
    Coverage:
      type: object
      additionalProperties: false
      required:
        - position_id
        - covered_position_code
        - start_date
        - end_date
        - source
        - status
      properties:
        position_id:
          type: string
          format: uuid
        covered_position_code:
          type: string
        covered_position_title:
          type:
            - string
            - "null"
        covering_member_id:
          type:
            - string
            - "null"
          format: uuid
        covering_member_name:
          type:
            - string
            - "null"
        start_date:
          type: string
          format: date
        end_date:
          type: string
          format: date
        source:
          type: string
          enum:
            - leave
            - week_off
            - escalation
        escalation_state:
          type:
            - string
            - "null"
        status:
          type: string
    CoverageListResponse:
      type: object
      additionalProperties: false
      required:
        - items
        - trace_id
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/Coverage"
        trace_id:
          type: string
          minLength: 1
          maxLength: 200
    RescheduleObligationRequest:
      type: object
      additionalProperties: false
      required:
        - due_at
      properties:
        due_at:
          type: string
          format: date-time
          description: The new due date for the obligation. Must be in the future.
        window_start:
          type:
            - string
            - "null"
          format: date-time
          description: Optional window start. Defaults to due_at if not provided.
        window_end:
          type:
            - string
            - "null"
          format: date-time
          description: Optional window end for the obligation. When provided, it must be greater than or equal to window_start.
    RescheduleObligationResponse:
      type: object
      additionalProperties: false
      required:
        - obligation_id
        - idempotent_replay
      properties:
        obligation_id:
          type: string
          format: uuid
          description: >-
            The obligation ID after rescheduling. If the original obligation was 'missed',
            this is a new obligation_id. Otherwise, it is the same as the path parameter.
        idempotent_replay:
          type: boolean
          description: True if this result was returned from a previous identical request (idempotency replay).
    VaccinationGapReasonCode:
      type: string
      description: >-
        Real, DB-backed reasons a live goat is excluded from the vaccination coverage denominator —
        derived from nullable goats columns (dob, breed/breed_id) the generation engine's eligibility
        selectors require to match a protocol rule.
      enum:
        - no_date_of_birth
        - no_breed_on_record
    VaccinationGapReasonSummary:
      type: object
      additionalProperties: false
      required:
        - reasonCode
        - reasonLabel
        - count
      properties:
        reasonCode:
          $ref: "#/components/schemas/VaccinationGapReasonCode"
        reasonLabel:
          type: string
        count:
          type: integer
          minimum: 0
    VaccinationGapRow:
      type: object
      additionalProperties: false
      required:
        - goatId
        - displayId
        - parkId
        - parkName
        - reasonCode
        - reasonLabel
      properties:
        goatId:
          type: string
          format: uuid
        displayId:
          type: string
        parkId:
          type: string
          format: uuid
        parkName:
          type: string
        shedId:
          type: string
          format: uuid
        partitionLabel:
          type: string
          nullable: true
          description: >-
            Raw stored partition label for the shed ('1', 'Part 3'). Null or absent
            means the shed is non-partitioned. Never the literal string 'whole' --
            that is a matching key, not user copy.
        operationalLocationDisplay:
          type: string
          description: >-
            User-facing location label composed by the backend (oploc.Display()).
            No partition -> bare shed name; partitioned -> 'Godel 1 - Part 3'.
            Clients RENDER this; they must not re-compose it.
        shedName:
          type: string
        reasonCode:
          $ref: "#/components/schemas/VaccinationGapReasonCode"
        reasonLabel:
          type: string
    VaccinationGapsResponse:
      type: object
      additionalProperties: false
      required:
        - source
        - reasons
        - rows
      properties:
        source:
          type: string
          enum:
            - api
        parkId:
          type: string
          format: uuid
        reasons:
          type: array
          items:
            $ref: "#/components/schemas/VaccinationGapReasonSummary"
        rows:
          type: array
          items:
            $ref: "#/components/schemas/VaccinationGapRow"
        nextCursor:
          type:
            - string
            - "null"
          maxLength: 500
    VaccinationCoverageProtocol:
      type: object
      additionalProperties: false
      required:
        - protocolId
        - name
        - givenCount
        - totalCount
        - coveragePercent
      properties:
        protocolId:
          type: string
          format: uuid
        name:
          type: string
        givenCount:
          type: integer
          minimum: 0
        totalCount:
          type: integer
          minimum: 0
        coveragePercent:
          type: integer
          minimum: 0
          maximum: 100
    VaccinationCoverageResponse:
      type: object
      additionalProperties: false
      required:
        - source
        - protocols
      properties:
        source:
          type: string
          enum:
            - api
        parkId:
          type: string
          format: uuid
        protocols:
          type: array
          items:
            $ref: "#/components/schemas/VaccinationCoverageProtocol"
    AppClientRuntimeConfig:
      type: object
      additionalProperties: false
      description: >-
        Backend-owned, backend-bounded client operational tuning — never business/medical policy
        (docs/mobile/backend-driven-config.md). Every value is clamped server-side to a safe min/max.
      required:
        - pageSizeDefault
        - syncBackoffBaseMs
        - syncBackoffMaxMs
        - refreshCadenceSec
        - cacheTtlSec
        - jankSamplingRate
      properties:
        pageSizeDefault:
          type: integer
          minimum: 1
        syncBackoffBaseMs:
          type: integer
          minimum: 0
        syncBackoffMaxMs:
          type: integer
          minimum: 0
        refreshCadenceSec:
          type: integer
          minimum: 1
        cacheTtlSec:
          type: integer
          minimum: 0
        jankSamplingRate:
          type: number
          minimum: 0
          maximum: 1
    AppConfigResponse:
      type: object
      additionalProperties: false
      required:
        - source
        - revision
        - cachePolicy
        - featureFlags
        - clientRuntimeConfig
        - policyRevision
      properties:
        source:
          type: string
          enum:
            - api
        revision:
          type: string
          minLength: 1
        cachePolicy:
          $ref: "#/components/schemas/AdminWebContractCachePolicy"
        featureFlags:
          type: object
          additionalProperties:
            type: boolean
        clientRuntimeConfig:
          $ref: "#/components/schemas/AppClientRuntimeConfig"
        policyRevision:
          type: string
          description: >-
            Read-only traceability echo (the governing business-rule doc, e.g.
            vaccination-rules.md) — never a raw threshold the client could compute from.
    HerdRegisterSummaryCounts:
      type: object
      additionalProperties: false
      required:
        - sex
        - lifecycleStatus
        - totalCount
        - activeCount
        - adultCount
        - kidCount
        - untaggedKidCount
        - deadCount
        - soldCount
        - culledCount
        - projectedAt
      properties:
        parkId:
          type: string
          format: uuid
          nullable: true
        farmId:
          type: string
          format: uuid
          nullable: true
        currentLocationId:
          type: string
          format: uuid
          nullable: true
        partition_label:
          type:
            - string
            - "null"
          description: >-
            Raw stored partition label for currentLocationId's shed ('1',
            'Part 3'). Null or absent means the shed is non-partitioned.
            Never the literal string "whole".
        source_shed_name:
          type:
            - string
            - "null"
          description: >-
            Original partition-bearing source name (e.g. "Castro 1"), kept for
            traceability only. Not a display field.
        operational_location_display:
          type:
            - string
            - "null"
          description: >-
            User-facing location label for currentLocationId. No partition ->
            bare shed name ("Yashoda"); numeric convention -> "Castro 2";
            prefixed convention -> "Godel 1 - Part 3".
        breed:
          type: string
          nullable: true
        sex:
          type: string
        lifecycleStatus:
          type: string
        totalCount:
          type: integer
          minimum: 0
        activeCount:
          type: integer
          minimum: 0
        adultCount:
          type: integer
          minimum: 0
        kidCount:
          type: integer
          minimum: 0
        untaggedKidCount:
          type: integer
          minimum: 0
        deadCount:
          type: integer
          minimum: 0
        soldCount:
          type: integer
          minimum: 0
        culledCount:
          type: integer
          minimum: 0
        projectedAt:
          type: string
          format: date-time
    HerdRegisterSummaryResponse:
      type: object
      additionalProperties: false
      required:
        - items
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/HerdRegisterSummaryCounts"
    CountsBreakdownRow:
      type: object
      additionalProperties: false
      required:
        - management_stage
        - breed
        - sex
        - count
      properties:
        park_id:
          type: string
          format: uuid
          nullable: true
        park_label:
          type: string
          description: >-
            Park code or name. The UI labels this column "Farm" because that is the
            business word and the source sheet's column name — the sheet's farm values
            (CBE/CPT) are loaded as locations of type 'park', so this is where that data
            lives. goats.farm_id exists but is unpopulated in real seeded data.
        shed_id:
          type: string
          format: uuid
          nullable: true
        shed_label:
          type: string
          description: Shed name or code; empty when the goat has no shed assigned.
        partition_label:
          type:
            - string
            - "null"
          description: >-
            Raw stored partition label for the shed ('1', 'Part 3'). Null or
            absent means the shed is non-partitioned. Never the literal string
            "whole".
        source_shed_name:
          type:
            - string
            - "null"
          description: >-
            Original partition-bearing source name (e.g. "Castro 1"), kept for
            traceability only. Not a display field.
        operational_location_display:
          type:
            - string
            - "null"
          description: >-
            User-facing location label. No partition -> bare shed name
            ("Yashoda"); numeric convention -> "Castro 2"; prefixed convention
            -> "Godel 1 - Part 3".
        management_stage:
          type: string
          description: >-
            Raw goats.management_stage. Free text with no CHECK constraint —
            near-duplicate source labels are reported verbatim, not normalized.
        breed:
          type: string
        sex:
          type: string
        count:
          type: integer
          minimum: 0
    CountsBreakdownSeriesPoint:
      type: object
      additionalProperties: false
      required:
        - key
        - label
        - count
      properties:
        key:
          type: string
        label:
          type: string
        count:
          type: integer
          minimum: 0
    CountsBreakdownCharts:
      type: object
      additionalProperties: false
      required:
        - breed
        - stage
        - sex
        - shed
      properties:
        breed:
          type: array
          items:
            $ref: "#/components/schemas/CountsBreakdownSeriesPoint"
        stage:
          type: array
          items:
            $ref: "#/components/schemas/CountsBreakdownSeriesPoint"
        sex:
          type: array
          items:
            $ref: "#/components/schemas/CountsBreakdownSeriesPoint"
        shed:
          type: array
          description: Top sheds by head count; display-capped, never the source of totals.
          items:
            $ref: "#/components/schemas/CountsBreakdownSeriesPoint"
    CountsBreakdownShedFacet:
      type: object
      additionalProperties: false
      description: >-
        One shed filter option. Carries park_id as its own field because SHED NAMES ARE NOT
        UNIQUE ACROSS PARKS — in real data 66 of 154 shed names exist in both parks, so a client
        cascading Park -> Shed must filter this list by park_id. key stays the shed UUID so an
        entry is unambiguous on its own, and the park is never encoded into label. When a shed
        has partitions, one row per partition is returned, each with its partition_label, count
        for that partition, and operational_location_display for the partition label.
      required:
        - key
        - label
        - count
        - park_id
        - shed_id
      properties:
        shed_id:
          type: string
          description: >-
            The PARENT physical shed uuid. Handed over explicitly so a client never parses it back
            out of the composite `key` — doing so once made the parent-aggregate option carry a
            partition key and silently broke partition filtering.
        key:
          type: string
          description: Shed UUID; empty for the bucket of animals with no shed assigned.
        label:
          type: string
          description: Shed name or code; for partitioned sheds, this is the shed name (without the partition suffix).
        count:
          type: integer
          minimum: 0
          description: Count of animals in this shed (or partition if partition_label is present).
        park_id:
          type: string
          description: >-
            Park UUID these animals sit in; empty only when the animals have no park assigned.
            Matches the park_id request filter, so count equals what
            ?park_id=<park_id>&shed_id=<key> returns.
        partition_label:
          type:
            - string
            - "null"
          description: >-
            Raw stored partition label for the shed ('1', 'Part 3'). Null or absent means
            the shed is non-partitioned. Never the literal string "whole".
        operational_location_display:
          type:
            - string
            - "null"
          description: >-
            User-facing location label for this operational location. No partition -> bare
            shed name ("Yashoda"); numeric convention -> "Castro 2"; prefixed convention
            -> "Godel 1 - Part 3".
    CountsBreakdownFacets:
      type: object
      additionalProperties: false
      required:
        - lifecycle
        - stages
        - breeds
        - parks
        - sheds
      properties:
        lifecycle:
          type: array
          description: >-
            Distinct lifecycle_status values present in the whole tenant herd (alive/dead/sold/
            culled/transferred), independent of the currently-selected lifecycle filter — this is
            what lets a client offer Live/Sold/Culled/Dead/Transferred as filter options rather
            than only ever showing the live herd.
          items:
            $ref: "#/components/schemas/CountsBreakdownSeriesPoint"
        stages:
          type: array
          description: Distinct management_stage values actually present, so a filter cannot offer a dead option.
          items:
            $ref: "#/components/schemas/CountsBreakdownSeriesPoint"
        breeds:
          type: array
          items:
            $ref: "#/components/schemas/CountsBreakdownSeriesPoint"
        parks:
          type: array
          description: >-
            Parks holding animals, keyed by park_id and labelled with the park code. Backs the
            Farm filter so its options are live data rather than a hardcoded park list.
          items:
            $ref: "#/components/schemas/CountsBreakdownSeriesPoint"
        sheds:
          type: array
          description: >-
            Sheds holding animals, keyed by shed_id and carrying park_id so a Park -> Shed
            cascade can filter them. Whole-result rollup, independent of limit/offset. UNCAPPED
            on purpose — unlike charts.shed, which is display-capped to the top 12 bars, this is
            a filter vocabulary and a silent truncation would present a partial shed list as the
            complete one. Bounded by the distinct shed vocabulary, not by herd size.
          items:
            $ref: "#/components/schemas/CountsBreakdownShedFacet"




    CountsBreakdownResponse:
      type: object
      additionalProperties: false
      required:
        - items
        - total_rows
        - total_count
        - total_kids
        - total_adults
        - charts
        - facets
        - projected_at
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/CountsBreakdownRow"
        total_rows:
          type: integer
          minimum: 0
          description: Distinct grain combinations across the FULL filtered set, independent of limit/offset.
        total_count:
          type: integer
          minimum: 0
          description: Sum of head counts across the FULL filtered set, independent of limit/offset.
        total_kids:
          type: integer
          minimum: 0
          description: >-
            Kid head count across the FULL filtered set. total_kids + total_adults always equals
            total_count exactly; an animal with an unknown age band counts as an adult rather
            than falling out of both buckets.
        total_adults:
          type: integer
          minimum: 0
          description: Adult head count across the FULL filtered set.
        charts:
          $ref: "#/components/schemas/CountsBreakdownCharts"
        facets:
          $ref: "#/components/schemas/CountsBreakdownFacets"
        projected_at:
          type: string
          format: date-time
    HerdAnalyticsSeriesPoint:
      type: object
      additionalProperties: false
      description: One composition bar. `key` is the raw stored value and is empty for an unassigned bucket; the client renders its own contract copy for that case rather than inventing a label here.
      required:
        - key
        - label
        - count
      properties:
        key:
          type: string
        label:
          type: string
        count:
          type: integer
          format: int64
          minimum: 0
    HerdAnalyticsMonth:
      type: object
      additionalProperties: false
      description: One India-calendar month of herd movement. net_change is births minus every exit, so the flow figures and the net always reconcile without the client re-deriving one.
      required:
        - month
        - label
        - births
        - deaths
        - sold
        - other_exits
        - movements
        - animals_moved
        - net_change
      properties:
        month:
          type: string
          description: IST calendar month key, "2026-08".
        label:
          type: string
          description: Farm-readable month, "Aug 2026".
        births:
          type: integer
          format: int64
          minimum: 0
        deaths:
          type: integer
          format: int64
          minimum: 0
        sold:
          type: integer
          format: int64
          minimum: 0
        other_exits:
          type: integer
          format: int64
          minimum: 0
          description: Culled, transferred or lost — carried so net_change reconciles honestly.
        movements:
          type: integer
          format: int64
          minimum: 0
          description: Applied shifting events completed in this month.
        animals_moved:
          type: integer
          format: int64
          minimum: 0
          description: Head count those movements carried.
        net_change:
          type: integer
          format: int64
    HerdAnalyticsTotals:
      type: object
      additionalProperties: false
      required:
        - live_animals
        - kids
        - adults
        - births
        - deaths
        - sold
        - other_exits
        - movements
        - animals_moved
        - net_change
      properties:
        live_animals:
          type: integer
          format: int64
          minimum: 0
          description: Census AS OF NOW, not a window figure.
        kids:
          type: integer
          format: int64
          minimum: 0
          description: >-
            kids + adults always equals live_animals exactly; an animal with an unknown age
            band counts as an adult rather than falling out of both buckets.
        adults:
          type: integer
          format: int64
          minimum: 0
        births:
          type: integer
          format: int64
          minimum: 0
        deaths:
          type: integer
          format: int64
          minimum: 0
        sold:
          type: integer
          format: int64
          minimum: 0
        other_exits:
          type: integer
          format: int64
          minimum: 0
        movements:
          type: integer
          format: int64
          minimum: 0
        animals_moved:
          type: integer
          format: int64
          minimum: 0
        net_change:
          type: integer
          format: int64
    HerdAnalyticsResponse:
      type: object
      additionalProperties: false
      required:
        - window_from
        - window_to
        - totals
        - months
        - breed
        - stage
        - sex
        - age_band
        - park
        - generated_at
      properties:
        window_from:
          type: string
          format: date
        window_to:
          type: string
          format: date
        totals:
          $ref: "#/components/schemas/HerdAnalyticsTotals"
        months:
          type: array
          items:
            $ref: "#/components/schemas/HerdAnalyticsMonth"
        breed:
          type: array
          items:
            $ref: "#/components/schemas/HerdAnalyticsSeriesPoint"
        stage:
          type: array
          description: Management stage (the pen tag) read RAW, exactly as Counts Breakdown reports it.
          items:
            $ref: "#/components/schemas/HerdAnalyticsSeriesPoint"
        sex:
          type: array
          items:
            $ref: "#/components/schemas/HerdAnalyticsSeriesPoint"
        age_band:
          type: array
          description: Two points, kid and adult, partitioning live_animals exactly.
          items:
            $ref: "#/components/schemas/HerdAnalyticsSeriesPoint"
        park:
          type: array
          items:
            $ref: "#/components/schemas/HerdAnalyticsSeriesPoint"
        generated_at:
          type: string
          format: date-time
    MilkPreparationSession:
      type: object
      additionalProperties: false
      required:
        - session_no
        - active
        - per_head_ml
        - required_ml
      properties:
        session_no:
          type: integer
          minimum: 1
          maximum: 4
        active:
          type: boolean
          description: False for sessions not used by this cohort, such as K3 sessions 2 and 3.
        per_head_ml:
          type: integer
          minimum: 0
        required_ml:
          type: integer
          format: int64
          minimum: 0
          description: Exact head_count x per_head_ml for this active session; zero only when active is false.
    MilkPreparationRow:
      type: object
      additionalProperties: false
      required:
        - park_id
        - park_label
        - shed_id
        - shed_label
        - management_stage
        - head_count
        - sessions
        - daily_required_ml
        - status
        - verification_status
      properties:
        park_id:
          type: string
          description: Canonical park UUID, or empty when source placement is missing.
        park_label:
          type: string
        shed_id:
          type: string
          description: Canonical physical shed UUID, or empty when source placement is missing.
        shed_label:
          type: string
        partition_label:
          type: string
          description: >-
            Raw stored partition label for the shed ('1', 'Part 3'). Empty or
            absent means the shed is non-partitioned. Never the literal string
            "whole".
        operational_location_display:
          type: string
          description: >-
            User-facing location label. No partition -> bare shed name
            ("Yashoda"); numeric convention -> "Castro 2"; prefixed convention
            -> "Godel 1 - Part 3".
        management_stage:
          type: string
          enum: [K1, K2, K3]
        head_count:
          type: integer
          minimum: 0
        sessions:
          type: array
          minItems: 4
          maxItems: 4
          items:
            $ref: "#/components/schemas/MilkPreparationSession"
        daily_required_ml:
          type: integer
          format: int64
          minimum: 0
        status:
          type: string
          enum: [ready, blocked]
        blocked_reason:
          type: string
          enum: [missing_shed]
        verification_status:
          type: string
          enum: [not_submitted, pending_verification, completed, rework]
          description: Park-day operational status; completion means the verifier approved all step videos.
        completion_id:
          type: string
          format: uuid
        attempt_no:
          type: integer
          minimum: 1
        rework_reason:
          type: string
    MilkPreparationSummary:
      type: object
      additionalProperties: false
      required:
        - scope
        - shed_count
        - cohort_count
        - head_count
        - total_required_ml
        - milk_direction
        - citric_acid_grams_per_litre
        - citric_acid_grams
        - blocked_row_count
        - park_count
        - not_submitted_farm_count
        - pending_verification_farm_count
        - completed_farm_count
        - rework_farm_count
      properties:
        scope:
          type: string
          enum: [filtered]
          description: Covers the complete requested park scope, independent of limit/offset.
        shed_count:
          type: integer
          minimum: 0
        cohort_count:
          type: integer
          minimum: 0
        head_count:
          type: integer
          minimum: 0
        total_required_ml:
          type: integer
          format: int64
          minimum: 0
        milk_direction:
          type: array
          description: Farm-level Milk Direction calculation lines derived from canonical K1/K2/K3 counts.
          items:
            $ref: "#/components/schemas/MilkPreparationDirectionLine"
        citric_acid_grams_per_litre:
          type: number
          format: double
          exclusiveMinimum: 0
        citric_acid_grams:
          type: number
          format: double
          minimum: 0
          description: Whole-scope milk litres x 5.5 grams, rounded to one decimal place.
        blocked_row_count:
          type: integer
          minimum: 0
        park_count:
          type: integer
          minimum: 0
        not_submitted_farm_count:
          type: integer
          minimum: 0
        pending_verification_farm_count:
          type: integer
          minimum: 0
        completed_farm_count:
          type: integer
          minimum: 0
        rework_farm_count:
          type: integer
          minimum: 0
    MilkPreparationFarmTask:
      type: object
      additionalProperties: false
      required:
        - park_id
        - park_label
        - cohort_count
        - head_count
        - total_required_ml
        - citric_acid_grams
        - verification_status
      properties:
        park_id:
          type: string
          format: uuid
        park_label:
          type: string
        cohort_count:
          type: integer
          minimum: 0
        head_count:
          type: integer
          minimum: 0
        total_required_ml:
          type: integer
          format: int64
          minimum: 0
        citric_acid_grams:
          type: number
          format: double
          minimum: 0
        verification_status:
          type: string
          enum: [not_submitted, pending_verification, completed, rework]
        completion_id:
          type: string
          format: uuid
        attempt_no:
          type: integer
          minimum: 1
        rework_reason:
          type: string
    MilkPreparationDirectionLine:
      type: object
      additionalProperties: false
      required: [management_stage, head_count, per_head_ml, session_count, required_ml]
      properties:
        management_stage: { type: string, enum: [K1, K2, K3] }
        head_count: { type: integer, minimum: 0 }
        per_head_ml: { type: integer, minimum: 1 }
        session_count: { type: integer, minimum: 1, maximum: 4 }
        required_ml: { type: integer, format: int64, minimum: 0 }
    MilkPreparationPage:
      type: object
      additionalProperties: false
      required:
        - preparation_date
        - feeding_date
        - generated_at
        - items
        - farm_tasks
        - summary
        - limit
        - offset
        - has_more
      properties:
        preparation_date:
          type: string
          format: date
          description: Current India business date on which this live direction is prepared.
        feeding_date:
          type: string
          format: date
          description: India business date immediately after preparation_date.
        generated_at:
          type: string
          format: date-time
        items:
          type: array
          items:
            $ref: "#/components/schemas/MilkPreparationRow"
        farm_tasks:
          type: array
          description: Bounded actionable farm_day worklist, independent of item pagination.
          items:
            $ref: "#/components/schemas/MilkPreparationFarmTask"
        summary:
          $ref: "#/components/schemas/MilkPreparationSummary"
        limit:
          type: integer
          minimum: 1
          maximum: 50
        offset:
          type: integer
          minimum: 0
        has_more:
          type: boolean
    MilkPreparationProofs:
      type: object
      additionalProperties: false
      required:
        - uht_milk_quantity_proof_ref
        - citric_acid_mixing_proof_ref
      properties:
        goat_milk_quantity_proof_ref:
          type: string
          format: uuid
        boiling_temperature_proof_ref:
          type: string
          format: uuid
        cooled_temperature_proof_ref:
          type: string
          format: uuid
        uht_milk_quantity_proof_ref:
          type: string
          format: uuid
        citric_acid_mixing_proof_ref:
          type: string
          format: uuid
    MilkPreparationAnswers:
      type: object
      additionalProperties: false
      required:
        - morning_milk_collected_litres
        - evening_milk_collected_litres
        - goat_milk_quantity_litres
        - boiling_temperature_c
        - cooled_temperature_c
        - uht_milk_quantity_litres
        - citric_acid_grams
      properties:
        morning_milk_collected_litres: { type: number, minimum: 0 }
        evening_milk_collected_litres: { type: number, minimum: 0 }
        goat_milk_quantity_litres: { type: number, minimum: 0 }
        boiling_temperature_c: { type: number, minimum: 0 }
        cooled_temperature_c: { type: number, minimum: 0 }
        uht_milk_quantity_litres: { type: number, exclusiveMinimum: 0 }
        citric_acid_grams: { type: number, exclusiveMinimum: 0 }
    MilkPreparationSubmissionRequest:
      type: object
      additionalProperties: false
      required:
        - park_id
        - preparation_date
        - goat_milk_used
        - answers
        - proofs
      properties:
        park_id:
          type: string
          format: uuid
        preparation_date:
          type: string
          format: date
        goat_milk_used:
          type: boolean
        answers:
          $ref: "#/components/schemas/MilkPreparationAnswers"
        proofs:
          $ref: "#/components/schemas/MilkPreparationProofs"
    MilkPreparationSubmissionResponse:
      type: object
      additionalProperties: false
      required:
        - completion_id
        - status
        - attempt_no
        - row_version
      properties:
        completion_id:
          type: string
          format: uuid
        status:
          type: string
          enum: [pending_verification]
        attempt_no:
          type: integer
          minimum: 1
        row_version:
          type: integer
          minimum: 1
    MilkFeedingWatchlistKid:
      type: object
      additionalProperties: false
      required: [goat_id, consecutive_yes, added_date, added_session]
      properties:
        goat_id: { type: string, format: uuid }
        consecutive_yes: { type: integer, minimum: 0, maximum: 1 }
        remarks: { type: string }
        added_date: { type: string, format: date }
        added_session: { type: integer, minimum: 1, maximum: 4 }
    MilkFeedingTask:
      type: object
      additionalProperties: false
      required: [task_id, park_id, park_label, feeding_date, session_no, due_time, available, available_at, head_count, verification_status, watchlist]
      properties:
        task_id: { type: string, format: uuid }
        park_id: { type: string, format: uuid }
        park_label: { type: string }
        feeding_date: { type: string, format: date }
        session_no: { type: integer, minimum: 1, maximum: 4 }
        due_time: { type: string, pattern: "^[0-2][0-9]:[0-5][0-9]$" }
        available: { type: boolean }
        available_at: { type: string, format: date-time }
        blocked_reason: { type: string }
        head_count: { type: integer, minimum: 0 }
        verification_status:
          type: string
          enum: [not_submitted, pending_verification, completed, rework]
        completion_id: { type: string, format: uuid }
        attempt_no: { type: integer, minimum: 0 }
        rework_reason: { type: string }
        watchlist:
          type: array
          items: { $ref: "#/components/schemas/MilkFeedingWatchlistKid" }
    MilkFeedingPage:
      type: object
      additionalProperties: false
      required: [feeding_date, generated_at, items, limit, offset, has_more]
      properties:
        feeding_date: { type: string, format: date }
        generated_at: { type: string, format: date-time }
        items:
          type: array
          items: { $ref: "#/components/schemas/MilkFeedingTask" }
        limit: { type: integer, minimum: 1, maximum: 20 }
        offset: { type: integer, minimum: 0 }
        has_more: { type: boolean }
    MilkFeedingWatchlistAnswer:
      type: object
      additionalProperties: false
      required: [goat_id, drank_milk]
      properties:
        goat_id: { type: string, format: uuid }
        drank_milk: { type: boolean }
    MilkFeedingNewRefusal:
      type: object
      additionalProperties: false
      required: [goat_id]
      properties:
        goat_id: { type: string, format: uuid }
        remarks: { type: string }
    MilkFeedingAnswers:
      type: object
      additionalProperties: false
      required: [watchlist_answers, total_kids_fed, attempt_1_not_drinking, attempt_2_not_drinking, new_refusals, udder_milk_not_drinking, ors_not_drinking]
      properties:
        watchlist_answers:
          type: array
          items: { $ref: "#/components/schemas/MilkFeedingWatchlistAnswer" }
        total_kids_fed: { type: integer, minimum: 0 }
        attempt_1_not_drinking: { type: integer, minimum: 0 }
        attempt_2_not_drinking: { type: integer, minimum: 0 }
        new_refusals:
          type: array
          items: { $ref: "#/components/schemas/MilkFeedingNewRefusal" }
        udder_milk_not_drinking: { type: integer, minimum: 0 }
        ors_not_drinking: { type: integer, minimum: 0 }
    MilkFeedingProofs:
      type: object
      additionalProperties: false
      required: [clean_bottles_proof_ref, mixing_and_filling_proof_ref]
      properties:
        clean_bottles_proof_ref: { type: string, format: uuid }
        mixing_and_filling_proof_ref: { type: string, format: uuid }
    MilkFeedingSubmissionRequest:
      type: object
      additionalProperties: false
      required: [park_id, feeding_date, session_no, answers, proofs]
      properties:
        park_id: { type: string, format: uuid }
        feeding_date: { type: string, format: date }
        session_no: { type: integer, minimum: 1, maximum: 4 }
        answers: { $ref: "#/components/schemas/MilkFeedingAnswers" }
        proofs: { $ref: "#/components/schemas/MilkFeedingProofs" }
    MilkFeedingSubmissionResponse:
      type: object
      additionalProperties: false
      required: [completion_id, status, attempt_no, row_version]
      properties:
        completion_id: { type: string, format: uuid }
        status: { type: string, enum: [pending_verification] }
        attempt_no: { type: integer, minimum: 1 }
        row_version: { type: integer, minimum: 1 }
    VerificationContextRow:
      type: object
      description: >-
        One backend-composed "what was expected" line on a verification item, e.g.
        label "Expected ration", value "Maize 12.5 kg · Soya 4 kg". Both fields are DISPLAY strings
        in farm language, composed by the producing module and rendered verbatim; neither is a
        config token and neither may be parsed back into business logic. The label is not an enum —
        producers choose their own, so a client must render whatever arrives rather than switching
        on a known set.
      required:
        - label
        - value
      properties:
        label:
          type: string
          description: What the value describes, e.g. "Expected ration".
        value:
          type: string
          description: The expectation itself, e.g. "Maize 12.5 kg · Soya 4 kg".
    VerificationItemStatus:
      type: string
      enum:
        - pending
        - approved
        - rejected
        # 'withdrawn' is NOT a verdict: nobody decided anything. The producing module
        # retired the item because the source record it points at is superseded (a
        # reopened weighing bucket withdraws its lump-sum submission), so the item must
        # stop being decidable. Terminal and never queryable as a queue filter -- a
        # withdrawn item is not work anyone still owes.
        - withdrawn
    VerificationSourceRef:
      type: object
      additionalProperties: false
      required:
        - module
        - ref_type
        - ref_id
      properties:
        module:
          type: string
        task_id:
          type: string
          format: uuid
        submission_id:
          type: string
          format: uuid
        ref_type:
          type: string
        ref_id:
          type: string
    VerificationMediaItem:
      type: object
      additionalProperties: false
      required:
        - proof_id
        - download_url
      properties:
        proof_id:
          type: string
        label:
          type: string
          description: Backend-authored task title displayed as the header for this evidence item.
        answer:
          type: string
          description: Backend-rendered operator response recorded for this task, omitted for action-only tasks.
        download_url:
          type: string
          format: uri
        mime_type:
          type: string
        duration_ms:
          type: integer
          minimum: 0
    VerificationMeasurementCorrection:
      type: object
      additionalProperties: false
      description: >-
        The correctable-measurement control for one verification item. Declared per category by
        the producing module at registration, so a phone and an admin-web drawer showing the same
        control cannot word it differently. Today only Weighing declares one: its proof shows a
        weight an operator typed, and the verifier may replace it.
      required:
        - ref_type
        - observation_id
        - title
        - help
        - value_label
        - submit_label
        - required_for_approve
      properties:
        ref_type:
          type: string
          description: >-
            Echoed from the item's source.ref_type. Post it back verbatim on the producing
            module's correction route; do not infer it.
        observation_id:
          type: string
          description: Echoed from the item's source.ref_id — the record the correction addresses.
        title:
          type: string
          description: Heading for the control. Rendered verbatim.
        help:
          type: string
          description: >-
            One sentence telling the verifier what the correction does. It says plainly that the
            value REPLACES the recorded one; render it, never paraphrase it.
        value_label:
          type: string
          description: Label for the number itself, unit included. Rendered verbatim.
        submit_label:
          type: string
          description: >-
            Label for the separate save control that used to sit under the field. THAT CONTROL IS
            GONE (maintainer decision 2026-08-20): the verifier types the number and presses
            Approve, and the approve carries it. Kept on the wire so an installed APK built before
            that decision still renders its own button against the producing module's route, which
            is also still served. Do not build a new client that reads it.
        required_for_approve:
          type: boolean
          description: >-
            Keep Approve disabled until a number is entered. TRUE for feed wastage, where the
            operator submits a video only and the reading is born on the verifier's screen, so
            approving without one would complete a pen-day with no wastage recorded at all. FALSE
            for weighing, where the operator already recorded a weight and a blank field means
            "his weight is right" — the normal case, which stays a single tap. An item already
            measured through the producing module's own route is still approvable without a
            number.
        count_label:
          type: string
          description: >-
            Label for an accompanying whole-number field (a lump-sum shed proof's head count).
            PRESENT ONLY on the ref types that carry one — absent means render the value field
            alone. An individual animal's proof carries no count, and the write path refuses one.
        fields:
          type: array
          description: >-
            The ordered per-item entry-box list for items whose approve carries one value PER FIELD
            (a feed packing item: one box per feed item of that pen-session, NAMES ONLY — the
            planned quantities are deliberately hidden so the verifier enters blind; maintainer
            decision 2026-08-21). Present and non-empty means render one labelled numeric box per
            field INSTEAD of the single value field, keep Approve disabled until every box is
            filled (required_for_approve is true for these categories), and send the verdict's
            measurement as `entries` echoing each field's key. Absent means the single-value
            contract.
          items:
            $ref: "#/components/schemas/VerificationMeasurementField"
    VerificationMeasurementField:
      type: object
      additionalProperties: false
      description: One per-item entry box on a verification item.
      required: [key, label]
      properties:
        key:
          type: string
          description: >-
            The producing module's stable token for this field (a normalized feed item key). Post
            it back VERBATIM as the entry's key; never parse or display it.
        label:
          type: string
          description: Backend-owned caption for the box ("Maize"). Rendered verbatim.
    VerificationQueueItem:
      type: object
      additionalProperties: false
      required:
        - item_id
        - vertical
        - module
        - category
        - status
        - captured_at
        - row_version
        - media
        - source
      properties:
        item_id:
          type: string
          format: uuid
        vertical:
          type: string
        module:
          type: string
        category:
          type: string
        subject_label:
          type: string
          description: Backend-owned goat/subject display label; never a raw UUID fallback.
        subject_note:
          type: string
          description: >-
            Optional free-text note written by whoever raised the underlying work (for a shifting
            movement, the operator's reason for moving the animals), shown to the verifier during
            evidence review. Absent when the producer supplied none. Distinct from subject_label,
            which is system-composed identity text.
        context_rows:
          type: array
          description: >-
            What the reviewed work was EXPECTED to be, so the verifier can judge the proof against a
            standard rather than only confirming a video exists. Backend-composed label/value pairs
            attached by the PRODUCING module at enqueue time, in the producer's order; for a feed
            packing proof these carry the frozen ration for that pen-session and the head count it
            was computed from. Rendered VERBATIM: clients must not parse, reorder, or re-label them,
            and must not assume a fixed set of labels — a producer may add rows at any time. Always
            present; empty when the producer attached none.
          items:
            $ref: "#/components/schemas/VerificationContextRow"
        measurement_correction:
          allOf:
            - $ref: "#/components/schemas/VerificationMeasurementCorrection"
          description: >-
            Backend-owned declaration that this item carries a number the VERIFIER may correct
            while reviewing the proof, plus every word of that control's copy. Absent — the normal
            case — means the client renders no correction control at all.


            It carries NO current value: the number is already in subject_label, which the
            producing module composes and the verifier is reading while she watches the video.
        status:
          $ref: "#/components/schemas/VerificationItemStatus"
        verdict_reason:
          type: string
        operator_id:
          type: string
          format: uuid
        operator_name:
          type: string
          description: Backend-owned display label for operator_id. Never a raw UUID.
        shed_id:
          type: string
          format: uuid
        partition_label:
          type: string
          description: >-
            Raw partition label ('1', 'Part 3') for sheds with partitions, or null/absent for
            non-partitioned sheds. Used alongside shed_label to identify operational location.
        operational_location_display:
          type: string
          description: >-
            Backend-owned composed display label for the operational location (shed + partition).
            Examples: 'Castro 2', 'Godel 1 - Part 3', 'Yashoda' (when unpartitioned).
            Render this field verbatim; do NOT compose it on the client.
        shed_label:
          type: string
          description: Backend-owned display label for shed_id. Never a raw UUID.
        park_id:
          type: string
          format: uuid
        park_label:
          type: string
          description: Backend-owned display label for park_id. Never a raw UUID.
        captured_at:
          type: string
          format: date-time
        verified_by:
          type: string
          format: uuid
        verified_by_name:
          type: string
          description: Backend-owned display label for verified_by. Never a raw UUID.
        verified_at:
          type: string
          format: date-time
        closed_by:
          type: string
          format: uuid
        closed_at:
          type: string
          format: date-time
        row_version:
          type: integer
          minimum: 1
        media:
          type: array
          items:
            $ref: "#/components/schemas/VerificationMediaItem"
        source:
          $ref: "#/components/schemas/VerificationSourceRef"
        watch:
          $ref: "#/components/schemas/VerificationWatchState"
    VerificationWatchState:
      type: object
      additionalProperties: false
      description: >-
        Lightweight per-item watch-telemetry summary for the queue table's "Watch" column, derived
        from verification_review_events. Absent from the item entirely when review-event telemetry
        is not wired for this deployment (distinct from "not opened", which is a real fact).
      required:
        - opened
      properties:
        opened:
          type: boolean
          description: True when at least one item_opened telemetry event exists for this item.
        percent_watched:
          type: integer
          minimum: 0
          maximum: 100
          description: >-
            max(video_position_ms)/max(video_duration_ms) across all actors' telemetry for this
            item, clamped to 0-100. Absent when no proof duration was ever reported.
    VerificationQueueResponse:
      type: object
      additionalProperties: false
      required:
        - items
        - filter_options
        - trace_id
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/VerificationQueueItem"
        filter_options:
          $ref: "#/components/schemas/VerificationFilterOptions"
        drive_closures:
          type: array
          items:
            $ref: "#/components/schemas/VerificationDriveClosure"
        next_cursor:
          type: string
        trace_id:
          type: string
    VerificationFilterOptions:
      type: object
      additionalProperties: false
      required:
        - modules
        - action_types
        - pages
        - statuses
        - parks
        - sheds
        - counts
        - business_timezone
        - missed_only
        - has_missed
      properties:
        module_key:
          type: string
          description: Stable backend drawer-module key for the selected verification page.
        module_label:
          type: string
          description: Backend-owned display label for the selected verifier module.
        modules:
          type: array
          description: >-
            Complete ordered verifier MODULE vocabulary from the registry — one entry per module
            however many categories it spans — for a cross-module renderer's module filter. Send a
            selected entry's key back as the `nav_module` query parameter. Independent of current
            queue rows, so an empty module never disappears from the filter.
          items:
            $ref: "#/components/schemas/VerificationModuleOption"
        action_types:
          type: array
          description: Complete ordered cross-module action-type filter vocabulary from the verification registry, independent of current queue rows.
          items:
            $ref: "#/components/schemas/VerificationActionTypeOption"
        pages:
          type: array
          description: Complete ordered page-tab set for the selected verifier module, independent of current queue rows.
          items:
            $ref: "#/components/schemas/VerificationPageOption"
        statuses:
          type: array
          description: >-
            Ordered secondary tabs at verification-item grain. The first is the "All" tab (no status
            filter); the remaining tabs are disjoint single statuses.
          items:
            $ref: "#/components/schemas/VerificationStatusOption"
        parks:
          type: array
          items:
            $ref: "#/components/schemas/VerificationLocationOption"
        sheds:
          type: array
          items:
            $ref: "#/components/schemas/VerificationLocationOption"
        counts:
          $ref: "#/components/schemas/VerificationStatusCounts"
        selected_business_date:
          type: string
          format: date
        business_timezone:
          type: string
          example: Asia/Kolkata
        missed_only:
          type: boolean
        has_missed:
          type: boolean
    VerificationStatusCounts:
      type: object
      additionalProperties: false
      description: >-
        Whole-filter row counts (pending/approved/rejected) for the SAME scope the queue page is
        reading — tenant, category/vertical/module, park, shed, and business date — computed by one
        indexed GROUP BY over verification_items, never derived from a fetched/paginated page. The
        three statuses are disjoint at verification-item grain (a row is exactly one of the three),
        so pending + approved + rejected is the whole in-scope backlog.
      required: [pending, approved, rejected]
      properties:
        pending:
          type: integer
        approved:
          type: integer
        rejected:
          type: integer
    VerificationModuleOption:
      type: object
      additionalProperties: false
      required: [key, label]
      properties:
        key:
          type: string
          description: Verifier-drawer module key; send it back as the `nav_module` query parameter.
        label:
          type: string
          description: Backend-owned display label for the module.
    VerificationActionTypeOption:
      type: object
      additionalProperties: false
      required: [key, label, category, module_key, module_label]
      properties:
        key:
          type: string
        label:
          type: string
        category:
          type: string
          description: Disjoint verification-item category predicate for this action type.
        module_key:
          type: string
        module_label:
          type: string
    VerificationPageOption:
      type: object
      additionalProperties: false
      required: [key, label, category]
      properties:
        key:
          type: string
        label:
          type: string
        category:
          type: string
          description: Disjoint verification-item category filter owned by this page tab.
    VerificationStatusOption:
      type: object
      additionalProperties: false
      required: [key, label]
      properties:
        key:
          type: string
        label:
          type: string
        status:
          $ref: "#/components/schemas/VerificationItemStatus"
          description: >-
            The item status this tab filters to. OMITTED for the "All" tab, which applies no status
            filter and therefore returns due, approved and rejected items together. A renderer must
            treat an absent status as "send no status query parameter", never as a default.
    VerificationLocationOption:
      type: object
      additionalProperties: false
      required: [id, label]
      properties:
        id:
          type: string
          description: >-
            Park options use a UUID. Shed options use an opaque operational-location filter key:
            `<shed UUID>#<normalized partition>`.
        label:
          type: string
        park_id:
          type: string
          description: >-
            Park the shed belongs to. Set on shed options only, and omitted when the option's rows
            disagree about the park rather than guessing one. Never folded into `label`.
        park_label:
          type: string
          description: >-
            Display name of `park_id`, for grouping the shed list by park. A shed NAME is not
            unique across the farm — Castro, Gandhi, Godel 1, Godel 2, Mandela 1, Mandela 2 and
            Yashoda each exist in both parks — so without this two different sheds render as
            identical adjacent options. Clients group by it; they must not concatenate it into
            the shed's operational-location display.
        partition_label:
          type: string
          description: Raw backend-owned partition label; omitted for an undivided shed.
        operational_location_display:
          type: string
          description: Backend-composed location display. Clients render this verbatim.
    VerificationDriveClosure:
      type: object
      additionalProperties: false
      required:
        - batch_id
        - total_count
        - approved_count
        - rejected_count
        - pending_count
        - video_count
        - approved_videos
        - rejected_videos
        - pending_videos
        - shed_count
        - ready
      properties:
        batch_id: { type: string, format: uuid }
        drive_key: { type: string }
        drive_label: { type: string }
        batch_label: { type: string }
        park_id: { type: string, format: uuid }
        park_label: { type: string }
        start_date: { type: string, format: date }
        end_date: { type: string, format: date }
        total_count: { type: integer }
        approved_count: { type: integer }
        rejected_count: { type: integer }
        pending_count: { type: integer }
        video_count: { type: integer }
        approved_videos: { type: integer }
        rejected_videos: { type: integer }
        pending_videos: { type: integer }
        shed_count: { type: integer }
        ready: { type: boolean }
    VerificationDecision:
      type: string
      enum:
        - approved
        - rejected
    VerificationVerdictRequest:
      type: object
      additionalProperties: false
      required:
        - decision
        - row_version
      properties:
        decision:
          $ref: "#/components/schemas/VerificationDecision"
        reason:
          type: string
          description: Required (non-empty) when decision is rejected; 422 otherwise.
        row_version:
          type: integer
          minimum: 1
        measurement:
          $ref: "#/components/schemas/VerificationVerdictMeasurement"
    VerificationVerdictMeasurement:
      type: object
      additionalProperties: false
      description: >-
        The number(s) the verifier read off the video, carried BY the approve (maintainer decision
        2026-08-20, replacing the separate save step). Send it ONLY on an item whose
        measurement_correction block is present. Single-value items (weighing, wastage) send
        `value`; per-field items (measurement_correction.fields present — feed packing) send
        `entries` with one reading per declared field, every field filled. Absent is the normal
        weighing case: blank means the operator's recorded weight is right. IGNORED on a reject,
        because rejection sends the work back to be recorded again and a value written onto a
        record about to be redone is a number nobody will use. The record it lands on comes from
        the item's own source, never from this request.
      properties:
        value:
          type: number
          minimum: 0
          description: >-
            The single-value reading in the category's own unit (kg for weighing and wastage).
            ZERO IS VALID for wastage — an empty trough is a real measurement — so omit the whole
            block rather than sending 0 to mean "not entered". Omit on per-field items; the
            readings travel on entries.
        count:
          type: integer
          minimum: 1
          description: >-
            The accompanying whole-number field, allowed ONLY where the item's
            measurement_correction carries a count_label (a lump-sum shed weigh's head count).
            Omit to leave the recorded count alone. Sending one where the item carries none is
            refused rather than dropped.
        reason:
          type: string
          description: The verifier's optional note on why the recorded number was wrong.
        entries:
          type: array
          description: >-
            One reading per measurement_correction.fields entry, keys echoed VERBATIM. Every
            declared field must be present for the approve to land (422 measurement_required names
            the missing box); a key the item never declared is refused rather than dropped. ZERO
            IS VALID — "this item was not packed" is a real observation.
          items:
            $ref: "#/components/schemas/VerificationVerdictMeasurementEntry"
    VerificationVerdictMeasurementEntry:
      type: object
      additionalProperties: false
      required: [key, value]
      properties:
        key:
          type: string
          description: The field's key, echoed verbatim from measurement_correction.fields.
        value:
          type: number
          minimum: 0
          description: The reading for this field in the category's own unit (kg).
    VerificationVerdictResponse:
      type: object
      additionalProperties: false
      required:
        - item
        - trace_id
      properties:
        item:
          $ref: "#/components/schemas/VerificationQueueItem"
        trace_id:
          type: string
    VerificationCloseRequest:
      type: object
      additionalProperties: false
      required:
        - row_version
      properties:
        row_version:
          type: integer
          minimum: 1
    VerificationReviewEventType:
      type: string
      enum:
        - queue_opened
        - item_opened
        - video_play
        - video_pause
        - video_seek_attempt
        - video_ended
        - proof_switched
        - fullscreen_toggled
        - verdict_recorded
    VerificationReviewEventPayload:
      type: object
      additionalProperties: false
      properties:
        video_position_ms:
          type: integer
          minimum: 0
        video_duration_ms:
          type: integer
          minimum: 0
        seek_from_ms:
          type: integer
          minimum: 0
        seek_to_ms:
          type: integer
          minimum: 0
        verdict:
          type: string
        category:
          type: string
          description: >-
            REQUIRED on a queue_opened event (funnel attribution when there is no item yet, see
            VerificationReviewEvent.item_id). Ignored/optional on item-scoped events, which already
            carry category via their item_id.
        park_id:
          type: string
          format: uuid
          description: Optional queue-scope attribution when the queue view was park-scoped.
        shed_id:
          type: string
          format: uuid
          description: Optional queue-scope attribution when the queue view was shed-scoped.
        status:
          type: string
          description: Informational client-declared queue-state context; carries no server-side meaning.
    VerificationReviewEvent:
      type: object
      additionalProperties: false
      required:
        - session_id
        - event_type
        - occurred_at
        - client_event_id
      properties:
        item_id:
          type: string
          format: uuid
          nullable: true
          description: >-
            REQUIRED (a real UUID) for every event type EXCEPT queue_opened. MUST be null/omitted
            for queue_opened -- that event fires before any item exists (landing on the queue
            screen), so there is no item to name yet. See migration 000119
            (verification_review_events_item_id_scope_check): item_id IS NULL if and only if
            event_type = 'queue_opened'. Sending a placeholder string for a queue_opened event
            (e.g. "queue") is REJECTED with a precise 422 field error naming item_id, not accepted
            and not a generic invalid_json.
        proof_id:
          type: string
          format: uuid
          description: >-
            Which proof video the event refers to. MUST be one of the item's OWN proofs (its
            media_refs) — a proof belonging to another item is REJECTED with a 422 field error naming
            proof_id (`proof_not_on_item`), even though it is a real, existing proof. The derived
            watch facts partition durations and watch intervals BY proof_id, so a foreign proof would
            put a duration this verifier never watched into the item's denominator and skew both its
            watch fraction and the CEO integrity aggregate. Omit it for queue_opened, which has no
            item and therefore no proof (`queue_scoped_proof_forbidden`).
        session_id:
          type: string
          maxLength: 200
        event_type:
          $ref: "#/components/schemas/VerificationReviewEventType"
        occurred_at:
          type: string
          format: date-time
        payload:
          $ref: "#/components/schemas/VerificationReviewEventPayload"
        client_event_id:
          type: string
          format: uuid
          description: >-
            Client-minted UUID. This is the idempotency key for THIS event -- a replayed batch
            (retry after a network blip) that repeats the same client_event_id inserts nothing new.
    VerificationReviewEventBatchRequest:
      type: object
      additionalProperties: false
      required:
        - events
      properties:
        events:
          type: array
          minItems: 1
          maxItems: 200
          items:
            $ref: "#/components/schemas/VerificationReviewEvent"
    VerificationReviewEventBatchResponse:
      type: object
      additionalProperties: false
      required:
        - inserted
        - trace_id
      properties:
        inserted:
          type: integer
          description: Count of ACTUALLY new rows persisted (excludes rows skipped as exact replays).
        trace_id:
          type: string
    VerificationItemReviewFactsEntry:
      type: object
      additionalProperties: false
      required:
        - actor_id
        - proof_duration_ms
        - watched_distinct_ms
        - watch_fraction
        - play_count
        - pause_count
        - seek_attempt_count
        - watched_full
      properties:
        actor_id:
          type: string
          format: uuid
        proof_duration_ms:
          type: integer
        watched_distinct_ms:
          type: integer
          description: >-
            Union of distinct-covered played spans (overlapping replays merge, never sum) -- see
            verification/adapters/postgres/review_events.go mergeIntervalsDistinctMs.
        watch_fraction:
          type: number
          format: double
        play_count:
          type: integer
        pause_count:
          type: integer
        seek_attempt_count:
          type: integer
        time_to_verdict_seconds:
          type: number
          format: double
        watched_full:
          type: boolean
          description: watch_fraction >= the configured threshold (0.9).
    VerificationItemReviewFactsResponse:
      type: object
      additionalProperties: false
      required:
        - facts
        - trace_id
      properties:
        facts:
          type: array
          items:
            $ref: "#/components/schemas/VerificationItemReviewFactsEntry"
        trace_id:
          type: string
    VerificationOversightAnalyticsResponse:
      type: object
      additionalProperties: false
      required:
        - kpis
        - pending_age_buckets
        - daily_volume_last_14d
        - pending_by_module
        - verifier_activity
        - trace_id
      properties:
        kpis:
          $ref: "#/components/schemas/VerificationOversightKPIs"
        pending_age_buckets:
          $ref: "#/components/schemas/VerificationPendingAgeBuckets"
        daily_volume_last_14d:
          type: array
          description: >-
            Fourteen consecutive Asia/Kolkata business days, oldest first, zero-filled. Days with no
            activity are present with zeroes so a chart cannot compress a quiet week.
          items:
            $ref: "#/components/schemas/VerificationDailyVolume"
        pending_by_module:
          type: array
          items:
            $ref: "#/components/schemas/VerificationModulePendingBacklog"
        verifier_activity:
          type: array
          items:
            $ref: "#/components/schemas/VerificationVerifierActivity"
        trace_id:
          type: string
    VerificationOversightKPIs:
      type: object
      additionalProperties: false
      required:
        - videos_waiting
        - verdicts_per_active_day_last_7d
        - per_module_median_review_latency_hours
      properties:
        videos_waiting:
          type: integer
          description: CEO-plain "videos waiting for review" count over the whole open queue.
        oldest_pending_age_hours:
          type: number
          description: Age in hours of the oldest still-pending item. Absent when the queue is empty.
        verdicts_per_active_day_last_7d:
          type: number
          description: >-
            Verdicts recorded in the last 7 days divided by the number of DISTINCT days in that
            window with at least one verdict.
        est_days_to_clear_backlog:
          type: number
          description: >-
            videos_waiting / verdicts_per_active_day_last_7d. Absent when the review rate is zero.
        per_module_median_review_latency_hours:
          type: array
          items:
            $ref: "#/components/schemas/VerificationModuleLatency"
        reject_rate_last_30d:
          type: number
          minimum: 0
          maximum: 1
          description: >-
            rejected / (approved + rejected) over verdicts recorded in the last 30 days. Absent
            when there were no verdicts in that window.
    VerificationPendingAgeBuckets:
      type: object
      additionalProperties: false
      description: >-
        The shape of the pending backlog by how long each video has waited. The four buckets are
        disjoint and are computed in the same statement as videos_waiting, so they always sum to it.
        Age is elapsed time since capture -- the same clock oldest_pending_age_hours reports.
      required:
        - up_to_1_day
        - one_to_three_days
        - three_to_seven_days
        - over_seven_days
      properties:
        up_to_1_day:
          type: integer
        one_to_three_days:
          type: integer
        three_to_seven_days:
          type: integer
        over_seven_days:
          type: integer
    VerificationDailyVolume:
      type: object
      additionalProperties: false
      description: >-
        One business day of flow through the queue. arrived vs verdicts is what says whether the
        backlog is growing or shrinking; throughput alone cannot.
      required:
        - business_date
        - verdicts
        - arrived
      properties:
        business_date:
          type: string
          format: date
          description: Asia/Kolkata calendar date.
        verdicts:
          type: integer
        arrived:
          type: integer
    VerificationModuleLatency:
      type: object
      additionalProperties: false
      required:
        - module
        - median_hours
      properties:
        module:
          type: string
          description: Source module code stored on the item (e.g. feed). Never rendered as copy.
        module_label:
          type: string
          description: >-
            Backend-owned display label for module, from the verification type registry ("Feed").
            Absent when the registry knows no such module.
        median_hours:
          type: number
    VerificationModulePendingBacklog:
      type: object
      additionalProperties: false
      required:
        - module
        - count
      properties:
        module:
          type: string
          description: Source module code stored on the item (e.g. feed). Never rendered as copy.
        module_label:
          type: string
          description: >-
            Backend-owned display label for module, from the verification type registry ("Feed").
            Absent when the registry knows no such module.
        nav_module:
          type: string
          description: >-
            The queue's own module-filter key for this module ("feed_direction" where module is
            "feed"), so a backlog row can link to the filter that lists exactly those items. Absent
            when the registry knows no such module.
        count:
          type: integer
    VerificationVerifierActivity:
      type: object
      additionalProperties: false
      required:
        - verifier_id
        - verdicts
        - approved
        - rejected
        - items_tracked
        - watched_to_end_count
        - verdict_without_play_count
      properties:
        verifier_id:
          type: string
          format: uuid
        verifier_name:
          type: string
          description: Backend-owned display label for verifier_id. Never a raw UUID.
        verdicts:
          type: integer
        approved:
          type: integer
        rejected:
          type: integer
        busiest_day:
          type: string
          format: date
          description: Asia/Kolkata calendar date with the most verdicts in the last 14 days.
        items_tracked:
          type: integer
          description: Items this verifier decided in the window that carry any review-event telemetry.
        watched_to_end_count:
          type: integer
        verdict_without_play_count:
          type: integer
          description: Items this verifier decided with no video_play telemetry event beforehand.
    VerificationVideoLogResponse:
      type: object
      additionalProperties: false
      required:
        - business_date
        - sheds
        - rows
        - rows_truncated
        - trace_id
      properties:
        business_date:
          type: string
          format: date
          description: The Asia/Kolkata day this log covers.
        sheds:
          type: array
          description: >-
            One row per operational location that had proof arrive on the day, ordered by park then
            shed then partition. Always present, including when it is empty.
          items:
            $ref: "#/components/schemas/VerificationVideoLogShed"
        selected_shed_id:
          type: string
          description: Echoes the requested shed_id. Absent when no shed was selected.
        rows:
          type: array
          description: >-
            Work in full, ordered by shed then earliest arrival. Populated for a selected shed, or
            for the whole day when all_sheds is set. EMPTY for the ordinary summary read -- that
            level deliberately does not carry every row.
          items:
            $ref: "#/components/schemas/VerificationVideoLogRow"
        rows_truncated:
          type: boolean
          description: >-
            True when the selected shed held more work than limit allowed, so a partial day is never
            presented as a complete one.
        trace_id:
          type: string
    VerificationVideoLogShed:
      type: object
      additionalProperties: false
      required:
        - shed_id
        - shed_key
        - operational_location_display
        - proof_count
        - item_count
        - awaiting_upload_count
        - modules
      properties:
        shed_id:
          type: string
          format: uuid
        shed_key:
          type: string
          description: >-
            Composite "<shed_uuid>#<normalized partition>" identity for this location. Send it back
            as shed_id to open the detail; a bare shed uuid cannot distinguish partitions.
        shed_label:
          type: string
          description: Raw shed name. Carried for filtering; never rendered on its own.
        partition_label:
          type: string
          description: >-
            Raw partition label ("Part 3"). Absent for a non-partitioned shed AND for weighing,
            shifting, birth and death items, whose producers do not record it. Never rendered on its
            own -- use operational_location_display.
        operational_location_display:
          type: string
          description: >-
            The ONLY location string a screen may render, composed backend-side by oploc.Display()
            ("Godel 1 - Part 3", or "Yashoda" when unpartitioned).
        park_id:
          type: string
          format: uuid
        park_label:
          type: string
        proof_count:
          type: integer
          description: Every proof that arrived for this location on the day, across all modules.
        item_count:
          type: integer
          description: >-
            How many pieces of work those proofs belong to. Always <= proof_count; the gap is the
            multi-proof categories (feed distribution's three, death's two).
        awaiting_upload_count:
          type: integer
          description: >-
            Proofs registered but not yet received. Included IN proof_count, not counted beside it.
        first_upload_at:
          type: string
          format: date-time
          description: Earliest arrival for this location on the day. Absent when nothing has landed.
        last_upload_at:
          type: string
          format: date-time
          description: Latest arrival for this location on the day. Absent when nothing has landed.
        modules:
          type: array
          description: >-
            Backend-owned display labels of the modules that contributed ("Feed", "Vaccination"),
            sorted. A module the registry does not know is omitted rather than shown as a raw code.
          items:
            type: string
    VerificationVideoLogRow:
      type: object
      additionalProperties: false
      required:
        - item_id
        - module
        - category
        - grain
        - status
        - captured_at
        - proofs
      properties:
        item_id:
          type: string
          format: uuid
        module:
          type: string
          description: Raw source module code ("feed"). Carried for links only; never rendered as copy.
        module_label:
          type: string
          description: Backend-owned display label for module ("Feed").
        nav_module:
          type: string
          description: >-
            The queue's own module-filter key for this module ("feed_direction" where module is
            "feed"), so a row can link back to the queue filtered to its module.
        category:
          type: string
          description: Raw category code ("feed_packing"). Never rendered as copy.
        category_label:
          type: string
          description: Backend-owned display label for category ("Feed packing").
        grain:
          type: string
          enum: [animal, shed]
          description: >-
            Whether this work was about specific ANIMALS (vaccination's per-goat clip, weighing's
            individual observation, a birth/death workflow, a shed move) or about a LOCATION (feed
            distribution, packing, transport, a lump-sum weigh). Derived backend-side from the
            producer's declared source_ref_type, never parsed out of the label. A client uses it to
            decide which column an identity belongs in and must not re-derive it.
        shed_id:
          type: string
          format: uuid
          description: The shed this work happened in.
        shed_label:
          type: string
          description: Raw shed name. Never rendered on its own.
        partition_label:
          type: string
          description: Raw partition label. Never rendered on its own.
        operational_location_display:
          type: string
          description: >-
            Backend-composed location for THIS row. Redundant while one shed's detail is on screen,
            and essential in the whole-day export, where a row could otherwise not say which shed
            its video came from.
        park_label:
          type: string
          description: >-
            Disambiguates the location in the whole-day export: shed NAMES repeat across parks, so a
            file carrying only the shed display renders two different sheds identically.
        subject_label:
          type: string
          description: >-
            The producing module's own composed description of the work ("Session 1 · Castro 2",
            "Godel 1 · Goat 4821 · PPR"). Rendered VERBATIM. Legitimately ABSENT for feed transport,
            whose producer writes no label because the shed header already names it -- render
            nothing there, not a placeholder.
        status:
          type: string
          description: >-
            The item's verdict state, so the log can show that an arrival was later rejected. The
            video log offers no verdict control; it is a read.
        operator_name:
          type: string
          description: >-
            Backend-resolved display name of whoever captured the work. Absent when the id resolves
            to no active roster member; an unresolved id is dropped, never rendered raw.
        captured_at:
          type: string
          format: date-time
          description: >-
            The producing module's own anchor for the work, and what the business day is cut on. NOT
            the upload time. Approximate for shifting, birth and death, whose producers stamp the
            enqueue instant.
        proofs:
          type: array
          items:
            $ref: "#/components/schemas/VerificationVideoLogProof"
    VerificationVideoLogProof:
      type: object
      additionalProperties: false
      required:
        - proof_id
        - ordinal
        - media_kind
        - registered_at
      properties:
        proof_id:
          type: string
          format: uuid
        ordinal:
          type: integer
          description: >-
            1-based position in the producing item's declared proof order. Feed distribution writes
            [weight photo, distribution video, water video] and the labels are positional against
            it.
        label:
          type: string
          description: >-
            Backend-owned header for this proof ("Water distribution video"), resolved from the
            artifact's own metadata first and the category registry second. Never composed by a
            client.
        media_kind:
          type: string
          enum: [photo, video, attachment]
          description: >-
            Feed distribution is the one category that mixes photo and video, so a screen must not
            call every proof on this log a video.
        uploaded_at:
          type: string
          format: date-time
          description: >-
            When the server accepted the bytes. ABSENT for a proof registered but never finished
            uploading -- a real state the log shows rather than hiding. Deliberately not a device
            capture time; no such column exists.
        registered_at:
          type: string
          format: date-time
          description: >-
            When the client took an upload URL. On mobile the outbox registers at capture and
            retries the upload later, so the gap to uploaded_at is real upload lag.
    VerificationCloseSubmissionResponse:
      type: object
      additionalProperties: false
      required:
        - items
        - trace_id
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/VerificationQueueItem"
        trace_id:
          type: string

    ShiftingDestinationsResponse:
      type: object
      additionalProperties: false
      required:
        - parks
      properties:
        parks:
          description: >-
            Active parks, ordered by name. Always present; an empty array means the tenant has no
            active park configured, never null.
          type: array
          items:
            $ref: "#/components/schemas/ShiftingDestinationPark"
        management_stages:
          description: Active backend-owned management-stage vocabulary.
          type: array
          items: { type: string }
    ShiftingDestinationPark:
      type: object
      additionalProperties: false
      required:
        - park_id
        - name
        - sheds
        - birth_placement
      properties:
        park_id:
          description: The park's location id. This is what the UI labels a 'farm'.
          type: string
          format: uuid
        name:
          description: Display name for the park option.
          type: string
        sheds:
          description: >-
            The park's active sheds, ordered by name. Always present; a park with no active sheds
            returns an empty array rather than null, and is still listed.
          type: array
          items:
            $ref: "#/components/schemas/ShiftingDestinationShed"
        birth_placement:
          $ref: "#/components/schemas/BirthPlacement"
    BirthPlacement:
      description: >-
        Where a newborn recorded in THIS park may be placed (maintainer decision 2026-08-20).

        The mode and the notice are backend-owned and are rendered verbatim. Clients must NOT
        re-derive the mode by filtering the park's sheds on destination_stage: the mode also governs
        whether the birth WRITE will accept a freely chosen pen, so a client that computed its own
        answer could offer a pen the birth then refuses with 400 invalid_newborn_placement.
      type: object
      additionalProperties: false
      required:
        - mode
        - notice
        - pens
      properties:
        mode:
          description: >-
            automatic - the park has exactly ONE kid pen; the form shows it read-only and the
            operator does not choose.
            choose - the park has SEVERAL kid pens; the picker offers only these.
            record_later - the park has NO kid pen; the operator picks freely from the full shed
            cascade and the kid's care steps carry "Record shed".
          type: string
          enum:
            - automatic
            - choose
            - record_later
        notice:
          description: >-
            Farm-worded copy shown above the placement field, rendered VERBATIM. Never blank - a
            record_later park still owes the operator the reason its kid pen is not set.
          type: string
        pens:
          description: >-
            The park's kid pens. Exactly one entry in automatic mode, several in choose mode, and
            EMPTY in record_later mode.
          type: array
          items:
            $ref: "#/components/schemas/BirthPlacementPen"
    BirthPlacementPen:
      description: >-
        One selectable newborn destination, carrying its whole operational location so the client
        never re-derives a display string from shed_id + partition_label.
      type: object
      additionalProperties: false
      required:
        - shed_id
        - shed_name
        - operational_location_display
      properties:
        shed_id:
          description: The PARENT physical shed's location id, never a partition-bearing alias.
          type: string
          format: uuid
        shed_name:
          description: The physical shed's display name ('Yashoda', 'Godel 1').
          type: string
        partition_label:
          description: >-
            The pen's HUMAN label ('1', 'Part 3'), omitted for a genuinely non-partitioned shed.
            Never the normalized matching key, and never the literal 'whole'.
          type:
            - string
            - "null"
          maxLength: 120
        operational_location_display:
          description: >-
            The backend-composed operator-facing label ('Yashoda 5', 'Godel 1 - Part 3'). Render this
            verbatim; do not join shed_name and partition_label on the client.
          type: string
    ShiftingDestinationShed:
      type: object
      additionalProperties: false
      required:
        - shed_id
        - name
        - management_stages
      properties:
        shed_id:
          description: >-
            The shed's location id, and the value that must be sent as destination_shed_id. Names
            repeat across parks, so this id - never the name - identifies the destination.
          type: string
          format: uuid
        name:
          description: >-
            Display name for the shed option. NOT unique across parks; only meaningful within its
            parent park.
          type: string
        management_stages:
          description: Distinct stages currently represented by live animals in this shed.
          type: array
          items: { type: string }
        partition_label:
          type:
            - string
            - "null"
          description: >-
            Raw stored partition label for this shed option ('1', 'Part 3').
            Null or absent means the shed is non-partitioned. Never the
            literal string "whole".
        source_shed_name:
          type:
            - string
            - "null"
          description: >-
            Original partition-bearing source name (e.g. "Castro 1"), kept for
            traceability only. Not a display field.
        operational_location_display:
          type: string
          description: >-
            User-facing label for this destination option. No partition ->
            bare shed name ("Yashoda"); numeric convention -> "Castro 2";
            prefixed convention -> "Godel 1 - Part 3".
        animal_count:
          type: integer
          minimum: 0
          description: >-
            Live animal count for this operational location option (shed or
            partition), used to render dropdown option counts.
        destination_stage:
          type: string
          description: >-
            The tag a movement into this pen would stamp when the raiser picks
            the "use destination tag" side of the raise form's tag toggle. Empty when
            the pen cannot supply one - then destination_stage_reason says why.
            Backend-resolved: the client must NOT derive this from
            management_stages, which is the residents' raw list rather than the
            resolved answer.
        destination_stage_reason:
          type: string
          description: >-
            Farm-worded reason the "use destination tag" option is unavailable for
            this pen, rendered VERBATIM under the greyed-out option. Empty when
            destination_stage is set; exactly one of the two is ever non-empty.
            One of "This destination has no tag set" (nothing authored, nothing living
            in it), "This destination holds a mix of tags" (several cohorts, no single
            answer), or "This destination's tag can only be set by the health team"
            (a clinical state such as ICU or Quarantine, which a movement may
            never assert).
    RecordShiftingEventRequest:
      type: object
      additionalProperties: false
      required:
        - destination_park_id
        - destination_shed_id
        - goat_ids
      properties:
        source_park_id:
          description: Park the cohort moved out of. Omit for an intake with no tracked origin.
          type: string
          format: uuid
        source_shed_id:
          description: Shed the cohort moved out of. Omit for an intake with no tracked origin.
          type: string
          format: uuid
        destination_park_id:
          description: >-
            The selected animal's current park. Shed shifts are intra-park only; clients derive and
            lock this value from the animal lookup rather than offering another farm picker.
          type: string
          format: uuid
        destination_shed_id:
          type: string
          format: uuid
        destination_partition_label:
          type:
            - string
            - "null"
          description: >-
            Raw stored partition label for the selected destination option
            ('1', 'Part 3'). Omit or null when the destination shed is
            non-partitioned or the whole shed was selected.
        effective_at:
          description: >-
            When the movement actually took effect. Defaults to the time the event is recorded.
            Normalized to UTC before the request is fingerprinted, so two representations of the same
            instant are the same request.
          type: string
          format: date-time
        priority:
          description: >-
            Governing-doc taxonomy (High/Low). Omit to accept the declared default ('low'). A present
            but unrecognized value is rejected, never rewritten to a default the operator never chose.
          type: string
          enum:
            - high
            - low
        category:
          description: >-
            THE SHIFT TYPE, and since the 2026-08-20 rewrite the type DECIDES what happens to the
            animals' tag - the raiser no longer chooses. health takes the destination pen's tag on
            both legs (the one type allowed to stamp a clinical state - a health shift IS the
            health team acting, and the animal's vaccinations defer until the return leg). growth
            takes the destination tag FORWARD ONLY along the lifecycle ladder (one reverse edge,
            Pregnant to Non-Pregnant); a backward or sideways raise is rejected. breeding never
            changes the tag. delivery takes the destination tag except it never stamps the newborn
            stage (that tag belongs to the kids); into an empty untagged shed the mother keeps her
            tag and the shed adopts it. spacing moves the WHOLE source pen carrying its tag - the
            destination must carry the same tag or be empty (an empty pen adopts the tag); partial
            groups are rejected. flushing moves females onto flushing ration - empty or
            already-flushing destinations only. A raise a rule refuses is rejected at RAISE time
            with a farm-worded reason, before approval and before any video. Omit only from a
            legacy client (the stage_mode toggle then governs, unchanged). A present but
            unrecognized value is rejected.
          type: string
          enum:
            - growth
            - health
            - breeding
            - delivery
            - spacing
            - flushing
        stage_mode:
          description: >-
            LEGACY tag toggle, governing only a raise that omits category (a client predating the
            2026-08-20 typed rewrite). When category is present the TYPE decides the tag and this
            field is ignored. 'destination_stage' makes the animals adopt the destination pen's
            tag; 'keep_current' leaves each animal on the tag it already carries. OMIT to accept
            the default, 'destination_stage'. A present but unrecognized value is rejected with
            invalid_stage_mode, never rewritten. The client sends only the MODE: the server still
            resolves the actual tag itself from the destination catalog, so a client can never
            name a cohort of its own (target_management_stage remains rejected as an unknown
            field).
          type: string
          enum:
            - destination_stage
            - keep_current
        comment:
          description: >-
            Optional free-text note from the operator raising the movement, explaining why the
            animals are being shifted. Shown to the park head deciding the approval and to the
            verifier reviewing the evidence. Blank or whitespace-only input normalizes to absent.
            A value longer than maxLength is rejected with comment_too_long, never truncated.
          type: string
          maxLength: 1000
        proof_ref:
          description: Optional reference to captured proof media for this movement.
          type: string
          minLength: 1
          maxLength: 200
        impacts:
          description: >-
            The structured cohort effect of the movement, at breed grain. OPTIONAL when goat_ids
            names EXACTLY ONE animal: the server then derives a single impact from that animal's own
            canonical facts (breed key/label, stage tag, age class, sex, head_count 1), so an
            operator moving one animal via RFID search does not have to describe a cohort the system
            already knows. REQUIRED for two or more animals, because the server will not invent the
            cohort split or the pregnancy/lactation/warm-up distribution across cohorts - omitting it
            there is rejected with 400 missing_impacts. When supplied, it is stored verbatim and no
            derivation runs. A derived impact never leaves pregnant/lactating/warmup non-zero; record
            those by supplying impacts explicitly.
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/RecordShiftingEventImpact"
        goat_ids:
          description: >-
            The individual animals this movement covers. REQUIRED, and load-bearing: approving the
            request relocates EXACTLY these animals to the destination shed and re-scopes their
            shed-scoped vaccination obligations. impacts remain an AGGREGATE model - they record
            head counts by breed grain, never which specific animals - so this list is the only
            per-animal linkage the movement has. It is required because an authorized movement that
            names nobody relocates nobody, leaving the herd register and the shed-scoped
            obligations disagreeing with the count just reported. The server will still never infer
            which animals a head count referred to, because guessing would relocate real animals
            that nobody selected - so the list is demanded at submit, while the reporting operator
            can still supply it. Duplicates are removed.
          type: array
          minItems: 1
          maxItems: 500
          items:
            type: string
            format: uuid
    RecordShiftingEventImpact:
      type: object
      additionalProperties: false
      required:
        - breed_key
        - head_count
      properties:
        breed_id:
          type: string
          format: uuid
        breed_key:
          type: string
          minLength: 1
          maxLength: 120
        breed_label:
          description: Human-readable breed label. Defaults to breed_key when omitted.
          type: string
          minLength: 1
          maxLength: 200
        stage_tag:
          type: string
          minLength: 1
          maxLength: 120
        age_class:
          type: string
          minLength: 1
          maxLength: 120
        sex:
          type: string
          minLength: 1
          maxLength: 20
        head_count:
          description: Total animals moved in this cohort. Must be greater than zero.
          type: integer
          minimum: 1
        pregnant_count:
          description: Must not exceed head_count.
          type: integer
          minimum: 0
        lactating_count:
          description: Must not exceed head_count.
          type: integer
          minimum: 0
        warmup_count:
          description: Must not exceed head_count.
          type: integer
          minimum: 0
    RecordShiftingEventResponse:
      type: object
      additionalProperties: false
      required:
        - shifting_event_id
        - approval_request_id
        - status
        - idempotent_replay
      properties:
        shifting_event_id:
          type: string
          format: uuid
        approval_request_id:
          description: >-
            The approval request a park_head decides. The movement is not applied, and no animal
            moves, until this request is approved.
          type: string
          format: uuid
        status:
          description: Status of the approval request. Always 'pending' on a fresh submission.
          type: string
          enum: [pending, approved, rejected]
        idempotent_replay:
          description: True when this result was returned from a previous identical request rather than a new write.
          type: boolean
    WorkflowSubject:
      description: The card's animal header (backend-owned display fields).
      type: object
      additionalProperties: false
      required:
        - goat_id
        - display_id
        - row_version
        - tag
        - role_label
        - sex
        - breed
      properties:
        goat_id:
          type: string
          format: uuid
        display_id:
          type: string
        row_version:
          description: Current goat concurrency token used by the Birth-owned permanent-RFID assignment.
          type: integer
          minimum: 1
        tag:
          description: The animal's working identifier (permanent RFID, else temporary tag; may be empty).
          type: string
        role_label:
          description: Operator-facing subject role ("Kid", "Mother", "Died").
          type: string
        sex:
          type: string
        breed:
          type: string
    WorkflowNextAction:
      description: The card's next-step summary.
      type: object
      additionalProperties: false
      required:
        - key
        - title
        - due_at
        - overdue
      properties:
        key:
          type: string
        title:
          type: string
        due_at:
          type:
            - string
            - "null"
          format: date-time
        overdue:
          type: boolean
    WorkflowChips:
      description: >-
        Per-day chip counts. Every bucket groups the SAME (tenant, module, event_date)
        workflow_instances key set the page reads, so the chips are independent of page size.
      type: object
      additionalProperties: false
      required:
        - all
        - overdue
        - due
        - completed
        - awaiting_video
      properties:
        all:
          type: integer
        overdue:
          type: integer
        due:
          type: integer
        completed:
          type: integer
        awaiting_video:
          type: integer
    WorkflowCard:
      description: One workflow list card (write-maintained counters; no join fan-out).
      type: object
      required:
        - workflow_id
        - module
        - template_key
        - subject
        - event_at
        - event_date
        - park_label
        - shed_label
        - actions_done
        - actions_total
        - next_action
        - awaiting_verification
        - state
      properties:
        workflow_id:
          type: string
          format: uuid
        module:
          type: string
          enum:
            - birth
            - death
        template_key:
          type: string
          enum:
            - birth_kid
            - birth_mother
            - death
        subject:
          $ref: "#/components/schemas/WorkflowSubject"
        event_at:
          type: string
          format: date-time
        event_date:
          type: string
          format: date
        park_label:
          type: string
        shed_label:
          type: string
        actions_done:
          type: integer
        actions_total:
          type: integer
        next_action:
          oneOf:
            - $ref: "#/components/schemas/WorkflowNextAction"
            - type: "null"
        awaiting_verification:
          type: boolean
        state:
          type: string
          enum:
            - open
            - completed
            - canceled
    WorkflowListResponse:
      type: object
      additionalProperties: false
      required:
        - items
        - chips
        - overdue_dates
        - next_cursor
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/WorkflowCard"
        chips:
          $ref: "#/components/schemas/WorkflowChips"
        overdue_dates:
          type: array
          maxItems: 5
          items:
            $ref: "#/components/schemas/WorkflowOverdueDate"
        next_cursor:
          type:
            - string
            - "null"
    WorkflowOverdueDate:
      description: One previous Asia/Kolkata business date containing actionable overdue workflow cards.
      type: object
      additionalProperties: false
      required:
        - date
        - workflow_count
      properties:
        date:
          type: string
          format: date
        workflow_count:
          type: integer
          minimum: 1
    WorkflowFact:
      type: object
      additionalProperties: false
      required:
        - label
        - value
      properties:
        label:
          type: string
        value:
          type: string
    WorkflowActionRow:
      description: One operator step of a workflow (template-instantiated; only status/answer/proof mutate).
      type: object
      required:
        - action_id
        - action_key
        - seq
        - section
        - action_type
        - title
        - detail
        - requires_video
        - due_at
        - status
        - blocked
        - answer_value
        - proof_ref
        - completed_by_label
        - completed_at
        - verification_status
      properties:
        action_id:
          type: string
          format: uuid
        action_key:
          type: string
        seq:
          type: integer
        section:
          type: string
          enum:
            - main
            - colostrum_session
        action_type:
          type: string
          enum:
            - question
            - question_select
            - action
            - approval
        title:
          type: string
        detail:
          type: string
        requires_video:
          type: boolean
        options:
          description: Configured question_select options; absent for free-value questions.
          type: array
          items:
            type: string
        due_at:
          type:
            - string
            - "null"
          format: date-time
        status:
          type: string
          enum:
            - pending
            - in_review
            - completed
            - rework
            - canceled
        blocked:
          description: True when the operator cannot start this action yet.
          type: boolean
        blocked_reason:
          description: Backend-owned reason; absent when blocked is false.
          type: string
          enum:
            - previous_action
            - not_yet_due
            - signoff
        answer_value:
          type:
            - string
            - "null"
        proof_ref:
          type:
            - string
            - "null"
        completed_by_label:
          type: string
        completed_at:
          type:
            - string
            - "null"
          format: date-time
        verification_status:
          description: Empty, or pending/rework/approved for verification-gated steps.
          type: string
    WorkflowDetailResponse:
      allOf:
        - $ref: "#/components/schemas/WorkflowCard"
        - type: object
          required:
            - facts
            - actions
          properties:
            facts:
              type: array
              items:
                $ref: "#/components/schemas/WorkflowFact"
            actions:
              type: array
              items:
                $ref: "#/components/schemas/WorkflowActionRow"
    AnswerWorkflowActionRequest:
      type: object
      additionalProperties: false
      required:
        - answer_value
      properties:
        answer_value:
          type: string
          minLength: 1
          maxLength: 200
        proof_ref:
          description: Server-minted proof id from /app/proofs/*; MANDATORY when the question requires_video.
          type: string
          minLength: 1
          maxLength: 200
    CompleteWorkflowActionRequest:
      type: object
      additionalProperties: false
      properties:
        proof_ref:
          description: Server-minted proof id from /app/proofs/*; MANDATORY for requires_video actions.
          type: string
          minLength: 1
          maxLength: 200
    WorkflowActionWriteResponse:
      type: object
      additionalProperties: false
      required:
        - workflow_id
        - action_id
        - status
        - workflow_state
        - actions_done
        - actions_total
        - awaiting_verification
        - completed_at
        - idempotent_replay
      properties:
        workflow_id:
          type: string
          format: uuid
        action_id:
          type: string
          format: uuid
        status:
          type: string
        workflow_state:
          type: string
        actions_done:
          type: integer
        actions_total:
          type: integer
        awaiting_verification:
          type: boolean
        completed_at:
          type:
            - string
            - "null"
          format: date-time
        idempotent_replay:
          type: boolean
    OpenHealthCaseRequest:
      type: object
      additionalProperties: false
      required: [goat_id, disease_key, age_band, start_date]
      properties:
        goat_id: { type: string, format: uuid }
        disease_key: { type: string, minLength: 1 }
        age_band: { type: string, enum: [adult, kid] }
        start_date: { type: string, format: date }
    OpenHealthCaseResponse:
      type: object
      additionalProperties: false
      required: [case_id, first_session_id, session_count, duration_days, idempotent_replay]
      properties:
        case_id: { type: string, format: uuid }
        first_session_id: { type: string, format: uuid }
        session_count: { type: integer, minimum: 1 }
        duration_days: { type: integer, minimum: 1, maximum: 90 }
        idempotent_replay: { type: boolean }
    HealthWorkItem:
      type: object
      additionalProperties: false
      required: [health_session_id, case_id, goat_id, goat_display_id, disease_key, disease_name,
        age_band, day_no, duration_days, business_date, session, due_at, status, park_id, park_label,
        shed_id, shed_label, partition_label, operational_location_display, step_count, medication_count, has_critical_step]
      properties:
        health_session_id: { type: string, format: uuid }
        case_id: { type: string, format: uuid }
        goat_id: { type: string, format: uuid }
        goat_display_id: { type: string }
        disease_key: { type: string }
        disease_name: { type: string }
        age_band: { type: string, enum: [adult, kid] }
        day_no: { type: integer, minimum: 1 }
        duration_days: { type: integer, minimum: 1, maximum: 90 }
        business_date: { type: string, format: date }
        session: { type: string, enum: [morning, afternoon, evening, unscheduled] }
        due_at: { type: string, format: date-time }
        status: { type: string, enum: [scheduled, due, in_progress, completed, rework, held, canceled_death] }
        park_id: { type: [string, "null"], format: uuid }
        park_label: { type: string }
        shed_id: { type: [string, "null"], format: uuid }
        partition_label:
          type: string
          nullable: true
          description: >-
            Raw stored partition label for the shed ('1', 'Part 3'). Null or absent
            means the shed is non-partitioned. Never the literal string 'whole' --
            that is a matching key, not user copy.
        operational_location_display:
          type: string
          description: >-
            User-facing location label composed by the backend (oploc.Display()).
            No partition -> bare shed name; partitioned -> 'Godel 1 - Part 3'.
            Clients RENDER this; they must not re-compose it.
        shed_label: { type: string }
        step_count: { type: integer, minimum: 0 }
        medication_count: { type: integer, minimum: 0 }
        has_critical_step: { type: boolean }
    HealthSummary:
      type: object
      additionalProperties: false
      required: [total, due, scheduled, in_progress, completed, rework, held, canceled_death]
      properties:
        total: { type: integer, minimum: 0 }
        due: { type: integer, minimum: 0 }
        scheduled: { type: integer, minimum: 0 }
        in_progress: { type: integer, minimum: 0 }
        completed: { type: integer, minimum: 0 }
        rework: { type: integer, minimum: 0 }
        held: { type: integer, minimum: 0 }
        canceled_death: { type: integer, minimum: 0 }
    HealthDateMarker:
      type: object
      additionalProperties: false
      required: [date, count]
      properties:
        date: { type: string, format: date }
        count: { type: integer, minimum: 0 }
    HealthFilterOption:
      type: object
      additionalProperties: false
      required: [key, label]
      properties:
        key: { type: string }
        label: { type: string }
    HealthFilterOptions:
      type: object
      additionalProperties: false
      required: [diseases, parks, sheds]
      properties:
        diseases: { type: array, items: { $ref: "#/components/schemas/HealthFilterOption" } }
        parks: { type: array, items: { $ref: "#/components/schemas/HealthFilterOption" } }
        sheds: { type: array, items: { $ref: "#/components/schemas/HealthFilterOption" } }
    HealthWorkItemPage:
      type: object
      additionalProperties: false
      required: [items, summary, date_markers, filter_options, next_cursor]
      properties:
        items: { type: array, items: { $ref: "#/components/schemas/HealthWorkItem" } }
        summary: { $ref: "#/components/schemas/HealthSummary" }
        date_markers: { type: array, items: { $ref: "#/components/schemas/HealthDateMarker" } }
        filter_options: { $ref: "#/components/schemas/HealthFilterOptions" }
        next_cursor: { type: [string, "null"] }
    HealthTreatmentStep:
      type: object
      additionalProperties: false
      required: [step_id, day_no, session, seq, record_type, medicine_name, dosage_text,
        dosage_denominator, medicine_route, instruction, critical_action_type, status]
      properties:
        step_id: { type: string, format: uuid }
        day_no: { type: integer, minimum: 1 }
        session: { type: string }
        seq: { type: integer, minimum: 1 }
        record_type: { type: string, enum: [action, medication, critical_action] }
        medicine_name: { type: [string, "null"] }
        dosage_text: { type: [string, "null"] }
        dosage_denominator: { type: [string, "null"] }
        medicine_route: { type: [string, "null"] }
        instruction: { type: [string, "null"] }
        critical_action_type: { type: [string, "null"] }
        status: { type: string, enum: [pending, completed, guarded] }
    HealthWorkItemDetail:
      allOf:
        - $ref: "#/components/schemas/HealthWorkItem"
        - type: object
          required: [steps]
          properties:
            steps: { type: array, items: { $ref: "#/components/schemas/HealthTreatmentStep" } }
    CompleteHealthWorkItemRequest:
      type: object
      additionalProperties: false
      properties:
        proof_ref: { type: string }
    CompleteHealthWorkItemResponse:
      type: object
      additionalProperties: false
      required: [health_session_id, status, completed_at, medication_count, idempotent_replay]
      properties:
        health_session_id: { type: string, format: uuid }
        status: { type: string, enum: [completed] }
        completed_at: { type: string, format: date-time }
        medication_count: { type: integer, minimum: 0 }
        idempotent_replay: { type: boolean }
    RecordBirthEventRequest:
      description: >-
        The goat-creation request for a newborn. origin_type is pinned to 'birth' by the endpoint: it
        may be omitted, but if present it must be 'birth'.
        For this birth route the server ignores child identifiers from the app and generates one
        provisional identifier per child from the canonical park code (`CBE-` or `CPT-`) plus five
        deterministic digits. One request fans out according to litter_size, so Twins creates two
        distinct canonical goats and Triplets creates three. The app never scans a child RFID at birth
        (docs/decisions/birth-death-workflows.md); the kid is promoted to its permanent RFID later
        through the "Tag the kid" step / Awaiting RFID flow.
      type: object
      additionalProperties: false
      required:
        - species
        - breed
        - sex
        - dob
        - entry_date
        - dam_id
        - litter_size
        - evidence_refs
        # park_id and shed_id are REQUIRED by the endpoint and always have been: park_id derives the
        # provisional tag prefix (400 missing_park_id without it) and shed_id is the kid's placement
        # (400 missing_shed_id). They were absent from this list while the handler rejected a request
        # without them, so the schema promised a request the server refuses. Declared here so schema
        # and implementation agree.
        - park_id
        - shed_id
      properties:
        animal_identifier_1:
          description: The permanent RFID. Provide this OR temporary_identifier, never both.
          type:
            - string
            - "null"
          minLength: 1
          maxLength: 200
        temporary_identifier:
          description: >-
            A provisional tag for a newborn not yet permanently tagged. Provide this OR
            animal_identifier_1, never both. Stored as a temporary_tag identifier; the kid still reads
            as untagged until promoted to a permanent RFID.
          type:
            - string
            - "null"
          minLength: 1
          maxLength: 200
        animal_identifier_2:
          description: Optional until double RFID tagging is live; that rollout must make this mandatory in both app validation and DB constraints.
          type:
            - string
            - "null"
          minLength: 1
          maxLength: 200
        species:
          type: string
          enum:
            - goat
            - sheep
        farm_id:
          type: string
          format: uuid
        farm_code:
          type: string
          minLength: 1
          maxLength: 120
        park_id:
          type: string
          format: uuid
        park_code:
          type: string
          minLength: 1
          maxLength: 120
        shed_id:
          description: >-
            The pen the newborn is placed in. It must be one of the park's KID PENS - a pen whose
            configured tag is K0 - whenever that park has at least one, and a request naming any
            other pen is REJECTED with 400 invalid_newborn_placement rather than silently corrected
            (maintainer decision 2026-08-20). The kid pens a park offers are published per park on
            GET /app/counts/shifting/destinations as birth_placement.pens; resolve the placement
            there rather than filtering the shed list on destination_stage.

            A park with NO kid pen configured accepts any active pen of that park: a birth is never
            lost over missing setup. The kid's care workflow then carries a "Record shed" step whose
            completion places the animal and tags that pen for kids, so the park's next birth places
            automatically.
          type: string
          format: uuid
        shed_code:
          type: string
          minLength: 1
          maxLength: 120
        partition_label:
          type:
            - string
            - "null"
          maxLength: 120
          description: >-
            The pen within shed_id the newborn is placed into ('1', 'Part 3'), matching a row in
            shed_partitions for that shed. OPTIONAL and additive: omitting it keeps the previous
            behaviour exactly (the animal is placed at shed level with no goat_shed_partitions
            row), so clients that predate this field continue to work unchanged. When present it
            is validated against the shed's real partitions and a mismatch is rejected rather
            than stored, and it is persisted in the SAME transaction as the goat insert. Never
            the literal string "whole" - that is a matching sentinel, not a pen.
        breed:
          type: string
          minLength: 1
          maxLength: 120
        sex:
          type: string
          enum:
            - female
            - male
        dob:
          description: Date of birth. Required, and must not be after entry_date.
          type: string
          format: date
        dob_estimated:
          type: boolean
        time_of_birth:
          description: >-
            Optional birth time (HH:MM, 24-hour, IST wall clock). Stored as goats.time_of_birth and
            used by the birth follow-up workflow to anchor time-offset steps; absent means unknown
            and readers fall back to 07:00 IST.
          type: string
          pattern: "^([01][0-9]|2[0-3]):[0-5][0-9]$"
        origin_type:
          description: Optional. When present it must be 'birth'; any other value is rejected.
          type: string
          const: birth
        entry_date:
          type: string
          format: date
        management_stage:
          type: string
          maxLength: 80
        health_status:
          type: string
          maxLength: 80
        weight_kg:
          type: number
          minimum: 0
        dam_id:
          description: >-
            Required mother RFID. The server resolves it tenant-safely to a canonical female goat
            before queueing approval, replaces this queued value with that goat UUID, and stores only
            the canonical mother_goat_id in goat_births.
          type: string
          minLength: 1
          maxLength: 120
        litter_size:
          description: >-
            Number born in this delivery. This one request immediately creates that many distinct
            canonical children; the same mother and litter size are stored on every sibling row.
          type: integer
          enum: [1, 2, 3]
        sire_or_lot:
          type: string
          maxLength: 120
        photo_url:
          type: string
          maxLength: 500
        source_record_id:
          type: string
          maxLength: 200
        evidence_refs:
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/EvidenceRef"
        vaccination_history:
          type: array
          items:
            $ref: "#/components/schemas/EvidenceRef"
    PromoteIdentifierRequest:
      description: >-
        Assign a permanent RFID to a temporary-tagged goat. The temporary tag to retire is found
        server-side, so only the permanent RFID and the goat's current row_version are sent.
      type: object
      additionalProperties: false
      required:
        - permanent_identifier
        - row_version
      properties:
        permanent_identifier:
          description: The permanent RFID to attach as the goat's primary animal_identifier_1.
          type: string
          minLength: 1
          maxLength: 200
        animal_identifier_2:
          description: >-
            Optional second permanent RFID, attached as the goat's non-primary animal_identifier_2 in
            the SAME atomic promotion (exactly like the birth flow's optional second identifier). Omit
            or send empty to attach only the primary. When present it must differ from
            permanent_identifier.
          type: string
          maxLength: 200
        row_version:
          description: The goat's current optimistic-concurrency token (from the temporary-tagged list).
          type: integer
          minimum: 1
    PromoteIdentifierResponse:
      type: object
      additionalProperties: false
      required:
        - goat_id
        - idempotent_replay
      properties:
        goat_id:
          type: string
          format: uuid
        idempotent_replay:
          description: True when this returned a prior identical promotion rather than a new one.
          type: boolean
    RecordDeathEventRequest:
      description: >-
        The guardrailed critical-death exit. lifecycle_status and exit_reason are constants: the
        dead+died pairing is the guardrail, and any other combination is rejected.
      type: object
      additionalProperties: false
      required:
        - goat_id
        - lifecycle_status
        - exit_reason
        - reason
        - evidence_refs
        - row_version
      properties:
        goat_id:
          description: The animal that died. Carried in the body because this route is not addressed per-animal.
          type: string
          format: uuid
        lifecycle_status:
          type: string
          const: dead
        exit_reason:
          type: string
          const: died
        reason:
          description: The operator's account of the death.
          type: string
          minLength: 3
          maxLength: 500
        occurred_at:
          description: When the death occurred. Defaults to the time the event is recorded.
          type: string
          format: date-time
        evidence_refs:
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/EvidenceRef"
        row_version:
          description: Optimistic-concurrency guard against the animal's current row version.
          type: integer
          minimum: 1
    CountsGoatWriteResponse:
      description: The identity module's goat-write result, returned unchanged by the birth and death endpoints.
      type: object
      additionalProperties: false
      required:
        - goat
        - identifiers
        - decision
        - events
        - idempotency
        - generation_status
        - trace_id
      properties:
        goat:
          $ref: "#/components/schemas/GoatSummary"
        identifiers:
          type: array
          items:
            $ref: "#/components/schemas/GoatIdentifier"
        decision:
          $ref: "#/components/schemas/CountsDecisionRecordSummary"
        events:
          description: The domain events emitted by the write (goat.created for a birth, goat.exited for a death).
          type: array
          items:
            $ref: "#/components/schemas/CountsEventSummary"
        idempotency:
          $ref: "#/components/schemas/IdempotencyMeta"
        generation_status:
          $ref: "#/components/schemas/CountsGenerationStatus"
        trace_id:
          type: string
          minLength: 1
    CountsDecisionRecordSummary:
      type: object
      additionalProperties: false
      required:
        - decision_id
        - decision_type
        - decision_result
        - decision_state
        - policy_version
        - created_at
      properties:
        decision_id:
          type: string
          format: uuid
        decision_type:
          type: string
          minLength: 1
          maxLength: 120
        decision_result:
          type: string
          minLength: 1
          maxLength: 120
        decision_state:
          type: string
          enum:
            - proposed
            - approved
            - rejected
            - needs_review
        policy_version:
          type: string
          minLength: 1
          maxLength: 120
        created_at:
          type: string
          format: date-time
    CountsEventSummary:
      type: object
      additionalProperties: false
      required:
        - event_id
        - event_type
      properties:
        event_id:
          type: string
          format: uuid
        event_type:
          type: string
          minLength: 1
          maxLength: 120
    CountsGenerationStatus:
      description: >-
        Whether the write queued downstream obligation generation. A birth queues the kid's
        vaccination obligations; a death cancels open obligations instead of generating any.
      type: string
      enum:
        - queued
        - skipped_needs_review
        - skipped_ineligible
        - not_applicable
    CountsApprovalSubmitResponse:
      description: >-
        The result of raising a lifecycle approval. Birth creates its canonical children immediately
        and returns them here; its pending approval controls only herd-count eligibility. Death still
        applies the exit only after approval and therefore returns no children.
      type: object
      additionalProperties: false
      required:
        - approval_request_id
        - request_type
        - status
        - raised_at
        - idempotent_replay
      properties:
        approval_request_id:
          type: string
          format: uuid
        request_type:
          type: string
          enum: [birth, death, shifting]
        status:
          type: string
          enum: [pending, approved, rejected]
        raised_at:
          type: string
          format: date-time
        idempotent_replay:
          description: True when this result was returned from a previous identical request rather than a new write.
          type: boolean
        children:
          description: Canonical children created by this birth submission; absent for death.
          type: array
          maxItems: 3
          items:
            $ref: "#/components/schemas/CountsBirthChildResult"
    CountsBirthChildResult:
      type: object
      additionalProperties: false
      required:
        - goat_id
        - temporary_identifier
        - child_ordinal
      properties:
        goat_id:
          type: string
          format: uuid
        temporary_identifier:
          description: Provisional CBE-/CPT- identifier retired when permanent RFID is promoted.
          type: string
          pattern: '^(CBE|CPT)-[0-9]{5}$'
        child_ordinal:
          type: integer
          minimum: 1
          maximum: 3
    CountsApprovalListItem:
      description: One row of the approver's queue - who raised what, and when.
      type: object
      additionalProperties: false
      required:
        - approval_request_id
        - request_type
        - status
        - raised_by_user_id
        - raised_at
        - summary
      properties:
        approval_request_id:
          type: string
          format: uuid
        request_type:
          type: string
          enum: [birth, death, shifting]
        status:
          type: string
          enum: [pending, approved, rejected]
        raised_by_user_id:
          description: The operator who reported the event.
          type: string
          format: uuid
        raised_at:
          type: string
          format: date-time
        raised_by_name:
          description: >-
            The raiser's name, for display. BACKEND-OWNED COPY: clients render this verbatim and
            must never compose their own label from raised_by_user_id. Absent when the raiser has
            no roster row; a client drops the line rather than falling back to the id.
          type: string
        summary_line:
          description: >-
            A one-line, farm-readable description of what the request contains, composed by the
            backend from the payload with every id already resolved to a name (for example
            "12 animals - Gandhi 1 to Gandhi 2 - Routine"). BACKEND-OWNED COPY: clients render it
            verbatim. Absent when the payload holds nothing nameable; the row's request type still
            names the work. A fact whose name cannot be resolved is DROPPED from this line - it is
            never rendered as a raw id.
          type: string
        shifting_event_id:
          description: Present only for a shifting request - the pending movement this authorizes.
          type: string
          format: uuid
        subject_goat_id:
          description: Present only for a death request - the animal the request would exit.
          type: string
          format: uuid
        subject_animal_location:
          description: >-
            Present only for a death request whose animal resolves to a real park/shed - the
            animal's CURRENT operational location ("park, shed" or "park, shed partition"),
            already resolved to names by the backend and already folded into summary_line. A
            death is terminal and never moves the animal's shed, so this is read from the
            animal's live location, not snapshotted at raise time. BACKEND-OWNED DISPLAY COPY:
            clients render it verbatim; absent when the animal's location cannot be resolved.
          type: string
        summary:
          description: The submitted payload, for rendering the row without a second fetch.
          type: object
          additionalProperties: true
        decided_by_user_id:
          type: string
          format: uuid
        decided_at:
          type: string
          format: date-time
        decision_reason:
          type: string
    CountsApprovalListResponse:
      type: object
      additionalProperties: false
      required:
        - items
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/CountsApprovalListItem"
        next_cursor:
          description: >-
            Opaque keyset cursor for the next page. Absent when this is the last page. Keyset, not
            offset: the queue is appended to continuously, so an offset page would skip or repeat
            rows as new requests arrive while an approver pages.
          type: string
    CountsApprovalDecisionRequest:
      type: object
      additionalProperties: false
      properties:
        reason:
          description: >-
            Why the request was decided this way. REQUIRED when rejecting (a rejection without a
            reason is not actionable by the operator who raised it); optional when approving.
          type: string
          maxLength: 2000
    CountsApprovalDecisionResponse:
      type: object
      additionalProperties: false
      required:
        - approval_request_id
        - request_type
        - status
        - idempotent_replay
      properties:
        approval_request_id:
          type: string
          format: uuid
        request_type:
          type: string
          enum: [birth, death, shifting]
        status:
          type: string
          enum: [pending, approved, rejected]
        decided_by_user_id:
          type: string
          format: uuid
        decided_at:
          type: string
          format: date-time
        decision_reason:
          type: string
        applied_result_type:
          description: >-
            What the approval produced, recorded in the SAME transaction as the status flip: 'goat'
            for an approved birth or death, 'shifting_event' for an approved movement. Absent on a
            rejection, which applies nothing.
          type: string
          enum: [goat, shifting_event]
        applied_result_id:
          description: The id of the created/exited animal, or of the authorized shifting event.
          type: string
          format: uuid
        idempotent_replay:
          description: >-
            True when the request was already decided this way and this call applied nothing. A
            second approve never double-applies.
          type: boolean
    CountsShiftingCancelRequest:
      type: object
      additionalProperties: false
      required:
        - reason
      properties:
        reason:
          description: >-
            Why this authorized movement will never be executed. REQUIRED and enforced by the
            database: an abandoned movement that disappears with no explanation is
            indistinguishable from one that was carried out.
          type: string
          minLength: 1
          maxLength: 2000
    CountsShiftingExecutionResponse:
      type: object
      additionalProperties: false
      required:
        - shifting_event_id
        - event_status
        - destination_park_id
        - destination_shed_id
        - moved_goat_ids
        - moved_count
        - idempotent_replay
      properties:
        shifting_event_id:
          type: string
          format: uuid
        event_status:
          description: >-
            'applied' after a completion (the animals moved); 'canceled' after a cancellation
            (nothing moved).
          type: string
          enum: [applied, canceled]
        destination_park_id:
          type: string
          format: uuid
        destination_shed_id:
          type: string
          format: uuid
        moved_goat_ids:
          description: >-
            Exactly which animals were relocated. Empty for a cancellation, which moves nobody.
          type: array
          items:
            type: string
            format: uuid
        moved_count:
          type: integer
          minimum: 0
        applied_at:
          description: When the operator confirmed the movement, as an absolute instant (UTC).
          type: string
          format: date-time
        applied_at_ist:
          description: >-
            The same instant rendered in the Asia/Kolkata business calendar. Goat OS business
            meaning is always India business-calendar meaning - a movement completed at 02:00 IST
            belongs to that IST date, not the previous UTC one.
          type: string
          format: date-time
        applied_by:
          type: string
          format: uuid
        canceled_at:
          type: string
          format: date-time
        canceled_at_ist:
          description: The cancellation instant in the Asia/Kolkata business calendar.
          type: string
          format: date-time
        canceled_by:
          type: string
          format: uuid
        cancel_reason:
          type: string
        idempotent_replay:
          description: >-
            True when the movement had already reached this state and this call changed nothing. A
            replayed completion relocates nobody a second time.
          type: boolean
    TemporaryTaggedGoatsResponse:
      type: object
      additionalProperties: false
      required:
        - items
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/TemporaryTaggedGoat"
        next_cursor:
          description: >-
            Present only when another page exists. The last row's display_id; pass back as ?cursor=.
          type: string
    TemporaryTaggedGoat:
      description: One goat awaiting a permanent RFID (it still carries an active temporary tag).
      type: object
      additionalProperties: false
      required:
        - goat_id
        - display_id
        - temporary_identifier
        - location_display
        - row_version
      properties:
        goat_id:
          type: string
          format: uuid
        display_id:
          type: string
          pattern: ^G-[0-9]{6,}$
        temporary_identifier:
          description: The active temporary tag value that promotion will retire.
          type: string
          minLength: 1
          maxLength: 200
        location_display:
          description: The animal's own shed (then park) name.
          type: string
        row_version:
          description: >-
            The goat's current optimistic-concurrency token. Sent back verbatim in the promote call
            so a stale in-hand row is rejected instead of clobbering a newer change.
          type: integer
          format: int32
          minimum: 1
    CountsShiftingPendingExecutionResponse:
      type: object
      additionalProperties: false
      required:
        - items
        - status_counts
        - previous_dates
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/CountsShiftingPendingExecutionItem"
        next_cursor:
          description: Present only when another page exists. Opaque; pass back as ?cursor=.
          type: string
        status_counts:
          $ref: "#/components/schemas/CountsShiftingActionStatusCounts"
        previous_dates:
          type: array
          items:
            $ref: "#/components/schemas/CountsShiftingPreviousDate"
    CountsShiftingActionStatusCounts:
      type: object
      additionalProperties: false
      required: [all, pending, authorized, rework, completed]
      properties:
        all: { type: integer, minimum: 0 }
        pending: { type: integer, minimum: 0 }
        authorized: { type: integer, minimum: 0 }
        rework: { type: integer, minimum: 0 }
        completed: { type: integer, minimum: 0 }
    CountsShiftingPreviousDate:
      type: object
      additionalProperties: false
      required: [date, action_count]
      properties:
        date: { type: string, format: date }
        action_count: { type: integer, minimum: 1 }
    CountsShiftingPendingExecutionItem:
      type: object
      additionalProperties: false
      required:
        - shifting_event_id
        - event_status
        - verification_state
        - primary_action_key
        - priority
        - category
        - destination_park_id
        - destination_park_name
        - destination_shed_id
        - destination_shed_name
        - raised_at
        - raised_at_ist
        - effective_at
        - animal_count
        - animals_truncated
        - animals
      properties:
        shifting_event_id:
          type: string
          format: uuid
        event_status:
          description: Canonical movement state; applied is shown as rework only when verification was rejected.
          type: string
          enum: [pending, authorized, applied]
        verification_state:
          description: Evidence-review state. Rejected requires fresh evidence and never rolls back an applied move.
          type: string
          enum: [unverified, verified, rejected]
        primary_action_key:
          description: Backend-owned row action. Completed rows are none and cannot open execution.
          type: string
          enum: [execute, none]
        priority:
          type: string
          enum: [high, low]
        category:
          type: string
          enum: [growth, health, breeding, delivery]
        source_park_id:
          description: Where the animals are NOW. Null when the movement recorded no origin.
          type: string
          format: uuid
        source_park_name:
          type: string
        source_shed_id:
          type: string
          format: uuid
        source_shed_name:
          type: string
        destination_park_id:
          type: string
          format: uuid
        destination_park_name:
          type: string
        destination_shed_id:
          type: string
          format: uuid
        destination_shed_name:
          type: string
        source_partition_label:
          type:
            - string
            - "null"
          description: >-
            The pen this movement runs FROM. Absent for an intake with no tracked origin.
        destination_partition_label:
          type:
            - string
            - "null"
          description: The pen this movement runs INTO.
        source_operational_location_display:
          type:
            - string
            - "null"
          description: >-
            Backend-composed operator-facing label for the source end ("Castro 1"). Render this
            verbatim; do not rebuild it from shed name + partition on the client. Absent when the
            movement has no tracked origin.
        destination_operational_location_display:
          type: string
          description: >-
            Backend-composed operator-facing label for the destination end ("Castro 2"). Added
            2026-08-06: this item previously shipped shed names only, so approve/execute rendered
            "Castro -> Castro" for a Castro 1 -> Castro 2 move while the Android DTO already
            declared the partition fields and deserialized them to null.
        approved_by_user_id:
          description: Who authorized this movement - the operator's basis for acting.
          type: string
          format: uuid
        approved_at:
          type: string
          format: date-time
        approved_at_ist:
          description: The approval instant in the Asia/Kolkata business calendar.
          type: string
          format: date-time
        raised_by_user_id:
          type: string
          format: uuid
        raised_at:
          type: string
          format: date-time
        raised_at_ist:
          type: string
          format: date-time
        effective_at:
          type: string
          format: date-time
        animal_count:
          description: >-
            The FULL number of animals this movement covers - not the length of the animals preview
            below. This is what an operator needs in order to judge whether they can do the movement
            now.
          type: integer
          minimum: 0
        animals_truncated:
          description: True when animal_count exceeds the number of animals returned in the preview.
          type: boolean
        animals:
          description: >-
            A bounded preview of at most 5 animals, for recognition. A movement may name up to 500
            animals; embedding all of them in a 20-row page would be a 10,000-row read to draw one
            phone screen, so the full roster belongs to the drill-down rather than the list row.
          type: array
          maxItems: 5
          items:
            $ref: "#/components/schemas/CountsShiftingPendingExecutionAnimal"
        feed_requirement:
          description: Backend-owned destination ration for high-priority shifting; absent for low priority.
          $ref: "#/components/schemas/CountsShiftingFeedRequirement"
    CountsShiftingFeedRequirement:
      type: object
      additionalProperties: false
      required: [status, animal_count, items]
      properties:
        status:
          type: string
          enum: [ready, blocked]
        blocked_reason:
          type: string
        fingerprint:
          type: string
          description: Present only when ready; completion must echo it to reject stale feed config.
        target_management_stage:
          type: string
        animal_count:
          type: integer
          minimum: 0
        items:
          type: array
          items:
            type: object
            additionalProperties: false
            required: [feed_item_label, quantity_grams]
            properties:
              feed_item_label: { type: string }
              quantity_grams:
                type: string
                description: Exact decimal total grams for all animals in this movement.
    CountsShiftingPendingExecutionAnimal:
      type: object
      additionalProperties: false
      required:
        - goat_id
        - display_id
      properties:
        goat_id:
          type: string
          format: uuid
        display_id:
          type: string
        tag:
          description: >-
            The animal's active primary ear-tag/RFID identifier, when it has one. This is the label
            physically attached to the animal, so it is what an operator actually reads in a shed.
          type: string
