{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "repo=\"${CLAUDE_PROJECT_DIR:-${CODEX_PROJECT_DIR:-}}\"; if [ -z \"$repo\" ] || [ ! -f \"$repo/tools/agent-hooks/ai-setup-guard.sh\" ]; then repo=\"$(git rev-parse --show-toplevel 2>/dev/null || pwd)\"; fi; script=\"$repo/tools/agent-hooks/ai-setup-guard.sh\"; if [ -f \"$script\" ]; then out=\"$(printf '' | bash \"$script\" 2>&1)\" || true; if [ -n \"$out\" ]; then printf '%s\\n' \"$out\"; fi; fi; exit 0"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Grep|Glob|Read|Bash|Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "repo=\"${CLAUDE_PROJECT_DIR:-${CODEX_PROJECT_DIR:-}}\"; if [ -z \"$repo\" ] || [ ! -f \"$repo/tools/agent-hooks/ai-setup-guard.sh\" ]; then repo=\"$(git rev-parse --show-toplevel 2>/dev/null || pwd)\"; fi; script=\"$repo/tools/agent-hooks/ai-setup-guard.sh\"; if [ -f \"$script\" ]; then bash \"$script\"; fi"
          }
        ]
      },
      {
        "matcher": "Grep|Glob|Read|Bash",
        "hooks": [
          {
            "type": "command",
            "command": "repo=\"${CLAUDE_PROJECT_DIR:-${CODEX_PROJECT_DIR:-}}\"; if [ -z \"$repo\" ] || [ ! -f \"$repo/tools/agent-hooks/pre-search-guard.sh\" ]; then repo=\"$(git rev-parse --show-toplevel 2>/dev/null || pwd)\"; fi; script=\"$repo/tools/agent-hooks/pre-search-guard.sh\"; if [ -f \"$script\" ]; then bash \"$script\"; fi"
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "repo=\"${CLAUDE_PROJECT_DIR:-${CODEX_PROJECT_DIR:-}}\"; if [ -z \"$repo\" ] || [ ! -f \"$repo/tools/agent-hooks/pre-rtk-git-diff.sh\" ]; then repo=\"$(git rev-parse --show-toplevel 2>/dev/null || pwd)\"; fi; script=\"$repo/tools/agent-hooks/pre-rtk-git-diff.sh\"; if [ -f \"$script\" ]; then bash \"$script\"; fi"
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "repo=\"${CLAUDE_PROJECT_DIR:-${CODEX_PROJECT_DIR:-}}\"; if [ -z \"$repo\" ] || [ ! -f \"$repo/tools/agent-hooks/pre-rtk-noisy-commands.sh\" ]; then repo=\"$(git rev-parse --show-toplevel 2>/dev/null || pwd)\"; fi; script=\"$repo/tools/agent-hooks/pre-rtk-noisy-commands.sh\"; if [ -f \"$script\" ]; then bash \"$script\"; fi"
          }
        ]
      },
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "bash tools/agent-hooks/check-write-scope.sh"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "bash tools/agent-hooks/format-touched.sh && bash tools/agent-hooks/check-boundaries.sh && bash tools/agent-hooks/check-contract-drift.sh && bash tools/agent-hooks/check-e2e-kernel-integrity.sh"
          },
          {
            "type": "command",
            "command": "repo=\"${CLAUDE_PROJECT_DIR:-${CODEX_PROJECT_DIR:-}}\"; [ -n \"$repo\" ] || repo=\"$(git rev-parse --show-toplevel 2>/dev/null || pwd)\"; code-review-graph update \"$repo\" >/dev/null 2>&1 &"
          },
          {
            "type": "command",
            "command": "repo=\"${CLAUDE_PROJECT_DIR:-${CODEX_PROJECT_DIR:-}}\"; [ -n \"$repo\" ] || repo=\"$(git rev-parse --show-toplevel 2>/dev/null || pwd)\"; command -v repowise >/dev/null 2>&1 && [ -d \"$repo/.repowise\" ] && ( cd \"$repo\" && nohup repowise update >/dev/null 2>&1 & )"
          },
          {
            "type": "command",
            "command": "bash tools/agent-hooks/update-docs-graph.sh"
          }
        ]
      }
    ]
  }
}
