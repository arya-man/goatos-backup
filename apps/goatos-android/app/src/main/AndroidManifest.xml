<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- CameraX proof capture (docs/mobile/trd-operator-mobile.md §2/§7). Needed on every
         supported OS version (minSdk 29) — always requested, never SDK-gated.
         required="false": proof capture is an optional capability (TRD §14 renderer,
         never a gatekeeper) — a camera-less device must still be installable. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera.any" android:required="false" />

    <!-- Obligation/escalation alert notifications rendered from FCM
         (docs/mobile/trd-operator-mobile.md §8). Runtime-requestable only on Android 13+
         (TIRAMISU); declaring it on older OS versions is a documented no-op. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- RFID keyboard-wedge reader (docs/mobile/rfid-keyboard-reader.md). Android owns the
         HID connection; these gate paired-device readiness + optional discovery. -->
    <uses-permission
        android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />
    <uses-permission
        android:name="android.permission.ACCESS_FINE_LOCATION"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />

    <application
        android:name=".GoatOsApplication"
        android:allowBackup="false"
        android:label="Mesha"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.GoatOs">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.GoatOs">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- DEV-ONLY screenshot harness (GalleryActivity.kt). Not in launcher; adb-launched
             to render one screen with mock fixture data for pixel-diffing against the mock. -->
        <activity
            android:name=".GalleryActivity"
            android:exported="true"
            android:theme="@style/Theme.GoatOs" />

        <!-- Disable WorkManager's default on-startup initializer: GoatOsApplication implements
             Configuration.Provider to supply a Hilt HiltWorkerFactory (on-demand init), so the
             default initializer must NOT run first with a factory that can't build @HiltWorker. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>
    </application>
</manifest>
