<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Adaptive-icon background tile = the web login page bg (token bg = #0E1512). Fills the
         entire 108dp layer (incl. bleed zone) so any mask crop shows the same near-black,
         never a gap. The foreground is the green Devanagari "मे" glyph only (no ring, no fill
         disc); its fully transparent layer composites over this near-black bg so the icon reads
         as a thin green glyph on near-black. -->
    <color name="ic_launcher_background">#0E1512</color>
</resources>
