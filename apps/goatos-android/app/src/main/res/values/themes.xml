<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base platform theme; Compose owns the real (dark-default) Material3 theme
         from core-designsystem. NoActionBar + transparent bars for edge-to-edge. -->
    <style name="Theme.GoatOs" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/black</item>
    </style>
</resources>
