// Public entrypoint for the verification-review feature. App routes and other
// features must import from here, not deep-reach into module files (enforced by
// the admin-web feature deep-import boundary guard).
export { VerificationReviewPage } from "./verification-review-page";
