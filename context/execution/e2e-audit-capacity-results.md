# E2E Capacity/Batching Audit — VAXCAP fixes vs live data

- Repo: `/Users/ravi/mesha/goatos` @ `e451ad93`
- Stack: API `http://127.0.0.1:8081`, DB `postgres://postgres:goatos@127.0.0.1:15544/goatos` (temp e2e stack, not the dev :8080/:3300/:5433 stack)
- Tenant: `00000000-0000-4000-8000-000000000001`, capacity config `max_per_day=100`, policy `last-safe-may-exceed`
- Seed data: 47 planned batches (all `status='planned'`; no `in_progress`/`completed` batches exist in seed), 43 distinct park/date combinations, 2 parks (Coimbatore `...003001`, Channapatna `...003002`)

## Summary table

| Case | Result | Notes |
|---|---|---|
| C1 Cell ledger truth | **PASS** | 43/43 park/date combos match exactly between `CountDriveCellsForParkDate`'s exact SQL and an independently-written recompute |
| C2 Park/date cap | **PASS** | Only 1 over-cap park/date exists (Channapatna 2026-08-12 = 166, the known sanctioned case); every one of its 166 cells is boundary-pinned (`window_end` = day before planned_date, clamp-forced schedule-now); no other over-cap rows found |
| C3 Co-due = 2 cells | **PASS** | Verified live: Channapatna 2026-07-27 batch has `planned_quantity=57`, 57 instances, but only 39 distinct goats (18 goats co-due with 2 shots) — cell math counts 2 cells per co-due goat while animal count is 1 |
| C4 Batch status capacity (planned→in_progress) | **BLOCKED-BY-DESIGN** | No code path writes `obligation_batches.status='in_progress'` anywhere in the backend. It is a legal value in the `obligation_batches_status_check` CHECK constraint and appears throughout read-side projections (`vaccinationexecution`, `calendar`, `processintegrity`) as a *derived/computed* status, but there is no HTTP route, service method, or SQL writer that transitions a batch from `planned` to `in_progress`. Missing surface: an execution-start API/service that persists the batch status transition. |
| C5 Overdue clamp (schedule-now) | **PASS** | Live-tested with the real `obligation-sweeper` binary against the temp DB: took an unbatched `scheduled` obligation (due `2027-01-05`, `window_end=2027-02-04`) and ran the sweeper with `-as-of=2027-01-20 -due-before=2027-01-20` (15 days overdue, well past the due+7 hold boundary, window still open). Result: `batches=2 obligations=24`; the obligation is now in a `planned` batch with `planned_date=2027-01-20` — i.e. it batched on the sweep day itself (schedule-now), not stranded past its hold boundary. |
| C6 D→D+7 hold (LIFE-002) | **PASS** | Live API check on a genuinely held batch (`obligation_instances.due_at=2026-07-21`, batch `planned_date=2026-07-22`): `GET /calendar/vaccination/events?from=2026-07-22&to=2026-07-22` returns the park-drive event with `due_at=2026-07-22` (= planned_date, not the stale per-instance due date). `GET /calendar/vaccination/events/{event_id}/targets` returns that same obligation (`60af6d87-b121-46f7-804e-1cc06c64114a`) with `scheduled_at=2026-07-22T05:30:00+05:30` — matching planned_date, not the stale `due_at=2026-07-21`. Event date and target `scheduled_at` agree on D+7. (No separate persisted "roster" table with a `scheduled_at` column exists in this codebase — `workforce_roster_assignments` is a staffing/shift table unrelated to vaccination-drive obligation scheduling; the LIFE-002 fix surface is the calendar `ListEvents`/`ListDriveTargets` read path, confirmed above and matching the unit test `TestCalendarHeldDriveTargetsReturnPlannedDateNotStaleDueAt`.) |
| C7 Mixed-dose exactness | **PASS** | Queried all live batches whose instances span >1 distinct `protocol_rules.rule_id` (mixed-dose/mixed-vaccine batches, e.g. 7 rules in one batch). In every case `obligation_batches.planned_quantity` equals the exact live instance count (e.g. batch `27ca02da...` planned_quantity=57, 57 instances across 7 rules) — no batch has a planned_quantity that is an approximation rather than the exact attached-cell sum. |
| C8 Micro-drives | **FAIL (genuine gap)** | `tools/dev/check-vaccination-drive-clubbing-proof.sh --self-test` passes. Full run against live temp DB: **exit 1**. Finding: `2026-07-26 | Coimbatore | species:goat | animals=1 | sheds=Godel 1 - Part 8 | due_from=2026-07-22 | safe_until=2026-07-29 | compatible_nearby=2026-07-24:animals=53:cells=97:sheds=...:vaccines=ET+TT` — a single-animal park/date drive on 2026-07-26 that is still inside its safe window (due 2026-07-22, safe until 2026-07-29) and has a same-park, same-planner-group compatible drive on 2026-07-24 with only 97/100 cells used (3 cells of free capacity) within that goat's due→safe-until range. The sweeper did not club this goat into the 2026-07-24 drive. All other capacity/non-park-scope/non-integral-quantity checks in the script passed (no non-park planned batches, no non-integral planned_quantity, no over-cap park/date with movable goats). This is the one real defect found in the audit — see "Other diagnostics" below for the exact script section. |

## Other diagnostics

- Every park/date combination and its cell totals (C1 evidence, all 43 rows MATCH ledger vs independent recompute): 2026-07-22 CBE=100/CPT=100; 07-23 CBE=100/CPT=100; 07-24 CBE=97/CPT=70; 07-25 CBE=100/CPT=100; 07-26 CPT=91; 07-27 CPT=57; 07-28 CBE=100; 07-29 CBE=12; 08-02 CBE=2; 08-05 CBE=18/CPT=39; 08-06 CPT=70; 08-07 CPT=95; 08-11 CPT=11; 08-12 CBE=64/CPT=166(sanctioned); 08-16 CBE=4; 08-19 CPT=84; 08-24 CPT=31; 08-25 CBE=19; 08-29 CBE=1; 09-02 CPT=52; 09-09 CPT=18; 09-15 CBE=9; 09-18 CPT=35; 09-20 CPT=2; 09-22 CBE=6; 09-30 CPT=35; 10-09 CPT=9; 10-11 CBE=3; 10-16 CPT=6; 10-20 CBE=5; 11-03 CBE=1; 12-18 CPT=16; 12-19 CPT=96/CBE=100; 12-20 CBE=100; 12-21 CBE=81 (3 batches); 12-31 CBE=100; 2027-01-01 CBE=24.
- C4/C5 required identifying the real mutation surface. Confirmed via `grep` across `backend/internal` that `obligation_batches.status = 'in_progress'` is written nowhere — only read/derived. `obligation-sweeper` (`backend/cmd/obligation-sweeper`) is the real path used for C5; flags required `-actor-id` (dev token subject `90000000-0000-4000-8000-000000000101`) and BOTH `-as-of` and `-due-before` set to the same simulated date (the sweeper's `-due-before` defaults to real wall-clock `now()`, not `-as-of`, if not passed explicitly — passing only `-as-of` silently swept 0 obligations).
- Mutations performed: one live `obligation-sweeper` binary run against the temp DB (build via `go build ./cmd/obligation-sweeper`), batching a previously-unbatched obligation. No other writes were made; all other cases were read-only SQL/API checks. No writes were made to :8080/:3300/:5433.

## Action item

C8's finding (`Godel 1 - Part 8`, single goat stranded on 2026-07-26 despite 3 cells of free capacity on a compatible 2026-07-24 drive in its own safe window) indicates the drive-clubbing/merge logic still leaves at least one genuine micro-drive un-clubbed even after the park/date-grain fix. Needs follow-up in the sweeper's park-consolidation/merge path (`backend/internal/obligation/app/park_consolidation.go` / drive planner).
