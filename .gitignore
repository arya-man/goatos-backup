# secrets
*.env
*.env.*
**/service-account*.json
**/service-account-key.json
**/google-services.json
**/GoogleService-Info.plist
**/*-key.json

# real data / PII / exports
*.xlsx
*.xls
*.csv
source-material/
!backend/internal/legacy_import/testdata/
!backend/internal/legacy_import/testdata/*.xlsx

# generated / dependency noise
node_modules/
.next/
dist/
build/
.turbo/
coverage/
# ...but the admin-web roster /coverage API route is real product code, not
# test-coverage output — the generic `coverage/` rule must not eat it.
!/apps/admin-web/app/api/admin/roster/coverage/
!/apps/admin-web/app/api/admin/roster/coverage/route.ts

# Terraform local state/cache
.terraform/
*.tfstate
*.tfstate.*
tfplan
*.tfplan

# logs / local
firebase-debug.log
*.log
.DS_Store

# media and generated heavy artifacts
*.mp4
*.mov
*.avi
*.zip
*.apk
*.aab
*.ipa
*.parquet
*.duckdb
screenshots/
Screenshots/
**/Screenshot *.png
**/Screenshot *.jpg
**/Screenshot *.jpeg

# local agent/render scratch
.codex-goatos-render/
.codex-visual/
.codex-exports/
.understand-anything/
**/.claude/launch.json
# local durable-store scratch (bulk-status worker / dev runtime state)
/data/
.code-review-graph/

# graphify: commit ONLY the generation skills/scripts (tools/agent-hooks/*.sh),
# NEVER the generated graphs themselves (user rule 2026-06-25). Graphs are
# machine-regenerated locally by the docs-graph hook; they do not belong in git.
graphify-out/graph.json
graphify-out/manifest.json
graphify-out/GRAPH_REPORT.md
graphify-out/graph.html
graphify-out/cost.json
graphify-out/.graphify_python
graphify-out/cache/

# repowise: same policy — commit ONLY the setup script (tools/agent-hooks/
# repowise-setup.sh). The index/caches and repowise's per-clone project
# registration are machine-regenerated by `make ai-setup`; never commit them.
.repowise/
/.mcp.json
/.vscode/

# generated telemetry report (make ai-telemetry-ui) — local-only, has no secrets
# but is machine-specific; never commit
/ai-telemetry.html

wiki/

# Claude subagent worktrees (isolation:worktree) — local scratch, never commit
.claude/worktrees/

# compiled cmd binaries accidentally produced by `go build ./cmd/<x>` (no /...)
# in the module root — build artifacts, never commit
/backend/seed-shed-positions
/backend/seed-roster-real
/backend/seed-position-duties
/backend/seed-vaccination-real
/backend/analytics-rollup

# provisional shed->manager fixture (round-robin fill, contains staff names) —
# local seed input, replace with a reviewed source; never commit
/backend/cmd/seed-shed-positions/shed_manager_mapping.csv

# Python bytecode
__pycache__/
*.pyc
